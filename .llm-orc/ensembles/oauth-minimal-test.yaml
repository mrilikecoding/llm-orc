name: oauth-minimal-test
description: Minimal test for OAuth authentication

agents:
  - name: oauth-agent
    role: assistant
    model: anthropic-claude-pro-max
    system_prompt: "You are Claude Code, Anthropic's official CLI for Claude."

coordinator:
  synthesis_prompt: "No synthesis needed for single agent."
  timeout_seconds: 30

task: "Hello, how are you?"
