# Domain Model: Agentic Serving

*Scoped to the agentic-serving feature. Extends the project-level domain model (`docs/domain-model.md`), whose vocabulary and invariants (1-14) are assumed.*

## Concepts (Nouns)

| Term | Definition | Product Origin | Related Terms |
|------|-----------|----------------|---------------|
| **Orchestrator Agent** | A ReAct-loop agent that sits behind the serving layer, receives requests from clients, and delegates to llm-orc operations as tool calls. Powered by a configurable LLM via model profile. | "orchestrator" (operator) | "controller", "router" (too narrow -- it also composes and reasons) |
| **Session** | A stateful conversation between a client and the orchestrator agent, bounded by budget constraints. Begins when a client connects; ends when the client disconnects or the budget is exhausted. | -- | "conversation" (ambiguous with LLM conversation), "connection" |
| **Serving Layer** | The OpenAI-compatible endpoint surface (`/v1/chat/completions`, `/v1/models`) added to the existing FastAPI server. Handles protocol translation: SSE streaming, tool-call formatting, model listing. | -- | "API", "server" (the server already exists; this is the new surface) |
| **Orchestrator Tool** | An operation the orchestrator agent can invoke via tool call: `invoke_ensemble`, `compose_ensemble`, `list_ensembles`, `query_knowledge`, `record_outcome`. A fixed set, not dynamically extensible. | -- | "action", "function" |
| **Routing Decision** | The orchestrator agent's choice of which ensemble handles a task. Recorded in the knowledge graph with provenance. Over time, shifts from reasoning (LLM figures it out) to retrieval (knowledge graph provides the answer). | "routing" (operator) | "dispatch" (already used for agent dispatch in project model) |
| **Dynamic Invocation** | Tool-mediated ensemble invocation by the orchestrator agent, outside the ensemble reference graph. Analogous to a user invoking an ensemble via the CLI. Not governed by Invariant 7. | -- | "dynamic reference" (misleading -- it is not a reference in the Invariant 7 sense) |
| **Composition** | The orchestrator agent creating a new ensemble at runtime from available primitives: model profiles, scripts, and other ensembles. The primitives are fixed; only their arrangement is dynamic. | "compose" (operator) | "generation", "creation" (too broad -- composition specifically means assembling from existing primitives) |
| **Primitive** | A building block the orchestrator uses to compose ensembles: a model profile, a script, or an existing ensemble. The set of primitives is determined by what exists in the library. | "primitives" (operator) | "component" (used generically elsewhere) |
| **Library** | The collection of available ensembles, model profiles, and scripts across all tiers (local, global, library). The menu the orchestrator draws from for routing and composition. | "library" (operator) | "registry", "catalog" |
| **Budget** | The resource constraints on a session: turn limit (maximum iterations of the ReAct loop) and token limit (cumulative token spend). Enforced at the control plane level, not the model level. Circuit breakers that prevent runaway sessions. | -- | "limit" (too vague), "quota" |
| **Result Summarization** | Condensing ensemble output before it enters the orchestrator agent's context. A correctness requirement, not an optimization -- full ensemble result dictionaries would cause context rot. May itself be implemented as an ensemble. | -- | "compression" (ambiguous with conversation compaction) |
| **Conversation Compaction** | Compressing prior turns in the orchestrator's conversation when context exceeds a threshold. Preserves tool-call/result correlations to maintain reasoning coherence. | -- | "pruning" (implies deletion; compaction preserves meaning) |
| **Context Injection** | Pre-loading knowledge from Plexus into the orchestrator agent's system prompt at session start. Provides baseline awareness without burning tool-call turns. | -- | "pre-fetch", "priming" |
| **Ingestion** | Loading source material (file content, not LLM-generated summaries) into the knowledge graph. The quality gate: what enters the graph is the actual source, not an interpretation of it. | -- | "import" |
| **Enrichment** | Background process that extracts signal from ingested sources. Runs asynchronously after ingestion. Declarative adapters on the llm-orc side and core enrichments on the Plexus side reinforce stronger signals in the graph. | "enrichment" (operator) | "analysis", "processing" |
| **Quality Signal** | A measurement of ensemble effectiveness attached to a routing decision or execution outcome. Distinguishes "this worked well" from "this was used often." Governs stabilization. | -- | "score" (too narrow), "rating" |
| **Stabilization** | The organic process by which a repeated, quality-gated pattern becomes a reliable library entry. Not curated -- emerges from accumulated quality signals over multiple executions. | "stabilize" (operator) | "promotion" (promotion is an explicit operator action; stabilization is organic) |
| **Bootstrapping** | Pre-populating the knowledge graph from existing context (library contents, execution artifacts) so the system starts warm instead of cold. The bootstrapping pipeline: artifacts → ingestion → enrichment → queryable graph. | "warm start" (operator) | "seeding" |
| **Autonomy Level** | A per-session configuration controlling how much the orchestrator can do without operator approval. Ranges from fully autonomous (within guardrails) to fully supervised. | -- | "permission level" |
| **Calibration** | The evaluation period for a newly composed ensemble. During calibration (first N invocations), results are always checked. The ensemble must demonstrate quality before earning trust. | -- | "trial", "probation" |
| **Calibration verdict** | The output the Calibration Gate (L1) produces for each dispatch decision. Trichotomy: *Proceed* (above-threshold confidence; dispatch as-is), *Reflect* (below-threshold confidence without anomaly; orchestrator may escalate per ADR-015 or reformulate), *Abstain* (severe trajectory anomaly or post-hoc hard failure; dispatch blocked). | "verdict" (operator/orchestrator) | Introduced ADR-014 |
| **Tier-escalation router** | The Tool Dispatch (L2) interposition logic that routes dispatched tasks to the cheap-tier or escalated-tier Model Profile per the calibration verdict. Per-skill (using Topaz's eight-skill taxonomy) tier defaults are operator-configured at deployment. | "router" (operator) | Introduced ADR-015 |
| **Per-skill tier default** | The operator-configured pair of Model Profiles (cheap-tier + escalated-tier) for each Topaz skill. The configuration unit operators work with at deployment. | "tier defaults" (operator) | Introduced ADR-015; 8 skills × 2 tiers = 16 Model Profile slots, with deployment-time defaults shareable across skills |
| **Structured-handoff artifact** | The operator-facing name for the artifact set Session Registry (L3) maintains across session boundaries: feature-list-with-monotonic-passes, append-only progress log, deterministic init.sh. Distinct from the methodology-voice generalization "externalized structured state" — operators work with the artifacts by name, not with the abstract category. | -- | Introduced ADR-013 |
| **Write-gate validation** | The structural validation layer between the orchestrator's write requests and the Session Registry's persistent artifacts. Three classes: JSON schema validation (feature-list); append-only constraint enforcement (progress-log); signed-script integrity verification with operator-managed hash rotation (init.sh, tamper-detection scope). Violations produce typed errors. | -- | Introduced ADR-013; class (a)/(c) hybrid in CAAF intervention-class taxonomy |
| **Cluster determination at session-start** | The session-shape declaration mechanism that classifies a session as Cluster 1 (specialist-dispatch — RESEARCH/DECIDE/SYNTHESIZE), Cluster 2 (continuous-routing — BUILD/ARCHITECT), or Cluster 3 (conversational — DISCOVER/PLAY). Determines whether the structured-handoff artifact set is required (Cluster 2 default) or optional (Cluster 1, 3). | -- | Introduced ADR-013; cross-cluster sessions default to required per ADR-013 disposition (i) |
| **Tool-call structural validation guard** | The Tool Dispatch (L2) interposition logic that scans orchestrator responses for prose claims of tool calls and validates the claims against actually-emitted tool-call structures. Mismatches produce typed `phantom_tool_call` errors. | "structural validation" (operator) | Introduced ADR-017 |
| **Phantom tool-call** | The failure mode where an orchestrator response contains prose claiming a tool call has occurred without emitting the corresponding tool-call structure. The orchestrator's downstream reasoning incorporates the claimed tool-call's effects without the tool actually having run. | -- | Introduced ADR-017; documented at small-local-model class in Wave 1.A; surfaced at cheap-cloud tier in Wave 3.A Trial 3 (under adversarial diagnostic prompt) |
| **Liveness signal** | Telemetry that surfaces in-flight state — cloud LLM inference pending, tool call emitted but not yet dispatched, network round-trip in progress — as distinct from completion events (tool dispatch result, ensemble result). Two named candidate patterns: **tool-call-emit logging** (emit a log line when the orchestrator emits a tool call into the response stream, before dispatching) and **inference-wait heartbeat** (emit a "still waiting" line when a request has been open for more than N seconds without tool activity). | "liveness signal" (operator, Cycle 6 — post-hotfix verification 2026-05-14, finding 7) | Distinct from **Quality Signal** (ensemble effectiveness; governs stabilization) — liveness is in-flight-state telemetry, quality is post-dispatch effectiveness. Pairs with completion events to give end-to-end coverage; sharpens the Cycle 4 PLAY "failure-mode-conditional visibility" framing into "additionally long-inference-wait-conditional." |
| **Dispatch timing** *(Cycle 6 candidate; pending DECIDE)* | The temporal data attached to an ensemble invocation: start_time, end_time, computed duration. Not currently a field on any of the four Cycle 5 BUILD typed event types (`TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`); surfaced as a gap by Cycle 6 DISCOVER snapshot Action 2 field-read. The orchestrator-context destination requires dispatch timing to answer the load-bearing practitioner question (PLAY note 12: *"What was the total run-time of the ensemble?"*). **DECIDE Action C (per Cycle 6 MODEL snapshot)** — multiple architectural alternatives satisfy this requirement; T15 deliberates among them: (i) **event-model extension** — add `start_time` / `end_time` fields to existing dispatch event types, or add a new `DispatchTiming` event (the path Inversion N+2's unified-substrate framing favors); (ii) **sidecar log** — route dispatch timing to orchestrator-context only via a parallel log stream that does not participate in the typed-event surface, leaving the existing event model unchanged; (iii) **orchestrator-context-only via separate mechanism** — surface dispatch timing through the orchestrator's reasoning context without routing it through the typed-event infrastructure (e.g., context injection at session start with rolling summary). DECIDE Action C tests whether the sidecar-log alternative (ii) satisfies PLAY note 12's question before scoping the event-model-extension path (i) as a requirement. | "how long did the ensemble run?" (operator, Cycle 5 PLAY note 12 verbatim) | Candidate concept; pending DECIDE resolution of T15. The architectural alternatives differ in cost-shape: event-model extension is closer to the unified-substrate Inversion N+2 framing (one event source, two routing destinations); sidecar log is closer to a destination-specific solution that leaves the event model unchanged. Related: **Calibration Verdict** is a bare `Literal` with no call-site context — its routing to either destination requires call-site composition (ensemble name + timestamp); whether this composition is captured by an event-model extension (i), a sidecar log (ii), or a separate mechanism (iii) is part of the same DECIDE deliberation. |
| **Bilateral visibility absence** | The operator-experience state where both the operator-terminal surface (logs / TUI dashboard) and the tool-user narration surface are empty of dispatch telemetry. Cross-cycle persistent finding across Cycles 1, 4, 5 PLAY. Names what the operator *experiences*; pairs with the architectural-framing term "infrastructure-complete / routing-incomplete" (in §Methodology Vocabulary) which names the underlying architectural state. | Cycle 5 PLAY note 19 + Cycles 1, 4 PLAY (operator) | Operator-voice. The Cycle 5 PLAY susceptibility snapshot sharpened the cross-cycle framing — the experience persists at the operator-experience layer even as architectural progress (typed events shipped in code, `execution.json` writes) narrows the gap at the infrastructure layer. |
| **Information-finding overhead** | The wall-clock cost of the orchestrator's repeated information-gathering work (file globs, re-reads, re-summarizations) that the orchestrator could have skipped by using context it already had earlier in the session. Practitioner-voice latency-experience term; pairs with the analytical metric question of useful-work-time vs. total-elapsed-time as the operator-experience characterization of latency. | "the time to find trivial info was long. The whole task took 44 minutes" (operator, post-hotfix verification 2026-05-14 finding 6 verbatim) | Operator-voice; practitioner-named. The empirical context: the orchestrator narrated "I saw it in the list" early in the session (meaning `list_ensembles()` had surfaced the target) but bypassed `invoke_ensemble` on first attempt, then entered an information-gathering loop before eventually dispatching. The overhead arose from the orchestrator not *using* information it already had. |
| **Routing surface behavior** *(Cycle 6 candidate; pending T14 DECIDE deliberation — three operationally distinct dispositions)* | The orchestrator's observed dispatch ordering under a given request shape: under both tested client configurations (curl tool-less; OpenCode tool-rich), the empirical sequence is *direct LLM completion → client-tool delegation → `invoke_ensemble`*. The concept names the **empirical observation**; the practitioner's defect-or-intended-scope question (*"is this the routing surface the system wants, or is it a defect?"*) is the entry framing T14 deliberates. Three operationally distinct dispositions: (a) **intended scope** — the observed ordering is the routing surface the system wants; ADR-021's natural-language-supported clause was over-broad; (b) **defect to remediate** — design intervention restores ensemble-first routing under NL framing; (c) **configuration-conditional behavior** — the ordering is right under some orchestrator-profile + client-tool-set combinations and wrong under others. **Empirically conditional on spike γ** for the third disposition (M2.5-free-reasoning-shape-specific or systemic to cheap-cloud-orchestrator + tool-rich-client). | "is this the routing surface the system wants, or is it a defect?" (practitioner-verbatim, cycle-status §Cycle 6 question framing) | Distinct from **Routing Decision** (the orchestrator's per-task choice). Routing surface behavior is a *pattern across configurations*; Routing Decision is a *per-task event*. **Concept-name attribution note**: the prior candidate name "routing preference / operational routing preference" was the agent-named pattern attempted at Cycle 6 DISCOVER; the Cycle 6 MODEL snapshot Action A reframed the §Concepts entry to "Routing surface behavior" so the constitutional vocabulary does not pre-privilege the "preference" framing at the layer downstream ADRs read. The "preference" variant remains in §Concepts attribution and in `product-discovery.md` Tension 14 deliberation; the vocabulary stabilizes only after T14's DECIDE deliberation. |
| **Explicit-naming bypass** *(Cycle 6 candidate; pending T14 DECIDE)* | The observed phenomenon where explicit naming of an ensemble + dispatch verb (e.g., *"dispatch the code-generator capability ensemble via invoke_ensemble"*) fails to route on first attempt under client-tool-rich clients — the orchestrator uses a client tool (e.g., `Write`) instead of `invoke_ensemble`, requiring practitioner intervention to redirect. Extends the Cycle 5 PLAY note 20 finding that NL framing alone never dispatches; explicit-naming-also-fails-under-tool-rich-clients is a stronger statement. | Post-hotfix verification 2026-05-14 finding 3 + Cycle 5 PLAY note 20 (operator) | The routing-preference defect's scope may be wider than "NL framing alone." Spike γ characterizes whether this phenomenon is profile-specific or systemic. |
| **Output-spec drift** *(Cycle 6 candidate; pending DECIDE)* | The pattern where capability-ensemble output deviates from its `default_task` spec — e.g., `claim-extractor`'s spec mandates `(established)` / `(contested)` labels per bullet; observed output was plain bulleted claims. The cause is open: synthesizer agent deviation, orchestrator restructuring during response shaping, or natural-language narration overriding raw output. | Post-hotfix verification 2026-05-14 finding 2 (operator) | If T16 resolves toward artifact-as-substrate, output-spec drift becomes inconsequential (the artifact is the canonical deliverable; orchestrator-narration restructuring is decoupled). Per the **Artifact-as-substrate** entry's inversion-form collapse, output-spec drift is one of three findings the proposal dissolves. |
| **Artifact-as-substrate** *(Cycle 6 candidate; architectural commitment pending T16 DECIDE — scope as first sub-question)* | The architectural commitment that capability-ensemble deliverables (code, structured analyses, long-form text) are written as artifacts to disk and referenced by the response, not carried inline. The response carries `{artifact_path, content_type, summary, metadata}`; the orchestrator's context never carries the deliverable's content. **Agent-composed claim (pending DECIDE Action B testing)**: the proposal collapses three existing findings simultaneously — output-spec drift becomes inconsequential, information-finding overhead stays bounded, and AS-7 result-summarizer content-stripping (Cycle 5 PLAY note 6) dissolves because the summarizer summarizes metadata, not content. The Cycle 6 MODEL snapshot identified this collapse claim as agent-amplified relative to the practitioner verbatim ("a strategy"); DECIDE Action B tests each of the three findings against alternative remediation paths independently before accepting the collapse framing as T16's deliberation substrate. | "this also to me points to an ensemble design shape for agentic-serving... one strategy could be to **always** rely on artifact writing to be the substrate" (operator-verbatim, post-hotfix verification 2026-05-14 finding 8) | Candidate ADR territory for Cycle 6 DECIDE (amends ADR-021 per-capability dispatch contract and ADR-004 result-summarizer role). **Scope is the first DECIDE sub-question (per snapshot Action 4)**: always (practitioner verbatim) / when substantive (agent-introduced refinement) / operator-configured. Other five sub-questions (boundary, contract, client access, cleanup, backward compat) depend on scope. The three-findings-collapse claim is **agent-composed framing of downstream implications** — not a structural property of the concept; DECIDE Action B tests it. May trigger an amendment to invariant **AS-7** depending on T16 resolution per OQ #15. |

## Methodology Vocabulary

*Research-voice and architectural-framing terms used in essays, ADR drafting, and provenance chains. The operator does not typically work with these terms operationally — they are how the analyst explains why the architecture is shaped the way it is. Distinguished from operator-voice product vocabulary (in `product-discovery.md`'s vocabulary table) and from operator-voice system concepts (in §Concepts above).*

| Term | Definition | Source | Notes |
|------|-----------|--------|-------|
| **Layer-conditional composition** | The cycle's central architectural finding: the cheap-orchestrator + ensemble pattern's value is not located in any single layer, but in cross-layer composition where components have measurably-different error distributions, with composition shapes appropriate to each layer's task class. | essay `005-layer-conditional-composition.md` | Practitioner gate-confirmed at discover→model boundary as research-voice rather than operator-voice (*"I would not use those terms ... unless they became more operationalized"*). The principle threads through ADR candidate #6's motivation; DECIDE-phase ADRs may reference but do not need to operationalize the phrase itself. |
| **MOP / meltdown-on-paradox** | Khanal, Tao, and Zhou's empirical finding (arXiv:2603.29231): on long-horizon tasks, the most-capable models in an open-source models cohort exhibit the *highest* meltdown rates, because they pursue ambitious strategies that spiral when exploratory paths fail. Reframes "throw a more-capable model at the hard cases" as a less-reliable backup than intuition suggests. | Khanal, Tao, Zhou (arXiv:2603.29231, 2026); cited in essay `005-layer-conditional-composition.md` | Provenance: open-source-models-cohort frontier (DeepSeek V3, MiniMax M2.5, others), not proprietary frontier (GPT-5, Claude Opus 4.5); extrapolation to proprietary frontier behavior is directionally plausible but not directly evidenced. The literature finding informs the cycle's local-first commitment as well-calibrated rather than as a compromise. |
| **Externalized structured state** | The category of session-continuity primitives the operator interacts with at session boundary: feature-list-with-monotonic-passes (structural non-regression at schema level), append-only progress log, deterministic init.sh environment bootstrap. The methodology-voice generalization of what the operator works with concretely (`feature_list.json`, `claude-progress.txt`, `init.sh`). | essay `005-layer-conditional-composition.md` (citing Anthropic initializer schema) | MODEL-phase evaluation (2026-05-05) confirmed at MODEL gate: the operator works with the *artifacts* by name (feature list, progress log, init.sh); the abstraction "externalized structured state" is the category-level analyst term. Practitioner warrant at gate: experience with the RDD framework's own externalized-state surface (cycle-status, gate reflection notes, dispatch log, `.rdd/audits/`) — operator-experience reaches for artifact names; the abstraction shows up only in essays. Relocated from product-discovery vocabulary to methodology vocabulary. |
| **Cross-layer calibration channel** *(conditionally accepted via ADR-016 on 2026-05-06; first-deployment evidence pending for full operational validation)* | The upward read-only signal path from L0 (ensemble outputs) to L1 (Calibration Gate dispatch decisions). ADR-016 amends ADR-002's "edges never upward" layering rule to permit this single narrow exception — read-only and signal-channel-specific (calibration only); not a general upward import permission. | ADR-016 + essay `005-layer-conditional-composition.md` §"ADR candidate #6" | Term promoted from candidate to confirmed methodology vocabulary on 2026-05-06. ADR-016's conditional-acceptance status is preserved here: the channel is structurally specified and synthetic-data + structural-transfer validated (per spike (b) and spike (d) at DECIDE close); first-deployment evidence on the cycle's North-Star benchmark remains the next validation gate. |
| **In-process trajectory-level calibration** | The category of calibration extension ADR-014 introduces: HTC trajectory features and AUQ confidence propagation operate on the dispatched ensemble's full trajectory in-process, rather than only on output-level result-checks (ADR-007's existing post-hoc layer). The two layers are complementary; together they fill ADR-007's Open Question #5 (what counts as a quality signal). | ADR-014 + AUQ (arXiv:2601.15703) + HTC (arXiv:2601.15778) | Introduced ADR-014 |
| **Trajectory feature** | The HTC-derived data type the Calibration Gate extracts across the dispatched ensemble's full trajectory: token-level entropy patterns, attention-weight distributions over tool-call sequences, decision-confidence trajectories across reasoning steps. More informative than output-level confidence alone for long-horizon decisions. | ADR-014 + HTC (arXiv:2601.15778) | Introduced ADR-014; extracted at L0 when ADR-016 is active and propagated through the cross-layer signal channel; extracted in-layer at L1 otherwise |
| **Topaz skill profile** | The role-profiling vocabulary the per-role tier-escalation router uses to determine each ensemble's primary capability dimension. Eight skills: code generation, tool use, mathematical reasoning, logical reasoning, factual knowledge, writing quality, instruction following, summarization. | ADR-015 + Topaz paper | Introduced ADR-015; ensemble YAML metadata field declares the primary skill per ensemble |
| **Bounding mechanisms (a–e)** *(conditionally accepted via ADR-016 on 2026-05-06; mechanism-specific readiness asymmetric)* | The five load-bearing constraints conditional on the channel amendment: **(a) fresh-context isolation in the consumer** — the calibration consumer reads signals in a context that does not carry prior signal forward (architectural-isolation pattern from RDD methodology tooling); **(b) time-decay windowing** — only recent signals influence current decisions, bounding the bias-compounding horizon (synthetic-data validated 2026-05-06; default 60-min/100-signal dual-bound with linear decay; smaller-window configurations track better than default in tested scenarios — operational tuning territory); **(c) categorical anchors via deterministic-tool-output** — where the ensemble has script-model slots, deterministic outputs anchor the feedback loop against probabilistic drift (Wisdom and Delusion of LLM Ensembles, arXiv:2510.21513); ensemble-composition-conditional; anchor strength scales with output verifiability (binary-verifiable strongest, interpretable-prose weakest); **(d) periodic out-of-band audit dispatch** — periodic auditor checks whether the calibration system itself is drifting (susceptibility-snapshot pattern transferred from RDD methodology; structural-transfer validated 2026-05-06; specification refined with quantitative-threshold drift criteria, operationally-applicable test, asynchronous-operator-review dynamic); detects mechanism (b) parameter drift via the (b)/(d) coupling; **(e) read-only structural validation at the consumer** — schema/type validation of signals before they influence decisions (CAAF's class-(a) pattern; codebase precedent at commit `9f86d0b`). | ADR-016 + essay `005-layer-conditional-composition.md` §"ADR candidate #6, gate-conversation refinement, 2026-05-04" + research logs `005e-spike-adr016-b-time-decay-windowing.md` and `005f-spike-adr016-d-structural-transfer-audit.md` | Asymmetric implementation-readiness preserved: (a) and (e) are precedent-based (methodology-tooling for (a), codebase for (e)); (c) is direct literature precedent but ensemble-composition-conditional; (b) and (d) are novel design work, validated at structural/logical level by DECIDE-phase spikes (2026-05-06), with first-deployment evidence pending for full operational validation. |
| **Infrastructure-complete / routing-incomplete** *(Cycle 6 — Cycle 5 PLAY susceptibility-snapshot reframing)* | The architectural state where dispatch telemetry exists in code (typed events shipped by Cycle 5 BUILD: `TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`; write to `execution.json` artifacts) but does not route to human-visible surfaces. The dispatch-event-emission surface is built; the dispatch-event-routing surface is not. Pairs with the operator-experience term **Bilateral visibility absence** (in §Concepts) — the architectural framing names the underlying state; the operator-experience term names what the operator encounters. | Cycle 5 PLAY susceptibility snapshot Advisory 3 + Cycle 6 DISCOVER snapshot Action 2 field-read | The Action 2 field-read sharpened the framing: the four typed events substantially cover the **operator-terminal destination**'s needs (model_profile, tier, topaz_skill, verdict, ensemble_name, dispatch_success, criteria findings); the **orchestrator-context destination** requires a bounded event-model extension because dispatch duration is not a field on any existing event type. "Routing-incomplete" thus includes one bounded event-model extension (dispatch-timing fields or a new `DispatchTiming` event) and `CalibrationVerdict` call-site composition — not just routing wiring. |
| **Common I/O envelope** *(Cycle 6 candidate; spike α / β analytical substrate; research-voice-leaning)* | A proposed common response shape for ensemble dispatches with four candidate field names: **`envelope`** (the response shape itself); **`primary`** (the canonical deliverable — content inline or artifact reference depending on T16 scope); **`artifact reference`** (`{artifact_path, content_type, summary}` when `primary` is artifact-substrate); **`diagnostics`** (operator-readable metadata about the dispatch — verdict, tier, audit consumption, etc.). The envelope is the structural substrate that lets ensemble outputs become composable without ad-hoc text-massaging at each stage boundary. | Cycle 6 spike α (common I/O envelope survey) + spike β (composition predictability under common envelope); cycle-status §Candidate spikes | Research-voice-leaning; whether the field names crystallize as operator-voice or stay analytical is open. Spike β tests whether the typed-output fields in the envelope are load-bearing for composition (i.e., does A→B composition become structurally predictable rather than ad-hoc text-passing). The envelope interacts with **Artifact-as-substrate** (§Concepts): under T16's *always* scope the `primary` field is always an artifact reference; under *substantive* scope it is conditional; under *operator-configured* scope it is per-ensemble. |

## Actions (Verbs)

| Action | Actor | Subject | Description |
|--------|-------|---------|-------------|
| **Route** | Orchestrator Agent | Task → Ensemble | Select an ensemble from the library to handle a task, informed by knowledge graph context and quality signals |
| **Compose** | Orchestrator Agent | Primitives → Ensemble | Create a new ensemble at runtime from available model profiles, scripts, and other ensembles |
| **Invoke (Dynamic)** | Orchestrator Agent | Ensemble | Execute an ensemble via tool call, outside the ensemble reference graph. Not governed by Invariant 7 |
| **Summarize** | Result Summarizer | Ensemble Output | Condense ensemble results before they enter the orchestrator's context window |
| **Compact** | Session | Conversation History | Compress prior turns when the orchestrator's context exceeds a threshold, preserving tool-call correlations |
| **Inject** | Serving Layer | Knowledge Graph → System Prompt | Query Plexus at session start and load relevant context into the orchestrator's system prompt |
| **Ingest** | Plexus (lib) | Source Material | Load file content into the knowledge graph. Driven by the client (push model) |
| **Enrich** | Plexus | Ingested Content | Extract signal from source material asynchronously, reinforcing strong patterns in the graph |
| **Query** | Orchestrator Agent | Knowledge Graph | Ask Plexus for knowledge during reasoning -- specific lookups when the orchestrator identifies a gap |
| **Record** | Orchestrator Agent | Routing Decision / Outcome | Store a routing decision, quality signal, or execution outcome in the knowledge graph with provenance |
| **Calibrate** | Orchestrator Agent | New Ensemble | Evaluate a newly composed ensemble over its first N invocations, checking results before the ensemble earns trust |
| **Stabilize** | Knowledge Graph | Pattern | Organically promote a repeated, quality-gated pattern to a reliable library entry -- not an explicit action but an emergent property |
| **Bootstrap** | Operator | Knowledge Graph | Pre-populate the graph from existing context (library contents, execution artifacts) via ingestion and enrichment |
| **Emit (tool call)** *(Cycle 6 candidate)* | Orchestrator Agent | Tool call into response stream | Surface the tool-call structure into the response stream before dispatching. Candidate observable point for **tool-call-emit logging** (one of two liveness-signal patterns); pending DECIDE on T15 (observability event-routing). |
| **Heartbeat** *(Cycle 6 candidate)* | Serving Layer | Operator terminal | Emit a "still waiting on cloud LLM" line when a request has been open for more than N seconds without tool activity. Candidate observable point for **inference-wait heartbeat** (the second liveness-signal pattern). Pending DECIDE on T15. |
| **Route event** *(Cycle 6 candidate; placeholder verb for the operation T15 deliberates)* | Event routing surface | Dispatch event → Destination | Route a typed dispatch event (`TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`, or candidate `DispatchTiming`) to one or both observability destinations (operator-terminal, orchestrator-context). The actor and exact verb stabilize after DECIDE deliberates the unified-substrate architecture per Inversion N+2. |

## Relationships

- An **Orchestrator Agent** *runs within* exactly one **Session**
- A **Session** *has* one **Budget** (turn limit + token limit)
- An **Orchestrator Agent** *uses* **Orchestrator Tools** (fixed set)
- An **Orchestrator Agent** *routes* tasks to **Ensembles** via **Routing Decisions**
- An **Orchestrator Agent** *composes* new **Ensembles** from **Primitives** (profiles, scripts, ensembles)
- A **Routing Decision** *is recorded in* the **Knowledge Graph** with provenance
- A **Quality Signal** *is attached to* a **Routing Decision** or execution outcome
- **Result Summarization** *condenses* **Ensemble** output before it enters the **Session** context
- **Conversation Compaction** *compresses* prior turns within a **Session**
- **Context Injection** *loads* **Knowledge Graph** data into the **Orchestrator Agent's** system prompt
- **Ingestion** *loads* source material into the **Knowledge Graph** (push model, client-driven)
- **Enrichment** *extracts signal from* ingested content asynchronously
- **Stabilization** *emerges from* accumulated **Quality Signals** over multiple **Routing Decisions**
- **Dynamic Invocation** *bypasses* the **Ensemble Reference Graph** (project-level Invariant 7 does not govern it)
- A composed **Ensemble** that includes ensemble-to-ensemble references *must satisfy* project-level Invariant 7 at load time
- The **Orchestrator Agent** *is parameterized by* a **Model Profile** (the system orchestrates itself)
- **Bootstrapping** *pre-populates* the **Knowledge Graph** via **Ingestion** + **Enrichment**
- The **Library** *is the source of* **Primitives** the orchestrator draws from
- **Calibration** *gates* a composed **Ensemble** before it earns trust
- **Typed dispatch events** (`TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`) *emit at* the dispatch surface and *write to* `execution.json` artifacts *(Cycle 5 BUILD shipped)*
- **Typed dispatch events** *route to* observability **Destinations** (operator-terminal, orchestrator-context) — destinations are routing endpoints of one shared emission infrastructure per **Inversion N+2** in product-discovery *(Cycle 6; T15 deliberation)*
- **Dispatch timing** (candidate) *attaches to* an **Ensemble Invocation** and *enables* the orchestrator-context destination to answer dispatch-graph questions *(Cycle 6; pending T15 DECIDE)*
- **Liveness signals** (tool-call-emit logging, inference-wait heartbeat) *complement* completion events and *fire during* the in-flight interval, not at completion *(Cycle 6; pending T15 DECIDE)*
- **Output-spec drift** *occurs at* the **Ensemble Response** boundary — synthesizer agent deviation, orchestrator restructuring, or natural-language narration *may produce* spec divergence *(Cycle 6; pending T16 / DECIDE)*
- **Artifact-as-substrate** (candidate commitment) *amends* the **Ensemble Response** contract to carry `{artifact_path, content_type, summary}` instead of inline content, and *collapses* three findings (output-spec drift, information-finding overhead, AS-7 summarizer content-stripping) *(Cycle 6; pending T16 / DECIDE)*

## Invariants

*Project-level invariants 1-14 remain in force. The following are specific to agentic serving:*

**AS-1. Dynamic invocations are outside the ensemble reference graph.** The orchestrator agent's tool-mediated ensemble invocations are analogous to CLI invocations -- they are not ensemble references in the Invariant 7 sense. Invariant 7 governs static YAML composition, not the orchestrator's runtime tool calls.

**AS-2. Composed ensembles must be validated before loading.** When the orchestrator composes a new ensemble that includes ensemble-to-ensemble references, those references must be validated against the existing ensemble reference graph before the ensemble is loaded. This satisfies project-level Invariant 7 for the composed ensemble's internal structure.

**AS-3. Budget enforcement is a control plane concern.** Turn limits and token limits are enforced at the session level, checked at each iteration of the ReAct loop, regardless of what the orchestrator LLM decides to do. These are harness-level circuit breakers, not model-level parameters.

**AS-4. Ingestion boundary is source material.** *(Applies when Plexus is active.)* The knowledge graph ingests file content (source material), not LLM-generated summaries or interpretations. Quality emerges from the enrichment pipeline, not from upstream curation of what gets ingested.

**AS-5. Quality signals govern stabilization, not frequency.** *(Applies when Plexus is active.)* A pattern does not stabilize into a reliable library entry by being used often. Stabilization requires accumulated quality signals demonstrating effectiveness. Frequency without quality is noise.

**AS-6. The orchestrator composes from existing primitives only.** The orchestrator agent can compose new ensembles from model profiles, scripts, and other ensembles that exist in the library. It cannot author arbitrary scripts or create new model profiles. The composable surface is fixed; only the arrangement is dynamic.

**AS-7 (amended 2026-05-15 via ADR-025). Result summarization is the default; substrate-routed ensembles skip content summarization.** Full ensemble result dictionaries must be summarized before entering the orchestrator's context — *unless* the ensemble routes its deliverable through artifact-as-substrate (per ADR-025), in which case the envelope's `primary` and `artifacts[0]` already carry summary-shaped content and the result-summarizer ensemble is not invoked. For inline-response ensembles (system ensembles per ADR-025; future ensembles opting out via `output_substrate: inline`), summarization remains correctness-required to prevent context rot. The original AS-7 ("Result summarization is a correctness requirement... unsummarized results cause context rot") is preserved within the inline-response scope; substrate-routing dissolves the concern at the substrate layer.

**AS-8. Plexus is optional.** The orchestrator agent, serving layer, budget enforcement, result summarization, conversation compaction, and ensemble composition all function without Plexus. When Plexus is absent, the orchestrator operates statelessly -- routing by reasoning rather than retrieval, with no cross-session memory, no stabilization, and no bootstrapping. Plexus transforms a stateless orchestrator into a learning one, but is not a prerequisite for agentic serving.

## Open Questions

1. **Knowledge-compensated model selection.** Can well-orchestrated smaller models + a populated knowledge graph compete with frontier models on routing quality while winning on cost? This is the most commercially relevant question and is testable: run identical tasks with and without Plexus context across model tiers. *(Source: reflections, product discovery tension #1)*

2. **What form does visibility take?** The operator wants to see what the orchestrator is doing -- for debugging, understanding, and tinkering. Structured events? Logs? Interactive dashboard? Information surfaced in the tool's output? The answer shapes the serving layer's event model. *(Source: product discovery tension #5)*

3. **Internal ReAct loop vs. external MCP model.** The essay chose the internal orchestrator as the pragmatic entry point. The conductor skill already implements the external model. Is the gap genuinely the execution model, or is it summarization quality? If fixing summarization is cheaper than building an internal agent loop, the architecture simplifies dramatically. *(Source: product discovery assumption inversion #5)*

4. **Bootstrapping quality.** How well does the knowledge graph bootstrap from existing context vs. learning through use? Structural bootstrapping (what exists) is straightforward. Behavioral bootstrapping (what works) requires execution history. Pattern bootstrapping (why things work) requires the conductor's accumulated knowledge -- which may not be ingestible. *(Source: product discovery tension #6)*

5. **Organic stabilization mechanism.** How does the knowledge graph distinguish a genuinely good pattern from one that was merely used often? What constitutes a quality signal in this context -- execution success, user satisfaction, task completion rate, something else? *(Source: product discovery tension #7)*

6. **Orchestration depth.** Is orchestration invariably a broad AND deep task requiring frontier models, or does accumulated knowledge eventually bring it within reach of consumer-hardware models for routine decisions? The answer determines the long-term cost model. *(Source: product discovery feed-forward signal #13)*

7. **Enrichment pipeline maturity.** The model currently asks enrichment to carry two loads: quality gate for ingestion (AS-4) and signal source for stabilization (AS-5). Plexus's enrichment model is under active investigation. If enrichment is weak, both invariants are hollow -- the graph fills with undifferentiated content and stabilization degrades to frequency-counting. This is not a blocker (AS-8 means the system works without Plexus) but determines whether the learning-system value proposition is real. *(Source: MODEL reflection gate)*

8. **Per-ensemble lens registration and intra-session cross-talk.** Plexus's query surface takes the shape of consumer-registered *lens grammars* — specifications that govern how content is encoded during enrichment and queried back. The prevailing assumption has been that llm-orc registers a single adapter/lens. If individual ensembles (or agents within an ensemble) can register their own lenses over the same Plexus context, the orchestrator's access becomes polyglot — each subagent writes into the shared enrichment through its own grammar and queries through its own lens. This shifts Plexus's value from cross-session memory alone to an intra-session multi-agent substrate. A sub-question travels with it: does KG-mediated communication via lens grammars avoid the bias-reinforcement effect documented in direct agent-to-agent communication, or do lens grammars themselves become a vector for bias? Depends on Plexus's lens specification work, which is in-progress. *(Source: DECIDE reflection gate, 2026-04-19)*

9. **How is the capability floor surfaced — static spec, runtime probe, or both?** The default `orchestrator-local` Model Profile may not meet the capability floor required to invoke the orchestrator's closed tool surface (play 2026-04-24 verdict: *"I would not be likely to use it again"*). Three design surfaces are open and compatible: (a) **static specification** — write the floor down in an ADR, scenario, or startup check; (b) **runtime probe** — ship a calibration ensemble that runs at install/startup, tests whatever local models exist against a baseline, and produces operator-readable recommendations when any tested profile falls below the floor; (c) **both** — static spec for the abstract floor, runtime probe for the concrete-against-operator-hardware mismatch detection. The floor's dependence on the operator's available local-model hardware makes pure design-time specification incomplete. *(Source: product discovery tension #8 + capability floor vocabulary refinement; carries to DECIDE as scenario candidates against the orchestrator-capability-floor missing-scenario)*

10. **Where does cheap-cloud-orchestrator-as-routing-layer end and "fallback to cloud" begin?** The architecture's local-first commitment exists to deliver local leverage; cloud-orchestrator-only mode "is not much different from using something like Sonnet, though it's cheaper" — failing the local-leverage value the architecture exists to deliver. But the cheap *cloud* orchestrator is itself the routing tier across the cycle's research findings (cheap-cloud-orchestrator routes; local models amplify deterministic and bounded-scope ensemble work). The boundary between value-preserving deployment and value-eroding fallback is unresolved. *(Source: product discovery tension #9 — operator-voice translation of essay 005's "local-first as load-bearing value" framing; the analyst frame for the same boundary appears in §Methodology Vocabulary as the architectural rationale)*

11. **Calibration signals as quality lever vs. feedback bias compounding.** ADR candidate #6's proposed cross-layer calibration channel routes ensemble-output signals upward to inform dispatch decisions. The mechanism is potentially powerful (in-process trajectory-level calibration). The risk is concrete: feedback paths can compound bias (Khanal et al.'s universal non-improvement from episodic memory; CAAF's prompt-engineering-artifact finding; Li et al.'s trigger-vulnerability finding). Five bounding mechanisms have been named as load-bearing rather than decorative. Open question: how much architectural cost is appropriate to bound a feedback risk that is real but unmeasured at the cycle's deployment shape? *(Source: product discovery tension #10; resolution territory for DECIDE pressure-testing of ADR candidate #6's bounding mechanisms (b) time-decay windowing and (d) out-of-band audit dispatch)*

12. **Whether ADR-002's layering rule admits a calibration-channel exception.** The architecture's "edges never upward" rule (stated in `system-design.md` and verified by FC-2 / FC-3; ADR-002 establishes the four-layer architecture but the explicit edge-direction rule lives in the system design) is the layering invariant ADR candidate #6 proposes amending. The amendment's scope is narrow (read-only, signal-channel-specific, calibration only) but consequential in principle. *(Source: essay 005 §"ADR candidate #6"; resolution territory for DECIDE belief-mapping per Cycle 4 research-gate Grounding Action 2 — the elaboration-vs-reorganization architectural choice posed as DECIDE-entry framing commitment)*

13. **Pending evidentiary review of AS-7's correctness-requirement framing.** Wave 3.A Trial 1 produced a single-trial specificity-loss observation that suggests but does not establish that ADR-004's "mandatory" stance on result summarization should become "default-with-conditional-skip." If a follow-up spike on diverse output sizes and ensemble configurations corroborates the specificity-loss observation, AS-7's "correctness requirement" framing may need to weaken to "default correctness mechanism with documented skip conditions." Below evidentiary threshold for amendment until the targeted spike completes. *(Source: Cycle 4 DECIDE carry-forward #5; spike candidate is Cycle 5+ territory unless DECIDE elects to run it before drafting ADR candidate #5)*

14. **Grounding mechanisms for cross-layer stages beyond ADR-016's L0→L1 channel.** Cycle 4 DECIDE invested heavily in grounding the L0→L1 calibration channel with five bounding mechanisms (a)–(e), spike validations, a concrete monitoring specification, and a falsification trigger. The cycle's other cross-layer stages — L1→L2 verdict→router (ADR-014→ADR-015), L3 cross-session artifact set (ADR-013), intra-L2 conversation-history boundary (ADR-012), orchestrator-response→tool-dispatch boundary (ADR-017), and L1→L4 Plexus integration — received the rigor that adoption-decision discipline produces (typed errors, structural validation, scope conditions) but not the operational grounding (precedent classification + readiness asymmetry + spike-validation pathway + falsification trigger) that ADR-016 received. The asymmetric-rigor risk is that ADR-016's bounding mechanisms operate in an environment where other cross-layer interfaces are uncontrolled variables. What would mechanism-(a)-(e)-style grounding look like for the other stages? Are the absent grounding mechanisms necessary, or is the asymmetric rigor justified by stage-specific characteristics (e.g., ADR-014→ADR-015 is a deterministic data-flow that may not need probabilistic bounding mechanisms)? *(Source: Cycle 4 DECIDE epistemic gate, 2026-05-08, practitioner pre-mortem response on ADR-016 falsification scenario; carries to Cycle 5+ as research-question-shape territory before any cross-layer stage moves to BUILD)*

    **Partial closure for the L1→L2 verdict→router stage (per ADR-018, 2026-05-11).** Spike β (research log `005h-spike-bounding-mechanism-transfer-l1-l2.md`, architect-gate continuation 2026-05-11) audited whether ADR-016's bounding mechanisms (a)–(e) transfer analytically to the L1→L2 verdict→router edge. Disposition: partial transfer — three mechanisms achieved without new architectural work ((a) by Router's stateless `select_tier` design; (b) operationalized one layer upstream at Calibration Gate per ADR-014; (e) by FC-17 typed-error infrastructure), one structurally inapplicable ((c) wrong layer for the anchor substrate), one transfers cleanly with novel design work — mechanism (d) periodic out-of-band audit dispatch. ADR-018 records the (d)-analog audit dispatch as an amendment to ADR-015's Tier-Escalation Router responsibilities (three drift criteria including the *escalation-vs-outcome correlation* criterion that is also the Sub-Q6 evidence surface). The L1→L2 stage moves from "BUILD evidence will inform" to "inline grounding mechanism specified as a WP-G4 design driver". The four other under-grounded cross-layer stages (L3 cross-session artifact set per ADR-013; intra-L2 conversation-history boundary per ADR-012; orchestrator-response→tool-dispatch boundary per ADR-017; L1→L4 Plexus integration) remain Cycle 5+ research territory.

15. **Parallel amendment pathway for invariant AS-7 via T16's artifact-as-substrate scope deliberation.** OQ #13 already flags AS-7 ("Result summarization is a correctness requirement") as pending evidentiary review under Wave 3.A Trial 1's specificity-loss observation. Cycle 6 T16 (Ensemble-response contract: content-bearing vs. artifact-as-substrate) opens a **second, structurally distinct amendment pathway**: if T16 resolves toward artifact-as-substrate at any scope (always / when substantive / operator-configured), AS-7's "result summarization is a correctness requirement" framing weakens — the summarizer summarizes the artifact's *metadata*, not its content, which makes the AS-7 invariant's load-bearing concern (context rot from unsummarized result dictionaries) inapplicable to substrate-routed ensembles. The two pathways converge on the same potential AS-7 amendment but through different evidentiary chains: OQ #13's pathway is specificity-loss + evidentiary threshold; T16's pathway is contract amendment + dissolution-by-design. DECIDE deliberation on T16 should examine the interaction explicitly — if T16 resolves toward artifact-as-substrate at the *always* scope, AS-7 likely amends to a default-with-conditional-skip framing for the inline-response path that remains for non-substrate ensembles; if T16 resolves toward *when substantive* or *operator-configured*, AS-7 amendment is scoped to the substrate-routed subset only. **Constitutional implication**: this is the first Cycle 6 question that may amend a project-level-scoped invariant; DECIDE deliberation must check the propagation (which ADRs and code reference AS-7 directly). *(Source: Cycle 6 MODEL phase, 2026-05-14; T16 scope sub-question (0) per snapshot Action 4; cross-references OQ #13 for the prior evidentiary pathway.)*

## Amendment Log

| # | Date | Invariant | Change | Propagation |
|---|------|-----------|--------|-------------|
| -- | -- | -- | Initial scoped model, no prior version | -- |
| 1 | 2026-04-17 | AS-8 (Plexus is optional) | Added. Plexus transforms stateless orchestrator into learning one, but is not prerequisite. AS-4 and AS-5 scoped to "when Plexus is active." | No prior ADR contradictions. Essay's four-layer architecture (Layer 4 = Knowledge Graph) should note optionality. |
| 2 | 2026-05-05 | (no invariant change) | Added §Methodology Vocabulary section to capture research-voice terms used in essays and ADR drafting that the operator does not work with operationally. Five entries: layer-conditional composition (essay 005), MOP / meltdown-on-paradox (Khanal et al.), externalized structured state (proposed relocation from product-discovery vocabulary, pending practitioner gate adjudication), cross-layer calibration channel (proposed, pending DECIDE on ADR candidate #6), bounding mechanisms a–e (proposed, pending DECIDE on ADR candidate #6). | Source: Cycle 4 discover-gate Specific Commitment #1 (relocate "layer-conditional composition" and "MOP / meltdown-on-paradox" to domain-model ubiquitous language with essay-005 attribution); cycle-status §Discover-phase MODEL feed-forward items #1, #2, and the bounding-mechanism vocabulary check. No invariant amendment; methodology-vocabulary expansion only. |
| 3 | 2026-05-05 | (no invariant change) | Added Open Questions #9 (capability-floor design surfaces), #10 (cheap-cloud-orchestrator-as-routing-layer vs. fallback boundary), #11 (calibration signals as quality lever vs. feedback bias compounding), #12 (whether ADR-002's layering rule admits a calibration-channel exception), #13 (pending evidentiary review of AS-7 framing). | Propagation: questions #9–#11 propagate unresolved value tensions from `product-discovery.md` (tensions #8, #9, #10 in the Cycle 4 update); #12 propagates the Cycle 4 research-gate Grounding Action 2 belief-mapping into DECIDE-resolution territory; #13 propagates the Cycle 4 DECIDE carry-forward #5's evidentiary threshold flag into the model's open-question record. No invariant amendment. |
| 4 | 2026-05-05 | (no invariant change) | MODEL-phase evaluation of three borderline product-discovery vocabulary terms (per cycle-status §Discover-phase MODEL feed-forward item #2), confirmed at MODEL gate: "tier escalation" — operator voice (an operator configuring confidence-gated routing uses this term); kept in product vocabulary. "Initializer-then-resume" — operator voice (an operator running session-bootstrap names what happens with this phrase); kept in product vocabulary. "Externalized structured state" — research voice (the operator works with `feature_list.json`, `claude-progress.txt`, `init.sh` by name; the abstraction is the analyst's category-level term); relocated to §Methodology Vocabulary. Practitioner warrant: experience with RDD's own externalized-state surface — operator-experience reaches for the artifact names. | Two of three retained in product vocabulary as operator voice; one relocated. `product-discovery.md` vocabulary table updated to remove the "externalized structured state" row with a cross-reference note pointing to domain-model §Methodology Vocabulary. |
| 5 | 2026-05-06 | (no invariant change) | Cycle 4 DECIDE close: methodology vocabulary entries for "Cross-layer calibration channel" and "Bounding mechanisms (a–e)" promoted from "proposed; pending DECIDE" to "conditionally accepted via ADR-016" with synthetic-data and structural-transfer validation outcomes (spikes (b) and (d), 2026-05-06) integrated into the entry text. Mechanism-specific readiness asymmetry preserved. ADR-002 received `> Updated by ADR-016 on 2026-05-06.` partial-update header; the layering rule's "edges never upward" property amended for the read-only signal-channel exception. | Cross-references: ADR-016, ADR-014 (data-flow consumer), ADR-015 (downstream router), research logs `005e-` and `005f-`. No invariant amendment; ADR-002 partial-update only. |
| 6 | 2026-05-06 | (no invariant change) | Cycle 4 DECIDE close: new domain-vocabulary terms introduced by the six new ADRs (ADR-012 through ADR-017) added to §Concepts and §Methodology Vocabulary as appropriate. New §Concepts entries: structured-handoff artifact (ADR-013); write-gate validation (ADR-013); cluster determination at session-start (ADR-013); calibration verdict (ADR-014; trichotomy: Proceed / Reflect / Abstain); tier-escalation router (ADR-015); per-skill tier default (ADR-015); tool-call structural validation guard (ADR-017); phantom tool-call (ADR-017). New §Methodology Vocabulary entries: in-process trajectory-level calibration (ADR-014); trajectory feature (ADR-014); Topaz skill profile (ADR-015). | Cross-references: ADR-012 through ADR-017 each with a Vocabulary Impact subsection in the Provenance check. No invariant amendment; vocabulary expansion only. |
| 7 | 2026-05-08 | (no invariant change) | Added Open Question #14 (Grounding mechanisms for cross-layer stages beyond ADR-016's L0→L1 channel). The cycle's DECIDE-gate pre-mortem surfaced asymmetric grounding-mechanism rigor across the cycle's cross-layer stages — ADR-016 received 5 bounding mechanisms with operational grounding, while ADR-014→ADR-015, ADR-013, ADR-012, ADR-017, and the Plexus integration boundary received less rigor. OQ #14 carries the question forward as research-question-shape territory for Cycle 5+. | Source: Cycle 4 DECIDE-gate (2026-05-08) practitioner pre-mortem response on ADR-016 falsification scenario. Cross-references: gate reflection note `housekeeping/gates/cycle-4-decide-gate.md` §"Asymmetric-grounding finding". No invariant amendment. |
| 8 | 2026-05-11 | (no invariant change) | OQ #14 partial closure for the L1→L2 verdict→router stage. Spike β (architect-gate continuation, research log `005h-spike-bounding-mechanism-transfer-l1-l2.md`) audited bounding-mechanism (a)–(e) transfer to the verdict→router edge; disposition partial transfer with mechanism (d) as the load-bearing addition. ADR-018 records the (d)-analog audit dispatch as an amendment to ADR-015's Tier-Escalation Router responsibilities. The L1→L2 stage moves from "BUILD evidence will inform" to "inline grounding mechanism specified as a WP-G4 design driver". Sub-Q6 (autonomous-routing evidence gap, ADR-015 §Consequences §Neutral) is coupled — the (d)-analog's *escalation-vs-outcome correlation* drift criterion is the operational measurement that closes Sub-Q6's evidence gap on first-deployment evidence. Four other under-grounded cross-layer stages remain Cycle 5+ research territory. | Source: Cycle 4 architect-gate continuation 2026-05-11, Spike β analytical transfer audit. Cross-references: ADR-018; ADR-015 (partial-update header + §Consequences §Neutral Sub-Q6 coupling); system-design.agents.md (Tier-Escalation Router module extension; FC-19 and FC-20 added); roadmap.md (WP-G4-2 sub-WP added). Spike α (research log `005g-spike-topaz-skill-classification.md`) concurrently confirmed ADR-015 primary-skill framing stands (21 of 21 clean-primary classifications); no amendment to that aspect of ADR-015. No invariant amendment. |
| 9 | 2026-05-12 | (no invariant change) | Cycle 5 DECIDE/BUILD vocabulary additions for the skill-framework-agnostic library reshape (ADR-019 / ADR-020 / ADR-021). New §Concepts entries (operator-facing): **Capability ensemble** (operation-named, `topaz_skill`-tagged ensemble dispatchable by any skill framework — the library-content unit operators author and reason about); **Skill orchestration user / Methodology consumer** (the human role that runs a skill orchestration process — a client-side framework that decomposes higher-level workflows into capability-typed sub-tasks; distinct from Tool User and Ensemble Author / Operator; humans wear multiple roles concurrently); **Operation-named ensemble** (naming principle for capability ensembles — verb-noun phrase naming the operation, not the methodology that consumes it). New §Methodology Vocabulary entries (research-voice / structural): **Three-layer architecture** (skill orchestration layer / capability dispatch layer / capability instance layer; the load-bearing separation ADR-019 establishes); **Skill-framework-agnostic dispatch** (the orchestrator's architectural commitment — routes by Topaz skill + calibration verdict without knowing which skill framework is composing against it); **Per-capability dispatch contract** (ADR-021's skill-framework-composition shape — each capability sub-task is a single `invoke_ensemble` call; skill framework owns workflow state client-side). Vocabulary disposition at BUILD close: concrete library authoring confirmed the operator/structural fit; entries remain settled. | Source: Cycle 5 DISCOVER gate close (2026-05-12); Cycle 5 DECIDE close (ADR-019/020/021 + scenarios + interaction-specs + skill-framework-capability-registry); Cycle 5 BUILD close (concrete library authoring confirmed the vocabulary's operator-voice fit). Cross-references: ADR-019, ADR-020, ADR-021; `skill-framework-capability-registry.md`; `product-discovery.md` Cycle 5 update (vocabulary three-tier disposition); `.llm-orc/ensembles/agentic-serving/README.md` (operator-facing usage of the operation-named principle). No invariant amendment; vocabulary expansion only. |
| 11 | 2026-05-15 | **AS-7 (Result summarization is a correctness requirement)** — amended to **default-with-conditional-skip** | Cycle 6 DECIDE close: substrate-routed capability ensembles per ADR-025 skip content summarization at the substrate layer (the envelope's `primary` and `artifacts[0]` already carry summary-shaped content; the result-summarizer ensemble is not invoked for substrate-routed dispatches). Inline-response ensembles (system ensembles per ADR-025; future ensembles opting out via `output_substrate: inline`) retain mandatory summarization to prevent context rot. The amendment resolves OQ #15 by codification and narrows OQ #13's scope to inline-response ensembles only. | Partial-update headers added to ADR-004 (Result Summarization Mandatory) and ADR-021 (Skill-Orchestration Composition via Per-Capability Dispatch) recording the conditional-skip. AS-7 invariant text in §Invariants updated to default-with-conditional-skip framing with the original "correctness requirement" language preserved within the inline-response scope. Other AS-7 references in essays 001/002/003, ADR-002, ADR-007, ADR-012, ADR-014 are contextual rather than load-bearing assertions of unconditional invariance; no supersession notes added (the Amendment Log entry is the load-bearing record). Cross-references: ADR-024 (envelope contract) and ADR-025 (substrate scope); spike α / β / γ research logs as substrate; cycle-status Spike findings subsection records the per-finding test result. |
| 10 | 2026-05-14 | (no invariant change at MODEL; AS-7 amendment pathway flagged via OQ #15) | Cycle 6 MODEL vocabulary expansion (mini-cycle, ensemble contract + observability + routing-preference scope). New §Concepts entries: **Liveness signal** (operator-voice; pairs with completion events; two named candidate patterns — tool-call-emit logging, inference-wait heartbeat); **Dispatch timing** (Cycle 6 candidate; surfaced by snapshot Action 2 field-read as event-model extension required for the orchestrator-context destination per Inversion N+2's unified-substrate framing of T15); **Bilateral visibility absence** (operator-experience term pairing with the architectural-framing term in §Methodology Vocabulary); **Information-finding overhead** (practitioner-voice operator-experience metric); **Routing preference / operational routing preference** (Cycle 6 candidate; three operationally distinct dispositions pending T14 DECIDE — intended scope, defect to remediate, configuration-conditional behavior; empirically conditional on spike γ); **Explicit-naming bypass** (Cycle 6 candidate; pending T14 DECIDE); **Output-spec drift** (Cycle 6 candidate; pending DECIDE — dissolves under T16 artifact-as-substrate resolution); **Artifact-as-substrate** (Cycle 6 candidate architectural commitment; T16 scope is first DECIDE sub-question per snapshot Action 4; may trigger AS-7 amendment per OQ #15). New §Methodology Vocabulary entries: **Infrastructure-complete / routing-incomplete** (Cycle 5 PLAY susceptibility-snapshot reframing, sharpened by Cycle 6 DISCOVER Action 2 field-read); **Common I/O envelope** (Cycle 6 candidate; spike α / β analytical substrate; field names `envelope` / `primary` / `artifact reference` / `diagnostics`). Three new actions (candidate): **Emit (tool call)**, **Heartbeat**, **Route event** — all pending DECIDE on T15. Eight new relationships covering typed dispatch events → observability destinations + dispatch timing as event-model extension + liveness signals as in-flight complement + output-spec drift / artifact-as-substrate at the ensemble-response boundary. **New Open Question #15** opens a parallel amendment pathway for invariant **AS-7** ("Result summarization is a correctness requirement") via T16's artifact-as-substrate scope deliberation; converges with OQ #13's evidentiary pathway. Vocabulary disposition: 4 §Concepts entries settled at DISCOVER (liveness signal, bilateral visibility absence, information-finding overhead, plus dispatch timing as candidate-but-low-controversy); 4 candidate-pending-DECIDE; 2 §Methodology Vocabulary entries (one settled framing reframe, one analytical substrate). | Source: Cycle 6 DISCOVER gate close (2026-05-14); cycle-status §From Cycle 6 DISCOVER feed-forward signals; susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-6-discover.md` with four grounding actions applied; gate reflection `housekeeping/gates/cycle-6-discover-gate.md`. Cross-references: `product-discovery.md` Cycle 6 update (Tensions 14-17 + Inversions N+1 through N+4); `housekeeping/cycle-status.md` (Cycle 6 active entry). No invariant amendment at MODEL; OQ #15 flags a downstream amendment pathway. |
