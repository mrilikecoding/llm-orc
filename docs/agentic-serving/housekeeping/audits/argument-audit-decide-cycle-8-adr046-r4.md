# Argument Audit Report — R4 (Re-audit After Revision)

**Audited document:** `docs/agentic-serving/decisions/adr-046-target-architecture-per-turn-handler-one-ensemble.md` (revised 2026-07-01, incorporating R1 + R2 + R3, including the full §3 component-level re-derivation)
**Predecessors:** `housekeeping/audits/argument-audit-decide-cycle-8-adr046.md` (R1: 4 P1, 7 P2, 2 P3; verdict CONTINUE); `housekeeping/audits/argument-audit-decide-cycle-8-adr046-r2.md` (R2: 4 P1, 3 P2, 4 P3; verdict CONTINUE); `housekeeping/audits/argument-audit-decide-cycle-8-adr046-r3.md` (R3: 1 P1, 2 P2, 0 new P3; verdict CONTINUE)
**Source material re-verified (full-body reads this round):** ADR-001 (header only, cross-check), ADR-002, ADR-003, ADR-006, ADR-007, ADR-009, ADR-010, ADR-011, ADR-012, ADR-013, ADR-014, ADR-015, ADR-016 (incl. WP-H4 close disposition), ADR-017, ADR-018, ADR-023, ADR-024, ADR-025 — all full text, not summary. `domain-model.md` (§Invariants AS-2/AS-5, §Open Questions #11/#12/#14, Amendment Log entries #3/#5/#7/#11/#22, the "Cross-layer calibration channel" Methodology Vocabulary entry). `housekeeping/cycle-status.md` (Cycle 8 DECIDE narrative + the Phase Status table).
**Genre:** ADR
**Date:** 2026-07-01 (R4)
**Scope:** Focused clean-pass re-verification per dispatch brief — confirm R3's fixes landed (5 change-sets), adversarially check whether the revision minted a fourth instance of the bundling pattern this audit sequence caught three rounds running (R1 AS-5, R2 AS-2, R3 ADR-014/AUQ-HTC), and independently spot-check the "full §3 component-level re-derivation found no 4th P1" claim against primary source. Not a from-scratch re-audit.

## Section 1: Argument Audit

### Summary

- **Genre:** ADR
- **Argument chains re-examined:** the five R3→R4 change-sets (ADR-014 AUQ/HTC resolution; `list_ensembles` disposition; ADR-009/010 backward-prop entry; the three deferred R2 P2s — rows 011/012/024/025; ADR-016's Sweep-Responsibility cycle-status entry); adversarial component-by-component re-derivation of every §3 row against its source ADR's full Decision text (003, 006, 007, 009/010, 011, 012, 013, 014, 015, 016, 017, 018, 023, 024, 025, 002).
- **R3 fix verification:** all five named change-sets land as claimed, verified against primary source text (see table below). No regression on any of R1/R2/R3's prior fixes (spot-checked against the current text; all remain byte-consistent with what R3 confirmed).
- **The adversarial component check found one residual gap of the same species as R1 AS-5 / R2 AS-2 / R3 ADR-014 — but narrower in kind and consequence.** Row 024's fix names `diagnostics.calibration_verdict` as "goes with the gate" but the envelope's `diagnostics` dict also carries `audit_findings` (list[dict]), which is wired to ADR-018's now-superseded Tier-Router-Audit mechanism the same way `calibration_verdict` is wired to 007/014's gate — and this second stale sub-field was not caught by the fix or added to the backward-propagation-owed list. Unlike R1-R3's findings, this does not strand an independently-valuable, actor-agnostic mechanism (there is nothing to carry forward — the audit-dispatch data has no successor use once ADR-018 is retired); it is a documentation-completeness gap of the same kind R2's P2-2 and R3's P2-new-2 already named and rated P2. No fourth P1 surfaces.
- **The full §3 re-derivation claim is substantially — not perfectly — accurate.** Independent re-derivation of every reclassified and every "un-swept" row (007, 012, 013, 015, 016, 017, 018, 023, 024, 025) against each source ADR's numbered Decision content corroborates ADR-016 as a clean supersession (all five bounding mechanisms are calibration-channel-specific, none has independent standing the way AS-2/AS-5/AUQ-HTC did) and ADR-013/ADR-017 as clean survivals. One residual gap found (above); one minor under-specification found (row 012's five-layer compaction pipeline is credited a session-substrate referent but not walked through layer-by-layer for whether Layer 1's cache-edit mechanism, specified for a continuously-growing LLM conversation prefix, still has a referent under discrete per-turn passes — rated P3, not P2, since nothing valuable is being silently dropped, only under-specified).
- **Issues found this round:** 0 P1 (new), 1 P2 (new), 1 P3 (new); 4 P3 items carried forward unresolved from R1/R2/R3 (unchanged in the current text, confirmed by direct comparison).
- **Pyramid coverage map:** N/A (ADR genre, not Essay-Outline).
- **Expansion-fidelity findings:** N/A (ADR genre, not Essay-Outline).

### R3-fix-verification table (the five change-sets named in the dispatch brief)

| Change-set | Recommendation (R3 / practitioner) | Landed? | R4 verification |
|---|---|---|---|
| 1. ADR-014 AUQ/HTC | Either argue explicitly that output-contracts suffice, or name ADR-014/AUQ/HTC as a Q2 candidate | **Yes, cleanly (option b taken)** | Row 014 (line 58) now reads "superseded (imperative Proceed/Reflect/Abstain machinery); AUQ/HTC signal → Q2 candidate" — the routing trichotomy is separated from the underlying confidence/trajectory *signal*, and the signal is explicitly "**not assumed subsumed**." §Open's Q2 (line 88) now names it directly: "**ADR-014's AUQ/HTC process-level confidence signal is a named candidate for the additional signal** (it catches contract-conforming-but-anomalous generations the output check can't see). Whether it's needed on top of contract assertions is a PLAY/BUILD validation target." Re-verified against ADR-014 §Decision directly — the trichotomy (Proceed/Reflect/Abstain) is genuinely the imperative control-flow mechanism AS-11 forbids; AUQ confidence + HTC trajectory features are a genuinely distinct data type from that routing logic. The unexamined-equivalence claim R3 flagged is gone; no residual overreach. |
| 2. `list_ensembles` disposition | Add a clause stating whether the capability is judged moot or needs an equivalent | **Yes, cleanly** | Row 003 (line 51): "`list_ensembles` (library enumeration) → **moot as a runtime tool** (no orchestrator LLM browses the library at dispatch time); operator-facing enumeration, if wanted, is Q4 registry tooling, not a serving-turn tool." This is a considered judgment (not a silent omission) and is coherent with §2's dispatch-by-resolved-name design (classify emits a `target` name directly; nothing in the validated skeleton has an LLM call `list_ensembles` to browse candidates at dispatch time). Cross-checked against ADR-003's own five-tool table — all five tools now have a stated disposition somewhere in row 003 (`invoke_ensemble` → dynamic dispatch; `compose_ensemble` → goes with 006; `query_knowledge`/`record_outcome` → engine/script per 009/010; `list_ensembles` → moot/Q4). |
| 3. ADR-009/010 backward-prop entry | Add both ADRs to the backward-propagation-owed list | **Yes, cleanly, in both locations** | Present in the backward-propagation-owed paragraph (line 74): "partial-update headers for **ADR-009/010** (both still cite the retired ReAct loop / ADR-003 tool surface as current premises: 009's Phase-1 tool-first premise is superseded and its Phase-2 hook-point premise needs re-examination; 010's `record_outcome`-path language is stale, the push-model itself unaffected)" and again in §Open (line 82). Re-verified against ADR-009 §Decision directly ("The orchestrator has `query_knowledge` and `record_outcome` as tool calls (per ADR-003)"; "does not require changes to the ReAct loop (ADR-001) or the tool surface (ADR-003)") and ADR-010 §Decision item 2 ("via its bootstrapping and `record_outcome` paths"). Both citations match. |
| 4a. Row 011 crediting (deferred R2 P2-1) | Reclassify with credit to §2's decider-seat design | **Yes, cleanly** | Row 011 (line 54): "superseded (actor-specific wording); principle re-homes — §2's decider-as-seat design is ADR-011's reasoning applied to the seat mechanism." Re-verified against ADR-011 §Decision directly: "the orchestrator agent's LLM is configured via a standard Model Profile... If tiered behavior is desired... it is expressed as a composed ensemble invokable by the orchestrator — not as a mechanism special to the orchestrator." This is, on direct comparison, functionally the same reasoning §2 applies to the decider seat. Not a bundling risk — this fix names an actor-agnostic principle's new home rather than silently retiring it, the opposite direction from the R1-R3 failure pattern. |
| 4b. Rows 024/025 calibration-verdict sweep (deferred R2 P2-2) | Add a caveat line to each row; add both to backward-propagation list | **Yes, structurally; one sub-field still uncovered — see new P2 below** | Row 024 (line 65): "survives (container); `calibration_verdict` field goes with the gate" — caveat present. Row 025 (line 64): "survives (container); calibration-eval subsection goes with the gate" — caveat present, matching ADR-025's own §"Calibration-gate evaluation surface under substrate-routing." Both added to the backward-propagation-owed list (line 74). Re-derivation of ADR-024's full `diagnostics` field set against source found a second calibration/tier-tied sub-field (`audit_findings`) that the fix did not name — see **P2-new-1** below. ADR-025's fix is complete (no comparable second gap found in ADR-025's text). |
| 4c. Row 012 compaction referent (deferred R2 P2-3) | Name the concrete referent or move to `[read]` | **Yes, structurally; layer-by-layer completeness not exhaustive — see new P3 below** | Row 012 (line 67): "survives — deterministic compaction re-homed as a `script:` op — its referent is now **ADR-013's session-scoped progress-log / structured-handoff artifacts**... this bounds the append-only-log growth ADR-013 itself flags as unaddressed. Re-triggered per-request against the session substrate rather than at continuous-loop turn boundaries (the ADR-012 'session start' circuit-breaker reset re-homes to the session-substrate lifecycle)." This names the referent (ADR-013's progress log, cross-checked against ADR-013 §Negative: "Append-only progress-log can grow unboundedly... archival policy is a deployment concern this ADR does not specify" — a genuine, apt connection) and addresses the circuit-breaker reset semantics specifically. It does not walk through whether ADR-012's other four layers (persist-large-tool-results, cache-edit, idle-expiry, LLM-semantic-summary) individually still have a referent under discrete per-turn passes — see **P3-new-1** below. |
| 5. ADR-016 Sweep-Responsibility cycle-status row | Add to backward-propagation-owed list | **Yes, cleanly** | Backward-propagation-owed paragraph (line 74): "for ADR-016 this includes the cycle-status Phase-Status row its own Sweep-Responsibility governance requires, recording the channel status as **superseded**." Repeated in §Open (line 82). Re-verified against ADR-016's own §"Sweep responsibility" (line 267): "The cycle-status `Phase Status` table for any cycle that touches ADR-016 includes a row noting the channel's status (conditional / fully accepted / superseded). Absence of the row constitutes structural evidence that the cycle has not exercised the channel." Cross-checked `housekeeping/cycle-status.md`'s Phase Status table (lines 81-93) directly — it does **not** yet carry this row (correctly so: the fix tracks the obligation as owed, pending the backward-propagation sweep, not as already-executed; ADR-046 makes no claim that it has been executed). |

### Component-by-component pass — full re-derivation independently re-verified

Per the dispatch brief's charge, every reclassified row and every row R3 flagged as "spot-checked, not fully swept" (007, 012, 013, 016, 017, 023, 024, 025) was independently re-derived against its source ADR's full Decision text this round, not merely against the specific clause a prior round's finding named.

- **003 (Fixed Orchestrator Tool Surface).** All five decided tools now dispositioned (see change-set 2 above). No residual gap.
- **006 (Composition Palette / AS-2 / AS-6).** ADR-006 §Decision items 1-2 split cleanly along the authorship-vs-validation line the row draws (`compose_ensemble` sole-entry-point authorship superseded; AS-2's reference-graph/cycle/depth validation survives, needed by Q4). No residual gap.
- **007 (Calibration Gate, post-hoc).** Re-verified directly: "Every ensemble produced by `compose_ensemble` enters a calibration state" — the mechanism is genuinely scoped to the now-retired self-composition surface end to end (calibration state, first-N result-check, promotion eligibility per ADR-008 — all `compose_ensemble`-authored-ensemble-specific). Flat "superseded" holds; no separable actor-agnostic residue found (parallel to ADR-008's control case).
- **009/010/AS-8 (Plexus Integration).** Confirmed consistent (row 003's "engine/script" framing and row 009/010's "substrate survives, access re-homes" no longer contradict each other; re-verified against both ADRs' full text). ADR-009's deferred, never-built Phase 2 (context injection into "the orchestrator's system prompt" at session start) is not separately dispositioned, but since Phase 2 was never implemented and presupposes a session-spanning prompt that has no analog in a per-turn pass, this is a non-issue rather than a stranded requirement (matches R3's own finding on this point; re-confirmed, not escalated).
- **011 (Orchestrator LLM Is a Model Profile).** Confirmed landed (see change-set 4a above).
- **012 (Conversation Compaction).** Confirmed landed with one residual under-specification (see change-set 4c above; P3-new-1).
- **013 (Session Registry).** ADR-013 §Decision's four components (feature-list-with-monotonic-passes, append-only progress-log, init.sh bootstrap, write-gate validation) are all session-persistence mechanisms with no dependency on an internal orchestrator actor — the write-gate's "orchestrator cannot bypass it through prompt compliance" language describes a structural property of the gate, not a claim that an internal ReAct-loop actor is the writer. Row 013's flat "survives" (line 63) holds cleanly; independently re-derived, not merely inherited.
- **014 (Calibration Gate Trajectory Extension).** Confirmed landed (see change-set 1 above).
- **015/018 (Tier-Escalation Router + Audit Dispatch).** Re-verified against both ADR-015 (already self-superseded via its own header: "> Superseded by ADR-045 on 2026-07-01... the tier-router-as-dedicated-L2-Python-layer mechanism... is retired") and ADR-018 (amends ADR-015; its (d)-analog audit dispatch's entire purpose — closing OQ #14 and Sub-Q6 for the verdict→router edge — has no referent once the verdict→router edge itself is retired). Flat "superseded" for both holds. See P2-new-1 below for a related but distinct gap in how ADR-018's audit output propagates into ADR-024's envelope.
- **016 (Upward L0→L1 Signal Channel).** Independently re-derived component-by-component against ADR-016's full text. The channel's sole purpose per its own Context section is feeding "L1 (Calibration Gate dispatch decisions)" — with both the post-hoc gate (007) and the in-process verdict trichotomy (014's routing half) now superseded, there is no more L1 dispatch-decision consumer for the channel to feed. Each of the five bounding mechanisms was checked individually for independent standing (the failure mode this audit sequence has caught three times): (a) fresh-context isolation in the consumer — specified entirely as a property of "the L1 calibration consumer," no residue without a consumer; (b) time-decay windowing — specified for "the bias-compounding horizon" of the cross-layer feedback loop specifically (distinct from, and narrower than, ADR-014's own separate in-layer windowing spec, which is itself moot along with 014's routing half); (c) categorical anchors via deterministic-tool-output — specified as anchoring "the feedback loop against probabilistic drift," ensemble-composition-conditional, no independent domain-model identity (not a named AS-invariant, unlike AS-2/AS-5); (d) periodic out-of-band audit dispatch — specified to detect drift *in the calibration system itself*, no referent without a calibration system; (e) read-only structural validation at the consumer — specified as schema validation "at the channel boundary," no referent without a channel. None of the five mechanisms is independently named in the domain model the way AS-2, AS-5, or AUQ/HTC are (the domain model's only related entry, "Cross-layer calibration channel," names the channel itself, not any individual bounding mechanism, as the Methodology Vocabulary term — and that entry is already tracked as stale in the backward-propagation-owed list). Row 016's flat "superseded" (line 59) holds. Also independently confirmed: ADR-014's own text specifies that when the cross-layer channel is absent, HTC trajectory features extract in-layer instead (ADR-014 §"Feature-extraction location": "When ADR-016 is rejected (no cross-layer channel), HTC trajectory features are extracted in-layer at L1") — meaning even if AUQ/HTC is later adopted via Q2, ADR-016's channel and its five mechanisms are not thereby revived; ADR-014 already designed for exactly this configuration. This closes the one plausible path by which ADR-016's supersession could have stranded something AUQ/HTC-adjacent, and confirms "clean supersession" as accurate.
- **017 (ReAct-loop instance vs. guard).** Re-verified against ADR-017's full text: 100% of its Decision content is the phantom-tool-call structural validation guard; zero ReAct-loop content. The row's disambiguation (line 61-62, ReAct-loop-instance attributed to ADR-001 via ADR-045's dual module attribution; guard survives) accurately reflects ADR-017's actual decided content. No residual gap.
- **023 (Observability Event-Routing).** Re-verified against ADR-023's full text. The row's "calibration/tier events go with their subsystems; in-turn orchestrator-context routing superseded; end-of-session summary survives; residual dispatch-timing/liveness observability survives as an engine-level requirement" (line 60) is directionally accurate at the event-type level (`TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal` all trace to now-superseded subsystems; `DispatchTiming`, the liveness-signal patterns, and validate-once-at-load are genuinely actor-agnostic infrastructure). This general statement is the source of the specific gap named below — it states the *principle* correctly but the principle was not fully propagated into ADR-024's specific field-level documentation.
- **024 (Common I/O Envelope).** See **P2-new-1** below — the one residual gap this round's adversarial check surfaces.
- **025 (Artifact-as-Substrate).** Re-verified against ADR-025's full text, including its own §"Calibration-gate evaluation surface under substrate-routing" (the subsection the row's caveat names) and its §"Backward propagation (Step 3.7)" precedent (the AS-7 sweep, which itself named ADR-007/014 as documents needing supersession notes — the precedent ADR-046's own current fix is now finally applying reflexively, per R2's original P2-2 observation). No further calibration-tied content found elsewhere in ADR-025's text; the fix is complete.
- **002 (Four-Layer Architecture).** Confirmed unchanged from R2/R3's finding — Layer 2's text ("ReAct loop powered by a model profile, exercising a fixed tool surface (ADR-003)") remains untouched, correctly tracked as owed backward-propagation work distinct from the mechanical layering-rule-revert edit. No new issue.

### P1 — Must Fix

None this round. No fourth instance of the R1-AS-5 / R2-AS-2 / R3-ADR-014 bundling pattern (an independently-valuable, actor-agnostic mechanism silently retired via an unexamined "replaced by X" or "goes with X" claim) was found anywhere in the §3 table on full component-by-component re-derivation.

### P2 — Should Fix

**P2-new-1. Row 024's fix names `diagnostics.calibration_verdict` as "goes with the gate," but ADR-024's envelope also carries a second sub-field — `diagnostics.audit_findings` — wired to the now-superseded ADR-018 Tier-Router-Audit mechanism, and this second stale field was not caught by the fix or added to the backward-propagation-owed list.**

- **Location:** ADR-046 §3 row `024` (line 65): "survives (container); `calibration_verdict` field goes with the gate... the envelope's `diagnostics.calibration_verdict` sub-field is wired to the superseded 007/014 gate and goes with it; the envelope shape (status/primary/structured/diagnostics/errors/artifacts) is unaffected." Compare ADR-024 §Decision's `DispatchEnvelope.diagnostics` field specification (verified directly): "Fields: `ensemble` (str), `dispatch_id` (str...), `duration_seconds` (float), `model_profile` (str), `tier` (str), `topaz_skill` (str), `calibration_verdict` (Literal[Proceed, Reflect, Abstain] | None), **`audit_findings`** (list[dict] — empty list when no drift criteria findings)." Compare ADR-023 §Context (verified directly): "`AuditDiagnostic` (Tier-Router-Audit drift criteria findings **per ADR-018**)" — and ADR-018's own Decision (verified directly): the drift criteria (verdict-distribution shift, escalation-vs-outcome correlation drift, bypass-rate trend) exist entirely to detect drift in the tier-escalation router's verdict-consumption behavior, which ADR-046's own row for 015/018 (already re-verified above) correctly marks superseded.
- **Claim:** The row's caveat, and the backward-propagation-owed list's ADR-024 entry, capture everything in the envelope's `diagnostics` sub-schema that is wired to now-superseded infrastructure.
- **Evidence gap:** `audit_findings` is populated from ADR-023's `AuditDiagnostic` event, which is itself sourced from ADR-018's periodic audit-dispatch mechanism — a mechanism with no referent once the tier-escalation router it audits (ADR-015, and by extension ADR-018) is superseded. This is structurally the same relationship `calibration_verdict` has to the superseded 007/014 gate — a `diagnostics` sub-field whose entire reason for existing traces to a now-retired subsystem — but it was not named in row 024's fix and does not appear in the backward-propagation-owed list (confirmed by direct search of the current ADR-046 text: zero matches for `audit_findings`, `AuditDiagnostic`, or `topaz_skill`). Row 023's own text already states the general principle that would have caught this ("calibration/tier events go with their subsystems") but the principle was not cross-applied when row 024 was fixed for the `calibration_verdict`-specific finding — the same "fix responds to the specifically-named clause, not a full re-sweep of the row's source content" pattern R3's central framing finding warned would keep recurring. Unlike R1-R3's findings, nothing valuable is stranded here (there is no plausible successor use for stale audit-dispatch findings once the audited mechanism is gone) — this is a documentation-completeness gap, the same class and severity as R2's P2-2 and R3's P2-new-2, not a new instance of the higher-stakes bundling pattern. A related, softer observation: `tier` and `topaz_skill` are also event-sourced from `TierSelection` (ADR-015-attributed), but unlike `audit_findings` these may have a plausible independent referent under the new architecture (which model-tier/Topaz-skill a seat used is generic dispatch metadata a seat could still record without ADR-015's specific escalation-router mechanism) — flagged for the practitioner's judgment, not asserted as stale.
- **Recommendation:** Add one clause to row 024 (e.g., "`diagnostics.audit_findings` is also wired to the superseded ADR-018 audit-dispatch mechanism and goes with it; `tier`/`topaz_skill` are dispatch metadata that may survive as generic seat-diagnostic fields independent of the escalation-router mechanism — practitioner judgment") and add `audit_findings` to the backward-propagation-owed list alongside `calibration_verdict`.
- **Routing:** N/A.

### P3 — Consider

**P3-new-1.** Row 012's fix (change-set 4c above) names ADR-013's session-scoped progress-log as compaction's referent and addresses the circuit-breaker's reset semantics, but does not walk through ADR-012's other four layers (persist-large-tool-results at a 50,000-character threshold; cache-edit, which is specified for deleting stale entries from a continuously-cached LLM conversation *prefix*; idle-expiry at a 60-minute window; LLM-semantic-summary as last resort) individually for whether each still has a referent under discrete per-turn passes rather than a continuously-growing single conversation. Layer 1 (cache-edit) in particular is specified around maintaining one long-lived cached prompt prefix — a concept that maps less obviously onto stateless per-turn ensemble passes than the session-scoped progress-log Layer 3 was re-derived against. Lower stakes than P2-new-1: nothing independently valuable is asserted-then-dropped, only under-specified; BUILD-time work would need to resolve this regardless. Recommend either a one-line acknowledgment that the layer-by-layer translation is BUILD-time work, or (if cheap) naming which of the five layers plausibly still apply as specified vs. need re-derivation.

**P3-1 (carried over from R1, restated by R2 and R3, still unaddressed).** Row for `018` ("018 with 015," line 68) still omits ADR-016 as ADR-018's second parent (per ADR-018's own Provenance check: "ADR-016 mechanism (d) is the structural precedent"). Confirmed unchanged this round (identical text to what R3 quoted). Both parents are now superseded regardless, so this remains low-stakes.

**P3-2 (carried over from R2, restated by R3, still unaddressed).** §Consequences Positive ("One architecture, validated end to end with real models and interactive on the target rig," line 100) still doesn't carry the path-3/direct-harness qualifier that §Context and §Provenance-check state clearly. Confirmed unchanged; low stakes since a top-to-bottom reader hits the correct hedge first (§Context) before reaching this line.

**P3-3 (carried over from R2, restated by R3, still unaddressed).** Row 005's `[read]` follow-on for AS-3 doesn't name ADR-013 as the obvious candidate cross-turn home for budget tracking; row 004's `[read]` follow-on for AS-7 doesn't note how much ADR-025's own 2026-05-15 amendment already settled structurally. Confirmed both rows textually unchanged from what R2/R3 quoted.

**P3-4 (carried over from R2, restated by R3, still unaddressed).** ADR-003's "predictable, auditable tool action space" concern and ADR-008's "irreversible library changes require operator approval" concern are not connected forward to §Open's Q4 (operator-curated registry). Confirmed unchanged; Q4 remains explicitly open, so nothing is misattributed, just unconnected.

## Section 2: Framing Audit

### Question 1: What alternative framings did the evidence support?

**R3's "checking discipline" framing finding is now partially resolved: the document makes the explicit completeness claim R3 called for, but the claim is not perfectly accurate.** R3's Q3 finding named the standing risk directly: "what the document would need to state, once and explicitly, is a completeness claim: 'every row in §3 has been checked against its full source-ADR decision text, component by component' — which is not currently true." The current revision's Status line (line 3) now states exactly this: "R1–R3 argument-audit findings incorporated and **the full §3 component-level re-derivation completed**." This is the explicit completeness claim R3 asked for — a genuine framing improvement, not cosmetic (the cycle-status narrative independently corroborates the claim was earned through real work: "Full component re-derivation of the un-swept rows... against primary Decision text found NO 4th P1"). But this round's adversarial check (P2-new-1) found the claim is not fully accurate: `audit_findings`, a `diagnostics` sub-field one level deeper than the top-level Decision items the re-derivation evidently checked, was missed. The pattern is consistent with the whole sequence's trajectory: each round's fix has operated at the granularity the *prior* round's finding described (R1 named AS-5 at the invariant level; R2 caught AS-2 at the invariant level; R3 caught ADR-014 at the ADR level; this round's re-derivation checked every ADR's top-level Decision items and found no gap at that granularity) — but a genuinely exhaustive check would need to reach one level deeper still, into the typed-contract sub-fields (`DispatchEnvelope.diagnostics`'s eight named fields) that downstream ADRs construct *from* the top-level decisions. The completeness claim, as now written, is honest about what was done (a full pass against source-ADR Decision text) without being falsely universal; the residual gap this round found is narrower in scope and lower in stakes than any of R1-R3's findings, which is itself evidence the discipline is converging, not evidence it has failed.

**A second, narrower framing worth naming:** three of ADR-046's own §3 rows (015, 016, 018) supersede subsystems whose *outputs* (typed events, diagnostic fields) are consumed by a fourth ADR (024) that the classification table treats as a clean "survives" container. The table's per-ADR granularity is well-suited to catching supersession-vs-survival at the mechanism level (which is what R1-R3 exhaustively checked) but is structurally less well-suited to catching stale *data* that flows from a superseded producer into a surviving consumer's typed schema — that requires reading across rows (015/018 → 023 → 024), not just within one row against its own source ADR. This is a different failure surface than "bundling" (retiring actor-agnostic content); it's closer to "typed-contract provenance tracking" across the classification table's row boundaries.

### Question 2: What truths were available but not featured?

- ADR-023's own general statement — "calibration/tier events go with their subsystems" (row 023, line 60) — already states the exact principle that would flag `audit_findings` as stale, but the principle was not cross-applied to row 024's field-level enumeration when that row was fixed for the `calibration_verdict`-specific R2/R3 finding.
- ADR-018's own Decision text (verified this round) makes explicit that its audit dispatch exists solely to detect drift in "the L1→L2 verdict→router edge" — a sentence that, read directly, immediately establishes `audit_findings` has no referent once that edge is retired; it was available on direct read but the row-024 fix did not re-read ADR-018 (it re-read ADR-007/014, the gate the `calibration_verdict` finding named).
- ADR-014's own "when ADR-016 is rejected (no cross-layer channel), HTC trajectory features are extracted in-layer" clause (already noted by this round's re-derivation of row 016) is available in ADR-014's own text and is the sentence that closes the one plausible path by which ADR-016's supersession could have stranded something — it was not previously cited in R1-R3's audits but independently confirms this round's "clean supersession" finding for row 016.
- ADR-025's own §"Backward propagation (Step 3.7)" — the AS-7 sweep precedent naming ADR-007/014 by name — was already cited by R2's P2-2 as the precedent ADR-046 should apply reflexively to its own calibration-tied content. The current fix (row 024/025 caveats) is that precedent finally applied, but applied at the granularity of the specific finding (`calibration_verdict`) rather than swept exhaustively against the precedent's own full scope (which named the envelope generally, not one field).

### Question 3: What would change if the dominant framing were inverted?

**Dominant framing (four rounds running):** the checking discipline is converging — issue counts have declined monotonically (4P1/7P2 → 4P1/3P2 → 1P1/2P2 → 0P1/1P2 this round), and each round's fix correctly resolves the specific problem named, evidence the process is working.

**Inverted framing:** the declining count is evidence the *easy-to-find* instances of the bundling pattern have been exhausted by four rounds of manual, targeted re-derivation — not evidence the underlying content is now exhaustively verified. Each round has checked one granularity level deeper than the last (R1: invariant-level; R2: invariant-level, different invariant; R3: ADR-level; R4: ADR-level exhaustively, plus one field-level gap found almost incidentally by extending the check one level further). Under this inversion, the honest state of the corpus is not "§3 is now complete" but "§3 has been checked to the depth four rounds of adversarial audit have reached, and the marginal cost of finding the next gap (if any) is rising while the stakes of what remains to be found are falling" — which is precisely the shape of evidence ADR-094's Convergence-Saturation Signal is designed to act on. The inversion doesn't argue for a fifth round; it argues for naming the boundary honestly: the practitioner should treat the backward-propagation sweep (§Open, already comprehensively enumerated) as the place where any further latent staleness gets caught structurally (each ADR gets read and header-stamped individually during that sweep), rather than expecting a hypothetical R5 argument-audit to find it first.

### Framing Issues

**P1 (Must Fix):** None this round.

**P2 (Should Fix):**
- Q1 — the explicit completeness claim ("the full §3 component-level re-derivation completed," line 3) is a genuine improvement responsive to R3's framing finding, but is not perfectly accurate at the sub-field level (P2-new-1). Recommend either scoping the claim precisely ("component-level re-derivation of every ADR's top-level Decision content completed; sub-field-level propagation into downstream typed contracts checked opportunistically, not exhaustively") or completing the one-field fix (P2-new-1's recommendation) and then the claim is accurate as written.

**P3 (Consider):**
- Q1 — recommend that the backward-propagation sweep (§Open), when executed, include a typed-contract cross-check step: for every envelope/event field that traces to a now-superseded subsystem, verify the field's disposition is named, rather than relying on row-level "goes with the gate" caveats to have caught every sub-field by inspection.

## Convergence-Saturation Signal (ADR-094)

**Convergence-Saturation Signal:** **TRIGGERED**

- Round number: R4. Per ADR-094's form-change baseline-reset rule, this round does not reset the baseline — the revision was an in-place revision of the same document form (row-level edits, hedge additions, and one added table row, all responsive to R3's findings), not a restructuring, consolidation, or replacement into a new form. R1 → R2 → R3 → R4 stands as the continuous round count for this document.
- P1 count this round: **0** (no Must-Fix findings in either Section 1 or Section 2).
- P2 count this round (new, non-carry-over): **1** (P2-new-1 — `diagnostics.audit_findings` wired to the now-superseded ADR-018 mechanism, not caught by row 024's `calibration_verdict` fix or added to the backward-propagation-owed list).
- New framings or claim-scope expansions: **none.** This round's Q1/Q3 observations (the completeness-claim-vs-sub-field-gap distinction; the "typed-contract provenance crosses row boundaries" observation) apply the audit sequence's already-established pattern (component-level re-derivation catching bundling one level deeper than the prior round reached) to one additional, narrower location. Neither is a new interpretive lens on the document the way R2's component-vs-ADR-level supersession inversion or R3's "checking discipline is the standing risk" framing were — both of those are prior rounds' finding records, and this round's findings are their direct, expected continuation, not a novel claim-scope expansion.
- Recommendation: **STOP at this round.** All three trigger conditions hold: P1 = 0, new P2 (1) ≤ 1, no new framings. **ADR-046 is argument-audit-clean pending the backward-propagation sweep.** The one residual finding (P2-new-1) is cheap and mechanical — a one-clause addition to row 024 plus one entry in the already-comprehensive backward-propagation-owed list — and can be folded into the backward-propagation sweep §Open already tracks rather than warranting a dedicated R5 dispatch. No fourth-round instance of the higher-stakes bundling pattern (an independently-valuable, actor-agnostic mechanism silently retired) was found; the pattern that recurred three rounds running (R1 AS-5, R2 AS-2, R3 ADR-014) does not recur a fourth time. The practitioner's remaining pre-Acceptance work is exactly what ADR-046's own §Open already enumerates: the backward-propagation sweep (header stamps, the ADR-002 Layer-2 amendment, the domain-model Amendment Log entry, the now-stale OQ #11/#12/#14 and Methodology Vocabulary entry, the AS-3/AS-5/AS-6/AS-7 dispositions) — with `diagnostics.audit_findings` now added to that list's scope.

*This is a re-audit-after-revision dispatched to verify R3's fixes and discharge the R4 charge (confirm the five named change-sets; adversarially check for a fourth bundling instance; independently spot-check the full-re-derivation claim) per ADR-094 §"Standard sequence vs. re-audit-after-revision." The verdict line above is included per the dispatch brief's explicit instruction, consistent with R1, R2, and R3's own precedent.*
