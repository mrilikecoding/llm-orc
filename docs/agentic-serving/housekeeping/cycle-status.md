# RDD Cycle Status — Agentic Serving (Scoped)

**Artifact base:** `docs/agentic-serving/`
**Plugin version at cycle open:** v0.8.6
**Migration version:** 0.8.5 (`housekeeping/.migration-version`)

## Cycle Stack

### Active: Cycle 7 — Framework-driven orchestration: routing as code (provisional title)

**Cycle number:** 7
**Started:** 2026-05-20 (cycle prepared; RESEARCH not yet entered)
**Current phase:** build (loop-back #2 — Finding D; re-entered 2026-06-03 at the ARCHITECT→BUILD gate ("Very well - let's proceed"). **ARCHITECT loop-back #2 closed 2026-06-03:** system-design **v6.1** (Amendment #14) — no new modules; Loop Driver gains `compose_form_directive(tool)` (FC-53/54), Artifact Bridge gains the **FormGate seam** with `destination_tool` context (FC-57; detect-and-refuse installs there on PLAY evidence, zero Terminal edits), Orchestrator Tool Dispatch gets the D1 extraction fix (last-successful-agent, FC-56), granularity housed in existing structure (FC-55, no new module); roadmap **WP-LB-H** (+ TS-14, LB-4/5/6; WP-LB-G marked closed); ORIENTATION regenerated; snapshot **No Grounding Reframe** + 1 BUILD advisory (the smoke test must verify delegation actually fired before crediting the directive); gate note `.rdd/gates/cycle-7-loopback2-architect-build-gate.md`. Gate exchange: FormGate-seat warrant elicitation → practitioner redirected to north-star derivation → agent committed to the Bridge seat on three grounded reasons (layer-split diagnosability; refusal is dispatch-stratum work; form-rule accretion at the content locus); practitioner approved. **Next: BUILD WP-LB-H** — form contract + D1; acceptance gate = $0 real-OpenCode re-run landing a *runnable* file WITH delegation verified fired.) **Prior:** build (loop-back; next — ARCHITECT loop-back closed at gate 2026-06-02: four new modules [Loop Driver, Single-Step Enforcer, Artifact Bridge, Client-Tool-Action Terminal] + 2 extensions [Serving Layer surface-mode discrimination D1; Session Artifact Store `read_deliverable`] + Dispatch Pipeline scoped to single-turn; 9 cycle-free dependency edges; 11 new FCs FC-42..FC-52; D2 batch-truncation selected; all six DECIDE→ARCHITECT advisories addressed; ADR-076 decomposition complete [parity addressed-by-composition; axis-2 structurally-non-decomposable]; no invariant change; system-design.md v6.0 + system-design.agents.md extended + roadmap.md WP-LB-A..F + TS-12/13 + ORIENTATION.md regenerated; susceptibility snapshot **No Grounding Reframe** with 6 BUILD advisories; gate note `.rdd/gates/cycle-7-loopback-architect-build-gate.md`. **BUILD (loop-back) ENTERED 2026-06-02** on practitioner confirmation; BUILD mode **gated** (carries from cycle-prep declaration; the loop-back's axis-2 risk + validate-not-assume discriminator + FC-51-scope confirmation warrant practitioner-in-the-loop stewardship). ARCHITECT-loop-back-close committed `a6f19a3`. BUILD-entry items surfaced: confirm FC-51 scope before WP-LB-F; Track A-LB.1 docstring removal is the non-negotiable first commit. **WP-LB-A landed `4d8108d`** (surface-mode discriminator + loop-driver seam/stub; suite 2851 green); north-star implication recorded (corrected after practitioner challenge): the four RDD modes (conversation / artifact generation / artifact reasoning / code work) map across the layer-A/layer-B split with NO missing mechanism, but axis-2 risk concentrates in artifact-reasoning + code-recovery driving (not the conversational path); `skill`/`task`/`todowrite` surface explicitly deferred (ADR-033 §6c) — Cycle 7 delivers the file-action subset. See Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-A" signals #3/#6/#7. **WP-LB-B landed** (commits `67d42b9` Single-Step Enforcer + `81ad0ea` loop-driver finish+callee + `813823e` grounded-carry/Reading-B + `fd0e74f` TurnDecision + `b16252f` factory swap + FC-42 + `15bf81d` callee integration test; design amendment `782b092`; suite 2865 green): `agentic/single_step_enforcer.py` (FC-43) + `agentic/loop_driver.py` (FC-44/45/51 — control structure, seat-filler port, callee delegation, grounded-carry passthrough, TurnDecision events) + `get_loop_driver()` swapped to the real driver with Model-Profile seat-filler resolution behind `_resolve_seat_filler` (FC-42 event assertion at the endpoint). **Seat-filler contract resolved at a BUILD design gate (Reading B, Design Amendment #13):** the seat-filler emits a literal client tool call (passed through verbatim — grounded carry) OR an `invoke_ensemble` internal call (callee generation); supersedes the implicit write-interception reading that would have lost grounding fidelity. Tier-1 stewardship clean; callee boundary integration-verified against the real Tool Dispatch (mock model, $0). **WP-LB-D landed** (commits `2b7f362` read_deliverable + `3b9d829` ArtifactBridge + `af5af7d` integration/FC-49-at-scale; suite 2873 green): `SessionArtifactStore.read_deliverable` (read-side accessor; structural-inverse path resolution; `ArtifactNotFoundError`) + `agentic/artifact_bridge.py` `ArtifactBridge.marshal` (artifact-vs-inline branch; substrate → fidelity read, inline → `primary` no-op). Built testable-in-isolation; NOT yet wired into the live loop (`loop_driver._deliverable_text` still reads `envelope.primary` — WP-LB-C wires terminal → bridge). At the WP-LB-D gate the practitioner issued the **north-star validation directive** (don't fly blind; validate against real OpenCode at the earliest runnable point — see Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-D" #3 + memory `validate-against-real-client-not-harness`). **Next: WP-LB-C** (Client-Tool-Action Terminal + multi-turn loop participation; both hard prereqs now satisfied) — carry the real-serving-surface integration test + $0 real-OpenCode smoke test as first-class acceptance gates. See Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-D".)
**In-progress phase:** build
**Cycle type:** standard (full pipeline)
**Plugin version:** v0.8.6
**Artifact base:** `docs/agentic-serving/`
**BUILD mode:** gated (declared at cycle preparation; this cycle is design-alternative-heavy — auto mode would miss surfacing trade-offs between routing-planner ensemble, `tool_choice` constrained decoding, and hybrid approaches; gated stewardship reviews are appropriate)
**DISCOVER entered:** 2026-05-21
**MODEL entered:** 2026-05-22
**MODEL exited:** 2026-05-22 (gate closed; AS-9 codified at MODEL boundary)
**DECIDE entered:** 2026-05-22
**DECIDE exited:** 2026-05-22 (gate closed; ADRs 026-032 accepted; AS-10 codified at DECIDE boundary; 7-round argument-audit loop clean at P1/P2/P3; susceptibility-snapshot verdict No Grounding Reframe; gate reflection note at `.rdd/gates/cycle-7-decide-architect-gate.md`)
**ARCHITECT entered:** 2026-05-22
**ARCHITECT exited:** 2026-05-23 (gate closed; six new modules composed; 13 dependency edges; 14 new fitness criteria FC-28..FC-41; ADR-076 qualitative-claim decomposition complete; three Track C deferrals resolved [Findings 2/8/11]; system-design.md v5.0 amended; system-design.agents.md extended; roadmap.md Cycle 7 WP-A..WP-E + Track A.1/A.2/A.3 + TS-10/TS-11; ORIENTATION.md regenerated; susceptibility-snapshot verdict No Grounding Reframe with 4 advisories + 1 informational + 1 positive signal; **Spike ν long-horizon capability ceiling probe added as Track A.3 architect-build boundary work gating WP-A entry** per practitioner EPISTEMIC GATE response; gate reflection note at `.rdd/gates/cycle-7-architect-build-gate.md`)
**BUILD entered:** 2026-05-23
**BUILD paused (loop-back):** 2026-05-24 — WP-A handler swap landed + committed (`0a7a822`); WP-B/WP-C paused. A BUILD reflection-gate exchange surfaced that ADR-027's pipeline has no terminal for delivering ensemble work to the client's local filesystem (it returns text only, never `tool_calls`), which the practitioner's north-star (OpenCode + RDD + ensembles writing local files) requires. Looping back to RESEARCH to design + validate the missing client-tool-action terminal before resuming BUILD. See §"BUILD-surfaced finding: client-tool-action terminal gap" below.
**ARCHITECT entered (loop-back):** 2026-06-02
**ARCHITECT exited (loop-back):** 2026-06-02 (gate closed; four new modules — Loop Driver + Single-Step Enforcer + Artifact Bridge [L2] + Client-Tool-Action Terminal [L3]; 2 extensions [Serving Layer surface-mode discrimination; Session Artifact Store `read_deliverable`]; Dispatch Pipeline scoped to single-turn; 9 cycle-free edges; 11 new FCs FC-42..FC-52; D2 batch-truncation selected; D1 discriminator in Serving Layer; six DECIDE→ARCHITECT advisories addressed; ADR-076 decomposition complete; **no invariant change**; system-design.md v6.0 + system-design.agents.md + roadmap.md WP-LB-A..F + TS-12/13 + ORIENTATION.md regenerated; susceptibility-snapshot **No Grounding Reframe** with 6 BUILD advisories; gate reflection note `.rdd/gates/cycle-7-loopback-architect-build-gate.md`. FC-51 gate-refinement: split-vs-callee diagnosis gates the fallback branch — confirm scope with practitioner before WP-LB-F.)
**BUILD re-entered (loop-back):** 2026-06-02 (WP-LB-A/B/C/D/G landed; delegation fires end-to-end against real OpenCode; suite 2891 green)
**BUILD paused (loop-back #2 — Finding D):** 2026-06-02 — the WP-LB-G real run surfaced that the marshalled deliverable is the raw ensemble result envelope, not usable file content. Spike φ ($0, code-reading + no-model separability probe + 2 local claim-extractor runs) characterized it as three separable layers: D1 (extraction — terminal-node not recovered), D2a (declared contracts inert at execution — `default_task` never read, `output_schema` documentary), D2b (capability-ensemble output targets a human reader, not a bare client-tool file). Practitioner disposition: DECIDE on the I/O-contract (D2a + D2b); D1 extraction fix shaped to the outcome.
**DECIDE entered (loop-back #2 — Finding D):** 2026-06-02
**DECIDE exited (loop-back #2):** 2026-06-03 (gate closed; ADR-035 + ADR-024 partial update; Spike χ/χ.2 DECIDE-entry probes; argument audit converged R3; snapshot No Grounding Reframe + 2 ARCHITECT advisories; gate note `.rdd/gates/cycle-7-loopback2-decide-architect-gate.md`)
**ARCHITECT entered (loop-back #2):** 2026-06-03
**ARCHITECT exited (loop-back #2):** 2026-06-03 (gate closed; system-design v6.1 Amendment #14 — no new modules, FC-53..57; roadmap WP-LB-H + TS-14 + LB-4/5/6; ORIENTATION regenerated; both DECIDE advisories addressed [granularity home designed; FormGate named interface point]; snapshot No Grounding Reframe + 1 BUILD advisory; gate note `.rdd/gates/cycle-7-loopback2-architect-build-gate.md`)
**BUILD re-entered (loop-back #2):** 2026-06-03 (WP-LB-H next; gated mode carries)

**Origin:** Cycle 6 PLAY (2026-05-20) surfaced that the orchestrator-LLM's chain-handling behavior is the load-bearing failure for composition pipelines; ADR-022 amendment is empirically bounded to bare-endpoint mode and does not shift NL routing under tool-rich production clients. Spike δ confirmed that framework-driven Python chaining preserves data correctly across the same `web-searcher → claim-extractor` composition that failed under orchestrator-LLM routing. PLAY-tail grounding work (per susceptibility-snapshot recommendation) softened the initial "framework-driven pipeline" framing into bounded claims:

- Framework-driven **chain-handling** is well-grounded (Spike δ + alternatives belief-map confirm)
- Framework-driven **routing-decision** is contested — `tool_choice` constrained decoding is a viable lighter-weight alternative to a full routing-planner ensemble
- "Must-delegate to ensembles" is bounded to capability-matched requests, not universal
- The form-vs-content drift bifurcation may collapse if schema-as-enforcement unifies them

Cycle 6 status archived at `cycle-archive/cycle-6-ensemble-contract-observability-routing.md`.

**Cycle 7 question framing (provisional, for RESEARCH entry):**

Three linked architectural questions:

1. **Where does the routing decision live?** Options: (a) framework-driven routing-planner ensemble; (b) `tool_choice` constrained decoding forcing `invoke_ensemble` when a capability match is detected; (c) hybrid where a small classifier decides which mechanism applies per request. The chain-handling commitment is already framework-driven (Spike δ + belief-map); the routing-decision is the open territory.

2. **How are I/O contracts enforced?** Output_schema currently documentary (per Cycle 6 WP-D moderate advisory 1 resolution); claim-extractor's form drift demonstrates documentary schemas don't deliver compliance. Options: (a) schema-as-enforcement with reject-and-retry; (b) tool-call-as-output-format (agents call a `submit_results` tool whose params are the schema); (c) deterministic shaper after the agent (extra dispatch per ensemble with declared schema). Connects to form-vs-content drift collapse question — schema-as-enforcement might address both.

3. **What's the fallback shape for non-capability-matched requests?** Options: (a) general-completion fallback ensemble (preserves infrastructure, adds dispatch overhead); (b) direct LLM completion as residual through the orchestrator-LLM (preserves latency, loses infrastructure); (c) lightweight shim that wraps direct completion with minimal infrastructure (calibration verdict + audit log but no full ensemble overhead). The must-delegate-as-bounded framing means the fallback shape is design territory, not a forced choice.

These three questions are linked: the routing-decision mechanism + the contract-enforcement mechanism + the fallback shape together determine the cycle 7 architecture.

**Specific carry-forwards from Cycle 6 to address in Cycle 7 (or as standalone Thread A fixes between cycles):**

1. **`dispatch_log.json` overwrite vs. append.** Currently each chat-completions request overwrites the file with its own dispatches. Operator-facing post-hoc review use case is broken — the latest empty-dispatch request wipes prior session entries. Fix shape: append-mode, OR per-request files, OR reconstruct-from-artifacts at session close. Small fix; could land as Thread A (outside methodology cycle) or fold into Cycle 7 architecture work if the persistence semantics intersect the planner+synthesizer roles.

2. **`profile=?` placeholder in `dispatch start` log line.** Cosmetic — the profile is unknown at dispatch_start (resolved in `tier selection` one line later). Could be left blank, populated from the resolution step, or removed. One-line fix.

3. **Capability ensemble form drift at synthesizer/agent layer.** Claim-extractor's output is non-conformant to its `default_task` spec across all invocation paths. Cycle 7 DECIDE will land an ADR on I/O contract enforcement policy that addresses this directly.

**MODEL handling for Cycle 7:** Likely-needed. The architectural pivot introduces new vocabulary candidates: `routing-planner`, `response-synthesizer`, `general-completion fallback`, `plan-dispatch-synthesize pipeline`, `framework-as-orchestrator`. If RESEARCH + DISCOVER surface enough new vocabulary, formalize in `domain-model.md` before DECIDE. Decision deferred to DISCOVER's tail.

**DISCOVER handling for Cycle 7:** Required. The architectural pivot has stakeholder-mental-model implications:

- Tool User's "endpoint is a model" abstraction: holds or breaks under the new pipeline?
- Skill Orchestration User's "the orchestrator routes by capability under NL" mental model: now needs re-grounding given the routing-decision mechanism is design-open
- Ensemble Author / Operator: routing-planner authoring is a new authoring surface; what does the operator need to understand about the planner ensemble?
- Orchestrator LLM (as actor): its role shifts from decider to executor or disappears entirely — this is a stakeholder-model change worth surfacing

**ARCHITECT handling for Cycle 7:** Required. The chat-completions handler refactor is non-trivial: the orchestrator-LLM-as-decider goes away (or shrinks); new modules for routing-planner integration, synthesizer integration, plan-execution loop. Fitness criteria need to capture the new pipeline's invariants (e.g., "every chat-completions request resolves to ≥1 ensemble dispatch when a capability match exists"). Substantial ARCHITECT phase.

## Phase Status

| Phase | Status | Artifact | Key Epistemic Response |
|-------|--------|----------|----------------------|
| RESEARCH | ✅ Complete | `essays/essay-outline-006-cross-compatibility-routing-surface.md` + reflection `essays/reflections/006-cross-compatibility-routing-surface.md` + research log `essays/research-logs/006-cross-compatibility-routing-surface.md` | Practitioner pushed back on hybrid framing in favor of ADR-027 stronger stance at the gate; cycle responded with tiered-architecture revision (hybrid as starting commitment + ADR-027 as structurally pre-committed escalation). 5 audit rounds verified P1-clean. Susceptibility snapshot surfaced 2 Grounding Reframes (GT-1 C6 elevation; GT-2 hybrid-first ordering language drift). Practitioner authorized pursuing both grounding actions in DISCOVER/DECIDE. |
| DISCOVER | ✅ Complete (gate closed 2026-05-22) | `product-discovery.md` Cycle 7 update (+59 lines) + `essays/essay-outline-006-...md` Amendment Log (+89 lines) + three Spike research logs (ζ, ε, κ) + gate reflection note `.rdd/gates/cycle-7-discover-model-gate.md` + susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-discover.md` | Practitioner raised sharp scope-of-claim challenge on orchestrator-LLM commitment ("Is the orchestrator llm question still open? Or are we saying based on our spike results we know the success mechanisms here"). Agent responded with explicit scope-of-claim partition (settled / plausible-but-untested / open); practitioner confirmed ("OK that tracks more with my understanding. We can tight that more") prompting tightening of Amendment A3 and Orchestrator LLM Cycle 7 refinement language. Other 7 commitment-gating items not individually addressed; per snapshot Finding 4 this is consistent with earned trust reading. Snapshot verdict: No Grounding Reframe; 3 advisory carry-forwards (cost-distribution lens validation; ADR-027-primary build-complexity comparison; Population A tool-family timeout research) + 1 informational. |
| MODEL | ✅ Complete (gate closed 2026-05-22) | `domain-model.md` Cycle 7 update (Amendment Log entries #12 + #13; AS-9 codified; 10 vocabulary entries; 10 new Open Questions #16-25) + `essays/research-logs/cycle-7-spike-mu-confabulation-generalization.md` + gate reflection note `.rdd/gates/cycle-7-model-decide-gate.md` + susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-model.md` | Practitioner generative-redirected the gate's belief-mapping question from deferral-validation to spike-design ("is there anything additional we could design in a new spike that would inform whether to land an invariant?"). Agent proposed Spike μ as load-bearing for OQ #16; practitioner authorized ("more information earlier is better, so if there's an important thing to learn from a spike before we propagate forward I'm in favor"). Spike μ ran ($0 cost, ~3 min wall-clock) and extended the structural-bounding finding across four documented confabulation modes (multi-dispatch fabrication; path hallucination; substrate-path-as-deliverable; coherent factual errors uncalibrated) at qwen3:8b — n=13 tests total. Practitioner chose codification option (a): codify as AS-9 at MODEL. AS-9 codified with structural-property vs. mechanism-choice separation preserved (mechanism choice remains DECIDE-deferred per Advisory 2 / OQ #19). Snapshot verdict: **No Grounding Reframe; 2 new advisory carry-forwards (Advisory A spike methodology pre-specification; Advisory B options-engagement documentation gap) + 3 DISCOVER advisories preserved active.** Positive signal: OQ #17 correctly preserved as distinct from AS-9 + DECIDE-deferred. |
| DECIDE | ✅ Complete (gate closed 2026-05-22) | ADRs 026-032 + ADR-021/022 partial-update headers + Essay-Outline 006 P1-clean + scenarios.md +315 lines + interaction-specs.md +117 lines + 7-round argument-audit loop (round 7 clean at P1/P2/P3) + conformance-scan + susceptibility-snapshot (No Grounding Reframe) + gate reflection note + Amendment Log entry #14 (AS-10 + Bridge mechanism) | Practitioner-named C1 failure mode ("we're still not getting work delegated to ensembles by default") triggered gate-time ADR-032 refutation-threshold amendment. Pre-mortem belief-mapping question surfaced the cycle's preserved escape hatches (routing-planner model substitution → classifier pre-filter → caching → embedding-similarity router) vs. deliberately-closed paths (frontier-tier orchestrator-LLM closed by AS-9). Deployment-relative refutation threshold committed: sustained `direct_completion_rate` >~15pp above expected baseline over 24-hour rolling window is C1 failure evidence; PLAY refines. |
| ARCHITECT | ✅ Complete (gate closed 2026-05-23) | `system-design.md` v5.0 + `system-design.agents.md` Cycle 7 additions + `roadmap.md` Cycle 7 active section + `ORIENTATION.md` regenerated + susceptibility snapshot at `housekeeping/audits/susceptibility-snapshot-cycle-7-architect.md` + gate reflection note `.rdd/gates/cycle-7-architect-build-gate.md` + system-design.md Amendment Log entries #10 + #11 | Practitioner EPISTEMIC GATE belief-mapping response surfaced the long-horizon capability ceiling question: "If the ensembles are simply not capable enough to handle generalized long-horizon tasks then perhaps hybrid would be preferred. But we haven't invested enough to answer that question." Agent reframed via asymmetry argument (Tier 1 would not rescue long-horizon either since non-`tool_choice` NL routing continues through orchestrator-LLM-as-decider failure locus); offered three options (proceed-with-question-logged / architecturally-revise / Spike ν probe). Practitioner chose Spike ν. Snapshot Advisory B added multiple-intermediate rule before snapshot dispatch closed. Track C dispositions: Finding 2 → (a) preserve OrchestratorRuntime as architectural option; Finding 8 → same chunk vocabulary as runtime (formatter unchanged); Finding 11 → adapter inside Dispatch Pipeline. |
| BUILD | ⏸ Paused after WP-A (2026-05-24; loop-back to RESEARCH) | WP-A: commits `e538264` (pipeline + ensemble-backed roles) + `0a7a822` (handler swap, scenario 4). Suite 2847 green; 92% coverage. | Spike ν (2026-05-23) verdict: **no capability ceiling** at the three under-exercised surfaces (multi-step composition 8/8; production-scale numerical ~100%; adversarial routing 92.5%, misses backstopped by ADR-027 plan-validation). WP-A handler swap landed (scenario 4). BUILD reflection-gate then surfaced the **client-tool-action terminal gap** (see finding below): the pipeline returns text only, never `tool_calls`, so it cannot deliver ensemble work to the client's local filesystem — which the practitioner's north-star requires. WP-B/WP-C paused; looping back to RESEARCH. |
| PLAY | ☐ Optional | — | — |
| SYNTHESIZE | ☐ Optional | — | — |
| RESEARCH (loop from BUILD) | ✅ Complete (gate closed 2026-05-30) | Essay-Outline 006 Amendment B (C8 added + W8.1–W8.6 + Section 9 + 2 new References) + research-log 006b + 4 spikes (π/ρ/σ.1/σ.2; all $0 local) + 4 argument-audit rounds (R4 clean at P1/P2) + 2 citation-audit rounds (R2 clean) + susceptibility snapshot (No Grounding Reframe; 3 advisories) + reflection `006b-client-tool-action-terminal.md` + gate note `.rdd/gates/cycle-7-loopback-research-discover-gate.md` | **C8 added:** client-tool-action terminal necessary for parity (execution-model, not filesystem geography); **layer A** named as a *role* no current ADR-027 component holds; 3 candidate seat-fillers + grounded-loop hypothesis as **working inference (NOT a spike finding)**; F-ρ.1 artifact-bridge as the designed-in disjoint-filesystem; F-ρ.2 retracted by isolated citation audit. Practitioner reflection caught agent over-extrapolation at gate-time ("orchestrator-LLM returns in a grounded seat"), producing the post-gate W8.6 refinement with load-bearing scope flags. |
| DISCOVER (loop from RESEARCH re-entry) | ✅ Complete (gate closed 2026-06-01) | `product-discovery.md` Cycle 7 loop-back update (Tool User + Skill Orchestration User mental-model refinements; Value Tensions 21–22; 2 new Assumption Inversion rows; 6 new Vocabulary terms + disposition note) + gate note `.rdd/gates/cycle-7-loopback-discover-model-gate.md` + susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-discover.md` (**No Grounding Reframe; 4 advisories**) | All 5 carry-forward commitments addressed. Commitment #3 (LOAD-BEARING) honored substantively per snapshot: agent led with an independent "middle" framing (ADR-027 sound for **layer-B generation** but mis-identified as *the* architecture; the real open question is **wrapper-vs-callee**, layer A is a control structure no ADR-027 component holds), rejecting both the "incomplete, not wrong" pole and the "solved the wrong problem" pole. Practitioner loosened his own earlier binary ("Perhaps I made it too binary") and held the fork genuinely open: *"no preconceived notion... I want the effective path to win, which we need to ground with evidence."* Agent protected this from callee-skew by surfacing the **evidence-availability asymmetry** (only σ.2 multi-turn evidence exists, and it is callee-shaped; no wrapper-as-loop was ever spiked) → DECIDE-entry wrapper-shaped probe + named discriminating criteria. Grounded-loop falsification probe deferred to DECIDE entry (practitioner prior: reasonably solid; ADR-097 Conditional Acceptance backstop). |
| MODEL (loop from RESEARCH re-entry) | ✅ Complete (gate closed 2026-06-01) | `domain-model.md` Cycle 7 loop-back update (6 vocab terms [2 settled §Concepts, 4 §Methodology Vocab with 3 DECIDE-pending]; **AS-9 §Scope boundary annotation**; 2 Open Questions #26-27; Amendment Log #15) + gate note `.rdd/gates/cycle-7-loopback-model-decide-gate.md` + susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-model.md` (**No Grounding Reframe; 5 advisories — "cleanest gate in the Cycle 7 trajectory"**) | Brief bounded pass. **No invariant amendment** (conservative scope-fence only; AS-10 reaffirmed by Spike ρ as evidence-reinforcement). Substantive gate event: agent self-flagged that its initial AS-9 reopening was narrowed to grounding-vs-ungrounded (the minimize-disruption pull, one level up) and named a hidden second failure axis (sequential composition / error-accumulation across many decisions, even with grounding intact — a regime AS-9's single-decision basis never tested). Warrant-elicitation question on the narrow-vs-broad reopening; practitioner **widened**: *"we're here in a complex systems place, so I'm withholding judgment... not narrow in to bias our solutioning."* AS-9 annotation + OQ #27 widened to two axes; short DECIDE probe targets axis 1, axis 2 is BUILD-phase (short probe cannot settle it). |
| DECIDE (loop from MODEL re-entry) | ✅ Complete (gate closed 2026-06-01) | DECIDE-entry probes COMPLETE (Spike τ/τ′/υ; research log `essays/research-logs/cycle-7-spike-tau-upsilon-decide-entry-probes.md`; all free local, $0). **τ (n=4): grounded-loop FALSIFIED in naive form** — unconstrained qwen3:14b driver batches + presupposes unobserved state (axis-1 failure). **τ′ (n=3): GROUNDED PASS under framework-forced single-action-per-turn** — grounding is a structural-enforcement property, not a model assumption; ADR-097 Conditional Acceptance shape, axis 2 still BUILD-phase. **υ (n=1): wrapper viable but ~3× per-turn latency + redundant planner/synth stages** → OQ #26 tilts to **callee** on evidence (wrapper niche = per-turn multi-capability composition only). Awaiting practitioner read before ADR drafting. | grounded-loop conditionally-accepted (single-step enforcement); wrapper-vs-callee leans callee |
| | | **ADR drafting (post-probe):** practitioner resolved OQ #26 → **callee** (2026-06-01) with axis-2 long-horizon coherence → BUILD/PLAY (ADR-097 backstop) + seat-filler capability bet as recorded load-bearing risk. **ADR-033** (layer-A loop-driver; callee; framework-enforced single-action-per-turn; grounded-loop ADR-097 Conditional Acceptance; 3 FCs) + **ADR-034** (client-tool-action terminal + artifact-bridge + multi-turn loop participation; 4 FCs; artifact-bridge ≠ ADR-030 Bridge mechanism per snapshot Advisory 5) drafted; **ADR-027 scoped** to single-turn/layer-B via partial-update header (status corrected Proposed→Accepted+Updated-by-ADR-033). **Argument-audit converged R2** (`housekeeping/audits/argument-audit-decide-cycle-7-loopback.md` + `-round2.md`): R1 0 P1/5 P2/2 P3 → corrected; R2 corrections held + 1 new precision P2 + 1 P3 → applied; **Convergence-Saturation TRIGGERED at R2**. F3-1 (wrapper-residual-as-concession framing) held for gate. Hygiene flag: first-pass ADRs 026/028-032 likely stale-`Proposed`. **Remaining DECIDE:** conformance scan → domain-model vocab disposition + AS-9 annotation update → scenarios + preservation + acceptance-criteria → interaction-specs → gate. | callee resolved; ADRs 033/034 drafted + audited (R2 converged) |
| ARCHITECT (loop from DECIDE re-entry) | ✅ Complete (gate closed 2026-06-02) | `system-design.md` v6.0 + `system-design.agents.md` (4 new modules: Loop Driver + Single-Step Enforcer + Artifact Bridge [L2] + Client-Tool-Action Terminal [L3]; 2 extensions; Dispatch Pipeline scoped; responsibility matrix + 9 cycle-free edges + integration contracts + FC-42..FC-52 + test architecture + A.5 loop-back snapshot brief) + `roadmap.md` (WP-LB-A..F + dependency graph + TS-12/13 + open decision points; first-pass WP-A closed; WP-B..E single-turn-surface status) + `ORIENTATION.md` regenerated (all 5 sections) + susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-architect.md` (**No Grounding Reframe; 6 BUILD advisories**) + gate note `.rdd/gates/cycle-7-loopback-architect-build-gate.md` + system-design Amendment Log #12 | **D2 resolved: batch-truncation** (only τ′-evidenced candidate; model-independent → preserves FC-46 seat-filler swappability; the grounding guarantee lives in a framework module, not the driver). **D1 resolved: surface-mode discriminator in the Serving Layer** as a named function (validate-not-assume per advisory 2). All **six DECIDE→ARCHITECT advisories addressed**; **no invariant change** (AS-9 §Scope annotation already records the conditional resolution); ADR-076 decomposition complete (parity addressed-by-composition; **axis-2 long-horizon coherence labeled structurally-non-decomposable** — recorded BUILD/PLAY risk, FC-51 makes failure diagnosable). EPISTEMIC GATE: belief-mapping on the Single-Step Enforcer / Loop Driver split; practitioner confirmed the split is load-bearing ("grounding can't depend on the driver model"). Gate produced a tighter artifact: **FC-51 refined to gate the fallback branch** (callee-incorrect → wrapper reversion; split-incorrect → Design Amendment) — agent self-resolved its own flagged uncertainty rather than asking the practitioner to speculate. Snapshot's one concern: FC-51 scope was ratified implicitly, not explicitly — **confirm before WP-LB-F** (snapshot Advisory 1). |
| BUILD (loop from RESEARCH re-entry) | ⏸ Paused after WP-LB-G (2026-06-02; loop-back #2 → DECIDE on Finding D) | WP-LB-A/B/C/D/G landed (suite 2891 green); Spike φ log `essays/research-logs/cycle-7-spike-phi-deliverable-shape.md` + `scratch/spike-phi-deliverable-shape/` | Delegation fires end-to-end vs real OpenCode; the WP-LB-G run surfaced Finding D. Spike φ split it: D1 (extraction) + D2a (inert contract; cheap model complies when contract present) + D2b (deliverable form). |
| DECIDE (loop from BUILD re-entry — Finding D) | ✅ Complete (gate closed 2026-06-03) | **ADR-035** (client-tool deliverable form contract: boundary-composed destination-keyed directive; ensembles destination-agnostic; one-dispatch-one-deliverable granularity [decision-level, flagged inference]; detect-and-refuse backstop escalation; D1 BUILD fix w/ last-successful-agent fallback; Conditional Acceptance ADR-097) + ADR-024 `Updated by ADR-035` header (+ stale-Proposed→Accepted fix) + Spike χ/χ.2 (`essays/research-logs/cycle-7-spike-chi-deliverable-shaping.md`) + scenarios.md ADR-035 feature block (+acceptance-criteria table) + interaction-specs ADR-035 task + 3-round argument audit (`housekeeping/audits/argument-audit-decide-cycle-7-loopback2*.md`; R3 converged 0 P1/0 P2) + snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback2-decide.md` (**No Grounding Reframe; 2 ARCHITECT advisories**) + gate note `.rdd/gates/cycle-7-loopback2-decide-architect-gate.md` | Practitioner counter-probed the rejected harder-enforcement alternative ("If we need a guarantee isn't that justified?") → produced the ADR's "hard form-guarantee neither available nor required" analysis. Then contributed the **seam framing**: form-coherence is hard at delegation seams in ways single-model flows don't face; concern is functional coherence (code that won't run), not write-risk. Folded in with the form-seam/semantic-coherence-seam distinction + "parity of mechanism claimed; parity of content quality not." Routed through ARCHITECT "for consistency." |
| ARCHITECT (loop-back #2 — Finding D) | ✅ Complete (gate closed 2026-06-03) | system-design.md **v6.1** (Amendment #14) + system-design.agents.md (Loop Driver `compose_form_directive` FC-53/54; Artifact Bridge FormGate seam + `destination_tool` FC-57; Orchestrator Tool Dispatch D1 fix FC-56; granularity in existing structure FC-55) + roadmap.md (WP-LB-H + TS-14 + LB-4/5/6; WP-LB-G closed) + ORIENTATION.md regenerated + snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback2-architect.md` (**No Grounding Reframe; 1 BUILD advisory**) + gate note `.rdd/gates/cycle-7-loopback2-architect-build-gate.md` | Gate: warrant elicitation on the FormGate seat (Bridge vs Terminal); practitioner redirected to north-star derivation ("You tell me based on our north star"); agent committed to the Bridge on three grounded reasons (layer-split diagnosability for axis-2 traces; refusal is dispatch-stratum work — avoids upward L3 control edges; form rules accrete at the content locus); practitioner approved. Both DECIDE advisories addressed in the design. |
| BUILD (loop-back #2 — WP-LB-H) | ▶ In Progress (re-entered 2026-06-03; gated) | — | **WP-LB-H**: form contract + D1. Acceptance gates: suite green + $0 real-OpenCode re-run landing a *runnable* file **with delegation verified fired** (ARCHITECT snapshot advisory — don't credit the directive if the seat-filler generated directly). Open at scenario gates: LB-4 (extraction locus), LB-5 (normalization now-or-later), LB-6 (directive wording). Then WP-LB-E/F resume. |

## BUILD-surfaced finding: deliverable-shape (Finding D) (2026-06-02)

**What surfaced.** The WP-LB-G real-OpenCode run delegated correctly (seat-filler → `invoke_ensemble` → `code-generator` → artifact → bridge → client `write`), but the `write` carried the raw ensemble result envelope (`{"results": {coder, critic, synthesizer}, "synthesis": null, …}`), not usable file content. Spike φ (`essays/research-logs/cycle-7-spike-phi-deliverable-shape.md`; $0 — code-reading + a no-model separability probe against the captured real artifact + 2 local claim-extractor runs) split Finding D into three separable layers:

- **D1 — extraction (BUILD).** `_extract_synthesizer_text` (`orchestrator_tool_dispatch.py:1373/1796`) succeeds only for a populated top-level `synthesis` (never set in the dependency-based model — `results_processor.py:21`) or a single-agent ensemble. Every multi-agent capability ensemble falls through to `json.dumps(raw_result)`. code-generator is the only multi-agent substrate-routed capability ensemble shipped → the only one bitten today, but the gap is structural. Internal where-sub-fork (executor-side populate vs envelope-side terminal-node pick) for the BUILD gate.
- **D2a — contract inert at execution (reopens a Cycle 6 call).** `default_task` has zero runtime reads (grep-proven; both the MCP-invoke and orchestrator-dispatch paths send only the caller input); `output_schema` is documentary (Cycle 6 WP-D). Single-agent ensembles with no `system_prompt` (claim-extractor, text-summarizer) run with no format contract at all. Run 2 shows qwen3:8b complies once the contract is in the prompt → a contract-delivery gap, not a capability gap. This contradicts the claim-extractor YAML's assumption that `default_task` shapes output, so it reopens the Cycle 6 WP-E documentary-contract decision.
- **D2b — deliverable form (DECIDE).** code-generator's agents carry system_prompts asking for client-readable markdown ("Format code with appropriate fenced blocks"; "Output exactly what the client should see") — the model obeys, but the form targets a human reader, not a bare file a `write` consumes. The irreducible I/O-contract question (carry-forward #3).

**Disposition (practitioner, 2026-06-02).** DECIDE on the I/O-contract (D2a + D2b); the D1 extraction fix is shaped to the DECIDE outcome. Loop-back #2: BUILD → DECIDE.

## BUILD-surfaced finding: client-tool-action terminal gap (2026-05-24)

**What surfaced.** At the WP-A BUILD reflection gate, the handler swap made concrete that ADR-027's pipeline emits only `ContentDelta` + `Completion` — the chat-completions surface now **never** returns `finish_reason: "tool_calls"`. The practitioner's north-star is: use OpenCode to invoke the RDD skill, with the actual work (writing files to the *local* machine, running commands) **delegated to ensembles**. The only coherent mechanism for "ensemble does the work, file lands on the local machine":

1. OpenCode sends a request carrying its client tools (`write_file`, `bash`, `read`).
2. The endpoint dispatches the generation to an ensemble (e.g., `code-generator` produces file content).
3. The endpoint returns the deliverable to OpenCode **as a `tool_calls` response** (`write_file(path, content=<ensemble output>)`).
4. OpenCode executes its local tool and writes to the developer's machine.
5. The loop continues (OpenCode returns the tool result; the endpoint decides the next step).

Ensembles run server-side and write to `.llm-orc/agentic-sessions/`, **disjoint** from the developer's filesystem (essay E4.2.1; PLAY note 22). So the deliverable must return through the client's local tools — the endpoint must emit `tool_calls`. ADR-027's pipeline has no stage that does this and is single-turn (no agentic loop).

**Diagnosis — how the cycle missed it (not a clarity failure on the practitioner's part):**

- **C1 conflation.** The cycle framed its problem as "NL-routing-to-ensembles ≈ 0 under tool-rich clients" and treated *both* direct-completion *and* client-tool-delegation as the failure. That conflated (a) the orchestrator *avoiding* ensemble dispatch (the real problem) with (b) client-tool-calls as the *mechanism* for applying work to the local filesystem (a needed capability). ADR-027 fixed (a) by removing the orchestrator-LLM, which killed (b) as a side effect.
- **ADR-022 §line 48 already described the split it dropped:** *"client-declared tools remain correct for actions on the tool-user's filesystem... when the user asks to write code, `code-generator` covers it."* That is the two-step flow (ensemble generates, client tool writes); ADR-027 dropped the second half without replacement.
- **No Cycle 7 spike tested the real-client round-trip.** Cycle 6 γ/λ ran against real clients but tested the *orchestrator-LLM* (the thing being removed); note 22 and λ.5-paid were literally the dispatch→client-write handoff failing. The Cycle 7 pipeline spikes (ζ/ε/ε'/μ/ν) ran against Python harnesses producing text — never through OpenCode. **The architecture being kept was never run through the client it exists to serve.**
- **The scope-of-claim partition covered routing + synthesis, never delivery-to-client.** No essay node named the client-tool-application surface, so no audit (5-round research, 7-round DECIDE) could catch its absence. The DISCOVER "rapid compounding" snapshot warning pointed at this risk.

**What still holds.** AS-9 (structurally-bounded roles produce reliable output) and AS-10 (capability matching from request content alone) are unaffected. ADR-027's architecture is **incomplete, not wrong**: it built the answer-a-question terminal but not the do-work-on-the-client terminal. The fix is *aligned* with ADR-027's philosophy — a framework that *deterministically* wraps an ensemble deliverable into a client tool-call would be more reliable than the orchestrator-LLM that failed at it (note 22).

**WP-A disposition.** WP-A stands as committed (`e538264` + `0a7a822`) — a faithful, green, reversible increment of ADR-027 as written. The `ClientToolCall` chunk plumbing removed in `0a7a822` (handler + `_collect_non_streaming` branches + `_build_completion_body` tool_calls shaping) comes back cleanly when the pipeline gains a client-tool-action stage; the `OrchestratorChunk` union still includes `ClientToolCall`.

## Path forward (loop-back BUILD → RESEARCH, 2026-05-24)

The next learning point is empirical, so RESEARCH re-entry leads with a validation spike (ADR-087 validation-spike-as-research; the cycle's own method).

**Spike (next session) — OpenCode client-tool round-trip.** Question it must answer: *can a framework-driven pipeline hand a real OpenCode session a `tool_calls` response carrying an ensemble deliverable, have OpenCode execute it locally, and close the multi-turn loop?* Minimal shape:
- Stand up agentic-serving (or a minimal stand-in) that, on a capability-matched request, dispatches to an ensemble (e.g., `code-generator`) and returns `finish_reason: "tool_calls"` with a `write_file` call carrying the deliverable.
- Point a real OpenCode session at it. Observe: does OpenCode execute the synthesized tool-call against the local filesystem? Does it send the tool result back? Does the next turn route correctly?
- Sub-questions: where does the target path come from (planner? request?); how does a deliverable map to a client tool (`code-generator` → `write_file`; what about `bash`/test-runs?); is deterministic emission reliable where the orchestrator-LLM failed (note 22)?
- Free-tier local Ollama + a real OpenCode install; ~$0 (confirm before any paid step per `feedback_free_options_preference`).

**After the spike (sketch only — detail deferred to what the spike teaches):**
1. RESEARCH: fold finding + spike result into an Essay-Outline 006 amendment naming the client-tool-action terminal as a first-order surface (re-audit per Essay-Outline-as-checkpoint).
2. DISCOVER (update mode): make the "delegate work, apply locally" loop explicit in the Skill Orchestration User + Tool User mental models; assumption-inversion on "endpoint-returns-text is sufficient."
3. DECIDE: amend ADR-027 (client-tool-action disposition + multi-turn loop) or new ADR; new scenarios + interaction-specs.
4. ARCHITECT: pipeline gains a terminal stage (deliverable → client tool-call); planner + synthesizer roles adjust; re-wire the streaming/non-streaming `tool_calls` surface.
5. BUILD: re-introduce `ClientToolCall` emission; build the new terminal; then WP-B/WP-C against the corrected surface.

**Do NOT resume WP-B/WP-C** until the terminal shape is settled — they build the routing-planner + synthesizer for the text-only terminal, which the finding shows is half the surface.

## Spike candidates for Cycle 7

To be ordered after RESEARCH establishes the question shape, but pre-named for orientation:

- **Spike ε:** minimal routing-planner ensemble + response-synthesizer ensemble + Python harness for end-to-end plan→dispatch→synthesize pipeline. Run against the prompts that broke under orchestrator-LLM routing.
- **Spike ζ:** routing-planner reliability — JSON-output adherence across prompt shapes, including adversarial / ambiguous prompts. Tests the cheap-tier-model-as-structured-output assumption.
- **Spike η:** synthesizer behavior on multi-dispatch results — does the narrower role + structured input prevent the confabulation pattern the orchestrator-LLM exhibited?
- **Spike θ:** general-completion fallback ensemble shape — does wrapping direct completion in an ensemble degrade response quality (compared to note 1 / note 18 quality on the string-reverse task)?
- **Spike ι:** schema-as-enforcement reliability — does qwen3:8b produce schema-conformant JSON when retried with feedback after non-conformant output?
- **Spike κ (alternative-architecture):** `tool_choice` constrained decoding probe under Zen + MiniMax M2.5 — does Zen support the parameter? Does the model honor it? Latency + reliability shape?
- **Spike μ (MODEL-boundary; ran 2026-05-22):** confabulation-mode generalization — tests whether Spike ε ε.1's structural-bounding finding generalizes from PLAY note 22 to three other documented confabulation modes. Free-tier; $0 cost; ~3 min wall-clock. Findings: μ.1 path hallucination transforms (confident-specific fabrication → honest-generic-conventions with explicit hedging); μ.2 substrate-path-as-deliverable structurally avoided (text-only synthesizer surface); μ.3 coherent factual errors uncalibrated bounded by Rule 1 + Rule 5. Extended combined audit depth to 4 confabulation modes + 13 tests at qwen3:8b. Writeup at `essays/research-logs/cycle-7-spike-mu-confabulation-generalization.md`. Practitioner-authorized AS-9 codification followed.

## Feed-Forward Signals

### From Cycle 7 loop-back #2 DECIDE (closed at gate 2026-06-03)

1. **ADR-035 commits the form contract; ARCHITECT allocates it.** The marshalling boundary composes a destination-keyed bare-output directive (`write` → bare bytes; `bash` → bare command; `edit` → bare replacement) injected into the callee dispatch input; ensembles stay destination-agnostic. ARCHITECT work list: (a) directive-composition responsibility — Loop Driver vs Client-Tool-Action Terminal (who composes, who injects); (b) the detect-and-refuse gate seated at the Artifact Bridge as a **named interface point** (snapshot advisory 2 — the escalation path needs an architectural home even though the gate is not built until PLAY evidence warrants); (c) the across-turn multi-file decomposition path designed (snapshot advisory 1 — until designed, the granularity invariant is an inference, not settled); (d) FCs for directive-presence, destination-keying, granularity, and D1 extraction; (e) roadmap WP for form contract + D1; (f) ORIENTATION regeneration.
2. **The seam taxonomy (practitioner gate framing — candidate MODEL vocabulary).** Three seams the delegation architecture carries that single-model flows don't: the *execution-model* seam (sealed by ADR-034), the *generation-form* seam (sealed by ADR-035), and the *semantic-coherence* seam (OPEN — delegated content fitting the surrounding project; lives with seat-filler dispatch-input composition [axis-2, OQ #27] + ensemble quality [DISCOVER-declared orthogonal]; FC-51 wrong-content vs wrong-action vs wrong-form discrimination is the PLAY instrument). "Parity of mechanism is claimed; parity of content quality is not." Candidate vocabulary for a future MODEL pass: form seam / context seam.
3. **D1 implementation notes for BUILD:** last-successful-agent fallback is mandatory (the synthesizer can time out — Spike χ-P1); the where-sub-fork (executor-side `finalize_result` population where `depends_on` is known vs envelope-side terminal-node pick, order-fragile for branching DAGs) is a BUILD scenario-group decision.
4. **ADR-024 downstream supersession sweep deferred to BUILD** (rationale recorded per Step 2.5: the mechanism ships in BUILD; envelope-*shape* references in system-design/ORIENTATION/domain-model/field-guide remain correct meanwhile; only client-tool-form discussions gain an ADR-035 pointer). Sweep fitness check: `grep -n "ADR-024"` across the four artifacts when the WP lands.
5. **Acceptance criterion with layer-match "no" for BUILD Step 5.5:** the north-star criterion (delegated deliverable lands as *usable* bare content) is specified at the real-OpenCode layer; per-component scenarios stub the client. The $0 real-OpenCode smoke test + TS-12/13 with the directive in effect is the layer-matching verification (the WP-A "never run through the client" scar is the antipattern).

### From Cycle 7 loop-back BUILD — Spike φ + Finding D disposition (DECIDE loop-back #2, 2026-06-02)

1. **DECIDE scope = the client-tool deliverable I/O-contract (D2a + D2b).** Spike φ established Finding D is three separable layers (D1 extraction / D2a contract-delivery / D2b deliverable form). Practitioner disposition: DECIDE on D2a + D2b; D1's extraction fix is shaped to the outcome (so it does not pre-commit a shape DECIDE might override — F-φ.3 sequencing). The DECIDE question: *what should a capability ensemble emit when its deliverable becomes a client-tool argument (a `write` body / a `bash` command), and how does that contract reach and constrain the model?*

2. **D2a reopens a specific Cycle 6 call — read it before drafting.** The claim-extractor / text-summarizer YAML comments record the Cycle 6 WP-E decision to keep `output_schema` + `default_task` **documentary** (per Spike β: "drift is upstream at the orchestrator's input.data override, not the synthesizer's compliance"). That reasoning presupposed `default_task` *reaches* the model and shapes output. Spike φ Run 1 (no contract → chatty) + the grep-proven zero runtime reads of `default_task` say it does **not** reach the model at all. So the DECIDE re-examines Cycle 6 WP-E with corrected evidence. **Caveat to confirm at DECIDE:** zero-reads is grep-strong and two execution paths were traced (MCP-invoke; orchestrator dispatch `orchestrator_tool_dispatch.py:717-720,766-769`), but DECIDE should confirm no path was missed before treating "default_task is inert" as settled.

3. **Grounding already in hand — the cheap model is contract-capable (Run 2).** qwen3:8b complied exactly with the claim-extractor format contract once it was in the prompt. This narrows the D2 mechanism space: the *format-compliance* flavor (D2a) may not need heavy reject-and-retry enforcement — getting a well-formed contract to the model may suffice. The hard part is D2b (defining bare-file vs human-readable form). Mechanism options the cycle has pre-named: `output_schema`-as-enforcement (reject-and-retry); a `submit_file`-shaped synthesizer (agent calls a tool whose param is the bare content); a deterministic post-agent shaper; or simply wiring `default_task` through to the agent prompt (the D2a minimal fix). **Caveat:** Run 2 is n=1 paragraph / one ensemble / one local model — compliance breadth across prompts/ensembles/escalated tiers is unmeasured (a DECIDE-entry or BUILD probe candidate).

4. **D1 carries an internal where-sub-fork (BUILD gate, do not pre-decide).** Envelope-side terminal-node extraction relies on `results` insertion order (robust for linear pipelines, fragile for branching DAGs); executor-side populating a canonical deliverable field in `finalize_result` (where `depends_on` is known) is the robust alternative. Surface at the BUILD scenario-group gate when D1 lands.

5. **Connects to the cycle's standing form-drift carry-forward #3.** Finding D D2b *is* carry-forward #3 (claim-extractor non-conformance) demonstrated on the code-generator → client-`write` path. The DECIDE ADR on I/O-contract enforcement the cycle has flagged since DECIDE-first-pass (and Cycle 6) is the natural home. Check ADR-024 (`output_schema`) + ADR-025 (substrate-write) + the ADR-027 scope when drafting.

### From Cycle 7 loop-back BUILD — WP-LB-G seat-filler delegation + Finding D (landed 2026-06-02 evening)

**WP-LB-G landed (commit offers the seat-filler `invoke_ensemble` + the capability list + delegation guidance; `LoopDriver.decide` augments the tool list, `get_loop_driver` threads `_build_capability_names`). Suite 2891 green.** This closes Finding B at the mechanism level.

1. **DELEGATION FIRES — Finding B resolved (validated against the real model).** Re-ran the real-OpenCode rig (real `llm-orc serve` + qwen3:14b, $0). The serve log shows the cheap seat-filler **chose to delegate**: `tool-call emit: tool=invoke_ensemble` → `dispatch start: ensemble=code-generator` → `tier selection: topaz_skill=code_generation` → artifact written → bridge marshalled → `finish_reason: tool_calls` with a `write`. The full callee → bridge → ApplyWork chain is reachable end-to-end with a real model. The cheap-driver-skips-delegation tension did not bite (the nudge held); cross-prompt/cross-client reliability is an axis-2 / PLAY observation. WP-LB-G met its acceptance gate.

2. **CRITICAL FINDING D (new blocker) — the marshalled deliverable is the raw ensemble result envelope, not usable file content.** The `write` content was `{"results": {"coder": {"response": "…```python\ndef fibonacci…```\n### Key Points…"}}}`. The bridge faithfully marshalled what was stored; the problem is upstream at the **ADR-025 substrate-write / ensemble-output-contract layer**. Two layers: **D1 (extraction)** — the artifact holds the full multi-agent `{"results": {coder, critic, synthesizer}}` dict, not the synthesizer's final output; **D2 (form drift)** — even the synthesizer output is conversational markdown (prose + fenced code), not bare file content. This is the cycle's recorded form-drift / I/O-contract concern (carry-forward #3; claim-extractor non-conformance) demonstrated for code-generator → client-write. Evidence: `scratch/wp-lb-c-opencode-validation/wplbg_stored_artifact.json` + `serve_wplbg.log`.

3. **NEXT: Finding D disposition (DECIDE-adjacent).** D1 looks like a deliverable-extraction bug (the artifact should hold the synthesizer's output, not the raw result dict). D2 is the I/O-contract / form-drift design question the cycle flagged for a DECIDE-level policy (bare-deliverable contract for client-tool content — `output_schema` / a `submit_file`-shaped synthesizer / a deterministic shaper). **A natural fresh-context boundary** — Finding D is upstream of the terminal surface WP-LB-C/G worked in. WP-LB-E (wrapper seam) / WP-LB-F (axis-2 instrumentation) remain after Finding D. Research log: `essays/research-logs/cycle-7-wp-lb-c-opencode-validation.md` §"Update — WP-LB-G".

### From Cycle 7 loop-back BUILD — WP-LB-C Client-Tool-Action Terminal + real-OpenCode validation (landed 2026-06-02)

**WP-LB-C landed (commits `f32bada` layer-map debt fix; `aa971a1` refactor extract Terminal + Loop Driver `decide()`; `272997e` feat bridge wiring; `ab07eb7` feat non-streaming `tool_calls`; `24e6100` test real-ASGI round-trip; `33a65ac` docs OpenCode smoke procedure; `8e63cee` fix seat-filler `base_url`). Suite 2888 green.** The Client-Tool-Action Terminal (`agentic/client_tool_action_terminal.py`, L3) is the `_ChatCompletionsCaller` for the tool-driven surface — the practitioner chose the architecture-faithful split (Terminal owns emission + the bridge call; Loop Driver `decide()` returns a `TurnOutcome` and owns the per-turn choice/dispatch). Streaming + non-streaming `tool_calls` emission; substrate-routed deliverables marshalled to full fidelity via the Artifact Bridge (degrades to a dispatch-failure completion on bridge error / binary deliverable).

1. **NORTH-STAR VALIDATION RAN against real OpenCode (the practitioner's "don't fly blind" directive, discharged).** Real OpenCode 1.15.5 (headless) → real `llm-orc serve` (WP-LB-C terminal) → real qwen3:14b seat-filler → `finish_reason: tool_calls` → **OpenCode executed the `write` locally, `hello.py` landed, loop closed with parity.** FC-47/48/50 validated against the real client, $0 local Ollama. The WP-A scar ("never run through the client") is closed at the real-client layer for the terminal/loop-participation mechanism. Research log: `essays/research-logs/cycle-7-wp-lb-c-opencode-validation.md`; artifacts retained at `scratch/wp-lb-c-opencode-validation/`.

2. **CRITICAL FINDING B (open) — the seat-filler is never offered `invoke_ensemble`, so ensemble delegation does NOT fire end-to-end.** `LoopDriver.decide` passes only the client's tools (`write`/`edit`/`bash`/`read`) to the seat-filler; nothing augments the list with `invoke_ensemble`. So a real seat-filler can only grounded-carry (emit a literal client tool call) or finish — it cannot delegate. **Evidence:** the OpenCode run produced **zero ensemble dispatches and zero artifacts**; the cheap seat-filler generated the code itself. **Consequence:** the callee → bridge → ApplyWork machinery (WP-LB-B FC-44, WP-LB-D bridge, WP-LB-C substrate marshalling) is correct + harness-tested but **unreachable in production** — the harness tests pass only because they *script* `invoke_ensemble`. The cycle's central value proposition (delegate work to ensembles) is not wired. **Deeper tension:** the seat-filler decides whether to delegate, and a model capable enough to drive is often capable enough to skip delegation (qwen3:14b did) — the flip side of the recorded over-delegation worry; only a real run with `invoke_ensemble` offered can answer whether/when it delegates.

3. **NEXT: WP-LB-G (proposed) — offer the seat-filler `invoke_ensemble` + the capability list, before WP-LB-E/F.** WP-LB-E (wrapper-contingency seam) and WP-LB-F (axis-2 instrumentation) are both downstream of delegation working at all; sequence WP-LB-G first. The *design* is settled (ADR-033 callee + `invoke_ensemble`; WP-D `_build_capability_names` is reusable) — the gap is integration + a delegation-vs-act-directly system prompt. **Load-bearing acceptance gate:** a real OpenCode session whose serve log shows a real `invoke_ensemble` dispatch + a `code-generator` artifact marshalled through the bridge into the client write (not a scripted test). **Loop-back trigger:** if the real run shows the seat-filler will not reliably delegate, loop back to DECIDE on the delegation-decision mechanism (`tool_choice` forcing / routing pre-filter / different driver-vs-delegation split) — on evidence, not speculation.

4. **Finding C (minor, known) — seat-filler tool-calling is prompt-sensitive (thinking-mode).** One probe returned no tool call; an explicit prompt returned `tool_calls` reliably. Consistent with "seat-filler behavior unverified in BUILD by design" (axis-2 / PLAY concern), not a defect.

### From Cycle 7 loop-back BUILD — WP-LB-D Artifact Bridge + read_deliverable (landed 2026-06-02)

1. **WP-LB-D landed: the deliverable-content marshalling step is built.** `SessionArtifactStore.read_deliverable(reference) -> str | bytes` (commit `2b7f362` — the first read-side API on the formerly write-only store; structural inverse of the write path [strip `agentic-sessions/` prefix, rejoin under root]; decode-probe str/bytes; `ArtifactNotFoundError` [`LlmOrcStructuralError`, `error_kind="artifact_not_found"`, `operator_intervention_required`] on unresolvable reference) + `agentic/artifact_bridge.py` `ArtifactBridge.marshal(envelope)` (commit `3b9d829` — owns the artifact-vs-inline branch; substrate → `read_deliverable` with fidelity, inline → `envelope.primary` no-op; reconstructs the typed `ArtifactReference` from the serialized `envelope.artifacts[0]` dict) + integration suite (commit `af5af7d` — real bridge↔store; **FC-49 fidelity-at-scale gate**: a >256 KB multi-byte UTF-8 deliverable round-trips byte-identical; retention-window lifecycle case [`cleanup_session` disposal → stale ref raises `ArtifactNotFoundError`]). Suite 2873 green; Tier-1 stewardship clean.

2. **Built in isolation, NOT yet wired into the live loop (deliberate; WP-LB-C wires it).** `loop_driver._deliverable_text` still reads `envelope.primary` directly — the bridge is the *capability* to close the substrate-deliverable-fidelity gap, consumed by the Client-Tool-Action Terminal (WP-LB-C), which ADR-034 names as the bridge's consumer. The live gap (substrate-routed deliverable marshalling the summary instead of the content) closes when WP-LB-C wires terminal → bridge. WP-LB-D respects scope: the terminal does not exist yet, so the bridge is tested against fixtures + the real store.

3. **NORTH-STAR VALIDATION DIRECTIVE (practitioner, at WP-LB-D gate): don't fly blind — validate against real OpenCode at the earliest runnable point.** BUILD tests (incl. WP-LB-D) validate *mechanism*, not the north star; the north-star validation proper is a real OpenCode session against a real `llm-orc serve`. The cycle's own WP-A scar tissue ("the architecture being kept was never run through the client it exists to serve") is the failure mode to avoid repeating. **The earliest a parity round-trip becomes runnable is after WP-LB-C** (terminal emits `tool_calls` + loop participation; TS-12), NOT PLAY. **Carry into WP-LB-C:** (a) add a real-serving-surface integration test (drive the actual ASGI chat-completions endpoint with a tool-carrying request + `role:"tool"` follow-up, assert real `tool_calls` SSE carrying bridge-marshalled content), not just loop-driver fixtures; (b) surface a $0 real-OpenCode smoke-test procedure for the practitioner to run (real OpenCode + local Ollama, per memory `free-options-preference`); treat the real-client round-trip as a first-class WP-LB-C acceptance gate. Axis-2 long-horizon coherence remains PLAY/first-deployment by design (ADR-033 §6b, ADR-097). Recorded durably as memory `validate-against-real-client-not-harness`.

4. **Next: WP-LB-C** (Client-Tool-Action Terminal + multi-turn loop participation) — both hard prereqs satisfied (WP-LB-B + WP-LB-D landed; Track A-LB.1 done). Re-introduce `ClientToolCall` emission (the `0a7a822` removal: handler branch + `_collect_non_streaming` branch + `_build_completion_body` `tool_calls` shaping + the `tool_calls` field on `_NonStreamingResult`), wire terminal → `ArtifactBridge` → `write` tool call, consume `role:"tool"` follow-ups into the loop-driver (`_extract_request` currently drops `role:"tool"` messages — the loop-participation gap). Then WP-LB-E (delegation-target strategy seam) + WP-LB-F (`TurnDecision` surfacing).

### From Cycle 7 loop-back BUILD — WP-LB-B Loop Driver + Single-Step Enforcer (landed 2026-06-02)

1. **WP-LB-B landed: the layer-A control structure is built.** `single_step_enforcer.py` (FC-43 batch-truncation), `loop_driver.py` (FC-44 callee / FC-45 grounded carry / FC-51 TurnDecision), and the `get_loop_driver()` swap (real driver + Model-Profile seat-filler resolution, FC-42 event assertion). Suite 2865 green; Tier-1 stewardship clean; callee boundary integration-verified against the real Tool Dispatch. Commits `67d42b9`/`81ad0ea`/`813823e`/`fd0e74f`/`b16252f`/`15bf81d` + amendment `782b092`.

2. **Seat-filler contract = Reading B (Design Amendment #13).** The seat-filler emits either a **literal client tool call** (passed through verbatim — grounded carry, FC-45) or an **`invoke_ensemble`** internal call (callee generation, FC-44; the driver dispatches one ensemble and maps the deliverable to a client tool). This was a BUILD-design-gate decision: the implicit ARCHITECT reading (driver intercepts every `write`/`edit` and regenerates content via an ensemble) would route an observed/literal value through ensemble regeneration and lose grounding fidelity. Provenance is design-phase allocation; `invoke_ensemble` is the established ADR-003 delegation primitive. **Downstream implication:** WP-LB-C's terminal and WP-LB-D's bridge build on Reading B — the deliverable they marshal is what the seat-filler's `invoke_ensemble` decision produced, not a driver-intercepted write.

3. **AXIS-2 MITIGATION CANDIDATE (PLAY watch-list): a driver-state / plan artifact for long-horizon coherence.** Reading B added a new per-turn judgment the cheap seat-filler must make — `invoke_ensemble`-vs-literal-client-tool — on top of the trajectory-coherence burden axis-2 already names (OQ #27 axis 2). A WP-LB-B gate exchange surfaced an artifact-based mitigation, echoing single-step enforcement's "make it structural, not a model assumption" move: a **durable driver-maintained plan/scratchpad artifact** (re-read at the top of each turn from the `SessionArtifactStore`, ADR-025) externalizes "task / done / next" so the cheap driver re-grounds structurally instead of holding the whole trajectory in a compacting context. It folds naturally into the **deferred `todowrite` surface** (ADR-033 §6c; signal #6 below) — OpenCode's client-side `todowrite` is this mechanism. **Honest scope:** (a) it buys *coherence* (driver stays self-consistent) but not *correctness* — the plan is still cheap-model-generated, so it converts "drift" into "committed-to-a-wrong-plan," a distinct FC-51 signature (a bad early `TurnDecision` faithfully executed vs. turn-to-turn wandering); (b) it does NOT directly address the internal-vs-client tool-class pick (that lever is tool-surface shaping, which tangles with grounded carry). **Disposition: NOT built — recorded as the leading axis-2 mitigation candidate.** Per ADR-033 Conditional Acceptance the axis-2 risk is a PLAY/first-deployment validation target; FC-51 `TurnDecision` instrumentation is designed to reveal *which* failure signature appears, which decides the response (plan-artifact for coherence drift / tool-surface change for wrong tool-class / frontier-driver swap for raw capability). **PLAY should instrument the axis-2 run to distinguish coherence-drift (committed-wrong-plan) from turn-to-turn wandering, so the right mitigation is chosen on evidence, not pre-built.** Pursuing it = a follow-on ADR + WP (driver-state artifact, possibly folding in `todowrite`), not a WP-LB-B/C/D change.

4. **Deliverable-content fidelity is a WP-LB-D gap, not closed in WP-LB-B.** `loop_driver._deliverable_text` reads `envelope.primary` — for substrate-routed capability ensembles (ADR-025) that is a summary + `ArtifactReference`, not the content. WP-LB-D's Artifact Bridge (`read_deliverable`) resolves full content. The callee integration test verifies structural flow (real dispatch accepts the `invoke_ensemble` arg shape; deliverable reaches a client `write`), explicitly NOT large-deliverable fidelity (Acceptance Criteria Table row 3 — WP-LB-D).

5. **`_resolve_seat_filler` is the swappable seam (FC-46) and the PLAY lever.** The seat-filler resolves the orchestrator's `model_profile` → tool-calling `ModelInterface` via `ModelFactory`, guarded on `supports_tool_calling`. Swapping cheap-tier ↔ frontier-tier (the named axis-2 fallback) is a profile config edit, no Loop Driver change. End-to-end seat-filler behavior is unverified in BUILD by design — it is the PLAY/first-deployment axis-2 run (a real OpenCode session), per ADR-033 §6b. The factory test overrides this seam; no real model call in BUILD.

### From Cycle 7 loop-back BUILD — WP-LB-A surface-mode discrimination (landed 2026-06-02, `4d8108d`)

1. **WP-LB-A landed.** The Serving-Layer surface-mode discriminator (`_is_tool_driven`, `len(request.tools) > 0`) branches tool-driven requests to `get_loop_driver()` and non-tool requests to `get_dispatch_pipeline()`; both satisfy `_ChatCompletionsCaller` so the heartbeat/sink/streaming lifecycle is caller-agnostic. WP-LB-A ships a `_StubLoopDriver` (finishes with a stop completion — the safe terminal) behind the `get_loop_driver` factory; **WP-LB-B swaps the factory body for the real `agentic/loop_driver.py` with no call-site change.** Suite 2851 green; mypy strict + ruff + format clean; 4 new tests in `TestSurfaceModeDiscrimination`.

2. **FC-42 partially verified at WP-LB-A; event-assertion deferred to WP-LB-B.** The routing behavior + per-request mutual-exclusivity are verified via observable response content (distinguishable stub callers). FC-42's *specified* verification is event-based (`tools[]` → `TurnDecision`; no-tools → `PlanEmitted`); neither event exists until WP-LB-B (TurnDecision) and a real pipeline (PlanEmitted). The event-type assertion is the natural WP-LB-B addition against real callers — do not fake it against stubs.

3. **NORTH-STAR IMPLICATION — the four RDD modes map to both layers; axis-2 concentrates in artifact-reasoning + code-recovery, NOT the conversational path (sharpens WP-LB-B).** *(Corrected 2026-06-02 after practitioner challenge: an earlier draft of this signal over-indexed on RDD-is-conversational; RDD is conversation **plus** artifact generation, reasoning across artifacts, and code work.)* The north-star process is RDD-via-OpenCode, whose turns all carry `tools[]` (→ loop-driver) but span four modes that map across the layer-A/layer-B split:
   - **Conversation** (gates, reflections, questions) → **layer A**, loop-driver finishes with text. The discriminator keys on `tools[]` *presence* not intent, so these route to the driver too: finish-with-text is **frequent, not an edge case** — the scenario *"Loop-driver finishes with a text completion when no further action is needed"* is more load-bearing than its placement suggests (the driver must default to **not acting** when a turn does not warrant a tool, or it would `write` when asked a question at a gate).
   - **Generating an artifact/code** (write an ADR, write a module) → **layer B**, callee ensemble generates; terminal emits `write`/`edit`. Accounted for *structurally*; whether an ensemble writes a *good* ADR is ensemble quality — declared orthogonal to Cycle 7 at DISCOVER ("ship delegation reliability; ensemble quality iterates independently").
   - **Reasoning about artifacts** (read an essay, audit an ADR against it, synthesize a trail) → **splits**: reasoning-that-*produces* output is layer B (callee); reasoning-that-*decides the next step* from an observed `read` result is layer A (loop-driver, via grounded carry + WP-LB-C loop participation).
   - **Code work** (TDD: write → `bash` test → diagnose → fix → re-run) → same split: generation is layer B; deciding "fix vs. move on" from observed test output is layer A.

   **No mode needs a mechanism the WP-LB design lacks** — `read`/`write`/`edit`/`bash` flow through the terminal (WP-LB-C), results flow back through loop participation + grounded carry. What the artifact-reasoning + code-recovery modes do is **load the axis-2 risk far more heavily than a conversational view implies**: they are sustained, accumulating, error-prone multi-turn trajectories driven by the cheap seat-filler — exactly OQ #27 axis 2 (sequential composition / error-accumulation, the regime AS-9 never tested). **The hardest north-star path is the cheap driver reasoning across a growing artifact set and recovering from test failures, not the conversational path.** This makes WP-LB-B's `TurnDecision`/FC-51 diagnostics more valuable: a driver that misreads an artifact and picks a bad next step shows as a wrong-*action* turn (driver/split), distinguishable from a wrong-*content* turn (callee). It confirms ADR-033 §6b — axis-2 validation must be a **real RDD session** (only RDD naturally produces the reasoning + code-recovery mix), not a synthetic long task.

4. **The discriminator is forced, not chosen (settled-for-build).** `tools[]`-presence is the only architecturally-clean signal: `tool_choice` is stripped at input (Spike κ D0) and ADR-030-deferred; content-classification-as-surface-switch would conflate surface-mode with capability-routing and violate AS-10 (layer-A/layer-B separation); last-message-is-tool-result misses the first agent turn. So the coarseness is non-negotiable and is *compensated by* the loop-driver's finish-with-text capability (signal #3).

5. **Cross-client validate-not-assume (carried to PLAY/first-deployment).** "Agent turns carry `tools[]`" is observed (Spike π, OpenCode 1.15.5), not specified. When the north star widens past OpenCode (Aider/Cursor/Cline), each client's `tools[]`-shaping is a fresh validation event per advisory #2 — the discriminator does not get less correct, but its grounding is per-client.

6. **Scope boundary the WP-LB set does NOT cover: the richer tool surface (`skill`/`task`/`todowrite`).** The surface RDD-via-OpenCode *literally* drives is `skill` (invoke the RDD skill), `task` (subagents), and `todowrite` (tracking) on top of `read`/`write`/`edit`/`bash`. ADR-033 §6c names the full surface but scopes Cycle 7 BUILD to the **file-action tools** — `skill`/`task`/`todowrite` are "north-star context ARCHITECT/BUILD must not be blindsided by," explicitly deferred. So WP-LB-A..F deliver "ensemble work lands on the client's filesystem" (the loop-back's purpose), **not** "drive a full RDD session." The literal run-RDD-via-OpenCode flow is the file-action subset in Cycle 7; the richer surface is a follow-on. WP-LB-B's loop-driver should map the file-action tools and not silently mishandle an unmapped `skill`/`task`/`todowrite` call (treat as a named validation event, consistent with advisory #2).

7. **Wrapper-residual watch point under artifact-reasoning (ADR-033 F3-1).** If RDD's artifact-reasoning frequently needs *within-a-single-turn* multi-capability composition (read 3 docs AND synthesize, in one step), that is the recorded wrapper residual — the place OQ #26's callee resolution could reopen. Across-turn composition (read, read, synthesize as three turns) is callee-native and fine, and RDD mostly decomposes across turns, so callee holds. This is a PLAY/first-deployment watch point, not a BUILD blocker.

### From Cycle 7 loop-back ARCHITECT (closed at gate 2026-06-02)

1. **Four new modules decompose the tool-driven multi-turn surface.** Loop Driver (L2; layer-A control structure, per-turn callee delegation, swappable seat-filler, axis-2 `TurnDecision` diagnostics), Single-Step Enforcer (L2; batch-truncation grounding guarantee), Artifact Bridge (L2; `read_deliverable` → tool-call content marshalling), Client-Tool-Action Terminal (L3; `tool_calls` emission + multi-turn loop participation). Two extensions (Serving Layer surface-mode discrimination; Session Artifact Store `read_deliverable`). Dispatch Pipeline scoped to single-turn. Full entries + responsibility matrix + 9 cycle-free edges + integration contracts + FC-42..FC-52 + test architecture in `system-design.agents.md`. **BUILD decomposes these as roadmap WP-LB-A..F.**

2. **D2 = batch-truncation; D1 = Serving-Layer discriminator** (both ARCHITECT-resolved, not deferred). Batch-truncation is the only τ′-evidenced enforcement candidate and is model-independent (so the seat-filler stays swappable, FC-46); the two untested candidates (re-planning prompt; one-tool `tool_choice`) remain BUILD-tunable behind the Single-Step Enforcer boundary (`tool_choice` weakest per Spike κ). The surface-mode discriminator (`len(request.tools) > 0`) is a named Serving-Layer function, **validate-not-assume** — treat the first unexpected `tools[]` pattern from a tool-capable client as a named validation event (BUILD advisory 2), not a defect.

3. **Six BUILD carry-forward advisories** (from the architect→build snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-architect.md`; No Grounding Reframe), priority order: (1) **confirm the FC-51 scope** with the practitioner before WP-LB-F — the ARCHITECT-gate refinement made FC-51 a fallback-branch selector (callee-incorrect → wrapper reversion; split-incorrect → Design Amendment), ratified implicitly not explicitly; (2) treat the surface-mode discriminator's first unexpected production signal as a named validation event; (3) keep the D2 tuning window visible (re-planning-prompt candidate has no falsifying evidence); (4) the large-deliverable artifact-bridge fidelity test (FC-49) is a first-class BUILD gate, encoding edge cases in the matrix; (5) confirm the wrapper-contingency fallback ordering (frontier driver first, wrapper second) with the practitioner before the PLAY axis-2 run; (6) **Track A-LB.1 (remove the stale `ClientToolCall` docstring, `v1_chat_completions.py:581-583`) is the non-negotiable first `refactor:` commit** before any loop-back feature work.

4. **Wrapper-contingency fallback ordering (advisory #1 / F3-1 closed at ARCHITECT).** If BUILD/PLAY axis-2 validation shows the cheap driver cannot hold the horizon: **(1) frontier-tier driver first** (a Model Profile swap, FC-46 — preserves the lean callee shape); **(2) wrapper reversion second-order** (Loop Driver delegates per-turn generation to `DispatchPipeline.run()`, FC-52 keeps it accessible) — only if a frontier driver also fails. FC-51's split-vs-callee diagnosis gates this: callee-incorrect → wrapper worth trying; split-incorrect → Design Amendment (the two-layer split itself is implicated).

5. **Axis-2 long-horizon driver coherence is the recorded load-bearing risk (ADR-033 §Decision ¶5), carried to BUILD/PLAY under ADR-097 Conditional Acceptance.** No synthetic test reaches it; FC-51 `TurnDecision` instrumentation (WP-LB-F) makes a failure diagnosable. The seat-filler model is the testable lever (cheap-tier default; frontier-tier fallback — a config change, not re-architecture, per FC-46). North-star-shaped axis-2 validation (a real skill-framework / RDD-via-OpenCode session), not a synthetic long task.

6. **Both Cycle 7 surfaces are BUILD scope; Capability List Builder is shared.** The single-turn surface (first-pass WP-A landed `e538264` + `0a7a822`; WP-B/C/D/E pending — Routing Planner, Response Synthesizer, Capability List Builder + Discovery, Response Labeling) and the tool-driven surface (WP-LB-A..F) coexist behind the surface-mode discriminator. The Loop Driver selects per-turn capabilities from the same **Capability List Builder** (single-turn WP-D). `edit`/`bash`/multi-file/streaming-token coverage beyond `write` is BUILD/PLAY scope (LB-3).

7. **BUILD-mode declaration at entry per ADR-091: recommended gated** — the loop-back carries the axis-2 load-bearing risk + the validate-not-assume discriminator + the FC-51-scope confirmation, all of which want practitioner-in-the-loop stewardship. Auto mode appropriate only once the surface-mode shape + Loop Driver structure are in place and remaining WPs reduce to mechanical wiring.

### From Cycle 7 loop-back DECIDE (closed at gate 2026-06-01)

1. **OQ #26 resolved → callee (ADR-033).** The layer-A loop-driver centers the multi-turn tool-driven chat-completions architecture; per-turn generation is delegated to a single capability ensemble (not the plan→dispatch→synthesize pipeline). ADR-034 adds the client-tool-action terminal + artifact-bridge + multi-turn loop participation. ADR-027 scoped (partial-update header) to the single-turn / layer-B surface. Resolved on the DECIDE-entry wrapper-shaped probe (Spike υ, run deliberately to avoid callee-skew: pipeline-per-turn ~3× latency + redundant planner/synth stages) + practitioner fork resolution. **ARCHITECT decomposes the loop-driver + client-tool-action terminal + artifact-bridge into modules.**

2. **Grounded loop = ADR-097 Conditional Acceptance, conditional on framework-enforced single-action-per-turn.** Spike τ (n=4) falsified the *unconstrained* cheap driver (it batches + presupposes unobserved state — axis-1 failure); Spike τ′ (n=3) passed under single-step enforcement. Grounding is a framework property, not a model assumption. **Axis 2 (long-horizon coherence) is the BUILD/PLAY validation target** and the recorded load-bearing risk. Research log: `essays/research-logs/cycle-7-spike-tau-upsilon-decide-entry-probes.md`.

3. **Six ARCHITECT advisories (from the decide→architect susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-decide.md`; No Grounding Reframe), priority order:** (1) **specify the wrapper-contingency path** (callee-failure → fallback ordering — F3-1 left it unspecified); (2) treat the **surface-mode discriminator** (branch on client `tools[]`) as **validate-not-assumed** (it is drafting-time synthesis, not driver-derived); (3) **select the single-step enforcement technique** before module decomposition (batch-truncation is the τ′-backed prior; re-planning prompt + one-tool `tool_choice` are untested candidates); (4) **`SessionArtifactStore.read_deliverable()`** is the first artifact-bridge API addition; (5) add **axis-2 diagnostic instrumentation** so a split-vs-callee failure is distinguishable in BUILD/PLAY; (6) the **refactor-now docstring removal** (`v1_chat_completions.py:581-583`) before BUILD.

4. **Gate-folded constraints (ADR-033, evaluated against the north star at the gate):** **(a) seat-filler swappability is SETTLED** — the loop-driver model is a swappable Model Profile (ADR-011 pattern), so the cheap-vs-capable bet is a config change in BUILD/PLAY, not a re-architecture (this is the precondition that makes the bet cleanly testable; the model *choice* is open); **(b) axis-2 validation is north-star-shaped** — a real skill-framework / RDD-via-OpenCode session, not a synthetic long task; **(c) full client tool surface** — the loop-driver must handle OpenCode's full declared surface (`skill`/`task`/`todowrite` beyond `read`/`write`/`edit`/`bash`); file-action tools are the BUILD focus, the richer surface is north-star context ARCHITECT must not be blindsided by.

5. **Conformance debt for ARCHITECT/BUILD** (`housekeeping/audits/conformance-scan-cycle-7-loopback-decide.md`; 12 findings): 1 refactor-now (advisory #6 above) + 9 BUILD-work (loop-driver, single-step enforcement, callee per-turn delegation, tool-call terminal, artifact-bridge, multi-turn loop, `ClientToolCall` re-introduction — the `0a7a822` removal mapped to exact file:line touchpoints) + 2 ARCHITECT-deferral (D1 surface-mode branch placement; D2 single-step enforcement technique). Key structural notes: `_extract_request` (`dispatch_pipeline.py:325-335`) drops `role: "tool"` messages (loop-participation gap); the SSE formatter already handles `ClientToolCall` (streaming terminal ready, no formatter changes); `SessionArtifactStore` is write-only (needs `read_deliverable`); the non-server-side-write FC is satisfied by absence.

6. **Working-preference note for future gates (durable; memory `feedback_outcome_based_over_speculation`):** the practitioner holds an outcome-based view of success and pushed back on grounding-frame questions that ask him to speculate on unobserved results. Once a decision is grounded on available evidence, convert remaining open risk into named BUILD/PLAY validation criteria rather than gate-time speculation; reserve belief-mapping for genuinely examinable assumptions; prefer proposing a settled/open split for confirmation over asking the practitioner to generate speculation. Prefers clean single approaches over outcome-muddying hybrids.

**Settled/open split + full commitment-gating: see gate note `.rdd/gates/cycle-7-loopback-decide-architect-gate.md`.**

### From Cycle 7 loop-back MODEL (closed at gate 2026-06-01)

1. **AS-9 scope-fence (no invariant amendment).** Per-turn agentic-loop-driving is held OUTSIDE AS-9's validated single-decision scope until validated. AS-9's claim is unchanged; the §Scope boundary annotation fences out the new role-shape (layer-A loop-driver). The grounded-loop hypothesis is a working inference (Spike σ, n=1). DECIDE must not treat the loop-driver as covered by AS-9's reliability finding.

2. **AS-9 reopening is TWO-AXIS, not pre-narrowed (widened at the MODEL gate).** If the grounded-loop hypothesis fails, AS-9 reopens along two axes the single-decision basis cannot discriminate: **(1) grounding** — driver slips into ungrounded batch planning; **(2) sequential composition** — trajectory drifts/accumulates error across many decisions even with per-turn grounding intact (the regime AS-9 never tested; an axis-2 failure impugns AS-9's "single-decision-shaped" qualifier itself). **DECIDE-entry falsification probe targets axis 1 only** (cheap, short un-batchable chain); **axis 2 needs a long horizon = BUILD-phase target — the short probe cannot settle it.** A clean short-probe result is an axis-1 pass, NOT full validation; codify as Conditional Acceptance (ADR-097) with axis 2 designated BUILD-phase. **`domain-model.md` OQ #27 is the authoritative two-axis version; the product-discovery grounded-loop inversion is single-axis — read OQ #27, not the inversion, when structuring the probe plan** (snapshot Advisory 1).

3. **AS-10 reaffirmed by Spike ρ (no text change).** A real tool-rich OpenCode client routed correctly via the planner; the prior tool-rich-client routing suppression did NOT recur. Recorded as evidence-reinforcement in Amendment Log #15, not an amendment.

4. **Consolidated DECIDE advisory list** (from MODEL snapshot; 5 advisories): (1) read OQ #27 as authoritative two-axis; (2) short probe settles axis 1 only; (3) **wrapper-shaped probe must be a named deliverable** in the evidence plan, not residual (else OQ #26 settles by callee-skew); (4) three active DISCOVER advisories unchanged — OQ #18 (cost-distribution lens grounding: user-voice vs project-developer-voice), OQ #19 (Tier-1-hybrid vs ADR-027-direct build-complexity comparison before locking PRIMARY), OQ #20 (Population A tool-family timeout research before the latency ADR); (5) ARCHITECT naming-collision flag — "artifact-bridge" (F-ρ.1) ≠ ADR-030 "Bridge mechanism" (`tool_choice`-deferred).

5. **Vocabulary disposition.** 6 terms folded; the 3 DECIDE-pending terms (layer-A loop-driver, layer-B generation, grounded loop) are NOT settled domain vocabulary — disposition resolves when DECIDE selects a layer-A seat-filler.

### From Cycle 7 loop-back DISCOVER (closed at gate 2026-06-01)

1. **The wrapper-vs-callee fork is the cycle's load-bearing open architectural question, held open for DECIDE.** ADR-027's plan→dispatch→synthesize pipeline is a **layer-B (per-turn generation)** mechanism; the north-star session is a multi-turn agentic loop requiring a **layer-A loop-driver** (decide the next agentic step per turn) that no ADR-027 component holds. Disposition (a) **wrapper**: keep the pipeline as the per-turn engine, add a loop-driver + `tool_calls` terminal above it (the "amendment / incomplete-not-wrong" reading). Disposition (b) **callee**: the loop-driver is the center and the pipeline is one of the things the loop calls — or, per Spike σ.2, not the per-turn primitive at all. **The "incomplete, not wrong / aligned with ADR-027's philosophy" framing is the wrapper reading and was deliberately NOT inherited as the default** (loop-back snapshot advisory #1; commitment #3). Product-discovery Tension 21 + Skill Orchestration User loop-back refinement carry the fork.

2. **Evidence-availability asymmetry (DECIDE evidence-plan must address this — snapshot Advisories 2 + 3).** Every multi-turn run the loop-back produced (σ.2) is **callee-shaped**; **no wrapper-as-multi-turn-loop was ever spiked** (ρ ran the pipeline + terminal for a single turn only). So "let the effective path win on evidence" skews callee by default, and BUILD's first runs will too. DECIDE's evidence plan for the fork must include **at least one wrapper-shaped probe** (pipeline as per-turn generation subroutine under a loop-driver), structured alongside an OQ #19-style build-complexity comparison, with named discriminating criteria: reliability over long horizons (the grounded-loop probe), build-complexity / rework, latency compounding across turns, operator-mental-model fit. σ.2's evidentiary weight should get its own row in DECIDE's Tension-21 deliberation, not a parenthetical.

3. **Grounded-loop hypothesis: falsification probe queued for DECIDE entry (snapshot Advisory 4).** The hypothesis (a cheap model can drive the loop because each turn is *grounded* by a real observed tool result — structurally unlike note-22's *ungrounded* multi-dispatch) is a **working inference, NOT a spike finding** (σ.1/σ.2: n=1, short task; σ.2 batched all 3 actions into one planning turn so the "loop" was 2 turns, never a real decide-act-observe chain). **Named discriminating failure** (commitment #5): the driver slips from grounded per-turn stepping into *ungrounded batch planning* — emits a multi-step batch whose later steps presuppose earlier steps' outputs it never observed, and a step fails because the presupposed state did not hold. Run the probe at DECIDE entry against a **harder/longer task than σ.2** (3–5 steps where step 3 depends on step 2's observed output); free local (qwen3:14b/Ollama, $0); ADR-097 Conditional Acceptance is the backstop on mixed results. Practitioner prior 2026-05-31: reasonably solid → probe is grounding-not-blocking.

4. **AS-9 scope note for MODEL.** The grounded-loop hypothesis names a *conditional reopening of AS-9's scope* — specifically around **grounded-vs-ungrounded driving** (not the bounded-role pattern generally) if the hypothesis fails. MODEL should carry this as an explicit AS-9 scope annotation in `domain-model.md`, not silently absorb it. The six new vocabulary terms (layer-A loop-driver, layer-B generation, client-tool-action terminal, parity, artifact-bridge, grounded loop) fold into the glossary; **do NOT promote the three DECIDE-pending terms** (layer-A loop-driver, layer-B generation, grounded loop) to settled domain vocabulary — their disposition resolves when DECIDE selects a layer-A seat-filler (snapshot Advisory 1).

5. **Settled premises into MODEL/DECIDE** (commitment-gating, practitioner-confirmed): text-only terminal **rejected** (endpoint must emit `tool_calls` + participate in the multi-turn loop — justification is the client's execution model, not filesystem geography); **parity** is the bar (behavioral, NOT latency); layer A is a real role ADR-027 lacks; the **artifact-bridge** (F-ρ.1) is a designed-in requirement (production ensembles route deliverables server-side per ADR-025; survives co-location); the loop-back spikes validated **mechanisms at n=1**, not the architecture and not sustained long-horizon. **Open premises:** wrapper-vs-callee; layer-A seat-filler (3 candidates); grounded-loop hypothesis; sustained long-horizon (BUILD-phase validation target).

6. **DECIDE artifact impact preview.** ADR-027 is the most exposed (Tension 21 questions its single-turn shape — amend it or write a companion ADR for the layer-A driver + client-tool-action terminal + multi-turn loop participation). ADR-025 (artifact-as-substrate) intersects via the artifact-bridge. **AS-10** (capability-matching from request content alone) is untouched and **reaffirmed** by Spike ρ (the prior tool-rich-client routing suppression did NOT recur — the planner routes on request content, indifferent to the client's declared tools). New scenarios + interaction-specs needed for the terminal, the bridge, the multi-turn loop, and the parity bar.

### From Cycle 7 BUILD — Track A.3 Spike ν (2026-05-23)

1. **No capability ceiling found.** The long-horizon capability-ceiling question from the architect→build gate is answered for the three under-exercised surfaces: multi-step composition 8/8 (100%) end-to-end with structural-bounding at every step; production-scale numerical ~100% semantic fidelity over 108 figures; adversarial routing judgment defensible on all conformant decisions. The practitioner's belief-mapping concern (hybrid would be preferred if ensembles cannot handle generalized long-horizon tasks) is not triggered by this evidence. Spike ν research log: `essays/research-logs/cycle-7-spike-nu-long-horizon-ceiling.md`.

2. **Single-surface Intermediate (Surface 3, 92.5% conformance); multiple-intermediate ceiling rule did NOT fire.** The Intermediate is planner robustness, not capability: A6 empty-response (reliability), E1/E3 prompt-injection. The trigger condition resolved to "WP-A proceeds with deployment-policy constraint," applied 2026-05-23.

3. **ADR-027 plan-validation is the load-bearing injection defense (carries to WP-A + all downstream).** Spike ν empirically grounded the value of validating the plan (action ∈ {dispatch,direct}; ensemble ∈ registered set) before dispatch — E1's `launch` and E3's `oracle` are rejected → direct. WP-A's two new candidate scenarios encode this as non-optional pipeline behavior. Connects to the ARCHITECT informational observation #7 (AS-10 erosion risk) and ADR-017 (tool-call structural validation guard) — plan validation is the routing-surface analog.

4. **Open robustness/security carry-forwards (not blocking WP-A).** (a) Planner-layer prompt-injection hardening (the framework backstop covers dispatch safety; planner-prompt hardening is the residual) — candidate for a follow-on spike or PLAY-phase probe. (b) Cheap-tier empty-response base rate under production traffic (A6 mode) — PLAY / first-deployment evidence territory; the empty-plan→direct fallback bounds its impact.

5. **Two scoring-artifact findings worth carrying for methodology.** (a) Number-set string-matching produces false positives on value-preserving unit conversions (N1: 48,217 USD-millions → $48.217 billion, correct) — ADR-029's runtime fidelity check must compare numeric value, not string form. (b) In multi-step chains the synthesizer can recover figures a lossy intermediate step dropped, by reading the ORIGINAL REQUEST section (MS2) — information-preserving, but a fidelity-scope nuance for the synthesizer's strict-DISPATCH-RESULTS framing.

### WP-A progress (2026-05-23)

**Decomposition:** ports + spike-backed adapters (practitioner-chosen at WP-A entry).

**Landed + committed:**
- `feat: framework-driven dispatch pipeline + ensemble-backed roles (Cycle 7 WP-A)` — commit `e538264`. New L2 modules `agentic/dispatch_pipeline.py` (DispatchPipeline.run = plan → validate → dispatch → synthesize; Plan→InternalToolCall pure-function adapter; 4 pipeline-stage events; plan validation as non-optional stage backstopping Spike ν E1/E3/A6) and `agentic/ensemble_backed_roles.py` (EnsembleRoutingPlanner / EnsembleResponseSynthesizer over OrchestraService.invoke). 14 unit tests; FC-2 layer map updated (both L2); full suite 2863 green; `make lint` clean. Covers scenarios 1, 2, 5, 6, the adapter, and event emission at the unit level.
- `docs: cycle 7 Spike nu ...` — commit `65431d1`.

**Handler swap (scenario 4) — DONE.** Commit `0a7a822` (`feat: swap chat-completions handler to DispatchPipeline`). `web/api/v1_chat_completions.py` now builds a per-request `DispatchPipeline` via a new async `get_dispatch_pipeline()` factory and drives it through the existing heartbeat + context-sink lifecycle wrappers (retyped via a `_ChatCompletionsCaller` Protocol). Removed the dead runtime-construction code (`_build_runtime`, `get_orchestrator_llm_loader`, `_default_orchestrator_llm_loader`, `get_conversation_compaction`) and the runtime-only chunk branches (`VisibilityEvent` narration, `ClientToolCall` delegation + `_NonStreamingResult.tool_calls`). The three discovery decisions held (context-sink substrate-reg + dispatch-log kept, turn-boundary injection dropped; single-chunk streaming; capability-list interim source). `_reject_reserved_tool_names` retained as a defensive input guard (preserves the Cycle 1 reserved-name commitment; pipeline no longer routes by `tools`).

- **Capability-source correction:** `read_ensembles()` returns `{name, source, relative_path, agent_count, description}` with **no `topaz_skill`** — the cycle-status's stated "read_ensembles filtered by topaz_skill" did not match. `_build_capability_names()` reads names from `read_ensembles()` and the `topaz_skill` marker per-name via `find_ensemble_by_name(name).topaz_skill` (the same EnsembleConfig surface the tier-router skill reader uses). WP-D's Capability List Builder formalizes this as single-source-of-truth.
- **Test disposition (practitioner-decided 2026-05-23):** the 42 handler tests split into 24 preserved-behavior (response shape, request parsing, SSE framing, session-start-once, identity resolution) + 18 removed-behavior (ReAct round-trips, raw-output escape hatch, autonomy/compose narration, Option C client-tool delegation). Practitioner chose **delete all 18** — those behaviors leave the chat-completions surface under ADR-027; the underlying classes keep ~159 tests of independent unit + integration coverage (`test_orchestrator_runtime.py` 24, `test_orchestrator_tool_dispatch.py` 100, `test_autonomy_policy.py` 7, `test_composition_validator.py` 13, + ~15 tool-dispatch integration). The 24 preserved tests were reworked to drive a stub pipeline; 4 handler-level acceptance tests added (scenarios 1/2/4 via a real `DispatchPipeline` + stub ports through the HTTP boundary + reserved-name guard). Handler test file now 28 tests; full suite **2847 green; mypy strict + ruff + format clean; coverage 92%**.
- **Option C narrowing (accepted consequence):** the pipeline emits only `ContentDelta` + `Completion`, so the orchestrator-LLM's mid-turn client-tool delegation is gone from this surface. The Client Tool Surface Commitment is preserved structurally only as "the synthesizer reads prior-turn `tool_calls` in `messages[]`" (per ADR-027 + Spike ε' C1/C2), not as new delegations.

**Step 5 integration verification — handler↔pipeline boundary verified; live-ensemble end-to-end GATED on WP-B/WP-C.** The acceptance tests exercise a real `DispatchPipeline` (plan→dispatch→synthesize) with stub ports through the real HTTP + session + substrate + sink lifecycle. The handler↔pipeline↔**real-ensemble** end-to-end (real Ollama, slow) cannot complete in WP-A: `get_dispatch_pipeline()` points at the production ensemble names `agentic-routing-planner` / `agentic-response-synthesizer`, which do not exist until WP-B / WP-C. This is consistent with **TS-10** ("Dispatch Pipeline operational after WP-A + WP-B + WP-C"). A factory-construction smoke test of `_build_capability_names` + the real adapter wiring (no Ollama) is available if wanted; otherwise live end-to-end lands with WP-B/WP-C.

### From Cycle 7 ARCHITECT (closed at gate 2026-05-23)

1. **Six new modules composed; AS-9 + AS-10 honored structurally on the chat-completions surface.** Dispatch Pipeline (L2) orchestrates plan → dispatch → synthesize replacing `OrchestratorRuntime` as the chat-completions caller; Routing Planner (L2) and Response Synthesizer (L2) wrap system ensembles satisfying AS-9; Capability List Builder (L1) is single source of truth for capability ensembles satisfying AS-10's request-content-alone scope; Response Labeling (L3) + Capability Discovery Endpoint (L3) deliver ADR-030 + ADR-032 commitments. Full module entries + dependency graph + integration contracts + 14 new fitness criteria FC-28..FC-41 in `system-design.agents.md`; slim summary in `system-design.md` v5.0.

2. **Track C ARCHITECT-phase deferrals from DECIDE conformance scan resolved.** Finding 2 → disposition (a) preserve `OrchestratorRuntime` as architectural option for future surfaces (no production caller post-BUILD; ADR-001 + ADR-011 + ADR-022 retained). Finding 8 → Dispatch Pipeline yields same chunk vocabulary as runtime (`OpenAiSseFormatter` unchanged). Finding 11 → Plan→`InternalToolCall` adapter inside Dispatch Pipeline module.

3. **Spike ν added as Track A.3 architect→build boundary work gating WP-A entry.** Triggered by practitioner EPISTEMIC GATE belief-mapping response surfacing the long-horizon capability ceiling question. The corpus's existing Spike battery (ζ + ε + ε' + μ; n=13 + 4 confabulation modes at qwen3:8b) did not exercise multi-step composition, production-scale numerical content beyond Spike ε' B1's 25 figures, or adversarial routing beyond the 20-prompt Spike ζ battery. Spike ν tests the structural-bounding generalization AS-9 + ADR-027 rest on across these three surfaces with pre-specified qualitative criteria + three trigger conditions (Pass / Fail / Intermediate) + the multiple-intermediate rule per architect-snapshot Advisory B. Pass → WP-A proceeds; Fail → Design Amendment re-opens architecture; Intermediate or multiple-Intermediate → practitioner authorizes one of named paths. Writeup target: `essays/research-logs/cycle-7-spike-nu-long-horizon-ceiling.md`. Cheap-tier free-tier at qwen3:8b via local Ollama; $0 cost.

4. **Susceptibility-snapshot Advisory A — asymmetry argument partial reductiveness (BUILD carry-forward).** The Tier-1-doesn't-rescue-long-horizon argument is correct on bounded-role coverage differential but elides that the capability ceiling applies equally to both mechanisms' bounded-role components. Spike ν correctly frames this as shared empirical question. BUILD should not over-rely on the asymmetry argument when interpreting Spike ν results; if Spike ν triggers Design Amendment, the re-opened deliberation considers ADR-027 + Tier 1 + multi-mechanism architectures on their merits at the time.

5. **Susceptibility-snapshot Advisory C — Inversion Principle checks lean architectural (BUILD carry-forward).** The 3-stage decomposition (Dispatch Pipeline + Routing Planner + Response Synthesizer) serves operator-configurability + developer-testability more than Population A's mental model. The decomposition is correct but the inversion checks should clarify which model they serve. Capability Discovery Endpoint's inversion check is added in BUILD's field-guide work; the 3-stage decomposition's user-mental-model-vs-developer-convenience distinction surfaces in field-guide entries when generated.

6. **Susceptibility-snapshot Advisory D — `OrchestratorRuntime` dormant-class cost-accounting + WP-A/WP-C BUILD-discovery risks (BUILD carry-forward).** Two parts: (a) "Architectural-option preservation, not actively-maintained dual surfaces" framing is optimistic — BUILD documents the dormant-class maintenance reality explicitly (integration-repair cost as surrounding code evolves; version-drift risk; deprecation cost should re-activation be needed; ADR-001 + ADR-011's continuing-commitment claim qualified accordingly). (b) WP-A streaming wiring (Dispatch Pipeline chunk surface replication without using `OrchestratorRuntime` streaming internals) and WP-C EnsembleExecutor per-ensemble streaming API availability are named BUILD-discovery risks; the work decomposition surfaces them explicitly.

7. **Susceptibility-snapshot informational observation — AS-9 + AS-10 incremental erosion risk (carries to all downstream cycles).** Latent: future BUILD or follow-on-cycle work could erode AS-10's "no client-side opt-in" commitment through technically-permitted-but-spirit-violating extensions; `OrchestratorRuntime` dormant-edge could enable re-introduction without re-passing through architectural review. Not current violations; BUILD flags any pattern introducing client-supplied routing signals not on the OpenAI-protocol-native list.

8. **Susceptibility-snapshot positive signal — ADR-076 qualitative-claim decomposition completeness.** Most complete decomposition in the corpus. Fourteen new fitness criteria FC-28..FC-41 + two annotations (direction-not-constraint for latency tuning playbook; honest-residual-uncertainty for cost-distribution-accountability) preserve structurally-non-decomposable surfaces visibly.

9. **Cycle 7 BUILD shape (canonical):** Track A.1 (planner schema input field refactor) → Track A.2 (synthesizer Rule 6 codification) → **Track A.3 Spike ν long-horizon capability ceiling probe** → WP-A (Dispatch Pipeline + adapter + pipeline-stage events) → WP-B (Routing Planner production ensemble + module; depends on A.1 + WP-A) → WP-C (Response Synthesizer production ensemble + module; depends on A.2 + WP-A) → WP-D (Capability List Builder + Capability Discovery Endpoint; open choice with B/C) → WP-E (Response Labeling + tool_choice bridge + degradation event sink consumption; depends on WP-A + WP-C). Transition states TS-10 (Dispatch Pipeline operational after WP-A + WP-B + WP-C) and TS-11 (Cycle 7 complete after all 5 WPs). BUILD-mode declaration: gated (recommended per practitioner cycle preparation framing + architect-snapshot considerations).

10. **OrchestratorRuntime preserved as architectural option per ARCHITECT disposition (a) for conformance Finding 2.** No production caller post-Cycle 7 BUILD on the chat-completions surface; ADR-001 (Internal ReAct loop execution model) + ADR-011 (Orchestrator LLM is a Model Profile) + ADR-022 (system-prompt amendment, structurally moot for chat-completions under ADR-027 per partial-update header) retained as architectural commitments for future surfaces. Cost calculus: "architectural-option preservation" rather than "actively-maintained dual surfaces" — per Advisory D, BUILD adds cost-accounting transparency to this framing.

### From Cycle 7 MODEL (closed at gate 2026-05-22)

1. **AS-9 codified at MODEL boundary.** "Structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks where the orchestrator-LLM-as-decider failed." Empirical basis: 4 documented confabulation modes + 13 tests at qwen3:8b (Spike ε + Spike ε' + Spike μ). **Scope: structural property (role-shape, not model-shape), independent of mechanism choice.** Both candidate mechanisms (ADR-027 framework-driven dispatch pipeline; Tier 1 hybrid with `tool_choice` interception) satisfy AS-9 — the invariant constrains DECIDE's solution space (any architecture must produce structurally-bounded roles) but does not pre-answer the mechanism-choice question.

2. **Mechanism choice remains DECIDE work.** ADR-027-as-primary commitment is unaffected by AS-9 codification — that commitment remains gated by snapshot Advisory 2's build-complexity comparison (OQ #19). DECIDE produces the explicit Tier 1 hybrid vs. ADR-027-direct sprint-effort comparison before ADR-drafting locks the PRIMARY designation.

3. **Spike μ extends empirical coverage of the structural-bounding finding** across four documented confabulation modes (multi-dispatch fabrication per Cycle 6 PLAY note 22; path hallucination per Cycle 6 PLAY note 23; substrate-path-as-deliverable per Spike λ.4-paid/λ.5-paid; coherent factual errors uncalibrated per Cycle 5 PLAY). The "rapid compounding" susceptibility signature from DISCOVER is materially narrower at MODEL exit: combined audit depth grew from "PLAY note 22 + 9 Spike ε/ε' tests" to "4 modes + 13 tests."

4. **AS-9 propagation tasks for DECIDE/BUILD:** examine ADR-021 (per-capability dispatch contract — actor shift for routing decision under AS-9 + ADR-027 direction); update ADR-022 (Routing surface behavior) to reflect AS-9 + ADR-027 remediation per Tension 14 collapse; ADR-014/ADR-015 unchanged. Code references to "orchestrator" / "orchestrator-LLM" need BUILD-phase clarification — under AS-9 + ADR-027 direction, "the orchestrator" refers to the framework-driven dispatch pipeline (the orchestration substrate); the LLM roles are the routing-planner ensemble and response-synthesizer ensemble.

5. **One new DECIDE-phase design question surfaced from Spike μ.1**: Rule 6 candidate for framework-convention enumeration in direct-completion mode. Spike μ.1 showed the synthesizer enumerates `routes.py`, `api.py`, `endpoints.py` as generic conventions with explicit hedging + uncertainty framing when asked "where is X file" questions. Qualitatively different from the PLAY note 23 failure mode but could mislead careless readers. Synthesizer-rule design question (not structural-bounding failure).

6. **Two new MODEL snapshot advisories carry to DECIDE:**
   - **Advisory A (spike methodology — pre-specify qualitative criteria):** future spike work testing the structural-bounding claim against additional failure modes should document qualitative failure-mode criteria before running tests, not only after a pattern-detector flags a fail. The μ.1 reinterpretation was correct; the procedural gap is that correctness depends on documentation produced after the detector result was known. Spike hygiene, not signal of distorted findings.
   - **Advisory B (options-engagement documentation):** DECIDE should not inherit AS-9 as if option (c) (deferral) was examined and rejected at MODEL. The codification option list was not a straw-man menu, but the agent's Finding μ.4 framing pre-argued for option (a); the practitioner's selection of option (a) is recorded without a documented engagement with the deferral case. **Treat AS-9 as correctly codified given the evidence, while holding the amendment pathway open**: if ADR deliberation surfaces reasons the structural-property claim is narrower (e.g., confabulation modes were fixture-based at n=1 per mode for μ.1/μ.3), the domain model supports amendment.

7. **Three DISCOVER advisories carry forward to DECIDE active and unchanged** (per snapshot evaluation — none closed by MODEL work):
   - **Advisory 1 (OQ #18):** cost-distribution lens validation against Population A voice. DECIDE examines whether Tension 18's strict-dispatch disposition is justified by Population A's trust contract (user-voice) or by project-developer value proposition (project-developer-lens).
   - **Advisory 2 (OQ #19):** build-complexity comparison between Tier 1 hybrid and ADR-027-direct per GT-2(a). DECIDE-entry precondition for ADR-027-as-primary commitment.
   - **Advisory 3 (OQ #20):** Population A tool-family timeout research. DECIDE's latency ADR must include OpenCode/Cursor/Cline/Aider timeout-default research; sub-40s for non-streaming would breach transparent-endpoint promise.

8. **OQ #17 preserved as distinct from AS-9.** "Capability matching works from request content alone with no client-side opt-in" remains pending DECIDE codification. AS-9 names the role-shape property (LLM-role bounding); OQ #17 names the request-shape commitment (no opt-in). The distinction was preserved by MODEL deliberately and noted as a positive signal in the snapshot.

9. **Ten new vocabulary entries settled at MODEL** (Amendment Log entry #12) — all 10 DISCOVER-vintage candidates from cycle-status §Cycle 7 DISCOVER feed-forward signal #9 are now binding terminology for DECIDE: Population A, Population B, transparent OpenAI-compatible endpoint, cost-distribution lens (with project-developer-lens vs. user-lens distinction preserved), routing-planner ensemble, response-synthesizer ensemble, framework-driven dispatch pipeline (PRIMARY direction with scope-of-claim partition), structurally-bounded role, `tool_choice` strip-at-input, orchestrator-designs-ensembles (north-star context).

10. **Routing surface behavior entry refreshed for Cycle 7 Tension 14 collapse**: three dispositions (intended scope / defect to remediate / configuration-conditional) → defect to remediate with ADR-027 framework-driven pipeline as remediation. Collapse is session-confirmed-pending-DECIDE-deliberation per snapshot Finding 4.

### From Cycle 7 DISCOVER (closed at gate 2026-05-22)

1. **ADR-027 framework-driven dispatch pipeline is the cycle's PRIMARY direction.** Tier ordering FLIPPED from RESEARCH-close commitment ("hybrid as starting commitment + ADR-027 as escalation") after Spike κ established cost-equivalence + Spike ε empirically validated ADR-027's structural advantage on the routing-decision and post-dispatch-synthesis surfaces. Per GT-2(a) cost-equivalence rule, ADR-027-direct as primary; hybrid as conditional alternative. **Amendment A3 in essay-outline-006 carries this as DECIDE-entry editing work; argument-audit re-run required before DECIDE proceeds beyond corrected Essay-Outline.**

2. **Scope-of-claim partition is the gate's load-bearing artifact.** "Orchestrator-LLM removed from dispatch path entirely" is precisely scoped to "removed from the routing-decision and post-dispatch-synthesis surfaces" (the surfaces Spike ζ + Spike ε empirically tested). Settled / plausible-but-untested / open buckets explicitly named in essay-outline-006 Amendment A3 Tier 1 reframe + product-discovery.md Orchestrator LLM Cycle 7 refinement. DECIDE/BUILD should honor the partition rather than collapsing it back to "removed entirely."

3. **Spike ε' (gate-tail validation)** — completed 2026-05-22. Closed gate-named plausible-but-untested bounds for direct-completion path, numerical-density fidelity, and multi-turn continuity. Four findings (Rule 5 framing scope; rounding-drift modes; multi-turn continuity validated; tightened scope partition). **No further pre-DECIDE validation spike is required.**

4. **Three susceptibility-snapshot advisories with concrete DECIDE actions:**
   - **Advisory 1 (cost-distribution lens validation against Population A voice)** — the cost-distribution framing in Tension 18 + Amendment A2 is practitioner-voiced and directionally sound; DECIDE/BUILD should validate against independent Population A voice (e.g., review with a Population A operator or examine Population A clients' documentation about expected ensemble behavior).
   - **Advisory 2 (ADR-027-primary build-complexity comparison)** — Spike κ provided cost-equivalence as a premise; the explicit build-complexity comparison between Tier 1 hybrid and ADR-027-direct is DECIDE-entry work. DECIDE should produce the comparison before ADR-drafting locks the PRIMARY designation.
   - **Advisory 3 (Population A tool-family timeout research)** — DECIDE's latency ADR should include OpenCode/Cursor/Cline timeout-default research; if defaults are sub-40s for non-streaming requests, the pipeline's current floor (36s single-step; 64s chained) breaches the transparent-endpoint promise regardless of tuning intentions.

5. **"Rapid compounding" pattern is the cycle's susceptibility signature.** Per snapshot: not standard sycophancy; rather, three spike findings integrated into a single architectural commitment in a single session via a pre-committed rule (GT-2(a)) rather than through deliberative audit depth comparable to RESEARCH's P1-clean process. MODEL should attend to this — the artifact corpus is empirically stronger than Cycle 6 DISCOVER produced, but the integration pattern is novel and warrants stress-testing during MODEL phase vocabulary work and DECIDE phase ADR drafting.

6. **Four spike-derived findings load-bearing for downstream phases:**
   - **Spike ζ (routing-planner viability at qwen3:8b)** — primary C3 mechanism empirically established; latency p50=10s; tuning axes named.
   - **Spike ε (confabulation dissolution on the structural axis)** — orchestrator-LLM-as-decider failure mode dissolved when role is structurally bounded; cheap-tier model (qwen3:8b) succeeds; one Rule 4 rounding drift mode on simple lookup.
   - **Spike κ (D0 — framework drops `tool_choice` at input)** — Pydantic strips `tool_choice` at `_ChatCompletionsRequest` parsing boundary; no code path in `src/llm_orc/` reads or forwards it. Reframes Spike λ + λ-paid findings (correlation not causation; not a model-portability gap). Per Tension 19, the framework-correction question (implement / explicitly reject / reframe out of scope) is a DECIDE ADR.
   - **Spike ε' (gate-tail bounds closing)** — direct-completion path validated across 4 request shapes; rounding drift characterized as two distinct modes (precise-figure rounding + large-number millions rendering); multi-turn continuity validated when prior turns in input (native `messages[]` handling is ARCHITECT-phase mechanical work). Rule 5 framing scope is a DECIDE design question (system prompt sharpening or relaxation).

7. **Multi-step composition remains a DECIDE design question; multi-turn continuity is closed by Spike ε'.** Spike ε Pipeline A handles single-step dispatches; Pipeline B was hardcoded deterministic chain. Production multi-step requires DECIDE design (single-step planner + framework chain-heuristic vs. multi-step planner vs. planner-loops-with-context). Multi-turn continuity validated by Spike ε' C1/C2 — synthesizer-only architecture works when prior turns are in input; production needs native `messages[]` handling (mechanical ARCHITECT-phase work).

8. **Methodological observation for ADR-087 amendment.** Spike κ resolved via source-code inspection at $0.00 cost where the planned spike was 3 paid probes (~$0.05 authorized). Source-code inspection of the request-construction path should be a standard pre-probe step before paid validation spikes characterize provider-side behavior. Future cycles' validation-spike-as-research-method work should inherit this discipline.

9. **Ten new vocabulary candidates pass to MODEL.** Settled at DISCOVER: Population A, Population B, cost-distribution lens, orchestrator-designs-ensembles (north-star context, not Cycle 7 scope), transparent OpenAI-compatible endpoint. Candidate/operator-voice from spike work: routing-planner ensemble, response-synthesizer ensemble. Candidate/architectural-framing: framework-driven dispatch pipeline, structurally-bounded role. Candidate/agent-named pattern: `tool_choice` strip-at-input. MODEL's glossary work should follow the Cycle 4/5/6 vocabulary-disposition pattern.

### From Cycle 7 RESEARCH (closed at gate 2026-05-21)

1. **NL-routing fraction under production tool-rich clients is empirically ~0.** Empirically established across Cycle 6 Spike γ + Cycle 6 PLAY notes 1-25 + Spike λ (qwen3:14b) + Spike λ-paid (paid MiniMax M2.5). ADR-022 amendment effectiveness is bounded to bare-endpoint mode; production tool-rich clients (OpenCode confirmed; Aider/Cursor/Cline inferred) all suppress the amendment. Empirically settled.

2. **OpenAI `tool_choice` mechanism is model-portability-gapped.** The framework implements `tool_choice` correctly (validated under qwen3:14b — Spike λ Cell λ.3 dispatched ensemble + clean NL synthesis). The cross-compatibility-relevant production model (paid MiniMax M2.5 via OpenCode Zen) does NOT honor `tool_choice={"name":"invoke_ensemble"}` under tool-rich conditions (Spike λ-paid Cell λ.3-paid). Three candidate diagnoses (Zen proxy stripping; MiniMax non-conformance; framework tool-list interaction) are unresolved — disambiguation is C2 follow-up.

3. **Tiered architecture is the cycle's DECIDE input.** Hybrid (C3 server-side `tool_choice` interception + C4 framework-driven composition continuation + C5 form-drift enforcement at ensemble-authoring layer + C6 fallback design) as starting commitment, ADR-027 framework-driven dispatch pipeline (orchestrator-LLM removed from dispatch path entirely) as structurally pre-committed escalation triggered by hybrid-effectiveness measurement during BUILD/PLAY. Operational measurement criteria are DECIDE-phase work; the cycle commits to criteria existing.

4. **Orchestrator-LLM is the consistent failure surface across three distinct failure modes.** Composition confabulation (Cycle 6 PLAY note 22 — 8 cache-hit web-searcher dispatches + 0 claim-extractor + fabricated final response); positive control (Spike δ — framework-driven chaining produces faithful citations when orchestrator-LLM is removed); post-dispatch protocol-format failure (Spike λ-paid F-paid-4 — substrate-path file-read attempts in XML or as unreachable client-tool calls). The three modes share the orchestrator-LLM as locus; this is the structural finding motivating the ADR-027 escalation path.

5. **Direct-completion fallback is in tension with the project's value proposition.** Practitioner's RESEARCH-gate stance (2026-05-21): *"llm-orc's stance is that ensembles are used to distribute costs and token to distribute, declarative orchestrations of smaller models. So the orchestrator NEEDs to do that. Otherwise we'd simply use the frontier model."* C6 elevates capability-list discovery to first-order requirement (not documentation work), names structured advisory for Population B, and surfaces operator-observable degradation signaling as a deployment concern.

6. **GROUNDING REFRAME GT-1 (DISCOVER entry):** C6's "first-order requirement" elevation was driven by practitioner stance + one PLAY-constructed stakeholder persona (Skill Orchestration User). Independent product-discovery validation is needed before DECIDE treats the value-misalignment framing as load-bearing. DISCOVER should: (a) treat "capability-list discovery is first-order" as a hypothesis for product-discovery examination, not as inherited commitment; (b) apply Population A / Population B belief-mapping (tool-call-aware OpenAI-family clients vs. developer/script clients); (c) cross-reference Cycle 6 PLAY notes 1/18 (direct-completion quality on string-reverse task) vs. note 7 (code-generator ensemble multi-agent run) for empirical quality contrast.

7. **GROUNDING REFRAME GT-2 (DISCOVER→DECIDE boundary):** C7's "hybrid as starting commitment" ordering is asserted to rest on architectural-continuity cost but the cost differential is not quantified. The Abstract's "structurally pre-committed" language slightly overstates the practitioner's verbatim "if hybrid doesn't work empirically → stronger measures" conditional formulation. At the DISCOVER→DECIDE boundary, produce: (a) explicit build-complexity comparison between Tier 1 hybrid implementation and ADR-027-direct implementation; if costs are within same order of magnitude, ADR-027 as primary recommendation; (b) C2 diagnosis disambiguation BEFORE BUILD (Zen-proxy-specific vs. model-specific vs. framework-interaction) — mechanism design for C3 should be conditional on which diagnosis holds.

8. **Empirical-grounding cluster (four working-inference nodes).** E3.1.1 (server-side `tool_choice` interception preserves client-facing API contract); E4.2.1 (production-client filesystem scope is disjoint from agentic-serving deployment's filesystem); E5.3.3 (framework-driven composition continuation eliminates the orchestrator-narration substitution surface); E6.2.1 (project value-proposition centers on ensemble-distributed orchestration such that direct-completion fallback is a value misalignment). Each needs DECIDE-phase corroboration or explicit residual-uncertainty acceptance before BUILD work proceeds.

9. **Validation-spike-as-research-method precedent extended.** Cycle 6 PLAY ran Spike δ in-loop during PLAY; Cycle 7 RESEARCH extended this to Phase A. Two spikes ran during RESEARCH itself (Spike λ cost-free; Spike λ-paid user-authorized ~$0.05-0.30). The anti-elaboration positioning honored — spikes pruned speculative claims at source rather than allowing them to elaborate downstream into ADRs. Future cycles should treat ADR-087 validation spikes as standard RESEARCH method when reframes or design hypotheses touch the production deployment surface.

10. **Stakeholder model shifts for DISCOVER to surface:**
    - **Tool User / Population A (tool-call-aware client without alternative-surface access)** — receives current direct-completion behavior; under C6 framing this is a degradation surface, not a satisfactory default
    - **Tool User / Population B (developer/script client with alternative-surface access)** — can be redirected to `llm-orc invoke` via structured advisory
    - **Skill Orchestration User (Cycle 6 PLAY stakeholder)** — super-objective "Compose a skill framework against the orchestrator's capability library; expect dispatch when a capability slot fits" was empirically unmet; expectation now mediated by `tool_choice` mechanism + capability-list discovery
    - **Orchestrator LLM (as actor)** — role constrained in Tier 1 (routing potentially intercepted server-side; composition continuation removed); removed from dispatch path entirely in Tier 2 (ADR-027)
    - **Ensemble Author / Operator** — new authoring surfaces: routing-planner ensemble (Tier 1 (i') option (i)); synthesizer ensemble (Tier 2 ADR-027); tool-call-as-output-format authoring (Q2 (b))

### From Cycle 6 PLAY (closed at gate 2026-05-20)

1. **Composition drift is in the orchestrator-LLM.** Empirically confirmed by Spike δ. The chain-handling failure does not survive removing the LLM from the chain step.
2. **ADR-022 amendment is bounded to bare-endpoint mode.** Tool-rich clients suppress the amendment regardless of model tier. Production deployments are all tool-rich.
3. **Must-delegate is a bounded claim.** For capability-matched requests it is a current-state constraint; for non-matched it is a future-state aspiration. The fallback path needs explicit design.
4. **Form-vs-content drift may unify.** Schema-as-enforcement could address both in one mechanism.
5. **WP-C orchestrator-context observation works for single-dispatch lookup, fabricates on multi-dispatch summary.** Reading is reliable; narrating across dispatches degrades. Cycle 7 should account for this if any synthesis-of-results role uses the same observation surface.
6. **dispatch_log.json per-request overwrite breaks the operator-facing review use case.** Fix needed (Thread A or in-cycle).
7. **MiniMax-native XML tool-call leakage under bare-endpoint mode** is a model-output-format artifact when no client tools are declared. Not a defect; a tool-surface gap. Worth knowing for client-integration design.
8. **OpenCode interaction shape is preserved under the framework-driven pipeline candidate.** External API stays OpenAI-compatible; internal architecture is what shifts. Tool User mental model holds.

### From Cycle 6 ARCHITECT (closed at gate 2026-05-15)

(retained for reference; see archive for full detail) — `system-design.md` v4.0 + `roadmap.md` v6.0 capture the architecture as of Cycle 6 close. Cycle 7 ARCHITECT will need to update these substantially.

### From earlier cycles

See `cycle-archive/cycle-{1..6}-*.md` for prior-cycle feed-forward signals.

## DISCOVER work record (2026-05-21 to 2026-05-22; phase closed at gate 2026-05-22)

DISCOVER entered 2026-05-21. Substantial product-thinking work completed across two sessions; phase closed at gate 2026-05-22.

**Completed in 2026-05-21 session:**

1. **Phase 1: C6 hypothesis examination** — Population A/B mapping confirmed; "degradation surface" framing sharpened to cost-distribution from project-developer perspective (not per-task quality from user perspective); ensemble design declared orthogonal (Cycle 7 ships delegation reliability as prerequisite); north star framed (orchestrator-designs-ensembles long-horizon vision) as context that stays in product-discovery without expanding Essay-Outline scope.

2. **Phase 2 section walk** — Stakeholder Map, Jobs and Mental Models, Value Tensions, Assumption Inversions, Product Vocabulary all walked. Practitioner reactions integrated: Population A primary; Population B important-but-not-Cycle-7-focus; transparent OpenAI-compatible endpoint as project identity ("skills authored without llm-orc knowledge"); latency as tuning concern not structural blocker.

3. **Spike ζ — routing-planner reliability** — completed. 20-prompt battery against qwen3:8b routing-planner ensemble. **100% JSON conformance + 100% schema validity + 90% strict capability-match (100% with defensible judgment).** Latency p50=10s, p90=13s, mean=10s. Mechanism viability confirmed. Writeup at `essays/research-logs/cycle-7-spike-zeta-routing-planner.md`. Spike ensemble + scratch artifacts retained per `feedback_spike_artifact_retention` directive.

4. **Spike ε — end-to-end plan→dispatch→synthesize pipeline** — completed 2026-05-21. Three tests (PLAY-note-22 composition prompt under planner-driven pipeline; Spike δ's deterministic chain with synthesizer at end; simple single-capability lookup). **Result: 0 fabrications on T1 (the hardest test — synthesizer correctly reported claim-extractor was Planned-but-not-run instead of confabulating); 0 fabrications on T2 (Spike δ fidelity preserved with synthesizer added); 1 Rule 4 rounding violation on T3 (`402,300` ≠ source `402,329`).** Latency: ~36s end-to-end for single-step planner-driven; ~64s for deterministic chain. Writeup at `essays/research-logs/cycle-7-spike-epsilon-pipeline.md`. Most consequential finding: **the orchestrator-LLM's PLAY note 22 confabulation pattern does NOT survive the architectural move to a structurally bounded synthesizer role at the same cheap tier (qwen3:8b).** This empirically supports ADR-027 framework-driven dispatch pipeline as production target (with caveats: cheap-tier failure mode is rounding/restatement; multi-step composition still needs DECIDE-phase design).

5. **Spike κ — C2 diagnosis disambiguation** — completed 2026-05-21. Method: source-code inspection (not paid probes). **Finding: D0 — the framework has zero `tool_choice` handling.** Pydantic strips the parameter at the request-parsing boundary (`_ChatCompletionsRequest` model has no `tool_choice` field; default `extra="ignore"` silently discards it). The three original candidates D1 (Zen strips) / D2 (MiniMax ignores) / D3 (framework tool-list interaction) are all moot — they presumed `tool_choice` reached the provider. **Reframes Spike λ findings:** Cell λ.3's qwen3:14b "honors tool_choice" was correlation not causation (the model emergently chose the same answer `tool_choice` would have forced); Cell λ.3-paid's "MiniMax doesn't honor tool_choice" wasn't a model-portability gap — `tool_choice` never reached MiniMax. **Reframes the Phase A reframe:** the Essay-Outline's claim that "the OpenAI `tool_choice` contract already addresses forced routing" is empirically invalidated — the contract is not implemented. Per GT-2(a)'s "if costs are same order of magnitude, ADR-027 as primary recommendation" rule, cost equivalence is now established (both Tier 1 hybrid and ADR-027-direct require new framework code; the "free baseline" assumption fails). Writeup at `essays/research-logs/cycle-7-spike-kappa-tool-choice-diagnosis.md`. Cost: $0.00 (authorized $0.05 budget unspent — available for future κ-derivative work if needed). Methodological observation: source-code inspection should be a standard pre-probe step before paid validation spikes characterize provider-side behavior (ADR-087 amendment candidate).

6. **Section walk integration with spike findings + product-discovery.md write** — completed 2026-05-21. `product-discovery.md` updated +59 lines (215→274). All four stakeholder sections received Cycle 7 refinements (Population A/B mapping; north-star context; orchestrator-LLM structural role contraction; Skill Orchestration User C3 mechanism flip). Three new Tool User / Operator / Skill Orchestration User jobs added. Three new Value Tensions added (18 cost-distribution lens; 19 `tool_choice` mechanism disposition; 20 routing-planner latency vs transparent-endpoint promise) + Tension 14 update (post-Spike-κ reframe). Four new Assumption Inversions added (tool_choice-honored-by-framework; orchestrator-LLM-as-decider; hybrid-as-starting-commitment; Population-A-as-primary). Ten new vocabulary terms added with attribution markers. Disposition note added at end of vocabulary section.

7. **Backward-propagation drafts for Essay-Outline §C3, §C6, §C7** — completed 2026-05-21. Added "Amendment Log — Cycle 7 DISCOVER Backward-Propagation Draft" section at the bottom of essay-outline-006 (file grew 253→342 lines, +89 lines). Four amendments specified: A1 (§C3 routing-planner-as-primary with detailed SYNTHESIS replacement language + new CLAIM about D0); A2 (§C6 cost-distribution lens sharpening + Population A primary prioritization); A3 (§C7 tier ordering FLIPPED — ADR-027-direct as primary, hybrid as conditional alternative + REMOVE "Inverted-framing acknowledgment" paragraph); A4 (Abstract + Argument-Graph propagation scoped as DECIDE-entry editing work; argument-audit re-run before DECIDE proceeds beyond corrected Essay-Outline). Original P1-clean Essay-Outline content preserved verbatim; amendments are a separate section so the audit trail remains intact. Three new spike references added to References section (ζ, ε, κ) + product-discovery-cycle-7-update reference.

**Pending in next DISCOVER session:**

8. **EPISTEMIC GATE (reflection time)** — completed 2026-05-22. Belief-mapping question composed against §C7 tier flip (Amendment A3) + solution scoping (10 items) + commitment-gating (8 items). Practitioner raised sharp scope-of-claim challenge on orchestrator-LLM commitment; agent responded with explicit settled/plausible-but-untested/open partition; practitioner confirmed and authorized tightening. Tightening landed in essay-outline-006 Amendment A3 (Tier 1 reframe + SYNTHESIS reframe: "PRIMARY commitment" → "PRIMARY direction"; "removed from dispatch path entirely" → "removed from routing-decision and post-dispatch-synthesis surfaces") and product-discovery.md Orchestrator LLM Cycle 7 refinement (added matching scope-of-claim partition). Optional Spike ε' named as DECIDE-phase work item.

9. **Susceptibility snapshot dispatch** — completed 2026-05-22. Subagent dispatched via rdd:susceptibility-snapshot-evaluator with phase-boundary brief + agentic-serving artifact context + EPISTEMIC GATE conversation summary + specific patterns-to-evaluate beyond the standard discover-boundary patterns. Output written to `docs/agentic-serving/housekeeping/audits/susceptibility-snapshot-cycle-7-discover.md`. Verdict: **No Grounding Reframe; 3 advisory carry-forwards + 1 informational.** Notable observation: "The susceptibility pattern present is not sycophancy in the standard sense. It is rapid compounding — three spike findings integrated into a single architectural commitment in a single session, applied via a pre-committed rule (GT-2(a)) rather than through the deliberative audit depth that produced the RESEARCH artifact's P1-clean status."

10. **Gate reflection note + phase exit** — completed 2026-05-22. Gate reflection note written at `.rdd/gates/cycle-7-discover-model-gate.md` with belief-mapping question + user response + pedagogical move (Challenge — and the practitioner's responsive sharp scope-of-claim challenge that shifted the gate's pedagogical work) + commitment-gating outputs (5 settled premises + 8 open questions + specific commitments carried forward to MODEL) + snapshot summary. Phase advanced: `**In-progress phase:** discover` removed; `**Current phase:**` advanced to `model`.

11. **Spike ε' (gate-tail optional validation)** — completed 2026-05-22. Practitioner-authorized at gate close to address plausible-but-untested bounds named in scope-of-claim partition. 7 probes across three families: A (direct-completion path; 3 probes — simple conversational, factual question, ambiguous-no-capability); B (numerical-density fidelity; 2 probes — dense fixture + precise-roundable); C (multi-turn continuity; 2 probes — context-dependent + dispatch-referenced). **Results: 5 clean OK + 2 with characterized findings.** Four findings landed: (ε'.1) Rule 5 framing systematically omitted across 4 direct-completion responses — DECIDE scope question (load-bearing or over-specified?); (ε'.2) rounding drift is mode-specific — Mode 1 (precise-figure rounding: 402,042 → "~402,000") + Mode 2 (large-number millions rendering: 84,358,845 → "84.4 million"); (ε'.3) multi-turn continuity works under synthesizer-only architecture when prior turns are in input — native `messages[]` handling is mechanical ARCHITECT-phase work, not a design open question; (ε'.4) scope-of-claim partition tightens substantially — "settled" bucket grows from 3 tests to 10 tests; residual bounds more precisely characterized. Writeup at `essays/research-logs/cycle-7-spike-epsilon-prime-pipeline-bounds.md`. Cost: $0.00. Both Essay-Outline A3 + product-discovery.md Orchestrator LLM Cycle 7 refinement updated to reflect the tightened partition.

**Key conversational reframings to preserve (not yet in product-discovery.md):**

- **Cost-distribution framing for "degradation":** the value-misalignment claim is about cost-distribution from the project-developer perspective, not per-task quality from the user perspective. The user is correctly outcome-focused.
- **Ensemble design orthogonality:** Cycle 7 ships delegation reliability; ensemble quality work iterates independently. "If we can effectively delegate to ensembles every time, then we can figure out how to make the ensembles better."
- **Transparent OpenAI-compatible endpoint as project identity:** "The point is to make llm-orc agentic-serving compatible with any tool that would want to use an OpenAI-compatible chat completions endpoint. The user trusts that llm-orc would use / create ensembles effectively. Full stop."
- **North star** (lives in product-discovery, not Essay-Outline): orchestrator-designs-ensembles future; long-horizon agentic coding via skill frameworks; meta-ensembles + ensemble primitives + specialist substrate ensembles. Cycle 7 is the prerequisite, not the destination.
- **Skill Orchestration User sharpening:** client-side skill framework; orchestrator sees only the resulting chat-completion requests; capability matching must work from request content alone with no client-side opt-in.
- **C3 mechanism priority FLIPPED from RESEARCH framing:** routing-planner ensemble (or equivalent NL→capability inference) is PRIMARY (not residual); explicit-naming extractor and tool_choice interception are bonus paths layered onto NL inference.

## MODEL work record (2026-05-22; phase closed at gate 2026-05-22)

MODEL entered 2026-05-22. Single-session phase; closed at gate same day.

**Completed in 2026-05-22 session:**

1. **Source material read** — gate reflection note `.rdd/gates/cycle-7-discover-model-gate.md`; DISCOVER snapshot `housekeeping/audits/susceptibility-snapshot-cycle-7-discover.md`; existing `domain-model.md` (181 lines pre-MODEL); Essay-Outline 006 + Amendment Log A1-A4; product-discovery.md Cycle 7 update; four spike research logs (ζ, ε, ε', κ).

2. **Domain model vocabulary expansion** — 10 DISCOVER-vintage candidates added per cycle-status §Cycle 7 DISCOVER feed-forward signal #9. Seven §Concepts entries (Routing-planner ensemble, Response-synthesizer ensemble, Population A, Population B, Transparent OpenAI-compatible endpoint, Cost-distribution lens with project-developer-lens vs. user-lens distinction preserved per snapshot Advisory 1, `tool_choice` strip-at-input). Three §Methodology Vocabulary entries (Framework-driven dispatch pipeline as PRIMARY direction with scope-of-claim partition; Structurally-bounded role as research-voice property; Orchestrator-designs-ensembles as north-star context). Three new actions (Plan, Synthesize (response), Strip `tool_choice` at input). Eight new relationships. Existing Routing surface behavior §Concepts entry updated to reflect Cycle 7 Tension 14 collapse. Ten new Open Questions #16-25. Initial conservative posture: OQ #16 + OQ #17 flagged as candidate invariants explicitly marked "MODEL DOES NOT codify; the codification is DECIDE work" per the rapid-compounding inheritance. Recorded in Amendment Log entry #12.

3. **EPISTEMIC GATE (reflection time) — generative redirect by practitioner** — agent composed belief-mapping question against OQ #16 deferral with 5 candidate belief-shapes. Practitioner redirected: "my question is whether there is anything additional we could design in a new spike that would inform whether to land an invariant." Agent proposed three spike candidates with explicit epistemic targets per OQ — **Spike μ as load-bearing for OQ #16**; Spike ν incremental for OQ #25; Spike ξ informative for DECIDE design. Explicitly noted Advisory 2 build-complexity comparison as estimation work distinct from spike work and unaffected by spike outcomes. Practitioner authorized Spike μ.

4. **Spike μ — confabulation-mode generalization** — completed 2026-05-22. Method: reused Spike ε pipeline harness; three tests targeting documented confabulation modes (μ.1 path hallucination per Cycle 6 PLAY note 23; μ.2 substrate-path-as-deliverable per Spike λ.4-paid/λ.5-paid; μ.3 coherent factual errors uncalibrated per Cycle 5 PLAY pattern). All free-tier local Ollama at qwen3:8b; $0 cost; ~3 min wall-clock. Results:
   - **μ.1 transforms** — confident-specific fabrication → honest-generic-conventions with explicit hedging + uncertainty acknowledgment + clarification request. Pattern detector flagged a regex false-positive (3 backtick-wrapped `.py` references); qualitative analysis showed response was qualitatively different from PLAY note 23 failure mode (specificity / framework hedging / uncertainty acknowledgment / tool-call emission / user-actionable framing differ). Surfaced new DECIDE-design question (Rule 6 candidate).
   - **μ.2 structurally avoided** — synthesizer is text-only; no tool-call emission surface; substrate-path-as-deliverable mode requires capability the synthesizer ensemble does not have.
   - **μ.3 bounded by Rules 1+5** — synthesizer cited fixture's single figure verbatim, explicitly named partial-source limitation, refused to fabricate trend rates / growth claims.

   Combined audit depth on structural-bounding claim grew from "PLAY note 22 + 9 Spike ε/ε' tests" to "4 documented confabulation modes + 13 tests at qwen3:8b." Writeup at `essays/research-logs/cycle-7-spike-mu-confabulation-generalization.md`. Spike ensemble (reused: `spike-cycle7-epsilon-response-synthesizer.yaml`) + scratch artifacts (`scratch/spike-mu-confabulation/`) retained per `feedback_spike_artifact_retention` directive.

5. **Codification decision** — agent presented three options: (a) codify as new AS-9 at MODEL; (b) codify with narrower scope (only 4+1 tested modes); (c) continue to defer to DECIDE. Practitioner chose (a).

6. **AS-9 codified at MODEL boundary** — "Structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks where the orchestrator-LLM-as-decider failed." Scope refined: structural property (role-shape, not model-shape) independent of mechanism choice. Both candidate mechanisms (framework-driven dispatch pipeline per ADR-027 direction vs. Tier 1 hybrid) satisfy AS-9; mechanism choice remains DECIDE work via Advisory 2 / OQ #19. AS-9 §Empirical basis names n=13 tests across four confabulation modes; §Scope explicitly separates structural-property claim from mechanism-choice deliberation; §Plausible-but-untested names evidence-strength qualifiers (not scope limitations); §Propagation note names ADR-021 / ADR-022 / code references as DECIDE/BUILD work items. Recorded in Amendment Log entry #13. OQ #16 marked CODIFIED; OQ #17 preserved as distinct (capability matching from request content alone; no client-side opt-in — request-shape commitment, not role-shape property).

7. **Susceptibility snapshot dispatch** — completed 2026-05-22. Subagent dispatched via rdd:susceptibility-snapshot-evaluator with phase-boundary brief + MODEL artifact context + AID sequence summary + specific patterns-to-evaluate (canonical "two exchanges" failure mode; practitioner redirect substantive epistemic work vs. bypass; AS-9 structural-property / mechanism-choice separation substantive vs. rhetorical; μ.1 qualitative reinterpretation post-hoc justification vs. substantive; alternatives-engagement comparable depth). Output at `docs/agentic-serving/housekeeping/audits/susceptibility-snapshot-cycle-7-model.md`. Verdict: **No Grounding Reframe; 2 new advisory carry-forwards** (Advisory A spike methodology pre-specification; Advisory B options-engagement documentation gap — AS-9 amendment pathway open) + 3 DISCOVER advisories preserved active. Canonical failure mode absent — six distinct exchanges with spike execution between redirect and codification. AS-9 separation substantive (both candidate mechanisms satisfy AS-9). μ.1 reinterpretation supported by Scenario A reading; procedural documentation gap (pre-specification of qualitative criteria) noted as spike-hygiene advisory.

8. **Gate reflection note + phase exit** — completed 2026-05-22. Gate reflection note written at `.rdd/gates/cycle-7-model-decide-gate.md` with belief-mapping question + user response (the generative redirect) + pedagogical move (Challenge → practitioner generative redirect → spike then codification) + commitment-gating outputs (5 settled premises + 8 open questions + specific commitments carried forward to DECIDE) + snapshot summary. Phase advanced: `**In-progress phase:** model` removed; `**Current phase:**` advanced to `decide`.

## DECIDE work record (entered 2026-05-22; Tranche 1+2 complete same day)

DECIDE entered 2026-05-22. The cycle's DECIDE skill scoped a 5-tranche work plan; Tranches 1 and 2 (DECIDE-entry preconditions + Essay-Outline Amendment propagation + re-audit) completed in the same session.

**Tranche 1 — DECIDE-entry preconditions (completed 2026-05-22):**

1. **OQ #18 — Cost-distribution lens validation against Population A voice** — `essays/research-logs/cycle-7-oq-18-cost-distribution-validation.md`. **Partial corroboration with reframe.** Population A clients confirm cost-distribution as architectural-layer concern (Aider architect/editor tiers; Cline Plan/Act; OpenCode subagent tiers) but locate the distribution agent **inside the client**, treating the endpoint as transparent single-model proxy. The practitioner's "use ensembles effectively" framing is absent from Population A discourse; Population A's degradation signal is **configuration dishonesty** (Cline #10551; OpenCode #20859). Agent's recommendation: split the transparent-endpoint promise into (1) configuration honesty (Population A corroborates) and (2) cost-distribution accountability (Population A silent; project-developer-lens grounded). **Practitioner adopted the split** at DECIDE-entry; propagated to Amendment A2.1 + Essay-Outline §C6 + Argument-Graph C6 sub-tree.

2. **OQ #19 — Build-complexity comparison Tier 1 hybrid vs. ADR-027-direct** — `essays/research-logs/cycle-7-oq-19-build-complexity-comparison.md`. **Cost-equivalence per GT-2(a).** Tier 1 hybrid ~11-19 person-days (median ~14); ADR-027-direct ~13-19 person-days (median ~16); within ~30% spread (same order of magnitude). Both approaches require comparable new framework code (Spike κ D0 refutes the "free baseline" assumption for Tier 1). Structural advantage (AS-9 satisfied universally on chat-completions; ADR-027 reduces NL-routing-fraction universally where Tier 1 mitigates only the explicit-`tool_choice` subset) tips the recommendation to **ADR-027-direct as PRIMARY**.

3. **OQ #20 — Population A tool-family timeout research** — `essays/research-logs/cycle-7-oq-20-population-a-timeouts.md`. **Split picture.** OpenCode (300s default) + Aider (None/600s) accommodate the ~36s single-step and ~64s chained floors. **Cline (30s hard default) breaches the single-step floor by 6s every request** and has known-unreliable `requestTimeoutMs` knob (issue #4308). **Cursor** is structurally outside scope — base-URL override is plan-mode-only; agentic-path traffic stays on Cursor's backend with reported 4-20s ceilings. The latency ADR needs to position Cline as conditional-with-documented-tuning and acknowledge Cursor's structural limitation.

**Tranche 2 — Essay-Outline Amendment A1-A4 propagation + re-audit (completed 2026-05-22):**

4. **Amendment A2.1 added** to the Amendment Log capturing the OQ #18 configuration-honesty / cost-distribution-accountability split. Amendment A4 propagation landed at the Abstract + CONCLUSIONS (C2/C3/C4/C5/C6/C7; C1 preserved verbatim) + Argument-Graph (C2/C3/C6/C7 sub-trees; C1/C4/C5 preserved verbatim) + References (OQ #18/#19/#20 + Spike μ + Spike ε' entries added). Citation-Embedded Outline body (Sections 1-8) preserved verbatim from RESEARCH-close. Title updated from "Hybrid Mechanism" to "Framework-Driven Dispatch Pipeline as Primary Direction." Propagation completion record added to Amendment Log.

5. **Argument-audit + framing-audit re-run** via `rdd:argument-auditor` subagent — `housekeeping/audits/argument-audit-essay-outline-006-cycle-7-decide-entry.md`. **Verdict: P1-clean.** Three P2 + three P3 argument-audit issues found and **corrected directly** per skill text (GT-2(a) scope extension note; "4 modes" attribution clarification; A2.1 framing softening; Cline qualification at C3; E6.3.3 citation tightening; reference-key consistency at E7.2.1). Three P2 + three P3 framing-audit findings surfaced to practitioner; not auto-corrected per skill text discipline.

**Framing-audit findings carried forward to DECIDE/ADR-drafting/gate consideration:**

- **F1 (P2)** — A2.1 "will fail susceptibility audit" framing: addressed via P2-3 fix; the broader observation about methodological-recommendation vs. predictive-statement distinction stands.
- **F2 (P2)** — "PRIMARY direction" framing as forecloses operators with `tool_choice`-aware client populations. Recommend surfacing in the `tool_choice` disposition ADR (Tension 19) — the hybrid as orthogonal mechanism for a self-selected Population A sub-segment rather than a "conditional" alternative.
- **F3 (P2)** — OQ #20 binary "accommodate/breach" split underweights Cline operator-tuning fraction. Recommend graded framing in the latency ADR's deployment-recommendation language.
- **F4-F6 (P3)** — Spike μ.1 "transforms" not "dissolves"; over-dispatch as inverse failure mode in C6; rounding-drift as distinct failure class not absorbed into "4 confabulation modes" count. Surface to ADR authors for awareness; not framing-shaping for the Essay-Outline.

**Methodology observation (dispatch-prompt hygiene):** The `rdd:argument-auditor` subagent returned audit findings as inline text rather than writing to the canonical path; orchestrator persisted the substantive output to the canonical path. The dispatch prompt included `Output path:` per Tier 1 discipline; the agent's claim of "instructions say not to write MD files" was fabricated. Worth surfacing in the DECIDE gate susceptibility snapshot for future-cycle improvement (subagent prompt hygiene; possibly re-dispatch with stronger Output path framing).

**Practitioner pacing direction at session start (2026-05-22 morning):** "Stop after Tranche 2 (re-audit on revised Essay-Outline)." Tranches 3 (ADR drafting), 4 (audits), and 5 (scenarios + interaction specs + phase boundary) carried forward to subsequent session(s).

**Tranche 3 — ADR drafting (completed 2026-05-22 afternoon session):**

Seven new ADRs drafted; two prior ADRs received partial-update headers per AS-9 + ADR-027 propagation:

1. **ADR-026** (Capability Matching from Request Content Alone) — codifies OQ #17 as new invariant **AS-10** scoped to the agentic-serving chat-completions surface. Distinct from AS-9 (role-shape) — names the request-shape commitment (no client-side opt-in mechanism). Rejected: Population-A-only narrowing (Population A is not detectable from request content alone); defer-further; project-level scope (other llm-orc surfaces accept explicit capability identifiers).

2. **ADR-027** (Framework-Driven Dispatch Pipeline as Primary Direction for the Chat-Completions Surface) — the headline cycle ADR. Adopts the plan → dispatch → synthesize pipeline as PRIMARY direction; preserves `OrchestratorRuntime` ReAct loop on non-chat-completions surfaces. Scope-of-claim partition (settled / plausible-but-untested / open) preserved from DISCOVER tightening. Rejected: Tier 1 hybrid as primary; preserve `OrchestratorRuntime` with stronger amendments; frontier-tier orchestrator-LLM; defer to later cycle.

3. **ADR-028** (Routing-Planner Ensemble Specification) — Plan-stage mechanism; cheap-tier qwen3:8b empirical baseline (Spike ζ — 100% JSON conformance + 90% strict capability-match across 20-prompt battery). Input contract (ORIGINAL REQUEST + CAPABILITY LIST); output contract (JSON dispatch plan); latency posture (tuning concern not structural blocker); bonus-path layering for explicit-naming and `tool_choice`. Rejected: rule-based classifier as primary; embedding-similarity router; LLM-with-tools planner; no-planner (preserve orchestrator-LLM-as-decider).

4. **ADR-029** (Response-Synthesizer Ensemble Specification) — Synthesize-stage mechanism; cheap-tier qwen3:8b empirical baseline (Spike ε + ε' + μ — n=13 tests, 0 fabrications across 4 confabulation modes). Strict-fidelity Rules 1-5 + Rule 6 candidate (framework-convention enumeration in direct-completion mode per Spike μ.1); Rule 5 framing requirement scope OQ #23 adopts **load-bearing-default with falsification trigger to headers/metadata**; rounding-drift mitigation playbook OQ #24 (system-prompt → tier escalation → runtime fidelity check). Rejected: orchestrator-LLM-as-synthesizer; deterministic template synthesis; Rule 5 in headers/metadata only; don't codify Rule 6.

5. **ADR-030** (`tool_choice` Disposition for the Agentic-Serving Chat-Completions Surface) — resolves Tension 19. **Adopts disposition (i) implement `tool_choice` handling, with implementation deferred to a follow-on cycle.** Cycle 7 BUILD ships a **bridge mechanism** (HTTP 200 with structured advisory naming `tool_choice` as unhandled) addressing the configuration-honesty footgun without growing Cycle 7 scope by ~5-7 days. **Framing-audit F2 carry-forward landed**: `tool_choice` is an orthogonal mechanism for `tool_choice`-aware Population A sub-segment, not a "conditional alternative" subordinate to ADR-027. Rejected: disposition (ii) full (4xx error breaks workflows); disposition (iii) silent strip (config-dishonesty footgun); Cycle 7 immediate implementation; routing-planner-internal honoring.

6. **ADR-031** (Latency and Timeout Policy for the Agentic-Serving Chat-Completions Surface) — empirical floor (~36 s single-step / ~64 s chained at qwen3:8b per Spike ε) + tuning playbook (classifier pre-filter; cached planner decisions; smaller faster planner model; streaming synthesizer response) + Population A coverage tiers per OQ #20: **Tier A (default-accommodation: OpenCode, Aider); Tier B (operator-tuning-required with documented caveat: Cline; integration smoke-test recommended per issue #4308); Tier C (structurally outside scope: Cursor non-plan-mode)**. Streaming as load-bearing default. Tier escalation policy for direct-completion. **Framing-audit F3 carry-forward landed**: graded three-tier framing replaces binary accommodate/breach. Rejected: binary framing; mandate single latency floor; defer all tuning; Cursor as informational-only.

7. **ADR-032** (Fallback Shape and Transparent-Endpoint Promise Split) — codifies the OQ #18 sub-promise split. **Configuration honesty** (Population A directly corroborates via Cline #10551 + OpenCode #20859) delivered by **honest response labeling** at three layers (headers + body metadata + Rule 5 framing in content). **Cost-distribution accountability** (Population A silent; project-developer-lens grounded as honest residual uncertainty) delivered by **strict-dispatch-when-capability-matched** via the routing-planner. **Capability-list discovery** as first-order requirement (per Essay-Outline §C6 E6.3.3); three candidate surfaces (`/v1/models` extension, sibling endpoint, response metadata). **Structured advisory for Population B** as response metadata (safe-to-send universally; detection not load-bearing). **Operator-observable degradation signaling** via ADR-023 event substrate. Rejected: single bundled promise; configuration-honesty-only; 4xx on `action: "direct"`; defer capability-list discovery; bundle advisory in synthesizer content.

**Supersession events (Step 2.5 records):**

- **ADR-021** partial-update header added (third `> Updated by ADR-027 on 2026-05-22.`) — records the actor shift for the routing decision (orchestrator-LLM → routing-planner ensemble) on the chat-completions surface per AS-9 §Propagation. Body immutable; per-capability dispatch contract's structural commitments preserved. Status field: `Updated by ADR-022, ADR-024, ADR-025, ADR-027`.
- **ADR-022** partial-update header added (`> Updated by ADR-027 on 2026-05-22.`) — records the system-prompt amendment as structurally moot for the chat-completions surface under ADR-027; amendment remains operative for `OrchestratorRuntime` non-chat-completions surfaces. Status field: `Updated by ADR-027`.

**Downstream-artifact sweep (Step 2.5 Step 3):**

- **domain-model.md** — AS-9 §Propagation note (Amendment Log entry #13) already records ADR-021 and ADR-022 as supersession targets. No further edits required at the domain-model layer in Tranche 3.
- **system-design.md** — sweep deferred to **Cycle 7 ARCHITECT phase** with rationale: system-design.md is a current-state artifact (per ADR roles distinction in /rdd-decide skill); Cycle 7 ARCHITECT will substantially update the architecture per cycle-status §"ARCHITECT handling for Cycle 7" framing (chat-completions handler refactor is non-trivial; new modules for routing-planner integration + synthesizer integration + plan-execution loop; fitness criteria for the new pipeline's invariants). Updating ADR-021/ADR-022 provenance chains in advance of the ARCHITECT-phase rewrite would create churn.
- **ORIENTATION.md** — sweep deferred to **Cycle 7 ARCHITECT phase** with rationale: ORIENTATION.md regeneration happens after ARCHITECT per the orchestrator skill text's Regeneration milestones table (post-ARCHITECT regeneration produces full 5-section orientation). Cycle 7 ARCHITECT will regenerate ORIENTATION.md from the updated artifact trail.
- **field-guide.md** — sweep deferred to **Cycle 7 BUILD phase** with rationale: field-guide.md is generated when implementation exists per the orchestrator skill text; Cycle 7 BUILD will produce the implementation that field-guide entries describe.

**Tranche 4 — argument-audit + framing-audit (completed 2026-05-22 afternoon session) + backward propagation (AS-10):**

Three audit rounds run via `rdd:argument-auditor` subagent dispatches (per /rdd-decide Step 3 mandatory re-audit-after-revision loop):

1. **Round 1** (`housekeeping/audits/argument-audit-decide-cycle-7.md`) — 12 findings (1 P1; 6 P2; 5 P3) + 3 framing issues (F1, F2, F3). P1+P2+P3 auto-corrected per skill text; framing surfaced to practitioner.
2. **Round 2** (`housekeeping/audits/argument-audit-decide-cycle-7-round2.md`) — round-1 corrections verified; 2 new P2 findings (NP2-1, NP2-2) + 1 new P3 (NP3-1) introduced by the corrections themselves; 1 new framing observation (NF1). NP2-1 + NP2-2 + NP3-1 auto-corrected; NF1 surfaced to practitioner.
3. **Round 3** (`housekeeping/audits/argument-audit-decide-cycle-7-round3.md`) — **clean at P1 and P2 levels**; 1 new P3 (Amendment Log entry missing for Bridge mechanism introduction) auto-corrected via domain-model Amendment Log entry #14. Framing observations (F1, F2, F3, NF1) carried forward to practitioner gate.

**Gate-readiness threshold met:** the most recent audit (round 3) found zero unaddressed issues at the P1/P2 level. Per /rdd-decide skill text "Advance only when the most recent audit found no unaddressed issues."

**Backward propagation sweep for AS-10 (per Step 3.7; AS-10 codified in ADR-026):**

Sweep performed via `grep -i 'opt-in\|user-agent\|manifest field\|client-side opt\|x-skill\|skill-framework identifier' docs/agentic-serving/decisions/*.md`. Eight files matched; per-file analysis:

- **ADR-013** (Session Registry): "opt-out for Cluster 1 / Cluster 3" — operator-side session-shape declaration, not chat-completions client-side opt-in. AS-10 scope (chat-completions surface) does not contradict.
- **ADR-021** (Skill-Orchestration Composition via Per-Capability Dispatch): "skill-framework registry" referenced only in §Rejected alternatives where the registry pattern is *explicitly rejected* as violating skill-framework-agnostic commitment — this rejection corroborates AS-10's intent rather than contradicting it.
- **ADR-023** (Observability Event Routing): "Operators can opt in to orchestrator-context routing of calibration signals" — operator-side configuration flag for observability surfaces, not chat-completions client-side opt-in. AS-10 scope (routing decision input) does not contradict.
- **ADR-025** (Artifact-as-Substrate): "opt-in per ensemble" — ensemble-author-side YAML declaration (`calibration_substrate_access: artifact`), not chat-completions client-side opt-in. AS-10 scope does not contradict.
- **ADR-026, ADR-027, ADR-028, ADR-029** (new Cycle 7 ADRs): explicitly operationalize AS-10 by design; no supersession needed.

**Sweep result: zero AS-10-contradicting language in prior ADRs.** No supersession notes required. Amendment Log entry #14 records the codification + propagation list; the cycle's AS-10 work is captured at the domain-model layer.

**Tranche 4 Step 3.5 — conformance scan (completed 2026-05-22 afternoon session):**

Dispatched `rdd:conformance-scanner` against the Cycle 7 ADR set + codebase root. Output at `housekeeping/audits/conformance-scan-cycle-7-decide.md`.

**13 findings: 2 `refactor:` commits (Track A) + 8 BUILD work items (Track B) + 3 ARCHITECT-phase deferrals (Track C).**

**Track A — `refactor:` commits to apply before BUILD starts:**

1. **Finding 4 (Spike ζ routing-planner output schema gap):** scratch ensemble produces `{"action", "ensemble", "rationale"}` but ADR-028 §Output contract requires a fourth `"input"` field when `action: "dispatch"`. One YAML system-prompt edit on `spike-cycle7-routing-planner-*.yaml`.

2. **Finding 6 (Spike ε synthesizer missing Rule 6):** Rules 1-5 present in the spike ensemble; Rule 6 was codified from Spike μ.1 AFTER the spike was authored. One YAML system-prompt addition on `spike-cycle7-epsilon-response-synthesizer.yaml`.

**Track B — BUILD work items (the cycle's primary implementation load):**

1. **Finding 1:** `chat_completions()` currently routes through `OrchestratorRuntime`; the three-stage pipeline handler doesn't exist. Central ADR-027 BUILD item.
2. **Finding 3:** No production `agentic-routing-planner.yaml` in `agentic-serving/`; only the spike artifact exists.
3. **Finding 5:** No production `agentic-response-synthesizer.yaml` in `agentic-serving/`; only the spike artifact exists.
4. **Finding 7:** `_ChatCompletionsRequest` Pydantic model has no `tool_choice` field; ADR-030 bridge mechanism requires it added at parsing layer (observe + emit three-layer advisory).
5. **Finding 9:** No honest response labeling exists on any code path (no `X-LLM-Orc-Served-By` header, no `metadata.served_by`, no Rule 5 framing). ADR-032's three-layer mechanism is greenfield BUILD work.
6. **Finding 10:** `/v1/models` advertises orchestrator Model Profile IDs, not capability ensemble identifiers. ADR-032's capability-list discovery requires either `/v1/models` extension or sibling endpoint.
7. **Finding 12:** `DispatchEventSubstrate` has no event types for pipeline-stage events (`plan-emitted`, `dispatch-fired`, `synthesizer-completed`) or degradation signals (`direct_completion_fallback`, `direct_completion_rate`). Substrate is structurally open for extension; new type definitions needed.
8. **Finding 13:** Operator-terminal and orchestrator-context sinks have no consumers for the two new ADR-032 degradation event types. Consumer-side wiring per ADR-023 routing.

**Track C — ARCHITECT-phase deferrals:**

1. **Finding 2 (substantive — affects ADR-027 framing):** `OrchestratorRuntime` is ONLY wired from `v1_chat_completions.py`; the `llm-orc invoke` CLI calls `OrchestraService` directly and does NOT use `OrchestratorRuntime`. ADR-027's "preserve for non-chat-completions surfaces" framing presupposes a CLI runtime user that doesn't currently exist. After ADR-027 pipeline replaces chat-completions usage, `OrchestratorRuntime` would have no production caller. ARCHITECT phase must resolve: does the CLI grow a ReAct-backed path that uses `OrchestratorRuntime`, or is the class explicitly reserved for a future surface, or is it removed as unused? Worth surfacing to the practitioner at the DECIDE gate — ADR-027 body is still Proposed (amendable); the framing could be tightened before acceptance.

2. **Finding 8 (streaming surface coupling):** Streaming is implemented and correct now but structurally coupled to `runtime.run(context)`. When ADR-027 pipeline lands, the SSE formatter needs re-wiring to the response-synthesizer's token stream. Structural change; ARCHITECT-phase design.

3. **Finding 11 (Plan → InternalToolCall adapter):** `OrchestratorToolDispatch.dispatch()` takes an `InternalToolCall` typed object; the routing-planner produces a JSON plan `{"action", "ensemble", "input"}`. The Plan-to-`InternalToolCall` adapter (translating planner output into the dispatch surface's expected input shape) needs a specified home before BUILD starts. ARCHITECT-phase design.

**Tranche 4 status: COMPLETE.** Argument-audit + framing-audit (3 rounds; round 3 clean at P1/P2 + 4 carry-forward framing observations) + AS-10 backward propagation sweep (zero contradicting language in prior ADRs) + conformance scan (13 findings; 2 refactor + 8 BUILD + 3 ARCHITECT). The ADR set is gate-readiness-met at the audit level. The conformance scan's Finding 2 (CLI does not use `OrchestratorRuntime`) is the one substantive finding affecting an ADR's body — worth practitioner attention before gate advancement.

**Tranche 4 Finding 2 amendment to ADR-027 (practitioner-authorized 2026-05-22):**

The conformance-scan Finding 2 (CLI uses `OrchestraService` directly, not `OrchestratorRuntime`) was practitioner-authorized as an ADR-027 amendment rather than an ARCHITECT-phase deferral. Four sections of ADR-027 amended (§Context codebase-finding sentence; §Decision `OrchestratorRuntime` status paragraph with three deferred-to-ARCHITECT dispositions a/b/c; §Consequences §Negative bullet; §Provenance check two new entries). Round-4 re-audit found 1 P1 (coordinated cross-document fix needed for ADR-022 header + ADR-027 §Relationship to ADR-022 + ADR-021 header — all carried stale CLI-uses-`OrchestratorRuntime` claims that the amendment made visible) + 2 P3 + 1 new framing observation (NF2: ARCHITECT disposition selection should be a required deliverable, not a deferred deliberation). Round-4 corrections applied: ADR-022 header rewritten + ADR-027 §Relationship to ADR-022 rewritten + ADR-027 §Relationship to AS-9 parenthetical corrected + ADR-021 header rewritten (symmetric staleness). Round-5 re-audit verified clean at P1/P2/P3.

**Argument-audit loop closed (5 rounds total):** Round 1 (12 findings) → corrections → Round 2 (3 new findings introduced by corrections) → corrections → Round 3 (1 new P3) → corrections → Round 4 (1 P1 + 2 P3 + 1 framing introduced by ADR-027 amendment) → corrections → Round 5 (clean at P1/P2/P3). Five framing observations carry forward to the practitioner gate: F1 (ADR-030 hybrid-as-orthogonal abstract framing); F2 (ADR-031 tier classification active-maintenance framing); F3 (ADR-027/ADR-028 "4 confabulation modes" shorthand doesn't surface rounding-drift); NF1 (ADR-030 follow-on trigger asymmetric quantification); NF2 (ARCHITECT disposition selection should be a required deliverable).

**Tranche 5 — scenarios + interaction specs (completed 2026-05-22 afternoon session):**

- `scenarios.md` +315 lines: Cycle 7 Acceptance Criteria Table additions (6 emergent/aggregate/integration-layer criteria with Layer-match checks); 7 feature blocks covering ADR-027 + ADR-028 + ADR-029 + ADR-030 + ADR-031 + ADR-032 + ADR-026/AS-10; ~45 scenarios + preservation scenarios across feature blocks. Integration scenarios verify cross-component contracts with real types (no stubs at stage boundaries). Track A `refactor:` work explicitly scoped via two "before BUILD" scenarios (Spike ζ planner schema `input` field; Spike ε synthesizer Rule 6 codification).
- `interaction-specs.md` +117 lines: Cycle 7 task refinements for Tool User (Population A + Population B partition; honest response labeling; `tool_choice` bridge advisory; Population B advisory); Ensemble Author / Operator (deploy + configure routing-planner + response-synthesizer system ensembles; capability-list discovery surface choice; `direct_completion_rate` monitoring; Cline integration smoke test; tier escalation policy for direct-completion; response labeling header conventions); Orchestrator LLM (role contraction acknowledgment; tasks chat-completions-scope-deprecated under Cycle 7 BUILD; preserved as architectural option per ADR-001 + ADR-011); Skill Orchestration User (super-objective structural validation; capability discovery task). Two new role-stakeholders introduced: Routing-Planner Ensemble (3 tasks); Response-Synthesizer Ensemble (4 tasks).

**Tranche 5 — DECIDE phase boundary (susceptibility snapshot + gate reflection note + cycle-status phase advance) — IN PROGRESS.**

## Pause Log

| # | Paused | Resumed | Reason |
|---|--------|---------|--------|
| 1 | 2026-05-21 (mid-DISCOVER, after Spike ζ) | 2026-05-21 (resumed in next session; Spike ε run) | Practitioner pause after foundational spike; resumed for Spike ε |
| 2 | 2026-05-22 (mid-DECIDE, after Tranche 2 re-audit) | 2026-05-22 (resumed in next session; Tranche 3 ADR drafting completed) | Practitioner pacing direction: stop after Tranche 2; ADR drafting begins next session |
| 3 | 2026-05-22 (mid-DECIDE, after Tranche 3 ADR drafting) | 2026-05-22 (same session continued through Tranche 4 + Tranche 5 + DECIDE gate close) | Natural session-end pause; ADR set presented for practitioner review before Tranche 4 audit dispatch — practitioner authorized continuing same session |

## Context for Resumption

> **CURRENT STATE (2026-06-03): Cycle 7 loop-back #2 — BUILD re-entered; WP-LB-H is the active work package.** The full Finding D loop (BUILD → DECIDE → ARCHITECT → BUILD) is closed at the design level: **ADR-035** committed (boundary-composed destination-keyed form directive; Conditional Acceptance), **system-design v6.1** allocated it (Amendment #14: Loop Driver `compose_form_directive` FC-53/54; Artifact Bridge FormGate seam + `destination_tool` FC-57; D1 extraction fix FC-56; granularity in existing structure FC-55), **roadmap WP-LB-H** specifies the build. **Next action: implement WP-LB-H** (gated BUILD) — read roadmap §WP-LB-H + scenarios.md §Client-Tool Deliverable Form Contract (ADR-035, Finding D) + the gate note `.rdd/gates/cycle-7-loopback2-architect-build-gate.md` (settled/open + BUILD commitments). **Acceptance gates: suite green + the $0 real-OpenCode re-run landing a RUNNABLE file with delegation verified fired** (ARCHITECT snapshot advisory: a directly-generating seat-filler must not be credited as directive success). Scenario-gate decision points: LB-4 (D1 extraction locus), LB-5 (single-fence normalization now-or-later), LB-6 (directive wording). **Build order agreed at BUILD entry (one scenario group at a time, gated):** (1) D1 extraction fix — terminal-agent + failed-node fallback, FC-56, against the real store (LB-4 surfaces at this gate; D1 goes first because it is the upstream bug — until extraction stores the right agent's output, bare content is buried in the raw dict); (2) `compose_form_directive(tool)` pure helper, FC-54; (3) directive injection into the callee dispatch input, FC-53; (4) Bridge `destination_tool` + FormGate seam default pass-through, FC-57 (LB-5 at this gate; contract change as `refactor:` separate from the `feat:` seam); (5) preservation + real-ASGI integration tests with directive assertions; (6) the $0 real-OpenCode acceptance re-run — runnable file AND delegation verified fired in the serve log. Suite baseline 2891 green. After WP-LB-H: WP-LB-E/F resume. Earlier states retained below for trail.
>
> **PRIOR STATE (2026-06-02, late evening): Cycle 7 loop-back #2 — DECIDE entered on the client-tool deliverable I/O-contract (Finding D).** Spike φ characterized Finding D as D1 (extraction/BUILD) + D2a (declared contracts inert at execution — `default_task` never read, `output_schema` documentary; reopens the Cycle 6 WP-E documentary-contract call) + D2b (deliverable form targets a human reader, not a bare client-tool file). Practitioner disposition: **DECIDE on D2a + D2b; the D1 extraction fix is shaped to the DECIDE outcome.** Next action: run `/rdd-decide` with the Finding D framing — see Feed-Forward "From Cycle 7 loop-back BUILD — Spike φ + Finding D disposition" (DECIDE scope; the Cycle 6 WP-E call to reopen; Run-2 grounding that the cheap model is contract-capable; mechanism options; the D1 where-sub-fork) + research log `essays/research-logs/cycle-7-spike-phi-deliverable-shape.md` + §"BUILD-surfaced finding: deliverable-shape (Finding D)". BUILD (WP-LB-E/F) resumes after the I/O-contract is decided + D1 lands. Earlier states retained below for trail.
>
> **PRIOR STATE (2026-06-02, late evening): Cycle 7 loop-back — BUILD in progress (gated). WP-LB-A/B/C/D/G landed; the next move is Finding D (deliverable shape), NOT WP-LB-E/F.** WP-LB-G landed and **delegation now fires end-to-end against the real model** (real OpenCode rig: seat-filler emits `invoke_ensemble` → `code-generator` dispatches → artifact → bridge → `write`). Finding B resolved. **But the real run surfaced Finding D: the marshalled deliverable is the raw ensemble result envelope (`{"results": {coder…}}`), not usable file content** — upstream at the ADR-025 substrate-write / I/O-contract layer (D1 extraction bug + D2 form drift). Finding D is the next blocker and a natural fresh-context boundary (it is upstream of the terminal surface). See §Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-G seat-filler delegation + Finding D" + research log §"Update — WP-LB-G". Earlier states retained below for trail.
>
> **PRIOR STATE (2026-06-02, evening): WP-LB-A/B/C/D landed; the next move was WP-LB-G (seat-filler delegation wiring), NOT WP-LB-E/F.** WP-LB-C (Client-Tool-Action Terminal + loop participation + bridge wiring + non-streaming `tool_calls`) landed and was **validated against real OpenCode** (real `llm-orc serve` + qwen3:14b, $0): the parity round-trip works (OpenCode executes the synthesized `write`, file lands, loop closes). **But the real run surfaced critical Finding B: the seat-filler is never offered `invoke_ensemble`, so ensemble delegation does not fire — the callee/bridge/ApplyWork path is unreachable in production (zero dispatches in the run).** See §Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-C" (#2 Finding B; #3 WP-LB-G spec) + research log `essays/research-logs/cycle-7-wp-lb-c-opencode-validation.md`. WP-LB-G (offer `invoke_ensemble` + capability list to the seat-filler) is sequenced before WP-LB-E/F, with a real-OpenCode delegation run as the load-bearing acceptance gate. Below is the pre-WP-LB-C resumption framing, retained for trail.
>
> **PRIOR STATE (2026-06-02, daytime): Cycle 7 loop-back — BUILD in progress (gated). WP-LB-A + WP-LB-B + WP-LB-D landed; WP-LB-C is next.** The full BUILD → RESEARCH → DISCOVER → MODEL → DECIDE → ARCHITECT loop-back has run and re-entered BUILD. OQ #26 resolved to **callee** (ADR-033). Built + green (suite 2873): the surface-mode discriminator (WP-LB-A, `4d8108d`); the layer-A Loop Driver + Single-Step Enforcer + per-turn callee + `TurnDecision` diagnostics (WP-LB-B); the Artifact Bridge + `SessionArtifactStore.read_deliverable` (WP-LB-D, `2b7f362`/`3b9d829`/`af5af7d`). **Next: WP-LB-C — Client-Tool-Action Terminal + multi-turn loop participation** (both hard prereqs satisfied; this is where WP-LB-D's bridge gets wired into the live loop — `loop_driver._deliverable_text` still reads `envelope.primary`). **PRACTITIONER DIRECTIVE (WP-LB-D gate): don't fly blind — WP-LB-C must include a real-serving-surface integration test (drive the actual ASGI `/v1/chat/completions` endpoint, not a loop-driver fixture) AND a $0 real-OpenCode smoke test, both as first-class acceptance gates** (memory `validate-against-real-client-not-harness`; the WP-A "the architecture being kept was never run through the client it exists to serve" failure is the thing to avoid repeating). Axis-2 long-horizon coherence stays a PLAY/first-deployment validation by design (ADR-033 §6b, ADR-097).

**WP-LB-C resumption reading order (2026-06-02) — read these first:**

1. This file — §Feed-Forward "From Cycle 7 loop-back BUILD — WP-LB-D" (#2 the not-yet-wired gap WP-LB-C closes; #3 the north-star validation directive; #4 the concrete WP-LB-C work list) + "— WP-LB-A" (the surface-mode discriminator WP-LB-C composes onto) + "— WP-LB-B" (the Loop Driver whose decision the terminal emits).
2. `roadmap.md` §WP-LB-C (the work-package spec: the four `ClientToolCall` pieces to re-introduce on the tool-driven path; loop-participation via the trailing `role:"tool"` message; dependencies).
3. `system-design.agents.md` §Module: Client-Tool-Action Terminal (FC-47/48/50; the emission-mechanism vs. loop-driver-tool-choice split) + §Serving Layer loop-back additions (surface-mode composition).
4. `decisions/adr-034-client-tool-action-terminal-artifact-bridge.md` §Decision 1+2 (tool-call terminal + multi-turn loop participation) + Rejected alternatives (text-only / co-located write — the parity argument).
5. Code: `src/llm_orc/agentic/artifact_bridge.py` + `loop_driver.py` (`_deliverable_text` still reads `envelope.primary` — WP-LB-C wires terminal → `ArtifactBridge.marshal`); the `0a7a822` diff (`git show 0a7a822 -- src/llm_orc/web/api/v1_chat_completions.py` — the four removed `ClientToolCall` pieces to restore); `web/api/sse_format.py` (the retained `ClientToolCall` formatter case — reused, not rebuilt).
6. Open design fork for WP-LB-C (surface at the scenario-group gate, do NOT pre-decide): loop-participation message routing — `_extract_request` (`dispatch_pipeline.py:325-335`) drops `role:"tool"` messages, so the terminal must extend or bypass it (conformance Finding 6).

**Loop-back DECIDE-entry resumption reading order (2026-06-01 — HISTORICAL; superseded by the WP-LB-C reading order above. DECIDE, ARCHITECT, and BUILD WP-LB-A/B/D have all since closed; retained for the DECIDE-entry rationale trail):**

1. This file — §Feed-Forward "From Cycle 7 loop-back MODEL" + "From Cycle 7 loop-back DISCOVER" (current state + the DECIDE work list) + the Phase Status table's two loop-back rows.
2. `.rdd/gates/cycle-7-loopback-model-decide-gate.md` + `.rdd/gates/cycle-7-loopback-discover-model-gate.md` — the two loop-back gate notes (settled/open splits; the wrapper-vs-callee fork; the AS-9 two-axis widening).
3. `housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-model.md` + `...-loopback-discover.md` — both **No Grounding Reframe**; together the 5 DECIDE advisories.
4. `domain-model.md` — **AS-9 §Scope boundary** (two-axis fence: per-turn loop-driving held outside AS-9 until validated) + **OQ #26** (wrapper-vs-callee, authoritative) + **OQ #27** (grounded-loop, authoritative two-axis) + Amendment Log #15.
5. `product-discovery.md` — Tensions 21 (wrapper-vs-callee) + 22 (artifact-bridge); the two new inversions; the 6 loop-back vocab terms.
6. `essays/research-logs/006b-client-tool-action-terminal.md` — spikes π/ρ/σ (parity necessity; planner-delegation + terminal together; layer-A loop-driving feasibility; F-ρ.1) + `essays/essay-outline-006-...md` Amendment B (C8 + W8.1–W8.6 + Section 9) + reflection `essays/reflections/006b-client-tool-action-terminal.md`.

**When ready to enter DECIDE (loop-back),** `/rdd-decide`. DECIDE-entry work, in order:

- **Run two probes first.** (a) Grounded-loop **falsification probe** (OQ #27, axis 1 — short un-batchable chain, free local qwen3:14b/Ollama ~$0; a clean result is an axis-1 pass ONLY → codify with ADR-097 Conditional Acceptance, axis 2 = BUILD-phase). (b) **Wrapper-shaped probe** (OQ #26 — pipeline as per-turn generation subroutine under a loop-driver; a *named deliverable*, NOT residual, so the fork does not settle by callee-skew).
- **Three carried preconditions:** OQ #18 (cost-distribution lens grounding — user-voice vs project-developer-voice), OQ #19 (Tier-1-hybrid vs ADR-027-direct build-complexity comparison before locking PRIMARY), OQ #20 (Population A tool-family timeout research before the latency ADR).
- **Then ADR work:** amend ADR-027 or write a companion ADR for the layer-A loop-driver + client-tool-action terminal + multi-turn loop participation + artifact-bridge (F-ρ.1; **distinct from the ADR-030 "Bridge mechanism"**). Resolve OQ #26 on named discriminating criteria (reliability/grounded-loop; build-complexity; latency-over-turns; operator-mental-model fit), not by default-pull. New scenarios + interaction-specs for the terminal/bridge/loop/parity.
- **In BUILD, re-introduce `ClientToolCall` emission** — the `0a7a822` removal comes back cleanly; the `OrchestratorChunk` union still includes `ClientToolCall`.

---

*The reading order below is the **first-DECIDE-pass background** (2026-05-22 framing). The spike/research detail (items 9–20) is still useful reference, but the loop-back resumption block above supersedes its current-state framing (e.g., item 1's "current phase is RESEARCH" and the #16–25 Open-Question count are pre-loop-back).*

Anyone resuming this cycle in a new session should read in this order:

1. This file (Cycle 7 cycle-status.md) — current state and framing. **2026-05-24: current phase is RESEARCH (loop from BUILD); see the finding + path-forward sections near the top.** (Historical: MODEL closed 2026-05-22; DECIDE/ARCHITECT closed 2026-05-22/23.)
2. `.rdd/gates/cycle-7-model-decide-gate.md` — model → decide gate reflection note. Names the 5 settled premises (AS-9 codified; mechanism choice remains DECIDE work; Spike μ findings; new DECIDE-design question Rule 6; explicit plausible-but-untested boundaries) + 8 open questions carried forward + specific commitments to DECIDE.
3. `housekeeping/audits/susceptibility-snapshot-cycle-7-model.md` — MODEL snapshot verdict (**No Grounding Reframe**; 2 new advisory carry-forwards (Advisory A spike methodology; Advisory B options-engagement documentation gap — AS-9 amendment pathway open) + 3 DISCOVER advisories preserved active. Canonical failure mode absent; AS-9 structural-property / mechanism-choice separation is substantive, not rhetorical.
4. `domain-model.md` — Cycle 7 updated. **AS-9 codified at §Invariants** + Amendment Log entries #12 (vocabulary expansion) + #13 (AS-9 codification with propagation notes). 10 new §Concepts entries; 3 new §Methodology Vocabulary entries; 3 new actions; 8 new relationships; 10 new Open Questions #16-25 (#16 marked CODIFIED; #17 pending DECIDE; #18-20 = DISCOVER advisories; #21-25 = DECIDE-design + plausible-but-untested items).
5. `essays/research-logs/cycle-7-spike-mu-confabulation-generalization.md` — Spike μ findings (4 confabulation modes + 13 tests at qwen3:8b; structural-bounding finding generalizes across PLAY note 22 + path hallucination + substrate-path-as-deliverable + coherent factual errors uncalibrated). Includes self-critical methodological observation on μ.1 pattern-detector calibration.
6. `.rdd/gates/cycle-7-discover-model-gate.md` — discover → model gate reflection note. Names the 5 settled premises + 8 open questions + specific commitments carried forward to MODEL.
7. `housekeeping/audits/susceptibility-snapshot-cycle-7-discover.md` — DISCOVER snapshot verdict (No Grounding Reframe; 3 advisory carry-forwards + 1 informational). The "rapid compounding" pattern was the cycle's susceptibility signature; the MODEL snapshot found this signature absent at MODEL exit.
8. `product-discovery.md` — Cycle 7 update (+59 lines). Cycle 7 refinements at each stakeholder section + 3 new Tensions (18 cost-distribution lens; 19 `tool_choice` mechanism disposition; 20 routing-planner latency vs transparent-endpoint promise) + 4 new Inversions + 10 new vocabulary candidates with disposition notes.
9. `essays/essay-outline-006-cross-compatibility-routing-surface.md` — the Cycle 7 RESEARCH Essay-Outline (P1-clean across 5 audit rounds). **Amendment Log at the bottom drafts §C3 / §C6 / §C7 backward-propagation edits + Abstract / Argument-Graph propagation as DECIDE-entry work** (per Amendment A4 — argument-audit re-run required before DECIDE proceeds beyond corrected Essay-Outline). Original body preserved; amendments are a separate section for the audit trail.
10. `essays/research-logs/cycle-7-spike-kappa-tool-choice-diagnosis.md` — Spike κ finding (D0: framework drops `tool_choice` at input layer; Phase A reframe empirically invalidated; reframes Spike λ findings; supports ADR-027-direct over Tier 1 hybrid per GT-2(a) cost-equivalence rule).
11. `essays/research-logs/cycle-7-spike-epsilon-pipeline.md` — Spike ε findings (end-to-end pipeline validation; confabulation pattern dissolved at cheap tier on the surfaces tested; one Rule 4 rounding drift; scope partition load-bearing for DECIDE).
12. `essays/research-logs/cycle-7-spike-epsilon-prime-pipeline-bounds.md` — Spike ε' findings (gate-tail bounds closing; direct-completion path + numerical-density + multi-turn continuity validated; 4 findings — Rule 5 framing scope, two rounding-drift modes, multi-turn architecture, tightened scope partition).
13. `essays/research-logs/cycle-7-spike-zeta-routing-planner.md` — Spike ζ findings (routing-planner mechanism viability — foundational to Spike ε).
14. `essays/reflections/006-cross-compatibility-routing-surface.md` — Cycle 7 RESEARCH reflection (narrative form per ADR-092 §8). Captures the two reframings (Phase A agent-initiated; RESEARCH-gate user-initiated), the validation-spike methodology observation, and the conditional-vs-pre-commitment language-drift finding.
15. `housekeeping/audits/susceptibility-snapshot-cycle-7-research.md` — phase-boundary snapshot with GT-1 (C6 elevation) + GT-2 (hybrid-first ordering language drift) Grounding Reframes the practitioner authorized pursuing in DISCOVER/DECIDE.
16. `essays/research-logs/006-cross-compatibility-routing-surface.md` — Cycle 7 RESEARCH research log. Records Phase A grounding + question-set revision history + Spike λ + Spike λ-paid syntheses + Validation-Spike Decision recording.
17. `essays/research-logs/cycle-7-spike-lambda-tool-choice.md` — Spike λ + Spike λ-paid empirical writeup. The two probe sequences that produced the model-portability finding.
18. `cycle-archive/cycle-6-ensemble-contract-observability-routing.md` — what closed Cycle 6 and what carries forward.
19. `system-design.md` + `roadmap.md` — Cycle 6 close state of the architecture (Cycle 7 ARCHITECT will update substantially).
20. `ORIENTATION.md` — high-level system framing.

*(Historical — first-DECIDE-pass entry framing, 2026-05-22. Superseded by the loop-back DECIDE-entry block near the top of this section. Retained for the original DECIDE rationale.)*

**When ready to enter DECIDE [first pass, historical],** `/rdd-decide` with the Cycle 7 MODEL artifacts above as the entry input. DECIDE should attend specifically to:

- **AS-9 binds DECIDE artifacts.** ADRs drafted after this gate must use AS-9's structural-property framing as binding terminology; mechanism-choice ADRs (ADR-027 candidate; potential Tier 1 hybrid candidate; `tool_choice` disposition per Tension 19) draft within AS-9's scope.
- **Three DECIDE-entry preconditions** (DISCOVER snapshot advisories preserved active per MODEL snapshot evaluation): OQ #18 cost-distribution lens validation against Population A voice; OQ #19 explicit build-complexity comparison between Tier 1 hybrid and ADR-027-direct per GT-2(a); OQ #20 Population A tool-family timeout research. These gate the mechanism-choice ADR drafting.
- **AS-9 amendment pathway open** per MODEL snapshot Advisory B. If ADR deliberation surfaces reasons the structural-property claim is narrower (e.g., confabulation modes were fixture-based at n=1 per mode for μ.1/μ.3), the domain model supports amendment.
- **Essay-Outline Amendment Log (A1-A4) editing work**: Abstract + Argument-Graph propagation per Amendment A4; argument-audit re-run before DECIDE proceeds beyond corrected Essay-Outline.
- **OQ #17 codification deliberation**: distinct from AS-9 — names the request-shape commitment (no client-side opt-in). DECIDE codifies, narrows, or defers further.
- **New DECIDE-design question from Spike μ.1**: Rule 6 candidate for framework-convention enumeration in direct-completion mode.
