# RDD Cycle Status — Agentic Serving (Scoped)

**Artifact base:** `docs/agentic-serving/`
**Plugin version at cycle open:** v0.8.7
**Migration version:** 0.8.5 (`housekeeping/.migration-version`)

## Cycle Stack

### Active: Cycle 8 — The declarative-ensemble collapse

**Cycle number:** 8
**Started:** 2026-07-01
**Current phase:** BUILD COMPLETE (all Cycle-8 WPs A8-F8 done, 2026-07-08); next = graduate-time. *(Phase history retained below as record.)* DECIDE — deliverables complete; DECIDE→ARCHITECT gate run 2026-07-02. 5 ADRs Accepted (044 AS-11, 045 clean-slate collapse, 046 target architecture/dissolution, 047 Q4 extensibility, 048 Q2 grounded acceptance), all argument-audit-converged. **Scenarios + interaction specs DONE 2026-07-02** (additive+supersession: the Cycle-8 Target Architecture section is the driving contract; pre-Cycle-8 blocks banner-marked, deletion rides with the `agentic/` code sweep at BUILD). Gate conversation resolved the accept-gate placement fork (default-on in the build-turn shape) with a Grounding Reframe owed at BUILD-entry. **ARCHITECT complete 2026-07-02; ARCHITECT→BUILD gate run.** Additive §Cycle-8 system-design (light: the L2 imperative Runtime cluster dissolves → ONE declarative ensemble on shipped primitives; classify → seat → shape/form-gate/emit; Accept Gate = a build-shape composition not a module; registry = Capability List Builder parts + Composition Validator admission + a new Shape Catalog, after the ARCHITECT-gate recast) + roadmap §Cycle-8 (WP-A8..F8). **Next: BUILD (gated mode)** — pending the practitioner's proceed confirmation. ORIENTATION regen + current-state sweep deferred to BUILD. Grounding Reframe (ADR-048 thinned-criteria spike) owed at WP-D8 entry; Q5 sequencing in WP-F8 (envelope-relocation-before-deletion fixed).
**Cycle type:** standard (full pipeline from DISCOVER; RESEARCH skipped — the Cycle-7 PLAY Ω-spikes are the research)
**Skipped phases:** research
**BUILD mode:** gated (declared at cycle open; design-alternative-heavy — extensibility architecture + the clean-slate removal sequencing warrant surfacing trade-offs; gated stewardship appropriate)
**BUILD entered 2026-07-02.** Starting WP-A8 (Serving Ensemble skeleton, classify → seat → marshal → TS-1). **Standing BUILD directive (practitioner, 2026-07-02):** ground each implementation with a real-`opencode` run at the earliest runnable point — do not build harness-only / in a vacuum (the WP-A failure mode; [[feedback_validate_against_real_client]]). Grounding Reframe (ADR-048 thinned-criteria spike) owed at WP-D8.

**DECIDE progress (2026-07-01):** ADR-044 (codifies invariant **AS-11** — declarative-ensemble-native serving; extend the engine, never a parallel layer) and ADR-045 (clean-slate collapse of the imperative serving layer) **Accepted**. Backward propagation done: supersession headers + Status-field updates stamped on ADR-033 through ADR-043 (each → ADR-045; behavioral requirement carried forward per ADR-045's table — the HOW is superseded, the WHAT survives); AS-11 codified in `domain-model.md` §Invariants + Amendment Log #22. **Deferred to Cycle-8 BUILD (Step-2.5 deferral-with-rationale):** the downstream current-state sweep (system-design.md / ORIENTATION.md / roadmap.md / field-guide.md) and the `src/llm_orc/agentic/` code deletion — both fire when the declarative form reaches parity; rewriting the current-state docs before then would document an unbuilt architecture. Supersession is mechanism-based (corrected across argument-audit R1–R5): ADR-015/021 also superseded (headers), ADR-027 already chained via ADR-043; every other `agentic/`-implemented ADR is deferred to the Cycle-8 target-architecture ADR via a **source-derived, complete-by-construction** deferred set (ADR-045 Appendix inventory — R2/R5 caught hand-lists omitting 003/008/009/010/011 and mislabeling orchestrator_tool_dispatch as forbidden when it's the ADR-003 contract). Full per-ADR classification is the target-architecture ADR's first task. Pre-existing drift flagged for the conformance audit: AS-10's invariant text is missing from §Invariants. **Argument audit CONVERGED R6** (P1=0/P2=0, Convergence-Saturation TRIGGERED; report `housekeeping/audits/argument-audit-decide-cycle-8.md`) — R1 caught an under-scoped (proximity-based) supersession, R2–R5 caught hand-list omissions/mislabels, closed at R6 by replacing hand-picked lists with a **source-derived complete deferred-set inventory** (ADR-045 Appendix; auditor re-derived it blind and it matched). Conformance-hygiene for the audit backlog (pre-existing, unrelated to this pair): ADR-025/026 still `Status: Proposed`; ADR-024's `Updated by ADR-035` lacks a forward pointer to ADR-045. **Next DECIDE decision:** the target-architecture ADR (general per-turn handler as ONE declarative ensemble + the full per-ADR survives-vs-superseded classification the collapse handed it), then extensibility (topaz dynamic dispatch), grounded acceptance (§6.2b), speed (§6.3); then scenarios + interaction specs.

**Target-architecture GROUNDING SPIKE entered 2026-07-01** (DECIDE-entry, per the practitioner's spike-then-DECIDE rhythm). Pre-reg: `essays/research-logs/cycle-8-spike-dynamic-dispatch-stage-swap.md`. Practitioner design frame: a minimal generalized lifecycle of **discrete stages, each seat swappable with an ensemble strategy to iterate on** (a "decider" seat prepends a role and delegates to a seat-filler). Spike setup (practitioner-confirmed): real-OpenCode serving is **a2-proven, NOT re-spiked** — reuse the Ω-serve foundation (`scratch/spike-omega-serve/`); the gap to build is **dynamic dispatch** (Phase-4 L0 primitive, runtime-resolved `ensemble:` target), built **TDD + shippable** like guard/loop, not throwaway. Validate: **swap-ability** (swap a structurally different seat strategy at zero skeleton change) + a **non-build turn** (generality, not the build path again). $0-local; ask-before-spend. Methods-review skipped (feasibility spike; falsification pre-stated). Inter-stage seam = the surviving common I/O envelope (ADR-024). Findings feed the target-architecture ADR.

**SPIKE CLOSED 2026-07-01 (success) + ADR-046 drafted.** The grounding spike resolved YES. The dynamic-dispatch L0 primitive shipped strict-TDD (commits `64c79ee` shared `${dep.field}` resolver / `c3709a0` primitive); the classify → seat → marshal serving turn is ONE declarative ensemble; all three criteria validated with real models (dynamic dispatch #1; swap-at-zero-skeleton-change #2; non-build turn #3). One integration finding: the seam is the phase-layer `results_dict` resolved guard-sibling (A2), not a prose string in the runner. **Q3 (speed, §6.3) RESOLVED:** the latency wall was thinking tokens, not throughput or architecture; Ollama's native `think` param, wired TDD (commit `6943f8e`), cuts build turns ~10x (63.5s→6.1s), interactive on the 32GB rig, no quality cost on easy tasks — thinking-mode is now a per-seat routing dimension. **ADR-046 (target architecture) drafted (Proposed):** §1 classify→seat→marshal as one ensemble; §2 seats-behind-contracts, contract = the existing `core/validation/` framework used seat-owned / black-box / deterministic-first, decider-is-a-seat; §4 thinking-as-routing-dimension. §3 per-ADR classification complete: [confirm] carry-forwards (003/004/005/006/008/009-010/011/012/024/025/001 survive as contracts); resolved reads (013 survives; 017 split — imperative ReAct superseded, tool-call guard survives; 018 superseded-with-015). **Calibration-subsystem scope decision = REPLACE (practitioner 2026-07-01):** 007/014/016 superseded (grounded acceptance re-homes to seat contracts; **AS-5 quality-not-frequency survives as an invariant**), 023 partially superseded (residual dispatch-timing/liveness observability survives as an engine-level requirement). **Backward-propagation touch:** 016's supersession reverts ADR-002's layering rule to no-exception (the L0→L1 read-only channel amendment is moot) — flagged for the ADR-002 sweep. **Argument audit on ADR-046 (R1, NOT converged: 4 P1 / 7 P2)** → `housekeeping/audits/argument-audit-decide-cycle-8-adr046.md`. It caught real overreach + a factual reversal and surfaced a ROOT question that reframes the replace decision: **does Cycle-8 dissolve the internal orchestrator actor?** (OpenCode owns the loop; per-turn classify→seat→marshal; no runtime self-composition/calibration/promotion). If yes: ADR-001 is **superseded, not survives** (P1-4 — ADR-001 decided the INTERNAL loop, verified against source; "OpenCode owns the loop" is its reversal, per OQ #26→ADR-033); AS-5's cross-session stabilization/promotion half has **no successor** (P1-3 — seat contracts are per-dispatch, not cross-session trust accumulation); and **7 [confirm] "survives" rows (003/004/005/006/008/009-010/011) presuppose an orchestrator actor the architecture may have dissolved** (P2-6 / Framing-B — should move to [read]). Also: decider-seat + real-`opencode`-client claims **overreach the spike** (P1-1/P1-2 — the reported runs used a direct `EnsembleExecutor` harness + a *script* classify node, not a dispatch decider or real `opencode run`). Framing-A: the include-vs-replace binary foreclosed a middle path (drop the AS-11-forbidden verdict machinery but re-home AS-5's stabilization as a slim declarative primitive). **Orchestrator-actor decision: DISSOLVE (practitioner 2026-07-01).** The client (OpenCode/any caller) owns the loop; each request is one declarative ensemble pass; NO persistent internal orchestrator (no runtime ReAct loop / `compose_ensemble` / trust-promotion). **Persistence RELOCATES to the substrate** the ensembles read/write turn to turn: artifact store (ADR-025) / session registry (ADR-013) / KG (ADR-009-010, optional). **ADR-046 revised (R1 audit fixes incorporated 2026-07-01):** §3 reclassified around the dissolution — 001/003/004/005/006/008/011 superseded (orchestrator-actor mechanisms), 007/014/016 superseded (calibration; AS-5 per-dispatch half → seat contracts, cross-session promotion half OUT OF SCOPE — governed the dissolved self-composition surface), 023 partially superseded; 013/024/025/009-010/012 + 017-guard survive (substrate + seams + deterministic work); 033 out-of-scope (client owns multi-turn loop; state in substrate), 039 → script node reading siblings from the substrate. §Context/§2 hedges added (direct-executor-not-opencode; decider-seat is design-not-spike; seat-contract wiring unvalidated). **R2 re-audit DONE (NOT converged: 4 P1 — all bundling / over-supersession).** R1 fixes landed, but flat-superseding 003/006 + bundling 007-with-014 retired **actor-AGNOSTIC** content (the R1→R2 pendulum: under-supersede → over-supersede; the fix is to SPLIT each bundled ADR like 017). **R2 fixes applied 2026-07-01:** 003 tool-surface-contract superseded but operations (ensemble-resolution / KG-access) re-home to engine/script — resolves the 003↔009/010 contradiction (Plexus access was implemented *as* ADR-003 tool calls); 006 mechanism superseded but **AS-2 (validate-before-load) SURVIVES** (actor-agnostic; Q4 registry needs it); 009/010 substrate survives, *access* re-homes (not "unchanged"); 014 unbundled from 007 (per-dispatch calibration → seat contracts, its own rationale); **ADR-002 Layer-2 definition (ReAct-loop / fixed-tool-surface) added to the sweep** (foundational ADR directly contradicted by the dissolution; was outside the `agentic/`-classification scope). **ARGUMENT AUDIT R3 DONE 2026-07-01 (NOT converged: 1 P1 / 2 new P2; CONTINUE to R4).** Trend sharply narrowing (R1 4P1/7P2 → R2 4P1/3P2 → R3 1P1/2P2). All four R2 P1 fixes verified landed (3 clean; the 014-unbundle landed structurally but its new rationale reproduces the bundling pattern); all three R2 P2s confirmed conscious deferrals (not silent drops). **R3 P1 (P1-new-1):** row 014's "superseded — replaced by the seat-contract per-dispatch pass/fail" asserts an unexamined equivalence — `core/validation/` is a deterministic OUTPUT-contract check; ADR-014's AUQ/HTC is a PROCESS-level generation-trajectory confidence signal (token-entropy / attention-weight / verbalized-confidence) built to catch contract-conforming-but-anomalous outputs an output check cannot see. AUQ/HTC is no more actor-bound than AS-2 was — the **third instance of the same bundling pattern** (R1 AS-5, R2 AS-2, R3 ADR-014). Bears directly on Q2 (grounded acceptance §6.2b): is the deterministic seat contract sufficient, or is a process-level signal still needed? Auditor's two fixes: (a) argue explicitly that output-contracts suffice + accept dropping AUQ/HTC, or (b) name ADR-014/AUQ/HTC as a Q2 candidate (defer the empirical question to PLAY/BUILD, parallel to how AS-2/AS-5 were handled). **R3 P2s:** `list_ensembles` (ADR-003's 5th tool) has no disposition anywhere in ADR-046; ADR-009/010 not added to the backward-prop list despite R2's explicit rec (both ADRs' own text still cites the retired ReAct loop / ADR-003 tool surface as current premises). **Audit's central framing finding:** the standing risk is the checking discipline itself — fixes respond to *specifically-named* findings rather than a full component-level re-derivation of §3, so each round finds a fresh bundling instance in a row the prior fix touched. R3 P3/Q3 recommendation: do a full component-by-component re-derivation of the ENTIRE §3 table against each source ADR's numbered Decision items before R4 (rows 007/023 spot-checked clean this round but not fully swept). Report: `housekeeping/audits/argument-audit-decide-cycle-8-adr046-r3.md`. **R3 FIXES APPLIED + FULL §3 RE-DERIVATION DONE + ARGUMENT AUDIT CONVERGED AT R4 (2026-07-01).** Practitioner chose (b): ADR-014's AUQ/HTC → **Q2 candidate** (not subsumed by the deterministic seat contract; the empirical "is it needed" defers to PLAY/BUILD). All R3 P2s + the three deferred R2 P2s cleared: `list_ensembles` moot-as-runtime-tool; row 011 principle re-homes to the decider-seat design; rows 024/025 `calibration_verdict` field / calibration-eval subsection go with the superseded gate; row 012 referent named = ADR-013 progress-log (bounds its own flagged growth risk). Full component re-derivation of the un-swept rows (007/012/013/016/017/023/024/025) against primary Decision text found **NO 4th P1** — ADR-016 is a clean supersession (its five bounding mechanisms are all moot once the calibration channel is gone, like ADR-008 the control case); ADR-013 survives cleanly (deterministic substrate + write-gate). One extra component-check catch: ADR-016's own Sweep-Responsibility governance wants a cycle-status Phase-Status row recording the channel = superseded — added to the backward-prop owed list. **ARGUMENT AUDIT CONVERGED R4** (Convergence-Saturation TRIGGERED: 0 new P1 / 1 P2; ADR-046-track P1 trend 4→4→1→0; R4 independently re-derived every §3 row and found no 4th bundling instance; the R4 P2 = a second stale envelope diagnostics sub-field `audit_findings` wired to ADR-018's superseded audit, now closed + folded into the sweep; report `housekeeping/audits/argument-audit-decide-cycle-8-adr046-r4.md`). **BACKWARD-PROPAGATION SWEEP DONE (decision-record level) 2026-07-01.** Supersession headers stamped on ADR-001/007/014/016; partial-update headers on ADR-009/010/024/025; **ADR-002 updated** (Layer-2 definition superseded → classify→seat→marshal, carried in the header per body-immutable convention; ADR-016 layering-rule exception reverted to no-exception). Domain-model: consolidated **invariant-disposition block** (AS-1/2/3/5/6/7/9 — AS-2 confirmed surviving, AS-6 superseded, AS-1/AS-9 survive re-homed, AS-3/5/7 mechanisms superseded/concerns re-home) + **Amendment Log #23**; OQ #11 moot / #12 resolved-reverted / #14 largely-moot; Methodology-Vocab "Cross-layer calibration channel" superseded. **ADR-016 §Sweep-Responsibility — channel status: SUPERSEDED** (recorded below per its own governance). **TWO DRAFTS FOR PRACTITIONER REVIEW (the judgment pieces):** (a) ADR-002's Layer-2 replacement wording; (b) the AS-3/5/6/7 invariant dispositions. **DEFERRED to Cycle-8 BUILD** (lands with the `agentic/` code deletion, not now — editing while the code still exists would document an unbuilt architecture): the `system-design.md` layering-rule text revert + Layer-2 description + calibration/tier module removal; ORIENTATION / roadmap / field-guide current-state sweep. **ADR-046 ACCEPTED 2026-07-02** — practitioner approved both judgment drafts (ADR-002 Layer-2 replacement wording + the AS-3/5/6/7 invariant dispositions) as-is. **Remaining in DECIDE:** Q4 extensibility (topaz-keyed dynamic-dispatch registry + binding-time fork) + Q2 grounded acceptance (beyond contract assertions; AUQ/HTC is a named candidate); then scenarios + interaction specs.

> **ADR-016 cross-layer calibration channel — status: SUPERSEDED** (Cycle 8, 2026-07-01, via ADR-046; the channel fed the now-dissolved calibration subsystem). Recorded here per ADR-016 §"Sweep responsibility" governance (a cycle-status entry noting the channel's status is owed for any cycle that touches ADR-016). Process note: BOTH R1 and R2 auditor verification sub-dispatches wrote to their report file directly (tooling issue flagged for the sub-dispatch maintainer); the auditor reconciled and independently re-verified all load-bearing claims against source. Spike artifacts + the classify/seat/marshal skeleton committed (`807f0b9`). **Next DECIDE (after ADR-046 accept):** Q4 extensibility (topaz-keyed dispatch registry + the binding-time fork: load-time curation vs turn-time vs hybrid) + Q2 grounded acceptance beyond contract assertions; then scenarios + interaction specs. Backend follow-up (optional, below the ensemble layer): llama.cpp/llama-server, scoped in `references/llama-server-serving-backend.md`; note thinking-off there needs the same lift-into-request wiring in `openai_compat.py`.

**Predecessor:** Cycle 7 (loop-driven serving + the agent-as-ensemble PLAY) — archived to `cycle-archive/cycle-7-loop-driven-serving-and-ensemble-play.md` 2026-07-01. Cycle 7's serving ADRs (ADR-033/036/037/039/041/043) remain live until Cycle 8 supersedes them.

**Cycle identity:** the agent *is* a declarative ensemble. Collapse the bespoke imperative serving architecture (`src/llm_orc/agentic/`) into the llm-orc-native declarative-ensemble form validated across the Cycle-7 PLAY spikes, establish serving defaults + extensibility, and retire the parallel layer clean-slate.

**Entry rationale:** research is done (the Ω-spikes). Two open items are product-shaped (general-vs-narrow behavior; interactive speed on the 32GB rig) and were routed to DISCOVER by the PLAY closeout, so Cycle 8 enters at DISCOVER in **update mode**, scoped to those two, then flows to DECIDE. MODEL is likely a light update or skip (decided at the DISCOVER-exit boundary). ARCHITECT is expected to be light (the flow is one ensemble; primitives shipped bar dynamic dispatch) — touches handled as amendments where possible.

## Settled Premises (carried into Cycle 8)

1. **Target form (practitioner, firm yes, 2026-07-01):** the agentic serving strategy is a declarative ensemble on the L0 engine, not bespoke imperative Python.
2. **Governing invariant (candidate ADR-044 — the first DECIDE item):** agentic serving composes as llm-orc-native ensembles (model-profiles + scripts + engine primitives). Control flow lives in the ensemble, not in an imperative driver. Where the engine is inadequate, **extend the engine** (new primitives), never build a parallel orchestration layer beside it.
3. **Removal strategy — clean-slate (practitioner, 2026-07-01):** the ~12.7K-LOC bespoke `agentic/` layer and the retained spikes are learning/reference, not a substrate to harden. Cycle 8 builds the declarative serving product fresh (informed by the spikes) and deletes the bespoke layer as the new form reaches parity. No strangler pattern on spike code. Rationale: "We've learned a lot, but I don't want to do a strangler pattern on spike code." (Warrants ADR-045-shaped record with rejected alternatives: strangler, parity-then-delete.)
4. **Ensemble-over-frontier lens (practitioner, 2026-07-02):** whenever the reasoning reaches "a small model can't do this," first ask what **ensemble orchestration** (bounded roles + structure + deterministic verification) can achieve before defaulting to "a frontier model should do it." Composition is the capability lever (north star); a frontier model is a fallback and a benchmark, not the reflex. Wired into ADR-047 (the composer's compose step) and applies corpus-wide as a design lens.

## Open Questions (Cycle 8 owns)

- **Q1 — general-vs-narrow behavior.** Does the daily-driver baseline need general TUI behavior (explain / fix / edit existing / run tests) or is build-new-files enough for the north star? (PLAY note 5.) [DISCOVER jobs → DECIDE/ARCHITECT scope]
- **Q2 — grounded acceptance (§6.2b). SPIKE-GROUNDED; ADR-048 drafted 2026-07-02.** The accept signal must be independent of the builder. Independence is NOT outsourced to the client (OpenCode calls back into llm-orc; it is the interface, not an oracle) — it is composed INSIDE the ensemble via seat isolation + deterministic execution. Grounding spike (`essays/research-logs/cycle-8-spike-q2-grounded-acceptance.md`): a minimal gate = deterministic executor seat (runs code+tests) AND isolated test-adequacy judge seat (think:false 8B) AND a guard, built only from shipped primitives (F1 no-new-primitive false; F2 8B-judge-capable false; F3 executor-runs false). **Load-bearing finding:** the two signals catch ORTHOGONAL failures (executor → wrong code; judge → trivial/under-covering tests); neither alone suffices, the conjunction is the gate. **Surprise:** the judge does requirement-COVERAGE analysis, not just triviality detection (caught a false-adequate leap-year gap because the requirement stated the century rule). Four-layer catch model; the oracle ladder (held-out/property/golden) is needed only for the true ceiling (bugs on requirement-UNSTATED inputs, un-spikeable by construction). ADR-048 (Proposed): composed gate; independence via isolation+determinism; deterministic-primary signal ordering (trajectory-confidence/AUQ-HTC advisory only); minimal-then-ladder; acceptance-criteria as a first-class contract input. Conditional Acceptance on judge-reliability (BUILD calibration). **Argument audit converged R3** (P1 3 → 0 → 0, Convergence-Saturation triggered; reports `argument-audit-decide-cycle-8-adr048.md` + `-r2.md` + `-r3.md`); **ADR-048 Accepted (Conditional) 2026-07-02.** R1 caught the load-bearing error: independence was claimed spike-proven when the spike only proved *discrimination* on static fixtures (no live builder), plus an unexamined produced-artifact injection channel; reframed independence as a BUILD/PLAY validation target. R1 also forced a reframe — **acceptance-criteria/specification quality is the load-bearing variable**, so acceptance-criteria-as-contract was elevated to §Decision-2 (primary) with a weak-criteria response (three independent angles converged on it: the practitioner's hierarchical-confidence musing, the spike caveat, and the isolated audit). AUQ/HTC re-homed as an anchored non-primary candidate (not asserted out). Seat-contract reconciliation added (loop-level accept gate vs ADR-046 per-seat admission; they compose). [DECIDE — Q2 done]
- **Q3 — interactive speed (§6.3). RESOLVED 2026-07-01.** The wall was thinking tokens, not throughput or architecture. Ollama's native `think` param (wired TDD, commit `6943f8e`) cuts build turns ~10x (63.5s→6.1s), interactive on the 32GB rig, no quality cost on easy tasks. Thinking-mode is now a per-seat routing dimension (ADR-046 §4). Hard-task quality boundary untested (a named edge). [DECIDE — done]
- **Q4 — extensibility architecture. DECIDED (ADR-047, 2026-07-02).** Dynamic dispatch shipped (Cycle-8 spike). ADR-047: registry = Topaz-keyed building-block parts + an operator-curated catalog of composition shapes; binding is declarative and load-time-first (Strategy A: classify selects a shape, dispatch fills slots); invention of new shapes is author-time and grows the catalog; the runtime-fill of fixed shapes is free on shipped primitives (feasibility spike). **Deferred forward direction:** the compose-at-runtime primitive + composer-ensembles (specialized ensembles that make ensembles from building blocks), gated on AS-11 need + the strategy pillars in ADR-047. **Argument audit converged R2** (3 P1 → 0, Convergence-Saturation triggered; reports `housekeeping/audits/argument-audit-decide-cycle-8-adr047.md` + `-r2.md`); **ADR-047 Accepted 2026-07-02.** R1's central finding: the composer-path pillars claimed essay-004 evidence for ensemble-first-*composition* that essay-004 doesn't give (it assigns *design* work to the capable tier, *routing* to cheap models; self-MoA beats mixed-MoA on open-ended generation). Fixed by scoping the evidence claim to routing/selection and holding ensemble-first-for-composition as an open, deferred, **not-ruled-out** hypothesis (a bet under the ensemble-over-frontier standing principle, validated at BUILD/PLAY against a capable-model-composed baseline). No pendulum over-correction (R2 verified the hypothesis stays intact, not conceded). Deferred to BUILD (ADR-047 P3): pre-register the compose-step hypothesis's success/refutation criteria at its spike. [DECIDE — done]
- **Q5 — removal execution.** Sequencing the clean-slate collapse: what lands first; ADR supersession of ADR-033/036/037/039/041/043; spike retirement (retained until corpus close per the retention discipline). [DECIDE/roadmap → BUILD]

## Feed-Forward Signals

### From Cycle 8 DECIDE (ADR-046 accepted 2026-07-02) → Q4 extensibility + Q2 grounded acceptance

**Settled premises Q4 builds on (do not reopen):**
1. **The dispatch mechanism is shipped, not Q4's to build.** Dynamic dispatch is an L0 engine primitive (runtime-resolved `ensemble:` target, resolved at the phase layer via the shared `${dep.field}` resolver). Q4 builds the *registry the target resolves against* + the binding, not the resolution mechanism.
2. **The registry's baseline is operator-curated** (operators register candidate seats per capability). What ADR-046 retired is one specific form — a *persistent orchestrator LLM* authoring ensembles at runtime via a `compose_ensemble` tool (AS-6 / ADR-006). **Runtime ensemble authorship is NOT ruled out** (practitioner, 2026-07-02): composing ensembles from other ensembles at runtime is a live direction of interest, shape deferred. The open boundary is *form* — it must be a **declarative engine capability** (AS-11: extend the engine — e.g. a runtime-composition primitive building on dynamic dispatch), not a resurrected persistent-LLM-actor-with-a-tool-surface. The line to keep clean: what *decides* the composition at runtime — a declarative guard/dispatch/decider-seat, vs. an unbounded LLM authoring step.
3. **AS-2 (validate-before-load) is the registry's admission gate.** Every candidate is validated against the ensemble reference graph (no cycle / within depth / resolves to an existing entry) before it can fill a seat. Surviving invariant to *wire*, not re-derive.
4. **Trust-promotion stays retired — and it is separate from runtime authorship.** AS-5's cross-session stabilization/promotion half was retired with the calibration subsystem (no successor). Composing ensembles at runtime (premise 2) is a live direction; *accumulating quality signals to auto-promote candidates across sessions* is the retired calibration machinery. Keep them distinct — the former is welcome, the latter is what ADR-046 dissolved. (Per-dispatch quality signal is Q2's concern.)
5. **Keyed on the Topaz capability taxonomy** (grounded 8-skill set: `code_generation`, `tool_use`, `mathematical_reasoning`, `logical_reasoning`, `factual_knowledge`, `writing_quality`, `instruction_following`, `summarization`). classify emits `{target}` → dispatch resolves it against the Topaz-keyed registry.
6. **Extend the engine, never a parallel layer (AS-11).** The registry is engine/config, not a bespoke orchestration surface.

**The open fork Q4 owns — binding time + composition depth:** (a) *binding time* — load-time curation vs. turn-time selection vs. hybrid; and (b) *composition depth* — selecting an existing registered ensemble vs. **runtime composition of a new ensemble from parts** (the practitioner's runtime-authorship interest, 2026-07-02; shape deferred, form must be declarative per premise 2). Plus how operators register/extend, and the serving default (the default generalist ensemble + the operator-extension path). ADR-046 §Open Q4.

**Kept separate (Q2, not Q4):** grounded acceptance beyond contract assertions, with ADR-014's AUQ/HTC as the named candidate. Q4 is registry *shape*; Q2 is the accept *signal*. Don't fold them.

**Reference:** `proposals/agentic-serving-library-structure.md` (extensibility), `skill-framework-capability-registry.md` (Topaz registry), ADR-046 §Open Q4.

### From Cycle 8 DECIDE — scenarios + interaction specs + DECIDE→ARCHITECT gate (2026-07-02)

**Deliverables done:** Cycle-8 scenarios (`scenarios.md`, top "Cycle 8 — Target Architecture Scenarios" section: 6 feature blocks — per-turn handler / dynamic dispatch / registry+shape catalog / grounded-acceptance gate / honest limits / thinking-mode routing — + a Cycle-8 acceptance-criteria table with 5 emergent/integration criteria, 3 coinciding with ADR-048's Conditional-Acceptance targets) and interaction-specs (`interaction-specs.md`, "Cycle 8 interaction-spec additions": Tool User general-parity / accept-gate / thinking-latency; Operator registry-and-catalog curation + removed promotion surface; Orchestrator LLM stakeholder dissolved). Organized **additive+supersession** (banner marks the pre-Cycle-8 dissolved-architecture blocks; deletion rides with the `agentic/` code sweep at BUILD).

**Gate resolution — accept-gate placement (carried to ARCHITECT):** the accept gate is **default-on in the build-turn shape**, not opt-in by criteria; the build-vs-non-build split follows classify's executable-deliverable routing (non-build turns run no gate); multiple rounds OK when each is short (~seconds) and well-scoped. Recorded in `scenarios.md` as gate-conversation synthesis / validation-pending (NOT ADR-derived). The conclusion rests on the spike-grounded triviality-floor argument (the judge's `assert True` rejection is criteria-independent, so a presence-gated opt-in reduces to the rejected executor-only mode).

**Grounding Reframe (owed at BUILD-entry, before wiring the gate unconditional):** rerun ADR-048's grounding-spike fixtures with criteria withheld/thinned to measure what the judge-half adds over the executor on thin-criteria turns. Source: susceptibility snapshot `housekeeping/audits/susceptibility-snapshot-cycle-8-decide.md` (caught the FF1 framing-absorption, the §2 gate-*power*-vs-*placement* over-read, and the richness-judged-vs-presence-gated conflation in the agent's original argument; the default-on conclusion survives on the corrected argument). Gate note: `.rdd/gates/cycle-8-decide-architect-gate.md`.

**ARCHITECT-facing sub-questions:** (a) confirm build-vs-non-build gate applicability in module terms; (b) round-multiplier bounding — per-round latency budget + ADR-048 §5's open composition rule (strict AND vs weighted/hierarchical) + judge false-reject rate.

**Graduate-time consideration (parked, practitioner 2026-07-02):** the `agentic/` **code** deletion (Q5/BUILD) is distinct from RDD **corpus** archival. What merges to `main` must be clean (native docs, no 8-cycle RDD scaffolding); the full corpus survives on a research branch for posterity; reconcile at graduate. The corpus is BUILD's input, so its archival is genuinely terminal.

**PLAY-facing signal — flow-engagement interaction mode (practitioner, speculative, 2026-07-02, at the ARCHITECT (b) discussion):** fast local rounds (e.g. 8B/no-think, ~seconds) may invert the human-loop economics vs. Claude-on-auto — cheap surfacing enables a tight human-ensemble *flow* loop rather than an autonomous-agent-that-surfaces-rarely. Connects to the practitioner's `/flow` skill (terse intent → agent builds design-as-contract → status line); the ADR-048 acceptance-criteria contract IS that "design as contract," and criteria-elicitation flips from a thin-criteria fallback shape to the flow-mode primary path. Agent synthesis to test (not a settled claim): the accept gate's *independence* role scales inversely with human-loop tightness — autonomous = gate is the independence anchor; flow = the human is, gate demotes to a fast pre-filter. Architecture keeps the door open with no change (fast rounds + ADR-047 shape catalog + ADR-048 criteria-contract compose toward it; the deterministic surface-triggers are the same for a human or a client at the outer loop). PLAY validates whether it feels like flow. Per [[feedback_live_multiturn_primary_spike]], this is a session-level experiential property, not a per-decision one.

**Coherence / confidence-propagation framing (practitioner, 2026-07-02, extends the flow thread; connects to the deferred directions):** the flow act of articulating a system in human language, drilling abstract→concrete, is structurally the same as **assume-guarantee reasoning** (compositional verification; Jones rely-guarantee, Abadi-Lamport) — the recursive generalization ADR-048 §Deferred names (this gate = the two-level base case; the recursive form = higher-abstraction seats asserting confidence over the verified contracts of lower ones) and ADR-047's composer-ensembles are its generative side. Honest split (applies the determinism lens): what falls out deterministically is the **verification composition** (confidence propagates upward by a deterministic rule GIVEN rich contracts + a deterministic anchor at each level, ADR-048 §4; AS-2 reference-graph coherence is the structural spine); what does NOT fall out is the **generation** (articulating the concrete from the abstract — the verification-vs-generation asymmetry, ADR-048 §Deferred: verifying is easier than generating); and the whole thing is conditioned on **contract richness at each level** (ADR-048 §2 primacy, now recursive — thin contracts break the upward propagation). Flow exploits the asymmetry: human owns generation (the irreducible half), deterministic machinery owns verification-composition (the falls-out half), fast rounds couple them. The KG (Plexus, ADR-009/010 substrate) is the natural persistence home for the contract hierarchy with provenance — deferred ("another time"). Forward/north-star, NOT Cycle-8 build scope.

**Generation-side refinement (practitioner, 2026-07-02):** verification-passing certifies generation *against the contract* (not against intent — the gap is the unstated-input ceiling), so the verification half is a cheap certificate OF generation, not a separate thing. The load-bearing move for the small-model bet: **good tests/specs convert open-ended generation (hard for small models) into constrained build-against-tests (tractable)** — TDD's mechanism; specification-by-example (Adzic) / ATDD at the spec level; red-green as a small-model-enablement loop (= the accept-gate build→gate→rebuild loop). The hard generation doesn't vanish, it **relocates up to spec-authoring** and lives on the word "good" (a thin test → thin-passing-but-wrong code from a small model). Leverage = the **testing pyramid**: a little hard spec-generation at the top (few high-level acceptance specs, capable-tier/human), a lot of tractable code-generation at the bottom (small models vs. concrete unit tests) — essay-004 Strategy A as a division of *generation* labor, specs as the tier interface. **Cycle-8 touch:** this makes the ADR-048 acceptance-criteria contract **dual-purpose** (enables tractable generation AND biting verification), promoting the elicit-then-build shape from thin-criteria fallback to a first-class small-model-enablement mechanism — still a PLAY-validated candidate shape (ADR-047 catalog), not a Cycle-8 forced decision.

**Generalization (practitioner, 2026-07-02): the invariant is verifiability, not tests.** A fast small-model loop yields a *checkable artifact*; cheap-check + fast-iterate converges. The encoding varies by abstraction level (prose→judge, unit test→execution, type/schema→compiler/validator, property/invariant→prover/fuzzer, "whatever"); one verifiable core, many encodings. **Grounds in the surviving code:** `core/validation/`'s `ValidationConfig` layers (structural/schema = shape/types; behavioral = tests; quantitative = metrics; semantic = prose via LLM judge) ARE "verifiable logic across encodings" as a shipped object — so ADR-046 §2's seat-contract reuse is the substrate this principle names, and the accept gate is it at two encodings (executor = deterministic test-verifiability, judge = prose-verifiability). **Sharpening (verification-strength gradient):** verifiability weakens as encoding gets abstract — concrete (types/tests) = deterministic/strong/builder-independent; prose = judge/soft/correlated-error (ADR-048 §4 ordering). "Verifiable at any level" holds only with a deterministic anchor under each level (ADR-048 §Deferred); hence the pyramid shape and why the fast loop has most torque where the check is cheapest (flow tightest at the concrete level; the abstract "whatever" is where human/capable-tier sits).

### From Cycle 8 BUILD — WP-A8 (in progress, 2026-07-02)

**Shipped (TS-1 core + scenario-5 envelope):** the per-turn handler as ONE declarative ensemble — `.llm-orc/ensembles/agentic-serving/serving.yaml` (classify → seat → shape → form-gate → emit) + scripts `.llm-orc/scripts/agentic_serving/{classify,shape,form_gate,emit,emit_envelope}.py`; the code seat `agentic-serving/code-seat.yaml` (reuses `code-generator` unchanged via a static `ensemble:` node + a deterministic ADR-024 envelope terminal). Caller `src/llm_orc/web/serving/serving_ensemble_caller.py` behind the endpoint's `_ChatCompletionsCaller` Protocol + light vendor-neutral introspection trace `turn_trace.py`. Endpoint re-point via a migration toggle `_serving_mode()` / env `LLM_ORC_SERVING_MODE=declarative-ensemble` (default = loop-driver; loop-driver path + its 37 tests untouched — parity-before-delete; toggle removed at WP-F8).

**Grounded against the real model (qwen3:8b, ~57s):** a real build turn writes clean valid Python to the correct file (`add.py`). Real-client grounding surfaced TWO fidelity defects harness-only would have shipped (the WP-A failure mode, caught not-after): (1) classify defaulted the filename → now extracts it from the turn; (2) marshal wrote the synthesizer's chatty prose → now the seat emits an ADR-024 envelope (code in `artifacts[0]`) and shape reads it faithfully. Both fixed + regression-locked.

**Literal `opencode run` GROUNDED 2026-07-02 (post-reboot).** The prior bootstrap wedge ([[project_opencode_run_wedge]]) cleared after a reboot. Real `opencode run --model ensemble/ensemble-agent` drove the declarative serve (`LLM_ORC_SERVING_MODE=declarative-ensemble`, port 8991) and wrote clean valid Python to disk (`is_even.py`; imports + runs, `is_even(4)=True`/`is_even(7)=False`). The emitted turn-trace confirms the full pipeline: classify (target=`code-seat`, `file` extracted from the turn, `build:true`) → seat (`code-generator` → ADR-024 envelope) → shape → form_gate (`valid:true`) → emit (file-write / `finish:false`). Direct-curl was the wedge-isolation PONG first (proved the pipeline before opencode), then the literal client run confirmed transparent serving. Evidence retained: `scratch/spike-omega-serve/wp-a8-opencode-run/` (opencode.json, opencode-run.log, is_even.py) + `.llm-orc/.serve-trace/turns.jsonl`.

**Scenario 2 (explain → prose) DONE 2026-07-02 (TDD + grounded).** The routing half already existed (classify → `target=explainer`, `build=false`; unit-locked) and the marshal prose path was pre-coded + unit-locked (shape graceful-degradation on raw prose; form-gate inert on non-build; emit prose finish). The only production gap was the seat itself + its dispatch wiring: shipped `.llm-orc/ensembles/agentic-serving/explainer.yaml` (single-model prose seat — one `explainer` agent, `agentic-tier-cheap-general` qwen3:8b, `options.think:false` for Q3 interactive latency, prose-only, no envelope terminal; `topaz_skill: summarization` for WP-C8 keying; TS-1 baseline mirroring text-summarizer's single-agent precedent, richer explain compositions deferred to WP-C8 catalog) + the top-level `explainer.yaml` symlink (non-recursive dispatch discovery, WP-A8 note b). TDD: RED discoverability test via the real `_find_ensemble_in_dirs` (`tests/unit/serving/test_serving_explainer_seat.py`) drove the symlink; a hermetic echo-explainer end-to-end contract test (`test_explain_turn_returns_prose_not_a_tool_call`) locks the explain→prose marshal path through the real skeleton. **101 serving+web tests green** (was 99; loop-driver path untouched — parity preserved), mypy/ruff clean. **Grounded both ways:** direct curl explain turn → prose finish, no tool_call, ~18.7s (think:false, ~3x faster than the ~56s build turn); literal `opencode run` explain turn → prose printed, no file written, rc=0. Evidence: `scratch/spike-omega-serve/wp-a8-opencode-run/explain-run.log`.

**Scenario 4 (model-backed classify decider) DONE 2026-07-02 (TDD + grounded; guarded-decider shape).** classify keeps its deterministic structural fast-path (explain markers incl. new "tell me"; build verbs `write|implement|create|build|generate|refactor|fix|add|code`; named file) and now emits `needs_decider: true` when NO structural signal resolves the turn (instead of defaulting to code-seat). A guarded `decide` model node (`when: ${classify.needs_decider}`, qwen3:8b think:false, `input_key: dispatch_input` so it reads the clean turn, few-shot artifact-vs-understanding prompt, closed target set) runs ONLY on the ambiguous path; a new `resolve` script merges the two — structural decision passes through, else the decider's bounded target with build/kind **derived deterministically** (code-seat→build, explainer→prose; an out-of-set decider output → empty target → deterministic dispatch fail). skeleton: classify → **decide (guarded) → resolve** → seat(`${resolve.target}`) → shape(reads resolve, back-compat) → form_gate → emit. **Determinism preserved** ([[feedback_determinism_over_carve_outs]]): the model *classifies* into a closed set; the *control* stays deterministic (fast-path + closed set + form-gate refusal bounds a misroute's blast radius). TDD: classify ambiguity tests + `resolve` merge tests (`test_serving_resolve.py`). **108 serving+web green** (+7), loop-driver untouched, ruff clean. **Grounded via real client:** structural catch ("Tell me about…" → explainer, decide guard-skipped); decider explain-ish ("difference between a list and a tuple", "when to use a set vs a dict" → explainer prose); decider code-ish ("a helper that removes duplicates from a list" → code-seat writes a file); literal `opencode run` ("a helper that pads a string to a fixed width" → decide→code-seat → wrote a valid `pad_string`). **Honest boundary (named, not overclaimed):** the decider is a small model on a stochastic routing task. Grounding first surfaced misroutes both directions (an explanation → code, then a code noun-phrase → explanation); two principled fixes closed them — clean input via `input_key`, and few-shot artifact-vs-understanding anchoring — validated on fresh non-overfit turns. Broad accuracy across the full ambiguity space is a **calibration boundary** (connects to Q2 judge-reliability, an ADR-048 Conditional-Acceptance validation target). The deterministic surround (closed set + form-gate) is the backstop that keeps a misroute from shipping a bad deliverable. Evidence: `scratch/spike-omega-serve/wp-a8-opencode-run/ambiguous-run.log` (solution.py) + `.llm-orc/.serve-trace/turns.jsonl`.

**Observability decision (practitioner, 2026-07-02):** deferred adopting a vendor (Langfuse/Phoenix). Introspection = consume the L0 engine's existing per-node execution surface into a light local turn-trace; FOSS, no infra, reads above the model layer so it can't couple to Ollama-vs-llama.cpp or any backend. If ever insufficient, the per-node shape maps to OpenTelemetry spans (Phoenix = free/local default; Langfuse = richer/heavier), additively.

**Discoveries feeding later WPs:** (a) **WP-B8 container list grew** — `orchestrator_chunk.py` AND `session_start.py` (SessionContext) are surviving containers still under `agentic/`, join `dispatch_envelope.py` for relocation. (b) **Dispatch discovery is non-recursive** — `_find_ensemble_in_dirs` only sees top-level `.llm-orc/ensembles/`; dispatchable ensembles need a top-level entry (repo convention = symlink, e.g. `code-generator.yaml -> agentic-serving/...`; added `code-seat.yaml` symlink). Any future seat (`explainer`, WP-C8 parts) needs the same. (c) Script-node quirks: inline-shell `script:` values containing `\` are read as file paths (use file scripts); inline-shell nodes wrap stdout as `{success,output}` while python-file nodes return stdout directly; child results also carry a top-level `deliverable` field.

**Preservation scenarios DONE 2026-07-02 (regression locks).** (1) *dynamic dispatch leaves guard + bounded-loop unchanged* — locked by the shared-resolver-identity test (`test_dispatch_resolver.py`: a dispatch node resolves `${dep.field}` via the same `phases.reference.resolve_reference` the guard partition uses, matching a direct call — no parallel resolver that could diverge; all primitive suites green: guard_evaluator/guard_integration/dynamic_dispatch_runner/dispatch_resolver). (2) *the ADR-024 envelope container shape is unchanged by the collapse* — locked by `test_serving_envelope.py`: a Cycle-8 seat emits the surviving container (status/primary/structured/artifacts/diagnostics) and the retired `diagnostics.calibration_verdict` / `audit_findings` subfields are absent. Compose-don't-duplicate: the primitives are already independently tested; these two add the "unchanged by collapse" locks the scenarios name.

**WP-A8 COMPLETE 2026-07-02.** All Cycle-8 Per-Turn-Handler scenarios done + grounded: TS-1 core (classify→seat→marshal) + scenario-5 envelope (prior session); literal `opencode run` (build / explain / ambiguous-via-decider, all real-client); scenario 2 (explain→prose); scenario 4 (model-backed decider); the two preservation scenarios. **126 serving+web+primitive tests green**, mypy/ruff clean, loop-driver path untouched (parity-before-delete). **Next: WP-B8..F8** — practitioner's sequencing call (roadmap dependency graph): WP-B8 envelope-relocation is the hard prereq of WP-F8 deletion; parity (A8/C8/D8/E8) before F8; Grounding Reframe (ADR-048 thinned-criteria) owed at WP-D8 entry.

### From Cycle 8 BUILD — WP-D8 (accept gate, in progress 2026-07-03)

**Grounding Reframe (owed at WP-D8 entry) DONE + recorded (commit `d1b0f0d`).** Reran ADR-048's grounding-spike fixtures with acceptance criteria thinned (hold code+tests constant, vary only requirement richness; N=5). **Default-on-for-build is grounded:** the anti-gaming catch (trivial tests) is 100% criteria-independent (10/10 rich+thin), so a presence-gated opt-in reduces to the rejected executor-only mode. **ADR-048 §2 corrected:** the judge's coverage catch is NOT simply criteria-dependent — it survives thinning via world-knowledge / naming-semantics / test-shape tells and collapses ONLY in the opaque-spec-with-no-tell corner (fixture e2: 0/5). Decision unchanged, rationale corrected; judge-consistency Conditional-Acceptance target now has supporting evidence (every cell unanimous). Full record: `essays/research-logs/cycle-8-spike-q2-grounded-acceptance.md` §"WP-D8 Grounding Reframe" + ADR-048 build-validation note. **Generation-side forward thread (practitioner):** criteria is one contract doing two jobs (constrains generation + grounds verification); the verification-strength gradient has a generation-strength dual; TDD-as-metaphor for any generative need; invariant is criteria-*present* (elicitation upholds it when the human is absent). Parked as forward/composer-ensemble direction.

**ODP-1 = internal sandboxed executor** (subprocess + timeout; heavy sandbox deferred). **ODP-2 = client owns the accept loop** (the gate emits accept+reason; the client decides another round). **Test-source decision (practitioner) = the TDD pipeline:** test-writer produces tests (the spec) → code-writer builds against them → verifier gate. Tests-first fixes naming coordination + keeps tests independent of the code.

**Slices done + committed:** (1) gate core — sandboxed `accept_executor`/`accept_executor_runner` + `accept_gate` AND (commit `0e93716`, 8 tests). (2+3) the gated build shape — `test-writer` seat + `build-gated` ensemble (test_writer → code_writer[reuses code-generator] → gather → executor → judge → accept_gate → envelope) + `accept_gather` + `build_gated_envelope` + executor dependency-mode (commit `682da6b`). **Grounded end-to-end with real models:** a build turn produced a correct non-trivial `is_prime` + accept verdict (executor pass / judge adequate / accept true), ADR-024 envelope. code-generator-reuse validated by probe (11/11 generated tests pass; naming coordinates). 38 serving tests green, loop-driver untouched.

**Engine mechanics learned (for WP-C8/F8):** sub-ensemble refs (`ensemble:`/`dispatch:`) need a **top-level symlink** (non-recursive discovery; added for test-writer + build-gated); a dependent node auto-gets base_input + deps via a template (no manual composition; code-generator reused as code_writer this way); sub-ensemble responses are nested `{ensemble,results}` envelopes needing `_terminal` peeling; script nodes get `{input_data, dependencies}` and `input_key` only narrows a dep's response.

**Slice 4 DONE + real-client-grounded (commit `9753d8f`).** Routing flipped: `resolve` maps the code intent → the default gated build shape (`build-gated`); classify + the decider stay semantic (WP-C8's registry will make the mapping operator-curated). Verdict threaded through the marshal (`shape`/`form_gate`/`emit` carry `accept`/`accept_reason`; a reject → prose "Another round needed: <reason>", no file-write, reusing the form-gate refusal path; ODP-2 client owns the loop). **Grounded end-to-end through the REAL L3 endpoint** (`LLM_ORC_SERVING_MODE=declarative-ensemble`): an OpenCode-shaped build turn (`gcd.py`, tools present) routed classify → resolve(`build-gated`) → the 7-node gate → `accept_gate {accept:true, tests_pass:true, tests_adequate:true}` → `emit` a `write` tool_call carrying correct Euclidean gcd. Trace confirms `resolve.target=build-gated`. **Real-client learning (caught by grounding, [[feedback_validate_against_real_client]]):** the `ServingEnsembleCaller` runs the build pipeline ONLY when `context.tools` is non-empty; a TOOLLESS request is treated as OpenCode's meta-call (session title/summary) and returns `_aux_reply` (first ~6 words), so a bare curl echoes the input — the pipeline must be exercised with a `write` tool in the request (as OpenCode sends). Regression: serving+web 130 green, dispatch/guard primitives 35 green, loop-driver path untouched (parity).

**WP-D8 COMPLETE** (gate built + wired default-on + grounded via the real client + Step 5.5 done). **Step 5.5 — Cycle-8 acceptance-criteria table (ADR-073), WP-D8 pass:** #3 judge reliability across runs/phrasings = **substantially closed** by the Grounding Reframe (N=5 × rich/thin × 6 fixtures, every cell unanimous; coverage-awareness confirmed + criteria-richness dependence located; broader task-type calibration is a named boundary). #5 sandboxed executor = **substantially closed** (subprocess + wall-clock timeout, unit-tested a runaway test is killed not frozen; heavy container/seccomp isolation is the named follow-up). #4 seat-contract wiring = **deferred to WP-E8** (its explicit scope). #1 full model-parity (build/explain/fix/edit/run-tests) = **partial** (build+explain real-client-grounded; fix/edit/run-tests are WP-C8 seats + a PLAY battery; emergent across WPs). #2 accept-gate independence vs a live non-cooperative builder = **open/deferred** — posture improved (separate test-writer seat, isolated judge, verifiers see no builder context) but the adversarial harness (artifact-influence channel, builder/judge weight correlation) is unrun; ADR-048 Conditional-Acceptance target → PLAY. WP-D8 closed its share (#3, #5); the table works as designed. **Remaining polish (non-blocking):** optional preservation test (per-seat contract composes with, does not bypass, the loop-level gate); roadmap Completed-Work-Log move for WP-D8 (ORIENTATION/current-state sweep stays deferred to WP-F8 per plan). Scratch grounding drives + probe retained under `scratch/wp-d8-accept-gate/`. **Next WPs:** B8 (envelope relocation, gates F8), C8 (registry + shape catalog — makes the resolve→shape mapping operator-curated), E8 (seat-contract wiring), then F8 (deletion, parity-before-delete).

### From Cycle 8 BUILD — WP-B8 (complete 2026-07-07)

**Full survivor relocation done (commits `a69f4e0` core/session, `fdb2acf` core/validation, `ee640a1` models envelope, `d43a0f2` web/serving contracts, `09839ec` test re-point).** Scope resolved at the WP gate: not just the three named containers but the **full survivor set per system-design §Cycle 8 — 10 modules**: session substrate (ADR-013 registry + write-gate, ADR-025 artifact store, ADR-012 compaction, ADR-009/010 plexus adapter) → `core/session/`; AS-2 CompositionValidator + ADR-017 guard → `core/validation/{composition_validator,tool_call_guard}.py`; ADR-024 envelope → `models/dispatch_envelope.py` (beside `LlmOrcStructuralError`, the cross-cutting-shared-type precedent its docstring claims); serving contracts (chunk types → `web/serving/chunks.py`, session-start contract → `web/serving/session_start.py`). `ChatMessage` moved DOWN to `core/session/messages.py` (it was parked in session_start only for the dissolved FC-4 surface; the registry's type-only import must not point web-upward); session_start re-exports it.

**Mechanism: move + re-export shim.** Old `agentic/` paths are one-line re-export shims, so the dissolved loop-driver chain and its 37 test files are byte-untouched (parity-before-delete literal); FC-2/FC-4 pass unchanged (their AST scans track only `llm_orc.agentic.*` edges). Surviving consumers (web src, web tests, FC-9) re-point to the new homes. **WP-F8 deletes the shims with the package** — the shim list is exactly: dispatch_envelope, orchestrator_chunk, session_start, session_registry, session_artifacts, session_artifact_store, conversation_compaction, plexus_adapter, composition_validator, tool_call_validation_guard.

**Discoveries feeding WP-F8:** (a) **`web/__init__` made import-light** (the unused `create_app` re-export dropped) — its eager app import made any `agentic` → `web.serving` contract import circular while the endpoint still imports `agentic`; keep it import-light after F8 too. (b) **Two location-pinning tests re-pointed** (surviving properties, new homes): FC-9's resolver-location assertions → `web/serving/session_start.py`; the single-AS-2-routine source check in `test_tool_dispatch_composition.py` → `core/validation/composition_validator.py`. `test_fc9_session_start_contract.py` + `test_session_start.py` + `test_session_registry.py`/`test_session_artifacts.py`/`test_conversation_compaction.py`/`test_composition_validator.py`/`test_tool_call_validation_guard.py` still live under `tests/unit/agentic/` but test SURVIVING modules — they migrate out (not delete) at F8. (c) **`sibling_interface_extractor` classified dissolving** — ADR-039's disposition ("script node reading siblings from the substrate") is a re-implementation, not a relocation; the module is loop-driver machinery. (d) **Pre-existing failure flagged (not WP-B8's):** `test_ensemble_script_cache_integration.py::test_cache_configuration_from_performance_config` fails identically at the pre-WP-B8 commit (local performance config has the script cache disabled; environment-dependent).

**Grounded through the real L3 endpoint:** `LLM_ORC_SERVING_MODE=declarative-ensemble` serve on 8991; a real OpenCode-shaped build turn (`clamp.py`, write tool present) ran the full relocated pipeline — trace: classify (structural fast-path, decider guard-skipped) → resolve(`build-gated`) → 7-node gate seat → shape → form_gate → emit `write` tool_call with correct code, ~2m23s. Regression: 2911 unit green (incl. FC-2/FC-4/FC-9), 77/78 integration (the 1 pre-existing above), mypy strict + ruff clean at every commit.

### From Cycle 8 BUILD — WP-E8 (complete 2026-07-08)

**Seat-contract wiring done (commits `5dbea09` policy, `dc4f2b5` skeleton wiring, `6d69c79` field-collision fix).** Closes the ADR-046 §2 F3 gap (seat correctness was asserted by inspection). New `src/llm_orc/core/validation/seat_contract.py` policy wraps the surviving `ValidationEvaluator` with the three ADR-046 §2 conventions: **seat-owned** (the seat declares the contract; the candidate is validated against it, never self-asserts), **black-box** (`admissible_layers()` drops the `structural`/`required_agents` layer that would couple a seat to a candidate's internals), **deterministic-first** (drops the advisory `semantic` LLM-judge layer; eligibility rests on the deterministic layers). It runs a **real** `ValidationEvaluator.evaluate` against the projected contract, adapting the seat's ADR-024 envelope under a canonical `seat` key. A `seat_contract` skeleton node (inserted between `seat` and the marshal in `serving.yaml`) loads the resolved seat's seat-owned contract and admits/rejects; the per-seat verdict rides through shape/form_gate/emit **alongside** the loop-level accept verdict (WP-D8) and refuses a rejected seat before the deliverable ships. `code-seat` + `build-gated` declare black-box behavioral contracts (envelope `status == success` + carries an artifact; no structural layer).

**Preservation grounded (scenarios.md):** per-seat admission and loop-level accept are different granularities that compose — a seat clears its own contract while the accept gate independently rejects (`build-gated` always emits a well-formed `status:success`+artifacts envelope even on an accept-reject, so the seat contract admits it and the accept verdict handles the reject; distinct `seat_admitted` vs `accept` fields).

**Design finding — real-client-caught ([[feedback_validate_against_real_client]]):** the seat-admission contract must live in a **distinct `seat_contract:` field**, NOT the engine's `validation:` field. The engine auto-runs an ensemble's `validation:` block against its own agents at execution (`ensemble_execution.py:882`), so declaring the admission contract there made the dispatched `build-gated` child evaluate `results['seat']` (the skeleton's adapter key, absent from its own agents) and **fail the seat**. The unit tests passed (they drive the node directly, bypassing dispatch + auto-validation); only the real-endpoint turn surfaced it (the WP-A failure mode, caught not-after). Fix: distinct `seat_contract:` field the engine ignores and the node reads directly (`yaml.safe_load`); locked by a regression test asserting neither seat sets `validation:`. Latent quirk noted-not-fixed: the framework's `validation:` YAML `schema:` key does not map to `ValidationConfig.schema_validations` (only the literal `schema_validations:` key does) — the seat contract uses the behavioral layer, which maps.

**Step 5.5 — Cycle-8 acceptance-criteria table (ADR-073), WP-E8 pass:** the **"seat contract (`core/validation/`) wired as the seat's pass/fail admission gate"** row (Layer-match `no`) is now **closed at its Integration layer**: `tests/unit/serving/test_serving_seat_contract.py` dispatches a candidate through the `seat_contract` node whose contract is a real `ValidationConfig` (loaded from the seat's `seat_contract:` block) and admits/rejects via the wired `ValidationEvaluator.evaluate` — not by inspection — plus the black-box fitness (contract declares no `structural` layer). Real-client grounded end-to-end through the real L3 endpoint (`LLM_ORC_SERVING_MODE=declarative-ensemble`, port 8991): a real OpenCode-shaped build turn (`palindrome.py`, write tool) routed classify → resolve(`build-gated`) → **`seat_contract {seat_admitted: true, seat_contract_reason: ""}`** → shape → form_gate → `emit` a `write` tool_call with correct `is_palindrome`. Trace confirms the 8-node pipeline with `seat_contract` between `seat` and `shape`. Evidence retained: `scratch/wp-e8-seat-contract/` (build-turn.json, build-turn2.out, turns.jsonl).

**Regression:** 2925 unit green (+14: 7 policy unit + 7 serving seat-contract), 77/78 integration (the 1 pre-existing `test_cache_configuration_from_performance_config` env failure), mypy strict + ruff clean at every commit, loop-driver path untouched (parity-before-delete). **Next: WP-C8 (Shape Catalog + registry reuse)** — makes the resolve→seat mapping operator-curated and Topaz-keys the parts; then WP-F8 (deletion, parity-before-delete; consumes §WP-B8's shim/test lists). WP-E8 gate note: `.rdd/gates/cycle-8-e8-gate.md`.

**WP-E8 gate → WP-C8 feed-forward (settled premise, 2026-07-08):** the seat contract stays a **deterministic admission floor** (well-formed, on-shape, non-empty output — a shape check, not a quality gate), black-box so seats stay swappable. **Quality verification lives in a separate rung whose encoding follows the deliverable's place on the verifiability gradient** (the cycle's "verifiability, not tests; many encodings; weakens as abstract" principle, ADR-048 §4): code → executor+tests (strong); structured data → schema/validator (fairly strong); free prose → adequacy judge (soft, honest about the unstated-input ceiling). So **WP-C8's registry associates each capability with its verification shape, not a global code+tests assumption** — the serve is full model-parity (explain/answer/plan/edit-prose), not only code, so deterministic-contract richness is bounded by how deterministically-verifiable the deliverable type is. **Concrete mechanism (the synthesis to carry into WP-C8): the Shape Catalog IS the verification-shape map** — a capability's registered shape already carries its verification rung, so "match the rung to the deliverable" reduces to "which shape a Topaz key routes to": `build-gated` bundles the accept-gate (executor+judge) for code; a prose capability registers a shape bundling a judge (no executor); a structured-data capability, a schema/validator rung. This keeps it declarative and dodges any parallel-orchestration temptation (AS-11), and makes the gate premise the *same* design surface as the WP-C8 scenarios, not extra work. Open for WP-C8: whether to smooth the `validation:` `schema:`-key quirk before operators author richer contracts. [[feedback_determinism_over_carve_outs]]

### From Cycle 8 BUILD — WP-C8 (complete 2026-07-08)

**Shape Catalog + Topaz-keyed registry done (three commits: registry+AS-2 admission; shape-catalog + retire-hardcoded-map; gen-review exemplar).** New L1 `src/llm_orc/core/serving/` package, derived from the ensemble library (AS-11 — no parallel structure):
- `admission.py` — the single AS-2 registration gate. `scan_admitted()` loads each library ensemble and partitions admitted/rejected via the shared `validate_ensemble_reference_graph` + `compute_reference_graph_depth` — the *same* routine the load path (`load_from_file`) and `CompositionValidator` call (the single-AS-2-routine preservation). A cyclic or too-deep candidate is rejected with a typed reason and never becomes dispatchable.
- `capability_registry.py` — `capability_parts()`: the Capability List Builder extended to key parts by Topaz skill. **Excludes shapes** (an ensemble declaring `serves` advertises its `topaz_skill` but is a catalog entry, not a fillable building-block part — ADR-047 §1 parts-vs-shapes), so a slot never fills with a composition.
- `shape_catalog.py` — `shape_catalog()`: intent→shape map derived from each shape's declared `serves` tag; `registered_shapes()`. Replaces the hardcoded `_SHAPE` map WP-D8 left in resolve.py.

**New EnsembleConfig metadata `serves`** (additive optional field mirroring `topaz_skill`): a shape declares the routing intent it serves. `build-gated` serves `code-seat`; `explainer` serves `explainer` (a solo shape). `resolve.py` now reads the catalog instead of the hardcoded map — routing is **unchanged (parity)** but operator-curated (drop a tagged shape YAML, no engine edit; AS-11).

**Scenario-4 slot-filling — the genuinely new binding (gen→review exemplar).** A fixed composition shape whose two slots are filled by dynamic dispatch resolving REGISTERED parts from the Topaz registry (ADR-047 §2 default binding, on shipped primitives — not the deferred runtime shape-invention). A deterministic `select_parts` script reads `capability_parts()` and picks the registered building-block part per slot capability (gen=code_generation→`code-generator`, review=logical_reasoning→`argument-mapper`); the gen/review nodes dispatch `${select_parts.gen}`/`${select_parts.review}`. Selection is declarative and external to the parts (Strategy A); an unfilled slot resolves empty (deterministic fail, closed-set discipline). This makes the Topaz registry **genuinely live** (consumed by a real dispatch), not a validated-but-unread surface. Top-level symlinks added (`gen-review`, `argument-mapper`) — dispatch discovery is non-recursive (WP-A8 note b).

**Grounded both ways** ([[feedback_validate_against_real_client]]): (1) `gen-review` invoked end-to-end with real models — `select_parts` chose code-generator/argument-mapper, both slots dispatched + ran (code-generator produced a `reverse_string` function; argument-mapper reviewed), all nodes `status:success` (`scratch/wp-c8-gen-review/evidence.json`). (2) A real serve build turn through the L3 endpoint drove `classify→decide→resolve(catalog)→seat→seat_contract→shape→form_gate→emit` a `write` tool_call with `filePath: sq.py` + real `is_perfect_square` code — the catalog-driven resolve works in the shipped serve, parity intact (`scratch/wp-c8-gen-review/serve-parity-evidence.json`).

**Regression:** 2941 unit green (+16: 6 registry + 5 shape-catalog + 4 gen-review + 1 resolve catalog-lock), loop-driver path byte-untouched (parity-before-delete), mypy strict + ruff clean at every commit.

**Design boundary settled at the WP gate (practitioner, 2026-07-08): good defaults + extensibility.** The framework ships good default seat arrangements (the verification-shape map correct out-of-box); operators extend by adding custom ensembles into seats (the registry) or authoring new flows (the catalog). **The verification-shape-map property is realized by shipping correct defaults, NOT enforced by a runtime rung-check** — a structural "build intent must resolve to a strong rung" gate was considered and rejected because it fights the extension freedom that is the point; rung-correctness of a custom extension is the operator's, parallel to operator-owned AS-2-valid parts. **Default coverage today = build + explain;** full model-parity's fix/edit/run-tests are the named frontier (future default shapes, grown the same way, PLAY-battery-validated per DISCOVER criterion #1). The `validation:` `schema:`-key quirk stays a **tracked polish** (address when operators author richer per-capability contracts, not speculatively). Gate note: `.rdd/gates/cycle-8-c8-gate.md`.

**Discovery feeding WP-F8:** `explainer` is now a *shape* (`serves: explainer`) so `capability_parts()` no longer lists it under `summarization` (only `text-summarizer` remains that part) — the serving registry's parts view and the web-layer `_build_capability_names` discovery endpoint now differ intentionally (different consumers). At F8, when the loop-driver + its `_build_capability_names` die, reconcile the Capability Discovery Endpoint to the keyed `capability_parts()` source of truth.

**Next: WP-F8 (the last WP).** Delete `src/llm_orc/agentic/` (~12.7K LOC, 32 files) + 37 agentic test files; supersede the Cycle-7 serving ADRs (033/036/037/039/041/043) with headers; run the deferred current-state doc sweep (`system-design.agents.md`, ORIENTATION, roadmap Cycle-4/6/7 WPs → Completed Work Log, field-guide). **Parity-before-delete is now MET** — WP-A8/C8/D8/E8 all reached parity + real-endpoint grounding. Consumes §WP-B8's shim-deletion list + surviving-test migration list.

### From Cycle 8 BUILD — WP-F8 (COMPLETE 2026-07-08) — the last WP

**Clean-slate removal shipped** (4 commits: `3fd9272` step 1 calibration-channel sever, `570f1ca` step 2 loop-driver-path removal, `53c86cb` step 0 allow-list relocation, `6974176` step 3+4 the deletion). The bespoke imperative `src/llm_orc/agentic/` layer (~12.7K LOC, 32 files) plus its 10 WP-B8 re-export shims are deleted; the declarative Serving Ensemble (ADR-046 §1) is the only serving path.

**Surgery-then-delete, re-sequenced from the pre-flight on code truth (1, 2, 0, 3+4).** The pre-flight ordered step 0 first, but step 2 (loop-driver removal) collapses `orchestrator_config`'s surviving surface to just the `/v1/models` allow-list, so step 0 relocated only the genuine survivor (a slim `core/config/model_profile_allowlist.ModelProfileAllowlist`), not the dissolving budget/calibration/tier config. Step 1 severed the L0 engine's dead ADR-016 calibration-channel coupling. Step 2 removed the loop-driver branch, the `_serving_mode` toggle, and ~20 agentic imports from `v1_chat_completions.py` (~950 to 290 LOC); the default is now declarative. The vestigial reserved-tool-name check (the 5 ADR-003 TOOL_NAMES) was dropped, since it protected the dissolved orchestrator tool surface.

**Pre-flight corrections (code truth beat the notes):** step-0 importers were 2 (both web-layer), not 4 (the 2 core/ "matches" were docstring/naming coincidences); `TOOL_NAMES` was vestigial, not a survivor; no Capability Discovery Endpoint route exists to reconcile; ~18 dissolving integration tests import agentic beyond the pre-flight's unit-test list; step 6 (ADR supersession headers 033/036/037/039/041/043) was already done in the DECIDE sweep; `test_plexus_adapter` was a survivor mislabeled delete (migrated, not deleted).

**Tests:** ~43 dissolving tests deleted (26 unit, 17 integration); 10 survivors migrated to their WP-B8 homes (core/session, core/validation, web/serving, models) with imports re-pointed; `test_structural_errors` trimmed to the surviving `LlmOrcStructuralError` base plus `ToolCallingNotSupportedError`; the 5 agentic-scoped FC tests (fc2/3/4/8/11) retired (the surviving no-cycle and validate-before-load properties are covered by the composition_validator and core/serving admission tests). Step-5's three external tests were handled inline (chat-completions in step 2, models in step 0, structural-errors in step 4).

**Verified:** 2307 unit green; integration was 31/32 at WP-F8 close (`test_cache_configuration_from_performance_config` failing), **since FIXED by follow-up `80ce39e`** so the suite is now 32/32 green. Its root cause was a committed test-project config disabling the script cache (`afc8f67`, a Cycle-7 spike-retention chore), NOT env-dependent as first labeled; the test broke there, not in WP-F8 (verified at the pre-WP-F8 tag). mypy strict + ruff clean; the app wires up (27 routes). **Real-endpoint grounded** ([[feedback_validate_against_real_client]]): with `agentic/` gone and declarative the only default path, a real build turn (`fib(n)`) drove the full 8-node pipeline (classify, decide, resolve, seat, seat_contract, shape, form_gate, emit) to a `write` tool_call carrying correct code, and an explain turn returned prose (`finish_reason: stop`). Safety tag `cycle-8-pre-agentic-deletion` retained.

**Doc sweep (coherent scope, practitioner 2026-07-08):** the system-design.md and system-design.agents.md pre-Cycle-8 sections were banner-marked historical and the ADR-002 layering rule reverted to no-exception (Amendment #23); ORIENTATION and field-guide regenerated to the post-collapse structure; the roadmap moved WP-F8 into the Completed Work Log. The 362KB system-design.agents.md historical content is banner-marked, not rewritten (graduate archives it). Spike retirement stays deferred to corpus-close per [[feedback_spike_artifact_retention]].

**Open follow-ups (graduate-time, non-blocking):** (1) **doc-drift:** a few surviving modules' docstrings (`models/dispatch_envelope`, `core/session/plexus_adapter`, `core/session/registry`, vestigial `web/serving/chunks` variants) still narrate the dissolved orchestrator; the code is current, the prose is not swept (field-guide regen finding; also recorded in the BUILD gate note `.rdd/gates/cycle-8-build-gate.md`). (2) **audit-location fragmentation** flagged by the build-phase susceptibility snapshot (`.rdd/audits/` vs the cycle's `docs/agentic-serving/housekeeping/audits/`; multiple `dispatch-log.jsonl` files), a corpus reconciliation for graduate. (3) the test-project `.llm-orc/config.yaml` disables the script cache (spike leftover from `afc8f67`); the `test_cache_configuration_from_performance_config` fix (`80ce39e`) isolated the test from it, so re-enabling the cache in the test project is optional.

**Cycle-8 BUILD is complete (all WPs A8/B8/C8/D8/E8/F8 done). Next is graduate-time:** a clean-`main` merge (native docs only) plus the full RDD corpus on a research branch for posterity ([[project_clean_main_research_branch_graduate]]).

### WP-F8 PRE-FLIGHT INVENTORY (2026-07-08) — EXECUTED (see the WP-F8 COMPLETE entry above; retained as the pre-flight record)

**Safety net in place:** annotated tag **`cycle-8-pre-agentic-deletion`** pushed to origin at commit `d21329b` (agentic/ intact, WP-C8 done). Everything F8 deletes is referenceable there.

**F8 is NOT a clean delete — it is surgery + deletions.** The `agentic/` blast-radius grep found surviving code still reaching into `agentic/`. Do the surgery/relocations FIRST, verify green, THEN delete. Order:

**Step 0 — resolve the `orchestrator_config` partial-survivor (a WP-B8 gap; B8 relocated its 10 named containers but missed this).** `agentic/orchestrator_config.py` (617 LOC) is on the deletion list but is imported by FOUR surviving modules: `core/validation/tool_call_guard.py`, `core/session/artifact_store.py`, `web/api/v1_models.py` (model-profile allow-list — `list_allowed_model_profile_ids` / `resolve_validated` for `/v1/models`), `web/api/v1_chat_completions.py`. Decide the split: relocate the surviving config-resolution parts (the model-profile allow-list + observability roots the survivors use) to a surviving home (candidate: `core/config/`); drop the orchestrator-actor-specific fields (budget, autonomy, ReAct config). This is a B8-style relocate-then-shim, or a full re-point. It gates the deletion.

**Step 1 — sever the engine coupling (L0 core).** `core/execution/ensemble_execution.py` imports `CalibrationSignal`/`CalibrationSignalChannel` (dissolving) as an optional `_calibration_signal_channel` param (default `None`; emits a read-only ADR-016 signal at successful dispatch, lines ~21-23, ~213, ~232, ~503-507). ADR-016's channel is superseded (ADR-046). Remove the param + import + emit block. Low-risk (optional/unwired post-collapse) but verify nothing surviving wires it (grep `_calibration_signal_channel=`).

**Step 2 — remove the loop-driver serving path from the endpoint.** `web/api/v1_chat_completions.py` carries the whole dissolving loop-driver path: ~20 `agentic` imports (loop_driver, calibration_gate, tier_router, tier_router_audit, artifact_bridge, client_tool_action_terminal, orchestrator_tool_dispatch, orchestrator_config[surviving-parts], result_summarizer_harness, single_step_enforcer, budget_controller, autonomy_policy, dispatch_event_substrate, inference_wait_heartbeat, operator_terminal_event_sink, orchestrator_context_event_sink, session_action_record). Remove the loop-driver branch + `_serving_mode()` toggle + `get_loop_driver`/`_resolve_seat_filler`/`_coder_escalation_ladder`/loop-driver `_build_capability_names` wiring; keep ONLY the declarative-ensemble path (`web/serving/serving_ensemble_caller.py`), default it on. **Reconcile the Capability Discovery Endpoint** to the keyed `core/serving/capability_registry.capability_parts()` (the loop-driver's flat `_build_capability_names` dies here; the discovery endpoint survives — flatten the keyed parts).

**Step 3 — the shims (delete with the package).** WP-B8's 10 re-export shims (all confirmed present in `agentic/`), each a one-line re-export of a relocated survivor: `dispatch_envelope`, `orchestrator_chunk`, `session_start`, `session_registry`, `session_artifacts`, `session_artifact_store`, `conversation_compaction`, `plexus_adapter`, `composition_validator`, `tool_call_validation_guard`. Their consumers already re-point to the new homes (B8), so deleting the shims is safe once Steps 0-2 land.

**Step 4 — delete `src/llm_orc/agentic/` (32 files) + migrate/delete tests.** The 37 `tests/unit/agentic/` files split by what they test:
- **MIGRATE (test surviving modules → move to the new home's test dir, do not delete):** `test_composition_validator`, `test_tool_call_validation_guard` (→ `tests/unit/core/validation/`); `test_session_registry`, `test_session_artifacts`, `test_session_artifact_store`, `test_conversation_compaction` (→ `tests/unit/core/session/`); `test_session_start`, `test_fc9_session_start_contract` (→ `tests/unit/web/serving/`); `test_dispatch_envelope` (→ `tests/unit/models/`); `test_orchestrator_config` (→ wherever Step 0 relocates the surviving parts; trim the dissolving-field assertions).
- **DELETE (test dissolving modules):** the rest — test_artifact_bridge, test_autonomy_policy, test_budget_controller, test_calibration_gate, test_calibration_signal_channel, test_client_tool_action_terminal, test_delegation_rate_meter, test_dispatch_event_substrate, test_inference_wait_heartbeat, test_loop_driver, test_operator_terminal_event_sink, test_orchestrator_context_event_sink, test_orchestrator_runtime, test_orchestrator_tool_dispatch, test_plexus_adapter, test_result_summarizer_harness, test_session_action_record, test_sibling_interface_extractor, test_single_step_enforcer, test_tier_router_audit, test_tier_router.
- **JUDGE (FC tests — rewrite or delete):** `test_fc2_layering`, `test_fc4_runtime_import_surface` AST-scan `llm_orc.agentic.*` edges — moot once the package is gone (rewrite for the new structure or retire). `test_fc3_no_cycles`, `test_fc8_summarizer_bypass`, `test_fc11_autonomy_gate` — assess per what they assert.

**Step 5 — three external surviving tests importing agentic (surgery, not delete):** `tests/unit/web/test_api_v1_chat_completions.py` (tests the loop-driver path — rewrite for the declarative path); `tests/unit/web/test_api_v1_models.py` (re-point orchestrator_config → the Step-0 home); `tests/unit/models/test_structural_errors.py` (drop the dissolving error-type assertions: CalibrationAbstainError, MalformedSignalError, tier_router errors — keep the surviving `LlmOrcStructuralError` hierarchy).

**Step 6 — ADR supersession headers:** Cycle-7 serving ADRs **033 / 036 / 037 / 039 / 041 / 043** get supersession headers → ADR-045/046 (mechanism-based, per the B8/domain-model precedent).

**Step 7 — current-state doc sweep (the deferred one):** `system-design.agents.md` (Layer-2 description + calibration/tier module removal + layering-rule revert per Amendment #23), `system-design.md` pre-Cycle-8 sections, ORIENTATION regen (BUILD milestone — now the declarative form exists), `roadmap.md` Cycle-4/6/7 WPs → Completed Work Log, `field-guide.md` regen. Spike retirement stays deferred to corpus-close per the retention discipline ([[feedback_spike_artifact_retention]]).

**Verify:** full unit suite green (expect a large net drop as dissolving tests go; loop-driver parity no longer applies — its path is gone); real-endpoint grounding (`LLM_ORC_SERVING_MODE=declarative-ensemble` is now the only mode / the default) with a real build + explain turn. Formerly-flagged failure `test_ensemble_script_cache_integration.py::test_cache_configuration_from_performance_config` is **FIXED** (follow-up `80ce39e`; it read the committed test-project config disabling the cache per `afc8f67`, not env-dependent) so the integration suite is fully green; nothing to ignore.

**Session discipline:** re-read `domain-model.md` §Invariants + `system-design.md` §Cycle 8 + this pre-flight before touching code. Do Steps 0-2 as separate `refactor:`/`feat:` commits (verify green between each) before the big `agentic/` deletion — small reversible steps, even with the tag as backstop.

### From Cycle 8 ARCHITECT (2026-07-02)

**The collapse is mostly subtraction (light + additive).** The L2 imperative Runtime cluster (Dispatch Pipeline, Loop Driver, Single-Step Enforcer, Delegation Rate Meter, Orchestrator Runtime, Orchestrator Tool Dispatch, Result Summarizer Harness, Tier-Escalation Router, Calibration Gate/Channel, Session Action Record) dissolves; the surviving L0 Ensemble Engine executes ONE declarative ensemble on shipped primitives (guard/loop + `dispatch_resolver.py`/`dynamic_dispatch_runner.py`). Artifacts: `system-design.md` §Cycle 8, `roadmap.md` §Cycle 8 (WP-A8..F8), Amendment Log #21. Snapshot `housekeeping/audits/susceptibility-snapshot-cycle-8-architect.md`; gate note `.rdd/gates/cycle-8-architect-build-gate.md`.

**Two boundary decisions made at the ARCHITECT gate (ensemble-spirit applied consistently):**
1. The **marshal** step is three bounded nodes — **shape** (fidelity marshalling; Artifact Bridge re-home) / **form-gate** (deterministic destination-validity; ADR-035 re-home; the cheapest verification-ladder rung) / **emit** (client tool_call / prose; Client-Tool-Action Terminal re-home). **emit is the reusable permission-seam node** → resolves ODP-1 (a client-delegated executor reuses emit).
2. **Grounding Reframe (snapshot-flagged):** the same warrant-probe wasn't applied to two asserted boundaries + a live inconsistency. Resolved by applying the ensemble-spirit consistently: the **Accept Gate is a build-shape composition** (executor/judge/gate seats), NOT a framework module; the **ADR-047 registry decomposes** into Capability List Builder (parts, Topaz-keyed) + Composition Validator (AS-2 admission) + a thin new **Shape Catalog**, NOT a standalone module. Makes the framework surface even smaller (AS-11 minimal addition).

**Hard sequencing constraints (roadmap):** WP-B8 (relocate the surviving ADR-024 envelope out of `agentic/dispatch_envelope.py`) is a hard prerequisite of WP-F8 (deletion); parity (WP-A8/C8/D8/E8) before WP-F8 (parity-before-delete).

**Four open decision points → BUILD:** ODP-1 executor home (internal sandbox vs client-delegated, reusing emit); ODP-2 accept-loop location (internal bounded `loop:` vs client outer loop); ODP-3 serving default shape; ODP-4 Q5 removal-sequencing detail.

### From Cycle 8 DISCOVER (update mode, 2026-07-01)

1. **Full model-parity capability via composition is SETTLED, not a scoping fork.** The serve does everything a single model does (explain / fix / edit existing / run tests / build), no capability degradation, and composition INCREASES the surface of what's possible. The narrow build-new-files serve (Cycle-7 PLAY Ω-serve) is a research demo of one sub-case. → DECIDE target architecture = a **general per-turn handler**, not decompose→build→write. (product-discovery.md: Tool User job + Cycle-8 mental-model refinement + settled-rejection inversion.)
2. **Standing requirement (practitioner-named anti-pattern):** never reduce capability scope while overcomplicating the mechanism. Applies to the system design AND to how the work is organized.
3. **Interactive speed is a first-class DECIDE constraint, not a downstream tuning knob** (Tension 23). On the permanent 32GB rig, "latency is a tuning concern" holds only up to a usability floor; past a per-session budget, slowness breaks the daily-driver job. The trade is the practitioner's call at DECIDE (thinking-mode control, sampling, inference-count-per-turn budget, model sizing).

### From Cycle 7 PLAY — the agent-as-ensemble arc (closed 2026-06-30)

1. **Engine expresses agentic control flow declaratively.** Guard/branch + a bounded `loop:` combinator shipped in L0 and validated as a real flow through the executor (commits `cb87ded`/`9d1a619`/`c5059bf`/`7ceab06`).
2. **The full generalist serving flow is ONE declarative ensemble** (Ω-P3: resolve-contract loop → plan → fan-out build [guarded code/prose branch] → score, no Python driver). Dynamic dispatch is the **(c) open-library lever**, not a **(b)** blocker (closed capability set → a guard). Remaining frictions are ergonomic, not missing primitives.
3. **Executional grounding is the wall.** A gameable gate does not vanish under the outer loop — it relocates to the loop's accept decision. Grounding must be independent of the builder (§6.2b). Corrects the earlier "never false-passed" (true only over an author-controlled test).
4. **a2 confirmed** — real `opencode run` (1.17.11) serves the ensemble transparently and wrote runnable code to disk, after 3 fixes: SSE streaming; short-circuit tool-less aux calls; all-8b config for the 32GB rig. Remaining limit is local speed, not correctness. The serve is **narrow** (build-new-files only), not a general TUI assistant.

**Entry docs:** `proposals/ensemble-agent-state-and-next-steps.md` (a→b→c ladder + open items + operational facts §7), `proposals/engine-control-flow-state-and-next-steps.md` (primitive substrate), `proposals/play-closeout-2026-06-30.md` (field notes + provenance index), `proposals/ensemble-serving-architecture.md` (settled architecture + composition strategies), `proposals/agentic-serving-library-structure.md` (extensibility), `skill-framework-capability-registry.md` (topaz capability registry). Spike artifacts retained under `scratch/spike-omega-*/`.

### Removal target (scoped 2026-07-01)

- `src/llm_orc/agentic/` — ~12,725 LOC / 32 files. Heaviest: `orchestrator_tool_dispatch.py` (1933), `loop_driver.py` (1519), `calibration_signal_channel.py` (1043), `calibration_gate.py` (893), `orchestrator_runtime.py` (742), `conversation_compaction.py` (682), `orchestrator_config.py` (617), `tier_router_audit.py` (554), `tier_router.py` (409). Plus 37 agentic test files and 48 retained spike dirs (~19MB in `scratch/`).
- ADRs specifying the bespoke serving chain (supersession candidates): ADR-033/036/037/039/041/043.

### Topaz, grounded (2026-07-01)

The 8-skill capability taxonomy (ADR-015): `code_generation`, `tool_use`, `mathematical_reasoning`, `logical_reasoning`, `factual_knowledge`, `writing_quality`, `instruction_following`, `summarization`. Skill frameworks decompose work into per-capability sub-tasks tagged with a Topaz skill; the orchestrator dispatches each to the registered capability ensemble (`code_generation` → `code-generator`, etc.). Dynamic ensemble loading = runtime-resolve the `ensemble:` target from the Topaz-keyed library, which is exactly the dynamic-dispatch primitive (Q4).

## Context for Resumption

Cycle 8 collapses the Cycle-7 bespoke serving architecture into the declarative-ensemble form (clean-slate). Governing invariant: extend the engine, never a parallel orchestration layer.

**FRESH-SESSION HANDOFF — CURRENT (2026-07-08): Cycle-8 BUILD COMPLETE.** All WPs (A8/B8/C8/D8/E8/F8) are done; the imperative `agentic/` layer is deleted and the declarative Serving Ensemble is the only serving path (build + explain grounded on the real L3 endpoint; 2307 unit + 31 integration green). Next is graduate-time (clean-`main` merge with native docs only, plus the full RDD corpus on a research branch for posterity). See the "From Cycle 8 BUILD — WP-F8 (COMPLETE 2026-07-08)" entry above for the full record. The paragraph below is the pre-WP-F8 handoff, retained as historical context.** Latest commits on `agentic-serving`: the three WP-C8 commits (Topaz-keyed registry + AS-2 admission; Shape Catalog + retire hardcoded map; gen→review exemplar). The per-turn declarative handler + default-on accept gate + wired per-seat admission gate + operator-curated registry/shape-catalog are live on the real L3 endpoint; every surviving container is out of `agentic/` (see §WP-B8 for the shim-deletion and test-migration lists F8 consumes). **Parity-before-delete is MET** (WP-A8/C8/D8/E8 all grounded through the real endpoint). **Sequencing decision (practitioner, 2026-07-08):** full parity run — WP-E8 → WP-C8 (both done) → WP-F8, gating at each WP boundary. **On entering a fresh session:** (a) re-read `domain-model.md` §Invariants + `system-design.md` §Cycle 8 + `scenarios.md` §Cycle 8 + `roadmap.md` §Cycle 8 (build-skill session discipline); `**In-progress phase:** build` is already set. (b) **Next: WP-F8 (clean-slate removal — the last WP). READ §"WP-F8 PRE-FLIGHT INVENTORY" below FIRST — F8 is surgery + deletions, NOT a clean delete.** The blast-radius grep found surviving code still importing `agentic/`: the L0 engine (calibration channel), the `/v1/models` + chat-completions endpoints (loop-driver path + the **`orchestrator_config` partial-survivor B8 missed = F8 step 0**). Do the surgery/relocations first (verify green), THEN delete `src/llm_orc/agentic/` (~12.7K LOC, 32 files) + the dissolving test files, migrate the surviving-module tests, supersede ADR-033/036/037/039/041/043, run the deferred current-state doc sweep. Safety net: tag `cycle-8-pre-agentic-deletion` (pushed). Then close: graduate-time clean-`main` merge + research-branch corpus (see the graduate-time consideration below). (c) **To ground:** from repo root `LLM_ORC_SERVING_MODE=declarative-ensemble uv run llm-orc serve --port 8991`, then drive a turn (curl with a `write` tool — a toolless request short-circuits to the aux path — or `opencode run`). Scripts/ensembles reload per request. Seat contracts live in the seat YAML's `seat_contract:` field (NOT `validation:`). **The serving registry + shape catalog live in `src/llm_orc/core/serving/`** (admission / capability_registry / shape_catalog); shapes declare `serves:` + parts declare `topaz_skill:` (a shape is excluded from parts). (c) **To ground:** from the repo root run `LLM_ORC_SERVING_MODE=declarative-ensemble uv run llm-orc serve --port 8991`, then drive a turn (curl with a `write` tool in the request — a toolless request short-circuits to the aux path — or `opencode run --model ensemble/ensemble-agent` with a `scratch/spike-omega-serve/wp-a8-opencode-run/opencode.json`-style config pointed at the port). Script/ensemble edits reload per request (fresh loader + executor) — no serve restart needed. The [[project_opencode_run_wedge]] is reboot-clearable if `opencode run` hangs at bootstrap. Formerly-flagged failure `test_ensemble_script_cache_integration.py::test_cache_configuration_from_performance_config` is FIXED (follow-up `80ce39e`); the suite is fully green (2307 unit + 32 integration). Root cause was a committed test-project config disabling the cache (`afc8f67`), not env-dependent.

**Resume state (2026-07-02 — DECIDE→ARCHITECT record, complete):** **ADR-044** (AS-11) + **ADR-045** (clean-slate collapse) Accepted, argument-audit-clean (R6). **ADR-046** (target architecture: per-turn handler as one declarative ensemble, classify→seat→marshal; orchestrator-actor dissolution) **Accepted 2026-07-02**, argument audit converged R4 (0 P1); backward-propagation sweep done at the decision-record + domain-model level (supersession/partial headers on ADR-001/002/007/009/010/014/016/024/025; domain-model Amendment Log #23 + AS-1/2/3/5/6/7/9 dispositions). The current-state doc sweep (system-design / ORIENTATION / roadmap / field-guide) is deferred to BUILD, landing with the `agentic/` code deletion. **ADR-047** (Q4 extensibility: Topaz-keyed registry of building-block parts + operator-curated shape-catalog; declarative load-time-first binding; author-time invention; composer-ensembles deferred) **Accepted 2026-07-02; argument audit converged R2** (3 P1 → 0). R1 caught the composer-path evidence overreach (ensemble-first-for-composition claimed essay-004 support it lacks); fixed by scoping the evidence to routing and re-marking ensemble-first-for-composition as an open, deferred, not-ruled-out hypothesis. Q4 closed. **ADR-048** (Q2 grounded acceptance: the accept decision as a composed verification gate — deterministic executor AND isolated coverage-aware judge; acceptance-criteria-as-contract is PRIMARY (§Decision-2); independence is a design commitment, BUILD-validation-pending, NOT spike-proven; deterministic-primary signal ordering with AUQ/HTC an anchored non-primary candidate; minimal-then-ladder) **Accepted (Conditional) 2026-07-02, argument audit converged R3** (P1 3 → 0 → 0), spike-grounded (`essays/research-logs/cycle-8-spike-q2-grounded-acceptance.md`; `.llm-orc/ensembles/q2-accept-gate.yaml`). **Conditional-Acceptance validation targets (BUILD/PLAY):** independence against a live builder (incl. the produced-artifact injection channel + builder/judge model-weight correlation); judge reliability + coverage-awareness (single-sample); the AUQ/HTC anchored candidate; sandboxed execution; the unstated-input oracle rung. Forward thread parked (research-log §Forward): the hierarchical-confidence / assume-guarantee generalization (this gate is the two-level base case; the recursive form is the composer-ensemble direction), verification-vs-generation asymmetry as its open boundary. Q2 done.

**Next steps, in order:** (1) **scenarios + interaction specs** — Q2's accept gate + Q4's registry + ADR-046's per-turn handler now give the behavioral surface. Scenarios should cover: the classify→seat→marshal turn; dynamic dispatch resolving a seat; the accept gate rejecting a gamed output and routing another round; the acceptance-criteria contract threading; and the honest limits (thin-criteria degradation, unstated-input ceiling). (2) then ARCHITECT (expected light) → BUILD. **Q1/Q5 still open** (Q1 general-vs-narrow scope; Q5 removal-execution sequencing + ADR-033/036/037/039/041/043 supersession) — revisit before ARCHITECT/roadmap; Q5 is roadmap/BUILD-facing. **Deferred to BUILD:** ADR-047 P3 (pre-register the compose-step hypothesis's criteria at its spike); ADR-048 Conditional-Acceptance validation (independence-against-live-builder incl. artifact-injection channel; judge-reliability calibration; sandboxed execution; oracle-rung trigger).

**Grounding inputs for ADR-047:** the runtime-composition feasibility spike (`essays/research-logs/cycle-8-spike-runtime-composition-feasibility.md`: fixed-shape runtime-fill is free on shipped primitives; a runtime-authored arbitrary DAG shape needs a new primitive, four gaps named) + the mining of the practitioner's local **llm-conductor** skill (`~/.claude/skills/llm-conductor/`: catalog-anchored hybrid; Strategy A beats Strategy B for small models; ADR-019 pattern-library structure is a ready registry model; trap = the Design→Calibrate→Trust→Promote populator, which is the retired AS-5 machinery). **Deferred forward direction:** composer-ensembles + the compose-at-runtime primitive (ADR-047 §Deferred), five strategy pillars. **Standing principle (Settled Premises #4):** ensemble-over-frontier — when tempted to say "a small model can't do this," first ask what bounded-role orchestration + deterministic verification can do; frontier is fallback and benchmark, not reflex.

**Commit state:** ADR-047 audit convergence + acceptance committed at **`0ddc741`**. The Q2 unit is committed this session on `agentic-serving`: the grounding spike (`.llm-orc/ensembles/q2-accept-gate.yaml`, `.llm-orc/scripts/spike-q2-grounded-accept/`, `scratch/spike-q2-grounded-accept/drive.py`), its research-log (`essays/research-logs/cycle-8-spike-q2-grounded-acceptance.md`), ADR-048 (Accepted-Conditional) + its three audit reports (R1–R3), and this cycle-status update. Tree clean. Operational facts (models, harnesses, engine quirks) live in `proposals/ensemble-agent-state-and-next-steps.md` §7 and `proposals/engine-control-flow-state-and-next-steps.md` §4. **Style:** the practitioner enforces no em-dashes in prose (global CLAUDE.md); minimize in docs too.

## Phase Status

| Phase | Status | Artifact | Key Epistemic Response |
|-------|--------|----------|------------------------|
| RESEARCH | ⏭ Skipped (the Ω-spikes are the research) | `proposals/*`, `scratch/spike-omega-*` | — |
| DISCOVER | ✅ Complete (update mode) | `product-discovery.md` | Full model-parity capability via composition SETTLED (no degradation; composition increases surface area); speed usability-floor trade recorded (Tension 23) |
| MODEL | ☐ Pending | — | — |
| DECIDE | ✅ Complete | ADR-044 (AS-11), ADR-045 (clean-slate collapse) — argument-audit-clean (R6); ADR-046 (target architecture) **ACCEPTED 2026-07-02** (argument audit converged R4, 0 P1; backward-prop sweep done at decision-record + domain-model level; current-state doc sweep deferred to BUILD) | Target architecture spike-grounded (dispatch/swap/non-build validated w/ real models); §3 classification complete incl. calibration-subsystem REPLACE; Q3 speed resolved (think param, ~10x). ADR-014 AUQ/HTC → Q2 candidate (practitioner). **Q4 CLOSED, ADR-047 Accepted** (argument audit converged R2, 3 P1 → 0; registry = Topaz-keyed parts + operator-curated shape-catalog; declarative load-time-first binding; invention author-time; compose-at-runtime primitive + composer-ensembles deferred; ensemble-first-for-composition held as an open deferred hypothesis after the R1 evidence-scope fix). **Q2 CLOSED — ADR-048 Accepted (Conditional)** (argument audit converged R3, P1 3→0→0; grounded acceptance = composed verification gate: deterministic executor AND isolated coverage-aware judge, orthogonal-failure complementarity; acceptance-criteria-as-contract PRIMARY §Decision-2; independence a BUILD-validation-pending design commitment NOT spike-proven, artifact-injection channel named; deterministic-primary ordering, AUQ/HTC anchored non-primary candidate; minimal-then-ladder w/ oracle rung for the unstated-input ceiling). **Scenarios + interaction specs DONE 2026-07-02** (additive+supersession; Cycle-8 section is the driving contract). DECIDE→ARCHITECT gate run: accept-gate default-on-in-build-turn-shape (gate-synthesis, validation-pending; Grounding Reframe — rerun ADR-048 fixtures with thinned criteria — owed at BUILD-entry). Next: ARCHITECT (light) → BUILD, pending proceed confirm |
| ARCHITECT | ✅ Complete | system-design §Cycle 8 + roadmap §Cycle 8 (WP-A8..F8) | Light additive collapse (L2 cluster → ONE declarative ensemble on shipped primitives). Gate: marshal → shape/form-gate/emit (ensemble-spirit; emit reusable → ODP-1); Grounding Reframe recast Accept Gate → build-shape composition + registry → Capability List Builder + Composition Validator + new Shape Catalog. ORIENTATION/current-state sweep deferred to BUILD |
| BUILD | ✅ Complete (**all Cycle-8 WPs A8/B8/C8/D8/E8/F8 done**; the imperative `agentic/` layer is deleted, the declarative Serving Ensemble is the only serving path, build + explain real-endpoint grounded) | WP-A8: Serving Ensemble skeleton classify → decide (guarded) → resolve → seat → shape/form-gate/emit on the real L3 endpoint, grounded via literal `opencode run`. WP-D8: composed accept gate (test-writer → code-writer → executor AND judge → gate), default-on for build turns, Grounding Reframe done, real-endpoint-grounded. WP-B8: full survivor set (10 modules) relocated out of `agentic/` (core/session, core/validation, models, web/serving) with WP-F8-deletable shims. WP-E8: per-seat admission gate wired — `core/validation/seat_contract.py` policy (black-box/deterministic-first over `ValidationEvaluator`) + a `seat_contract` skeleton node; seat-owned contracts in a distinct `seat_contract:` field; composes with the loop-level accept gate; real-endpoint-grounded. **WP-C8: operator-curated registry + Shape Catalog in `core/serving/` (admission/capability_registry/shape_catalog) — Topaz-keyed parts + intent→shape derived from a new `serves` EnsembleConfig field, AS-2 admission via the single shared routine, hardcoded `_SHAPE` retired; gen→review exemplar fills slots by dynamic dispatch from the registry (registry now live); grounded both ways (gen-review invoke + real serve build turn).** 2941 unit green, mypy/ruff clean, loop-driver byte-untouched; parity-before-delete MET | Gated mode; **ground each impl with a real client at earliest runnable point (no vacuum)**. **Good defaults + extensibility** settled at the WP-C8 gate: framework ships good default seat arrangements, operators extend seats (registry) + flows (catalog); verification-shape map realized by good defaults, not a runtime rung-check. WP-F8's shim-deletion + surviving-test-migration lists in §WP-B8; decider routing-accuracy is a named calibration boundary (→ Q2) |
| PLAY | ☐ Optional | — | — |
| SYNTHESIZE | ☐ Optional | — | — |
