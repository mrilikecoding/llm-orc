# System Design: Agentic Serving

**Version:** 5.0
**Status:** Current
**Last amended:** 2026-05-22 (Cycle 7 ARCHITECT)
**Scope:** Scoped RDD cycle at `docs/agentic-serving/`. Inherits the project-level domain model (Invariants 1-14, AS-1..AS-10 — AS-7 amended Cycle 6; AS-9 + AS-10 added Cycle 7) and existing system architecture.

---

## What this document is, who it's for, where to read what

This is the technical perspective on **agentic serving** — how llm-orc serves as the orchestrator backend for agentic coding tools (OpenCode, Roo Code, Cline, etc.) via OpenAI-compatible endpoints. It describes the four-layer system, the closed-set tool surface, the client-tool delegation boundary, and the supporting modules that govern Budget, Composition, Calibration, Autonomy, and the (optional) Plexus knowledge-graph integration.

**First-encounter readers** orient through the diagram, brief module summaries, and the Client Tool Surface Commitment below. **Agents constructing context** for code work read the companion file at [system-design.agents.md](./system-design.agents.md), which carries the full architectural drivers table, module decomposition (Owns/Depends/Inversion notes), responsibility matrix, dependency graph, integration contracts, fitness criteria, test architecture, and Appendix A per-phase susceptibility-snapshot briefs in the form best suited to that work.

The split between this artifact and `system-design.agents.md` is the **companion-file pattern** (ADR-084 Pattern B): a single human-facing surface here, with a parallel-sibling agent-context file at a predictable path. The diagram below retains its load-bearing role for human orientation; the dense reference material has been relocated rather than removed.

**Sequencing** lives in [roadmap.md](./roadmap.md). **Decisions** live in [decisions/](./decisions/). **Vocabulary** lives in [domain-model.md](./domain-model.md). **Behavior** lives in [scenarios.md](./scenarios.md) and [interaction-specs.md](./interaction-specs.md). If you are coming to this corpus with no prior context, read [ORIENTATION.md](./ORIENTATION.md) first.

---

## Architecture at a glance

```
                       OpenAI-compatible client
                       (OpenCode, Roo Code, Cline, Aider — Population A
                        primary; tool_choice-bearing requests honored
                        via bridge advisory per Cycle 7 ADR-030)
                                  │
                                  ▼  /v1/chat/completions, /v1/models,
                                     capability-list discovery (per ADR-032)
┌─────────────────────────────────────────────────────────────────────┐
│  L3 — Entry Layer                                                    │
│  Serving Layer (extended — observes tool_choice; composes Response  │
│  Labeling) · Session Registry · Bootstrapping Pipeline · Orchestra- │
│  tor Configuration · Operator-Terminal Event Sink · Response        │
│  Labeling (new C7) · Capability Discovery Endpoint (new C7)         │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│  L2 — Runtime                                                        │
│  Dispatch Pipeline (new C7) ── plan → dispatch → synthesize, AS-9   │
│    universally satisfied on chat-completions surface                │
│   ├─ Routing Planner (new C7) ── routing-planner ensemble wrapper   │
│   └─ Response Synthesizer (new C7) ── synthesizer ensemble wrapper  │
│  Orchestrator Tool Dispatch · Result Summarizer Harness ·           │
│  Conversation Compaction · Tier-Escalation Router ·                 │
│  Orchestrator-Context Event Sink                                    │
│  Orchestrator Runtime (preserved as architectural option per        │
│    Cycle 7 ARCHITECT disposition (a) — no production caller on      │
│    chat-completions surface post-BUILD; ReAct model preserved for   │
│    future surfaces per ADR-001 + ADR-011)                           │
└─────────────────────────────────────────────────────────────────────┘
                                  │     ▲
                                  ▼     │ (read-only signal channel —
┌─────────────────────────────────────────────────────────────────────┐
│  L1 — Domain Policy                  │ ADR-016 narrow exception)    │
│  Composition Validator · Budget Controller · Autonomy Policy ·       │
│  Calibration Gate (Reflect-trigger criteria extended for synthesizer│
│   Rule 5 absence + Rule 4 rounding drift + Rule 1 fabrication per   │
│   ADR-029) · Plexus Adapter · Calibration Signal Channel ·          │
│  Dispatch Event Substrate (extended — emits Cycle 7 pipeline-stage  │
│   events PlanEmitted + DispatchFired + SynthesizerCompleted +       │
│   DirectCompletionFallback) · Session Artifact Store ·              │
│  Capability List Builder (new C7)                                   │
└─────────────────────────────────────────────────────────────────────┘
                                        │
                                  ▼     │
┌─────────────────────────────────────────────────────────────────────┐
│  L0 — Core (existing)                                                │
│  Ensemble Engine ───── DAG executor (Layer 3 unchanged per ADR-002)  │
└─────────────────────────────────────────────────────────────────────┘
```

**Cycle 7 additions** (full module entries in [system-design.agents.md](./system-design.agents.md)):
- **L2 Dispatch Pipeline** — orchestrates plan → dispatch → synthesize on the chat-completions surface; replaces the chat-completions handler's `OrchestratorRuntime` call path; owns the Plan → `InternalToolCall` adapter and pipeline-stage event emission per ADR-027.
- **L2 Routing Planner** — wraps the routing-planner system ensemble (`agentic-routing-planner`); produces typed `DispatchPlan` from chat-completions request content + capability list per ADR-028.
- **L2 Response Synthesizer** — wraps the response-synthesizer system ensemble (`agentic-response-synthesizer`); produces user-facing chat-completion response from structured `(REQUEST + PLAN + DISPATCH RESULTS)` input under strict-fidelity Rules 1-6 per ADR-029; streams synthesizer tokens via existing `OpenAiSseFormatter`.
- **L1 Capability List Builder** — produces the canonical capability list (capability ensembles only, filtered from validate-once-at-load registry); single source of truth consumed by Routing Planner and Capability Discovery Endpoint.
- **L3 Response Labeling** — three-layer honest served-by signaling (headers + body metadata; content-layer Rule 5 framing delegated to Response Synthesizer) per ADR-032 §Sub-promise (1); also emits `tool_choice_handling: "deferred"` bridge advisory per ADR-030.
- **L3 Capability Discovery Endpoint** — OpenAI-protocol-compatible capability discovery surface per ADR-032; BUILD picks one of `/v1/models` extension, sibling `/v1/ensembles`, or response-metadata-only.
- **Constitutional invariants AS-9 and AS-10** added (domain-model Amendment Log entries #13 and #14): AS-9 names role-shape structural-bounding property; AS-10 names request-shape commitment (chat-completions surface; no client-side opt-in mechanism).
- **Orchestrator Runtime preserved as architectural option** per ARCHITECT disposition (a) for conformance Finding 2 — no production caller post-Cycle 7 BUILD; ADR-001 + ADR-011 + ADR-022 system-prompt amendment retained as architectural commitments for future surfaces.

**Cycle 6 additions** (full module entries in [system-design.agents.md](./system-design.agents.md)):
- **L1 Dispatch Event Substrate** — owns `DispatchTiming` event + `dispatch_id` correlation; unified emission substrate per Inversion N+2; routes to two destinations.
- **L3 Operator-Terminal Event Sink** — line-oriented `key=value` log lines per event; tool-call-emit logging + inference-wait heartbeats (liveness signals); validate-once-at-load startup-warning surface.
- **L2 Orchestrator-Context Event Sink** — JSON-shaped structured observations prepended to next ReAct turn; end-of-session summary under `execution.json::dispatch_log`.
- **L1 Session Artifact Store** — owns Artifact-as-substrate; the `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>` path layout; retention (`session` / `durable` / `ephemeral`); `output_substrate` / `output_retention` / `calibration_substrate_access` YAML field enforcement.

**Cycle 6 module extensions** — Orchestrator Configuration (ADR-022 system-prompt amendment + new observability/substrate config keys); Orchestrator Tool Dispatch (emits `DispatchTiming` events; constructs typed `DispatchEnvelope`; conditionally skips harness for substrate-routed dispatches per AS-7 amended); Result Summarizer Harness (skip honored via Tool Dispatch's interposition order); Calibration Gate (three evaluation surfaces under substrate-routing); Ensemble Engine L0 (new optional YAML fields; validate-once-at-load library cache).

**Shared type** — `DispatchEnvelope` (ADR-024) is the typed return value of `invoke_ensemble`; lives alongside `LlmOrcStructuralError` in shared types. Fields: `status`, `primary`, `structured?`, `diagnostics`, `errors?`, `artifacts?`.

**Layering rule.** Edges point from a higher layer to a same-or-lower layer; never upward — **with one narrow exception per ADR-016 (conditional acceptance; first-deployment evidence pending):** a read-only signal channel may flow from L0 (Ensemble Engine dispatch outputs) to L1 (Calibration Signal Channel module), restricted to calibration data and gated by five bounding mechanisms specified in ADR-016. The exception is signal-channel-specific (calibration only; not a general upward import permission) and read-only (no upward writes; ADR-002's layering rule for write paths is unchanged). All other layer pairs remain prohibited. FC-2 (static import check) and FC-3 (cycle detection) recognize the calibration-channel exception via an annotated allowed-edge in their layer map. Intra-L3 edges (Serving Layer ↔ Session Registry, Orchestrator Configuration; Cycle 6 adds Serving Layer → Operator-Terminal Event Sink) do not form cycles. Cycle 4's seven new dependency edges (Runtime → Conversation Compaction; Compaction → Ensemble Engine; Tool Dispatch → Tier-Escalation Router; Tier-Escalation Router → Calibration Gate; Tier-Escalation Router → Ensemble Engine; Calibration Gate → Calibration Signal Channel; Ensemble Engine → Calibration Signal Channel as the upward exception) are all verified cycle-free. **Cycle 6's 13 new edges** (the four producer→substrate edges; substrate→two-sink fan-outs; substrate-related Session Artifact Store + Tool Dispatch + Runtime edges; Serving Layer → Operator-Terminal sink; Operator-Terminal sink → Ensemble Engine for validate-once-at-load; conditional Calibration Gate → Session Artifact Store) are likewise verified cycle-free — the substrate's fan-out-to-sinks pattern is unidirectional and does not loop back to producers.

**Falsification trigger (ADR-016).** If BUILD or first-deployment evidence finds that mechanism (b) time-decay windowing or mechanism (d) periodic out-of-band audit dispatch cannot be operationalized within ADR-002's L0-L3 structure (e.g., the mechanisms require a top-level module orthogonal to the four-layer architecture), the elaboration-by-evidence framing commitment is invalidated, the reorganization branch re-opens, and ADR-016 is re-deliberated with reorganization on the table.

**Five-tool internal action surface (ADR-003).** The orchestrator's ReAct iterations call only: `invoke_ensemble`, `compose_ensemble`, `list_ensembles`, `query_knowledge`, `record_outcome`. No others. Client-declared tools flow through the response surface (turn-boundary delegation), not the action surface.

**Plexus is optional (ADR-002, AS-8).** L1's Plexus Adapter has no-op fallbacks exercised by stateless-mode tests (FC-7). The orchestrator works statelessly without Plexus; Plexus upgrades it to a learning system.

---

## Modules — brief

### L3 — Entry layer

| Module | One-line purpose |
|---|---|
| **Serving Layer** *(extended in Cycle 7 per ADR-027 + ADR-030 + ADR-032)* | Translates OpenAI-compatible wire protocol (`/v1/chat/completions`, `/v1/models`, SSE) into Session-scoped pipeline invocations. Cycle 7 — chat-completions handler invokes Dispatch Pipeline (replacing OrchestratorRuntime); `_ChatCompletionsRequest` observes `tool_choice`; composes Response Labeling decoration onto the response body. |
| **Session Registry** *(extended in Cycle 4 per ADR-013)* | Identifies and continues a multi-request Session; maintains the structured-handoff artifact set (feature-list-with-monotonic-passes, append-only progress log, init.sh deterministic environment bootstrap) for Cluster 2 sessions; runs three-class write-gate validation (JSON schema, append-only constraint, signed-script tamper-detection) on artifact writes; resolves cluster declaration at session-start (default-required for cross-cluster ambiguity per disposition (i)). |
| **Bootstrapping Pipeline** | Operator-triggered batch ingestion of the library into Plexus as source material per AS-4. Currently deferred (WP-J). |
| **Orchestrator Configuration** | Loads and resolves per-session config: Budget defaults, Autonomy defaults, Plexus enablement, orchestrator Model Profile (ADR-011), Conversation Compaction thresholds (ADR-012), per-skill tier defaults for the eight Topaz skills (ADR-015), structural validation guard pattern set (ADR-017). |
| **Response Labeling** *(new in Cycle 7 per ADR-032 + ADR-030)* | Decorates chat-completion responses with three-layer honest served-by signaling (`X-LLM-Orc-Served-By` header + `metadata.served_by` body field + content-layer Rule 5 framing delegated to Response Synthesizer); emits `tool_choice_handling: "deferred"` bridge advisory per ADR-030; emits Population B structured advisory per ADR-032. |
| **Capability Discovery Endpoint** *(new in Cycle 7 per ADR-032)* | OpenAI-protocol-compatible HTTP surface advertising the framework's loaded capability ensembles; BUILD picks one of three candidate surfaces (`/v1/models` extension, sibling `/v1/ensembles`, response metadata) per ADR-032 §Capability-list discovery. |

### L2 — Runtime

| Module | One-line purpose |
|---|---|
| **Dispatch Pipeline** *(new in Cycle 7 per ADR-027)* | Orchestrates the chat-completions plan → dispatch → synthesize flow for every chat-completions request. Replaces `OrchestratorRuntime` as the chat-completions caller; satisfies AS-9 universally on this surface; owns the Plan → `InternalToolCall` adapter; emits pipeline-stage events (`PlanEmitted`, `DispatchFired`, `SynthesizerCompleted`, `DirectCompletionFallback`); yields the same chunk vocabulary the runtime produces so `OpenAiSseFormatter` consumes pipeline output unchanged. |
| **Routing Planner** *(new in Cycle 7 per ADR-028)* | Wraps the routing-planner system ensemble (cheap-tier qwen3:8b empirical baseline; Spike ζ — 100% JSON conformance + 90% strict capability-match across 20-prompt battery); produces typed `DispatchPlan(action, ensemble, input, rationale)` from chat-completions request content + capability list; invocation flows through `OrchestratorToolDispatch.dispatch()` chokepoint so calibration gate + tier-router + autonomy interpositions fire. |
| **Response Synthesizer** *(new in Cycle 7 per ADR-029)* | Wraps the response-synthesizer system ensemble (cheap-tier qwen3:8b empirical baseline; Spike ε + ε' + μ — 0 fabrications across 13 tests + 4 confabulation modes); produces user-facing chat-completion response from structured `(REQUEST + PLAN + DISPATCH RESULTS)` input under strict-fidelity Rules 1-6; streams tokens via existing `OpenAiSseFormatter`; calibration gate fires three new Reflect-trigger criteria (Rule 5 framing absence; Rule 4 rounding drift; Rule 1 fabrication signal). |
| **Orchestrator Runtime** *(preserved as architectural option per Cycle 7 ARCHITECT disposition (a))* | Existing ReAct-loop class — preserved in codebase with no production caller on the chat-completions surface post-Cycle 7 BUILD. ADR-001 + ADR-011 + ADR-022 system-prompt amendment retained as architectural commitments for future surfaces. Cost: "architectural-option preservation" not "actively-maintained dual surfaces." |
| **Orchestrator Tool Dispatch** *(extended in Cycle 4 per ADR-015 + ADR-017)* | Defines the closed five-tool surface (FC-5) and dispatches each call to its downstream service. Interposes (in order on `invoke_ensemble`): structural validation guard (response-text scan for phantom_tool_call assertion patterns); Autonomy Policy gate; Tier-Escalation Router (verdict→tier selection); `EnsembleExecutor.execute`; Calibration Gate post-result; Result Summarizer Harness. **Cycle 7 — same chokepoint serves Dispatch Pipeline (capability ensemble dispatch via Plan → InternalToolCall adapter), Routing Planner (planner ensemble invocation), and Response Synthesizer (synthesizer ensemble invocation).** |
| **Result Summarizer Harness** | Unskippable interposition between ensemble completion and the orchestrator's context. Configured summarizer ensemble; raw-output escape hatch (ADR-004). |
| **Conversation Compaction** *(new in Cycle 4 per ADR-012)* | Runs the five-layer cheapest-first compaction pipeline at orchestrator turn boundaries. Layer 0 persist-large-tool-results (>50K char to disk); Layer 1 cache-edit; Layer 2 idle-expiry (60-min default); Layer 3 free summary via nine-section session-notes template (12K-token cap; zero LLM cost); Layer 4 LLM semantic summary with three-failure circuit-breaker (auto-resets at session start). |
| **Tier-Escalation Router** *(new in Cycle 4 per ADR-015)* | Selects per-dispatch Model Profile (cheap-tier or escalated-tier) for `invoke_ensemble` based on the dispatched ensemble's Topaz skill metadata and the Calibration Gate's verdict. Verdict→tier mapping: Proceed → cheap; Reflect → escalated; Abstain → typed `escalation_bypass` error. Honors ADR-011's session-boundary scope for the orchestrator's own LLM. |

### L1 — Domain policy

| Module | One-line purpose |
|---|---|
| **Composition Validator** | Validates a proposed composed ensemble against the existing reference graph using the same routine as load-time validation (FC-6). Stricter than load-time: enforces "compose from existing primitives only" (AS-6). |
| **Budget Controller** | Enforces turn and token limits at each ReAct iteration boundary (FC-10). Control plane, not model plane (AS-3). |
| **Autonomy Policy** | Gates orchestrator actions against the Session's Autonomy Level (FC-11). Surfaces composition events when configuration requires it. |
| **Calibration Gate** *(extended in Cycle 4 per ADR-014)* | Tracks Calibration state and produces dispatch-time calibration verdict (Proceed / Reflect / Abstain trichotomy). Verdict composition: AUQ verbalized confidence (System 1 + System 2 at default 0.85 threshold within 0.8–1.0 range), HTC trajectory features (token-level entropy, attention-weight distributions, decision-confidence trajectories), and ADR-007's existing post-hoc result-check signal. Time-decay windowing on trajectory features (60-min/100-signal dual-bound, linear decay). Three Abstain criteria (entropy collapse > 1.5σ; post-hoc hard failure; multiple drift criteria simultaneously). When ADR-016 channel is active, HTC features extracted once at L0 and propagated upward; otherwise extracted in-layer. |
| **Plexus Adapter** | Single place Plexus-aware code lives. No-op fallbacks when Plexus is absent (FC-7). WP-K (Plexus-active paths) deferred; WP-I shipped the skeleton. |
| **Calibration Signal Channel** *(new in Cycle 4 per ADR-016; conditional acceptance)* | Carries read-only calibration data upward from L0 (Ensemble Engine outputs) to L1 (Calibration Gate dispatch decisions), enforcing the five bounding mechanisms — (a) fresh-context isolation in the consumer, (b) time-decay windowing for the bias-compounding horizon, (c) categorical anchors via deterministic-tool-output (when ensemble has script-model slot), (d) periodic out-of-band audit dispatch (every 100 verdicts or 24 hours; severe drift triggers fail-safe), (e) read-only structural validation. Owns the upward L0→L1 read-only edge — the single narrow exception ADR-016 amends ADR-002 to permit. **Conditional acceptance:** synthetic-data validation (mechanism b) and structural-transfer validation (mechanism d) completed; first-deployment evidence on the cycle's North-Star benchmark closes the conditionality (or fires the falsification trigger). |
| **Capability List Builder** *(new in Cycle 7)* | Produces the framework's capability list — single source of truth consumed by Routing Planner and Capability Discovery Endpoint. Filters loaded-ensemble registry to capability ensembles (`output_substrate: artifact` + `topaz_skill` declared per ADR-019 + ADR-025); excludes system ensembles. Reads through the validate-once-at-load registry cache established Cycle 6 (FC-27). |

### L0 — Core (existing, unchanged per ADR-002)

| Module | One-line purpose |
|---|---|
| **Ensemble Engine** | Executes ensembles per the existing declarative DAG engine. Cross-ensemble references, depth limits, fan-out gather, child executor, immutable-vs-mutable state separation. |

---

## Client Tool Surface Commitment

**Decision.** The orchestrator's **internal** tool surface is exactly the five ADR-003 tools and no others. Client-declared tools (the `tools[]` array on a `/v1/chat/completions` request) become the orchestrator's **response surface**:

- The orchestrator's ReAct iterations call only the five internal tools.
- When a task step requires a client-side action (bash, file edit, etc.), the orchestrator closes the current turn with `finish_reason: tool_calls` and emits one or more client-tool `tool_calls[]` in the completion response.
- The client executes the tools and sends `role: tool` messages back in the next `/v1/chat/completions`. The orchestrator resumes the same Session's ReAct loop with those messages as observations.

**Provenance.** Committed in ARCHITECT 2026-04-20 on user direction. Honors ADR-003 strictly ("no others" refers to *internal* action surface; delegation at the turn boundary is response-surface behavior, not internal action). Supersedes the `interaction-specs.md` open-boundary note for the Tool User stakeholder.

**Scenario gate resolved (2026-04-22; Amendment #4).** The four stress scenarios from roadmap Open Decision Point #1 are written into `scenarios.md` §Client Tool Surface Commitment. All four are carried by Option C:

- Scenarios (a) and (b) — turn-boundary delegation path, Session continuity across a client-tool round trip.
- Scenario (c) — first-agent client-filesystem-file need carried by *pre-invoke* delegation: orchestrator reads at the prior turn boundary, folds into `invoke_ensemble`'s `input_data`.
- Scenario (d) — composed ensemble's un-predicted mid-execution client-tool need carried by the **retry pattern**: ensemble runs to completion; agent emits structured `needs_client_tool` signal; Result Summarization preserves it; orchestrator observes, closes next turn with client-tool delegation, re-invokes with client-tool result folded into `input_data`. The DAG engine never suspends; Layer 3 is unchanged.

Option D (mid-execution callback) would require amending ADR-001/ADR-002 and adding suspend/resume to the synchronous DAG phase loop. Out of scope for Cycle 1; reopens only at the architectural ADR level. The retry pattern is conditional on composed ensembles following a `needs_client_tool` convention; the enforcement mechanism is a build-time layering decision (roadmap Open Decision Point #8).

---

## Roadmap

See [`./roadmap.md`](./roadmap.md) for active work packages, classified dependencies, transition states, completed work log, and open decision points.

**Cycle 1 status (closed 2026-04-29):** TS-1 (stateless orchestrator serving OpenCode) reached at WP-F close; TS-2 (stateless baseline) reached at WP-H close; WP-I (Plexus Adapter skeleton) shipped with no-op fallbacks. WP-K (Plexus-active) and WP-J (Bootstrapping) deferred.

**Cycle 4 ARCHITECT close (2026-05-08):** Six new ADRs (012-017) integrated into the system design. Three new modules (Conversation Compaction L2; Tier-Escalation Router L2; Calibration Signal Channel L1 conditional). Four module extensions (Session Registry; Orchestrator Runtime; Orchestrator Tool Dispatch; Calibration Gate). Five new system-level fitness criteria (FC-14 through FC-18); ADR-076 qualitative-claim decomposition complete; design audit clean. Cycle 4 BUILD comprises eight WPs (WP-A4 through WP-H4) per the conformance-scan-recommended sequence: shared `LlmOrcStructuralError` base class first; FC-2/FC-3 automated checks; ADR-017 → ADR-013 → ADR-012 → ADR-014 → ADR-015 → ADR-016 (last; conditional on first-deployment evidence per ADR-016 §"Concrete monitoring specification").

**Cycle 7 ARCHITECT close (2026-05-22):** Seven new ADRs (026 / 027 / 028 / 029 / 030 / 031 / 032) integrated into the system design. **Six new modules** — Dispatch Pipeline (L2; ADR-027 framework-driven plan → dispatch → synthesize); Routing Planner (L2; ADR-028 routing-planner ensemble wrapper); Response Synthesizer (L2; ADR-029 response-synthesizer ensemble wrapper); Capability List Builder (L1; canonical capability list source per ADR-026 + ADR-028 + ADR-032); Response Labeling (L3; three-layer honest served-by signaling + bridge advisory per ADR-032 + ADR-030); Capability Discovery Endpoint (L3; capability-list discovery surface per ADR-032). **Two module status updates** — Orchestrator Runtime preserved as architectural option per ARCHITECT disposition (a) for conformance Finding 2 (no production caller on chat-completions surface post-BUILD; ADR-001 + ADR-011 + ADR-022 retained); Serving Layer extended (chat-completions handler invokes Dispatch Pipeline; `_ChatCompletionsRequest` observes `tool_choice`; composes Response Labeling). **Thirteen new dependency edges**; **fourteen new fitness criteria FC-28 through FC-41**; ADR-076 qualitative-claim decomposition complete (every Cycle 7 qualitative claim decomposed into refutable Fitness properties; one direction-not-constraint annotation on the latency tuning playbook; one honest-residual-uncertainty annotation on the cost-distribution-accountability sub-promise). **Two invariants added** — AS-9 (structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks; codified at MODEL boundary; ADR-027 satisfies universally on chat-completions surface) and AS-10 (capability matching on the agentic-serving chat-completions surface works from request content alone; codified at DECIDE boundary via ADR-026). **Three Track C ARCHITECT-phase deferrals resolved**: Finding 2 (`OrchestratorRuntime` disposition) → (a) preserve as architectural option; Finding 8 (streaming SSE re-wiring) → same chunk vocabulary as runtime (formatter unchanged); Finding 11 (Plan → InternalToolCall adapter home) → inside Dispatch Pipeline module. Cycle 7 BUILD shapes as **5 WPs (WP-A through WP-E)** plus 2 Track A `refactor:` commits (see [roadmap.md](./roadmap.md)).

**Cycle 6 ARCHITECT close (2026-05-15):** Four new ADRs (022 / 023 / 024 / 025) integrated into the system design. **Four new modules** — Dispatch Event Substrate (L1); Operator-Terminal Event Sink (L3); Orchestrator-Context Event Sink (L2); Session Artifact Store (L1). **Five existing-module extensions** — Orchestrator Configuration (ADR-022 system-prompt amendment + new config keys); Orchestrator Tool Dispatch (emits `DispatchTiming` events; constructs typed `DispatchEnvelope`; conditionally skips harness per AS-7 amended); Result Summarizer Harness (substrate-conditional skip enforced at Tool Dispatch's interposition); Calibration Gate (three evaluation surfaces per ADR-025); Ensemble Engine L0 (new optional YAML fields; validate-once-at-load library cache). **One new shared type** — `DispatchEnvelope` per ADR-024 (lives alongside `LlmOrcStructuralError`). **Seven new system-level fitness criteria (FC-21 through FC-27)**; ADR-076 qualitative-claim decomposition complete (every Cycle 6 qualitative claim decomposed into refutable Fitness properties or marked as direction-not-constraint with rationale); design audit clean. **One invariant amended** — AS-7 to default-with-conditional-skip per ADR-025 (substrate-routed ensembles skip content summarization; inline-response retains the mandate). Cycle 6 BUILD shapes as **5 WPs** (see [roadmap.md](./roadmap.md)).

---

## Design Amendment Log

| # | Date | What Changed | Trigger | Status |
|---|------|-------------|---------|--------|
| — | 2026-04-20 | Initial system design | ARCHITECT phase | Superseded by v2.0 |
| 1 | 2026-04-20 | Demote Context Injection Stage from module to typed function `resolve_session_start_context` owned by Serving Layer | ARCHITECT reflection-time Grounding Reframe (Item 1) | Current |
| 2 | 2026-04-20 | Mark Client Tool Surface Commitment scenario-gated; WP-F blocked until stress scenarios written | ARCHITECT reflection-time Grounding Reframe (Item 2) | Resolved by Amendment #4 |
| 3 | 2026-04-21 | Move Result Summarizer Harness dependency edge from Orchestrator Runtime to Orchestrator Tool Dispatch; FC-4 amended to exclude RSH from Runtime's allow list | WP-C close detected `Runtime → RSH` was inconsistent with RSH's own rationale; structural test already enforced corrected reading | Current |
| 4 | 2026-04-22 | Resolve Amendment #2 scenario gate. Four WP-F stress scenarios written; Option C carries all four (turn-boundary, session continuity, pre-invoke delegation, retry pattern). Option D out of scope this cycle | WP-F scenario-gate resolution (DECIDE mini-cycle) | Current |
| 5 | 2026-04-29 | Restructure per ADR-083 (F-pattern orientation lead) and ADR-084 (Pattern B companion-file split). Architectural drivers, full module decomposition, responsibility matrix, dependency graph, integration contracts, fitness criteria, test architecture, and new Appendix A per-phase susceptibility-snapshot briefs relocated to `system-design.agents.md`. Slim human-facing surface here. | Cycle 2 entry conformance to v0.8.5 (ADR-083, ADR-084); CHANGELOG note "system-design.md restructured to slim human-facing v14.0 with companion `system-design.agents.md`" applied to scoped corpus | Current |
| 6 | 2026-05-06 | Layering rule amended: read-only signal channel from L0 to L1 permitted (calibration-only), gated by five bounding mechanisms per ADR-016. All other layer pairs remain prohibited. Six new ADRs accepted (Conversation Compaction five-layer pipeline ADR-012; Session Registry initializer-then-resume schema ADR-013; Calibration Gate trajectory-level extension ADR-014; per-role tier-escalation router ADR-015; upward L0→L1 signal channel ADR-016 conditional acceptance; tool-call structural validation guard ADR-017). One ADR candidate deferred (ADR-004 amendment for Result Summarizer Harness reconsideration — below evidentiary threshold). Implementation gap inventory recorded in conformance-scan-cycle-4-decide.md; full BUILD work pending. | Cycle 4 DECIDE close (Mode B+ → DECIDE close shape); cross-references ADR-016 (load-bearing decision), conformance-scan-cycle-4-decide.md, argument-audit-cycle-4-decide.md (round 2 clean) | Current |
| 7 | 2026-05-08 | Cycle 4 ARCHITECT integration of ADRs 012-017. Three new modules: Conversation Compaction (L2; ADR-012 five-layer pipeline + circuit-breaker + nine-section session-notes template); Tier-Escalation Router (L2; ADR-015 verdict→tier mapping + Topaz skill metadata + per-skill tier defaults); Calibration Signal Channel (L1; ADR-016 read-only L0→L1 channel + five bounding mechanisms; conditional acceptance). Four module extensions: Session Registry (ADR-013 structured-handoff artifacts + write-gate validation + cluster determination); Orchestrator Runtime (ADR-012 turn-boundary compaction invocation; FC-4 amendment to add `conversation_compaction` to allowed imports); Orchestrator Tool Dispatch (ADR-017 structural validation guard + ADR-015 router interposition); Calibration Gate (ADR-014 verdict trichotomy + AUQ + HTC + time-decay windowing). Seven new dependency edges including the load-bearing read-only L0→L1 upward exception. Five new fitness criteria (FC-14 cheapest-first compaction; FC-15 every dispatch through tier router; FC-16 signal channel read-only; FC-17 typed errors derive from `LlmOrcStructuralError` base; FC-18 every ensemble declares Topaz skill). ADR-076 qualitative-claim decomposition: 24 fitness properties recorded inline; 3 direction-not-constraint annotations (ADR-013 narrative continuity; ADR-016 aggregate bias-bound; ADR-017 pattern-set adequacy). | Cycle 4 ARCHITECT phase (re-scoped 2026-05-08 from Mode B+ → DECIDE close to Mode A — extended through ARCHITECT and BUILD); cross-references conformance-scan-cycle-4-decide.md (BUILD sequencing recommendation honored), cycle-4-decide-gate.md (asymmetric-grounding finding carried forward to OQ #14), ADR-016 §"Concrete monitoring specification" (conditional-acceptance trigger artifact + sweep responsibility recorded). | Current |
| 8 | 2026-05-12 | Cycle 5 BUILD library reshape (ADR-019 / ADR-020 / ADR-021). No module changes; no fitness-criteria changes; no dependency-graph changes. The Tier-Escalation Router (L2) and Orchestrator Tool Dispatch (L2) modules continue to operate exactly as Cycle 4 specified — the reshape is at the *library content layer* (which ensembles exist, in which directory, with which `topaz_skill` tags) and at the *operator-facing deployment-shape layer* (which profile files exist; which `agentic-*` names the config references). Deliverables: per-skill Model Profile files in `.llm-orc/profiles/` (7 files, `agentic-*` prefix); `.llm-orc/ensembles/agentic-serving/` subdirectory with 6 capability ensembles (`code-generator` promoted from Cycle 4 PLAY's `agentic-coding-helper`; new `claim-extractor`, `argument-mapper`, `prose-improver`, `text-summarizer`, `web-searcher` per ADR-020) + 2 system ensembles moved into the subdirectory (`agentic-result-summarizer`, `agentic-calibration-checker`); operator-facing README; rewritten `agentic_serving:` config section referencing the new profile names. ADR-015 §Negative's "operator-driven library migration" framing is amended by ADR-019 — working defaults for the agentic-serving deployment shape are now in cycle BUILD scope, not deferred. | Cycle 5 BUILD (mini-cycle, Mode D, auto BUILD mode); cross-references ADR-019 (skill-framework-agnostic orchestrator + operation-named capability ensemble library); ADR-020 (`tool_use` script-agent web-searcher); ADR-021 (per-capability dispatch contract); ADR-015 partial-update header (2026-05-12); `proposals/agentic-serving-library-structure.md` (load-bearing handoff substrate from Cycle 4 PLAY); `skill-framework-capability-registry.md` (new corpus artifact). | Current |
| 11 | 2026-05-23 | Cycle 7 architect→build boundary: Spike ν long-horizon capability ceiling probe added to roadmap Track A.3 as a load-bearing prerequisite to WP-A entry. Triggered by the practitioner's EPISTEMIC GATE belief-mapping response surfacing the long-horizon capability ceiling question — the corpus's existing Spike battery (ζ + ε + ε' + μ; n=13 at qwen3:8b across short-horizon shapes + 4 confabulation modes) did not exercise multi-step composition (OQ #21), production-scale numerical content beyond Spike ε' B1's 25 figures, or adversarial routing beyond the 20-prompt Spike ζ battery. Spike ν tests the structural-bounding generalization AS-9 + ADR-027 rest on across these three surfaces with pre-specified qualitative criteria (per MODEL snapshot Advisory A — pre-specify before running, not after). Three trigger conditions: all-Pass → WP-A proceeds with strengthened empirical floor; any-Fail → Design Amendment re-opens architecture (ADR-027 vs. Tier 1 hybrid vs. multi-mechanism); any-Intermediate → caveat-with-deployment-policy updates ADR-029 or ADR-031. ADR-027 commitment remains as cycle's primary direction pending spike outcome; the spike is honest empirical testing, not architecture revision. **Asymmetry note recorded for the gate's belief-mapping context: Tier 1 hybrid would not rescue the long-horizon case because non-`tool_choice` NL routing under Tier 1 continues through the orchestrator-LLM-as-decider — the surface Cycle 6 PLAY note 22 + Spike λ-paid F-paid-4 established as the failure locus. Both candidate Cycle 7 mechanisms face the same long-horizon question; Spike ν tests the structural-bounding answer at cheap-tier directly rather than comparing mechanisms.** | Cycle 7 ARCHITECT EPISTEMIC GATE (2026-05-23) — practitioner belief-mapping response identified the long-horizon capability ceiling as the load-bearing gap; cycle-status.md Cycle 7 ARCHITECT entry; roadmap.md Track A.3 specification. | Current |
| 10 | 2026-05-22 | Cycle 7 ARCHITECT integration of ADRs 026-032. **Six new modules**: Dispatch Pipeline (L2; ADR-027 framework-driven dispatch pipeline orchestrates plan → dispatch → synthesize); Routing Planner (L2; ADR-028 routing-planner ensemble wrapper); Response Synthesizer (L2; ADR-029 response-synthesizer ensemble wrapper); Capability List Builder (L1; canonical capability list source); Response Labeling (L3; three-layer honest served-by signaling + bridge advisory per ADR-030/032); Capability Discovery Endpoint (L3; ADR-032 capability-list discovery surface). **Two module status updates**: Orchestrator Runtime preserved as architectural option per ARCHITECT disposition (a) for conformance Finding 2 (no production caller on chat-completions surface post-BUILD; ReAct model preserved per ADR-001 + ADR-011 + ADR-022); Serving Layer extended (chat-completions handler invokes Dispatch Pipeline; Pydantic model observes `tool_choice`; composes Response Labeling). **Thirteen new dependency edges**; **fourteen new system-level fitness criteria FC-28 through FC-41**; ADR-076 qualitative-claim decomposition complete (every Cycle 7 qualitative claim decomposed; one direction-not-constraint annotation on latency tuning playbook; one honest-residual-uncertainty annotation on cost-distribution-accountability sub-promise). **Two invariants added**: AS-9 (Cycle 7 MODEL — structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks where orchestrator-LLM-as-decider failed); AS-10 (Cycle 7 DECIDE — capability matching on agentic-serving chat-completions surface works from request content alone). **Three Track C ARCHITECT-phase deferrals resolved**: Finding 2 → disposition (a) preserve OrchestratorRuntime; Finding 8 → same chunk vocabulary as runtime (formatter unchanged); Finding 11 → adapter inside Dispatch Pipeline. Backward propagation completed at DECIDE close (domain-model Amendment Log entries #13 + #14). | Cycle 7 ARCHITECT (mini-cycle, Mode D); cross-references ADR-026, ADR-027, ADR-028, ADR-029, ADR-030, ADR-031, ADR-032; scenarios.md §Cycle 7 features (7 features + ~45 scenarios); interaction-specs.md §Cycle 7 updates; domain-model.md Amendment Log entries #13 + #14 (AS-9 + AS-10 + Bridge mechanism); cycle-status.md Cycle 7 entry; argument-audit rounds 1-5 clean P1/P2/P3 at Cycle 7 DECIDE; conformance-scan-cycle-7-decide Findings 2/8/11 dispositions; gate reflection notes `.rdd/gates/cycle-7-{research-discover,discover-model,model-decide,decide-architect}-gate.md`. | Current |
| 9 | 2026-05-15 | Cycle 6 ARCHITECT integration of ADRs 022-025. **Four new modules**: Dispatch Event Substrate (L1; ADR-023 unified emission substrate per Inversion N+2; owns `DispatchTiming` event + `dispatch_id` correlation + registered-sink dispatcher); Operator-Terminal Event Sink (L3; ADR-023 destination 1 — line-oriented log lines per event; tool-call-emit + inference-wait heartbeat liveness signals; validate-once-at-load startup-warning surface); Orchestrator-Context Event Sink (L2; ADR-023 destination 2 — JSON-shaped structured observations between turns; `dispatch_log` key in end-of-session `execution.json`); Session Artifact Store (L1; ADR-025 always-scope artifact-as-substrate for capability ensembles; owns `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>` layout + retention semantics + new YAML fields `output_substrate`/`output_retention`/`calibration_substrate_access`). **Five existing-module extensions**: Orchestrator Configuration (ADR-022 system-prompt amendment elevating `invoke_ensemble` under capability-matched NL framing; new observability + substrate config keys); Orchestrator Tool Dispatch (emits `DispatchTiming` events bracketing each `invoke_ensemble`; constructs typed `DispatchEnvelope` per ADR-024; conditionally skips Result Summarizer Harness for substrate-routed dispatches per AS-7 amended); Result Summarizer Harness (substrate-conditional skip enforced at Tool Dispatch's interposition; ADR-004's per-invocation `raw_output` escape hatch preserved within inline-response scope); Calibration Gate (three evaluation surfaces per ADR-025 — summary-only default; `structured`-augmented when `output_schema:` declared; artifact-content tool-call when `calibration_substrate_access: artifact` declared); Ensemble Engine L0 (new optional YAML fields; validate-once-at-load library cache replacing per-`list_ensembles()` re-validation). **One new shared type**: `DispatchEnvelope` per ADR-024. **Thirteen new dependency edges**; **seven new system-level fitness criteria FC-21..FC-27**; ADR-076 qualitative-claim decomposition complete (every Cycle 6 qualitative claim decomposed into refutable Fitness properties or marked as direction-not-constraint with rationale). **One invariant amended**: AS-7 to default-with-conditional-skip per ADR-025 — substrate-routed capability ensembles skip `agentic-result-summarizer` invocation; inline-response ensembles (system ensembles; future opt-outs via `output_substrate: inline`) retain mandatory summarization. Backward propagation per Step 3.7 completed at DECIDE close (domain-model Amendment Log entry #11). Three DECIDE susceptibility-snapshot advisory carry-forwards encoded structurally: `dispatch_id` cross-surface coupling (FC-22); always-scope dial-back indicators (Session Artifact Store §Direction-not-constraint); `output_schema:` as drift-detection-not-composition-substrate (Cycle 6 extension fitness on Ensemble Engine schema fields + ADR-024 BUILD-assumption note). | Cycle 6 ARCHITECT (mini-cycle, Mode D); cross-references ADR-022, ADR-023, ADR-024, ADR-025; scenarios.md §Cycle 6 features (4 features, 26 scenarios + preservations); interaction-specs.md §Cycle 6 updates; domain-model.md Amendment Log entry #11 (AS-7 amendment); cycle-status.md Cycle 6 entry; argument-audit-cycle-6-decide(+round2); conformance-scan-cycle-6-decide; susceptibility-snapshot-cycle-6-decide; gate reflection cycle-6-decide-gate.md. | Current |

---

## Provenance

The full architectural-drivers table, module decomposition, responsibility matrix, dependency graph, integration contracts, fitness criteria, test architecture, and Appendix A per-phase susceptibility-snapshot briefs are in [system-design.agents.md](./system-design.agents.md). That companion file is the verification surface for FC-1 through FC-13 and the canonical brief source for every phase boundary's susceptibility-snapshot dispatch.
