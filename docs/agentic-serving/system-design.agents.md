# System Design Companion: Agentic Serving — Agent Context

**Version:** 4.1
**Status:** Current
**Last amended:** 2026-06-18 (Cycle 7 loop-back #9 BUILD Design Amendment — ADR-043 collapse to one serving surface; the Dispatch Pipeline module is retired). Prior: 2026-05-15 (Cycle 6 ARCHITECT)
**Companion to:** [system-design.md](./system-design.md) (slim human-facing surface)
**Scope:** Scoped RDD cycle at `docs/agentic-serving/`. Inherits the project-level domain model (Invariants 1-14) and existing system architecture.

**Cycle 6 amendments at a glance:** ADRs 022 / 023 / 024 / 025 integrate via the **system-prompt amendment** (ADR-022, owned by Orchestrator Configuration), the **unified dispatch-event substrate** with two routing destinations (ADR-023 — adds three new modules: *Dispatch Event Substrate* L1, *Operator-Terminal Event Sink* L3, *Orchestrator-Context Event Sink* L2), the **typed `DispatchEnvelope`** as the `invoke_ensemble` response shape (ADR-024 — owned by Orchestrator Tool Dispatch as a shared type), and **artifact-as-substrate at always-scope for capability ensembles** (ADR-025 — adds one new module: *Session Artifact Store* L1). AS-7 amended to default-with-conditional-skip; Result Summarizer Harness extended to consult `output_substrate`. Total: **4 new modules, 5 extended modules, 1 shared type, 9 new dependency edges, 6 new system-level fitness criteria (FC-21..FC-26)**, plus per-module fitness properties per ADR-076.

---

## How to read this file

This is the **agent-context companion** to `system-design.md`. It carries the dense reference material that benefits agent context-construction for code work — full driver tables, module decomposition with Owns/Depends/Inversion notes, the responsibility matrix, the per-edge integration contracts, fitness criteria, test architecture, and the per-phase susceptibility-snapshot briefs (Appendix A).

The split is the **companion-file pattern** (ADR-084 Pattern B): the slim sibling at [system-design.md](./system-design.md) is for first-encounter human readers; this file is read end-to-end (or by section) when an agent needs full architectural detail. Both files share the same Version field; they are amended together.

The contracts and matrices in this file are the load-bearing artifacts FC-1 through FC-13 verify against. Appendix A is the canonical brief source for the **Phase Boundary: Susceptibility Snapshot Dispatch** subsection of every phase skill executing within this cycle's scope.

---

## Architectural Drivers

| Driver | Type | Provenance |
|--------|------|------------|
| Stateless-first operability — baseline product (Layers 1-3) runs with no Plexus dependency | Quality Attribute (primary) | AS-8; ADR-002; cycle-status §FF 15-16 |
| Deterministic control plane — Budget, Tool Surface, and Primitive set are harness-level circuit breakers, not model-level choices | Quality Attribute | AS-3, AS-6; ADR-003, ADR-005, ADR-008 |
| Orchestrator reasoning quality across long sessions | Quality Attribute | AS-7; ADR-004, ADR-005; essay 001 §Context Management |
| Swappable orchestrator LLM via Model Profile | Quality Attribute | ADR-011; OQ #1 (knowledge-compensated model selection) |
| Observability of orchestrator activity | Quality Attribute | Product discovery tensions #2 and #5; essay 002 (capability floor + observability) |
| Auditability — closed tool-call vocabulary | Quality Attribute | ADR-003 |
| OpenAI-compatible protocol: `/v1/chat/completions` + `/v1/models`, SSE streaming with tool-call round-trips | Constraint | Essay 001 §API Surface; interaction specs |
| Phase 2 Plexus injection hook point must be structurally reserved | Constraint | ADR-009 (post-gate reframe) |
| Ensemble Engine (Layer 3) unchanged | Constraint | ADR-001, ADR-002 |
| Project-level Invariants 1-14 remain in force; AS-1 through AS-8 layered on top | Constraint | Project domain model; agentic-serving domain model |
| Push-model Plexus ingestion; source-material ingestion boundary; async enrichment | Constraint | AS-4; ADR-010 |
| Orchestrator profile change is a session-boundary event | Constraint | ADR-011 |
| Existing FastAPI server and MCP handlers are extended, not replaced | Integration | Retrofit reconnaissance (2026-04-20) |
| Plexus (external lib) is optional; two code paths — with and without Plexus — must maintain feature parity on Layers 1-3 | Integration | ADR-002; AS-8 |
| Client-declared tools (OpenCode, Roo Code, etc.) flow through turn-boundary delegation, not through the orchestrator's internal action space | Integration | system-design.md §Client Tool Surface Commitment |
| Session sized for sustained agentic coding comparable to an RDD phase; multi-LLM-call-per-turn token accounting | Scale | ADR-005 |
| Orchestrator coherence across long-horizon sessions (50K-token degradation point per Chroma 2025; Khanal 2026 universal-non-improvement under episodic memory); requires automatic compaction at turn boundaries | Quality Attribute | ADR-012; essay 005 §"Long-Horizon Reliability"; Chroma 2025; Liu et al. TACL 2023; Khanal arXiv:2603.29231 |
| Externalized structured state for Cluster 2 sessions (BUILD/ARCHITECT/DEBUG/REFACTOR) — schema-level non-regression, narrative continuity, deterministic environment bootstrap | Quality Attribute | ADR-013; Confucius Code Agent arXiv:2512.10398; essay 005 §"Long-Horizon Reliability"; Anthropic published initializer schema |
| In-process trajectory-level calibration (AUQ verbalized confidence + HTC trajectory features) producing dispatch-time verdict trichotomy (Proceed / Reflect / Abstain) | Quality Attribute | ADR-014; AUQ arXiv:2601.15703; HTC arXiv:2601.15778; OI-MAS arXiv:2601.04861 |
| Cost-discriminate dispatch via confidence-gated tier escalation (OI-MAS pattern at +12.88% accuracy, −17–78% cost; SC-MAS, MasRouter heterogeneous-staffing evidence base) | Quality Attribute | ADR-015; OI-MAS arXiv:2601.04861; SC-MAS; MasRouter; Topaz paper |
| Cross-layer calibration as the cycle's most novel territory — bias-bounded by 5 mechanisms (a)–(e) per cycle/scale risk from feedback-bias compounding (Khanal universal non-improvement; CAAF prompt-engineering-artifact) | Quality Attribute (load-bearing; conditional acceptance) | ADR-016; essay 005 §"ADR candidate #6"; Khanal arXiv:2603.29231; CAAF arXiv:2604.17025; Wisdom and Delusion of LLM Ensembles arXiv:2510.21513 |
| Class (a) deterministic-override against phantom tool-call confabulation observed at cheap-cloud tier (Wave 3.A Trial 3 spike evidence; codebase precedent commit `9f86d0b`) | Quality Attribute | ADR-017; essay 005 §"Behavioral Spike"; CAAF arXiv:2604.17025 |
| Read-only upward signal channel from L0 to L1 (calibration-only; structurally typed at boundary; gated by 5 bounding mechanisms) — one narrow exception to ADR-002's "edges never upward" layering rule | Constraint (newly established Cycle 4) | ADR-016; ADR-002 partial-update header |
| Shared `LlmOrcStructuralError` typed-error base class binding eight `error_kind` surfaces (`tool_call_rejected_per_model` from commit `9f86d0b`; `phantom_tool_call`, `compaction_layer_4_failure`, `write_gate_rejection`, `calibration_abstain`, `escalation_bypass`, `missing_skill_metadata`, `malformed_signal`) with common fields (`error_kind`, `dispatch_context`, `recovery_action_required`, `operator_diagnostic`) | Integration | ADR-017 §"Shared typed-error base class"; codebase precedent commit `9f86d0b` |
| Topaz eight-skill role profiling (`code_generation`, `tool_use`, `mathematical_reasoning`, `logical_reasoning`, `factual_knowledge`, `writing_quality`, `instruction_following`, `summarization`) declared per-ensemble in YAML metadata | Constraint | ADR-015; Topaz paper |
| Per-skill tier defaults (8 skills × 2 tiers = 16 Model Profile slots) configured at deployment; defaults shareable across skills | Constraint | ADR-015 |
| Capability-matched natural-language framing prefers `invoke_ensemble` dispatch over direct LLM completion AND over client-declared tools (system-prompt-level commitment; effectiveness configuration-conditional per orchestrator-profile reasoning shape) | Quality Attribute (newly established Cycle 6) | ADR-022; spike γ research log `essays/research-logs/cycle-6-spike-gamma-routing-characterization.md`; product-discovery T14 |
| Unified dispatch-event emission substrate with two routing destinations (operator-terminal, orchestrator-context); Inversion N+2 forbids parallel-emission infrastructure for the same data | Quality Attribute (newly established Cycle 6) | ADR-023; Cycle 6 DISCOVER snapshot Action 3; PLAY note 12 |
| Bounded event-model extension — one new `DispatchTiming` event type + one new `dispatch_id` field on four existing event types (`TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`); existing event semantics preserved | Constraint (newly established Cycle 6) | ADR-023 §"Event-emission substrate" |
| Liveness signal during in-flight states — tool-call-emit logging fires before dispatch; inference-wait heartbeat fires after `N` seconds (default 30s) of tool inactivity; both at operator-terminal destination only | Quality Attribute (newly established Cycle 6) | ADR-023 §"Liveness-signal patterns"; post-hotfix verification 2026-05-14 finding 7 |
| Validate-once-at-load library cache — ensemble YAML validation runs at startup (or library reload); per-enumeration noise eliminated; legacy schema-drifted YAMLs produce one `WARN` line at startup, not per-`list_ensembles()` call | Quality Attribute (newly established Cycle 6) | ADR-023 §"Noise-floor remediation"; post-hotfix verification 2026-05-14 finding 7 |
| Typed `DispatchEnvelope` as the `invoke_ensemble` response shape — `{status, primary, structured?, diagnostics, errors?, artifacts?}`; field name `diagnostics` (not `metadata`); `output_schema:` per-ensemble declaration is advisory at dispatch time | Constraint (newly established Cycle 6) | ADR-024; spike α / β; domain-model §Methodology Vocabulary on Common I/O envelope |
| Artifact-as-substrate at always-scope for capability ensembles — capability-ensemble deliverables write to `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>`; envelope's `primary` carries the summary line; `artifacts[]` carries the typed reference; system ensembles remain inline | Quality Attribute (newly established Cycle 6) | ADR-025; practitioner-verbatim 2026-05-14 finding 8; spike α / β / γ |
| AS-7 amended to default-with-conditional-skip — substrate-routed capability ensembles skip `agentic-result-summarizer` invocation; inline-response ensembles (system ensembles; `output_substrate: inline` opt-outs) retain mandatory summarization | Constraint (invariant amendment Cycle 6) | ADR-025 §"AS-7 amendment"; domain-model Amendment Log entry #11; OQ #15 resolved by codification |
| `dispatch_id` correlation identifier — session-scoped monotonic counter or UUID4 shared across all events emitted during one dispatch AND across the dispatch's artifact path AND across the envelope's `diagnostics.dispatch_id`; coupling is acknowledged (Finding 2 of decide snapshot) — `dispatch_id` errors affect observability events and substrate paths simultaneously | Constraint (newly established Cycle 6 — owned by Dispatch Event Substrate) | ADR-023 §"`dispatch_id` correlation identifier"; ADR-025 §"Session-dir location"; Cycle 6 DECIDE snapshot Finding 2 (advisory carry-forward for ARCHITECT) |
| Structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks where the orchestrator-LLM-as-decider failed (AS-9) — the chat-completions surface routes through bounded roles, not through bundled-reasoning surfaces | Constraint (invariant added Cycle 7) | AS-9; ADR-027; Spike ε + ε' + μ (n=13 tests + 4 confabulation modes at qwen3:8b); domain-model Amendment Log entry #13 |
| Capability matching on the agentic-serving chat-completions surface works from request content alone (AS-10) — no client-side opt-in mechanism (no llm-orc-specific HTTP header, no skill-framework manifest field, no metadata convention) | Constraint (invariant added Cycle 7) | AS-10; ADR-026; domain-model Amendment Log entry #14 |
| Framework-driven dispatch pipeline (plan → dispatch → synthesize) as primary direction for the chat-completions surface; orchestrator-LLM removed from routing-decision and post-dispatch-synthesis surfaces | Quality Attribute (newly established Cycle 7) | ADR-027; AS-9; Essay-Outline 006 §C7 |
| Configuration honesty sub-promise — response declares its served-by path at three layers (headers + body metadata + content-layer Rule 5 framing on direct-completion); Population A degradation signal (configuration dishonesty per Cline #10551, OpenCode #20859) is structurally prevented | Quality Attribute (newly established Cycle 7) | ADR-032 §Sub-promise (1); OQ #18 Q3 finding; Cline #10551; OpenCode #20859 |
| Cost-distribution accountability sub-promise — capability-matched requests result in ensemble dispatch (strict-dispatch-when-capability-matched); operator-observable degradation signaling via `direct_completion_fallback` event + `direct_completion_rate` rolling metric; deployment-relative refutation threshold (sustained `direct_completion_rate` >~15pp above operator-estimated baseline over 24-hour rolling window) | Quality Attribute (newly established Cycle 7; honest-residual-uncertainty: project-developer-lens grounded; Population A voice silent) | ADR-032 §Sub-promise (2) + §Refutation threshold; Essay-Outline 006 §C6 W6.2; practitioner DISCOVER 2026-05-21 verbatim |
| `tool_choice` disposition — Cycle 7 bridge mechanism (HTTP 200 with three-layer `tool_choice_handling: "deferred"` advisory: headers + body metadata always; content-layer Rule 5-adjacent acknowledgment on `action: "direct"` responses only); disposition (i) full honoring deferred to follow-on cycle after ADR-027 reaches production deployment | Constraint (newly established Cycle 7; provisional mechanism per ADR-030) | ADR-030; Spike κ D0 finding; Tension 19 |
| Latency floor ~36 s single-step / ~64 s chained at qwen3:8b (Spike ε); Population A coverage graded per F3 framing-audit: Tier A default-accommodation (OpenCode, Aider); Tier B operator-tuning-required (Cline; smoke-test recommended per issue #4308); Tier C structurally outside scope (Cursor agentic path) | Performance Constraint (newly established Cycle 7) | ADR-031; Spike ε; OQ #20 |
| Streaming as load-bearing default — synthesizer response streams token-by-token via existing `OpenAiSseFormatter`; first-token latency drops to synthesizer-first-token time (~2-3 s) for streaming-default clients (OpenCode, Aider, Cline) | Quality Attribute (newly established Cycle 7) | ADR-031 §"Streaming as a load-bearing surface" |
| Capability-list discovery — OpenAI-protocol-compatible mechanism for clients to discover capability ensembles; available, not required; three candidate surfaces (`/v1/models` extension, sibling `/v1/ensembles`, response metadata); BUILD picks at least one | Constraint (newly established Cycle 7) | ADR-032 §Capability-list discovery; AS-10 |

---

## Module Decomposition

Twelve modules plus one typed extension-point function. Existing modules are marked **(existing)**; everything else is net-new surface area for agentic serving.

### Module: Serving Layer

**Purpose:** Translates the OpenAI-compatible wire protocol into Session-scoped orchestrator interactions.
**Provenance:** AS-8; ADR-001, ADR-002 (Layer 1); ADR-009 (Phase 2 injection reservation — function-level); Essay 001 §API Surface; Client Tool Surface Commitment.
**Owns:** Serving Layer (concept); SSE streaming; tool-call formatting; `/v1/chat/completions` and `/v1/models` endpoints; response-surface tool delegation; session-start flow including the typed `resolve_session_start_context` function (Phase 1 returns `[]`; Phase 2 reads from Plexus Adapter).
**Depends on:** Session Registry, Orchestrator Configuration, Orchestrator Runtime. In Phase 2 only: Plexus Adapter.
**Depended on by:** (external clients — FastAPI app)
**Phase 2 hook point.** ADR-009 requires the Phase 2 injection point to be structurally reserved. This is satisfied by the typed function `resolve_session_start_context(session: SessionContext) -> list[PromptFragment]` at a single call site in the session-start flow. Phase 1 returns `[]` unconditionally; Phase 2 populates the body by reading from Plexus Adapter. The contract is the load-bearing part of the reservation — signature and call site commit the interface now so Phase 2 is a function-body change, not a structural change.
**Inversion note:** The Serving Layer boundary serves the Tool User's "the endpoint is a model" mental model (they see only the HTTP surface) and the Operator's "I start the server and point a client at it" mental model. Both converge at this boundary, so the boundary serves both users.

### Module: Session Registry *(extended in Cycle 4 per ADR-013)*

**Purpose:** Identifies and continues a multi-request Session by reconstructing orchestrator state from the conversation history; maintains the structured-handoff artifact set for Cluster 2 sessions; enforces write-gate validation on artifact writes.
**Provenance:** Session (concept); ADR-005 (Budget is Session-scoped); ADR-008 (Autonomy is Session-scoped); ADR-011 (Model Profile is Session-scoped); Client Tool Surface Commitment (Session spans client-tool round trips); ADR-013 (structured-handoff artifact set + write-gate validation + cluster determination); Confucius Code Agent arXiv:2512.10398 (cross-session note-taking pattern empirical evidence); Anthropic published initializer schema (feature-list / progress-log / init.sh structure); OpenDev arXiv:Bui 2026 (class (a)/(c) hybrid schema-level enforcement); Mnemonic sovereignty arXiv:2604.16548 (memory-poisoning attack surface motivating write-gate validation).
**Owns:** Session identity; Session lookup by request; cumulative-turn-count and cumulative-token-spend derivation; persistence of Session state across HTTP requests when persistence is required by Autonomy Level or Calibration state; **Cycle 4 additions:** Structured-handoff artifact (concept) — operator-facing name for the three-component artifact set; the three artifact components — feature-list-with-monotonic-passes (JSON schema with monotonic `passes` field; structural non-regression at schema level), append-only progress log (free-text narrative continuity; filesystem-level append-only constraint), init-sh-style deterministic environment bootstrap (operator-authored shell script; hash-recorded at authoring time); cluster determination at session-start (concept) — Cluster 1 / Cluster 2 / Cluster 3 classification with disposition (i) default-required for cross-cluster ambiguity; write-gate validation (concept) with the three validation classes — (i) JSON schema validation for feature-list (rejects monotonicity violations without audit-logged operator override), (ii) append-only constraint enforcement for progress-log (rejects overwrite, truncate, mid-file edit), (iii) signed-script tamper-detection for init.sh (hash-rotation workflow for legitimate modifications); the typed `write_gate_rejection` error.
**Depends on:** Ensemble Engine (for profile resolution — optional); filesystem (artifact persistence); cryptographic hash routine (init.sh integrity check).
**Depended on by:** Serving Layer, Budget Controller, Autonomy Policy, Calibration Gate.
**Inversion note:** The Operator's mental model is "a Session is the thing with Budget, Autonomy, and orchestrator profile; requests are how clients interact with it." For Cluster 2 sessions, the model extends: "I work with `feature_list.json`, `claude-progress.txt`, `init.sh` by name; I declare cluster at session-start; the system prevents structural regressions and tampering." The methodology-voice term *externalized structured state* (per domain-model §Methodology Vocabulary) does not appear in operator-facing surfaces — operators work with the artifact set's named components.

**Cluster-conditional applicability (ADR-013):** the artifact set is **required** for Cluster 2 phase contexts (long-horizon continuous routing — BUILD/ARCHITECT/DEBUG/REFACTOR territory) and **supported but optional** for Cluster 1 (specialist-dispatch — RESEARCH/DECIDE/SYNTHESIZE) and Cluster 3 (conversational/exploratory — DISCOVER/PLAY) contexts. Cross-cluster sessions default to required per disposition (i). The cluster declaration is operator-set at session-start; mid-session reclassification (disposition (ii)) is a refinement available if BUILD evidence shows reclassification is operationally useful.

**Fitness:**
- **Fitness:** The feature_list.json's `passes` field is monotonic — a write transitioning a feature from `passes: true` to `passes: false` is rejected with `write_gate_rejection` typed error unless the write carries an audit-logged operator override field — verified by `test_write_gate_rejects_passes_regression_without_override` (unit).
- **Fitness:** The progress-log file rejects all non-append operations (overwrite, truncate, mid-file edit) with `write_gate_rejection` typed error; the file on disk is unchanged — verified by `test_progress_log_rejects_non_append_writes` (unit).
- **Fitness:** init.sh execution is gated on a hash match between the operator-recorded hash and the file's current content hash; mismatch produces `write_gate_rejection` typed error and execution is blocked — verified by `test_init_sh_hash_mismatch_blocks_execution` (unit).
- **Fitness:** Session creation honors the operator's cluster declaration; Cluster 2 default-required-artifact-set behavior is the default for ambiguous declarations per disposition (i) — verified by `test_cross_cluster_session_defaults_to_required_artifacts` (integration).

**Direction-not-constraint note (per ADR-076):** "Narrative continuity" of the progress log is a *direction* the artifact set optimizes toward — the log preserves text, but whether it actually serves narrative continuity for the operator is a UX-shaped property that resists single-test decomposition. The append-only constraint is the testable mechanism; the narrative-continuity outcome is the direction.

### Module: Budget Controller

**Purpose:** Enforces turn and token limits at each ReAct iteration boundary.
**Provenance:** AS-3; ADR-005; domain concept Budget.
**Owns:** Budget (concept); per-iteration circuit-breaker check; graceful termination with explicit exhaustion signaling.
**Depends on:** Session Registry.
**Depended on by:** Orchestrator Runtime.
**Inversion note:** Operator's mental model — "Budget is a thing I set; its enforcement is automatic and never negotiable by the LLM." The boundary is thin but load-bearing (AS-3 says control plane, not model plane). Kept separate from Session Registry because its change rate is different (Budget semantics will shift during rollout; Session identity rarely changes).

### Module: Orchestrator Runtime *(extended in Cycle 4 per ADR-012)*

**Purpose:** Runs the ReAct loop that delegates to llm-orc operations via a fixed tool surface; invokes Conversation Compaction at each turn boundary.
**Provenance:** ADR-001; domain concepts Orchestrator Agent, Routing Decision; ADR-012 (Cycle 4: invokes Conversation Compaction at turn boundaries; the *Conversation Compaction* concept ownership re-allocated from Runtime to the new Conversation Compaction module).
**Owns:** Orchestrator Agent (concept); Routing Decision (generation); Route, Invoke (Dynamic), Query, Record, Calibrate (as actor). **No longer owns** Conversation Compaction (re-allocated to the new Conversation Compaction module — Runtime now invokes it the same arms-length way it invokes Tool Dispatch and Budget Controller).
**Depends on:** Budget Controller, Orchestrator Tool Dispatch, **Conversation Compaction** *(new in Cycle 4)*. FC-4 amendment: the Runtime's allowed import set extends from `{Budget Controller, Orchestrator Tool Dispatch}` to `{Budget Controller, Orchestrator Tool Dispatch, Conversation Compaction}`.
**Depended on by:** Serving Layer.
**Inversion note:** The Orchestrator LLM's mental model is "I reason, I emit tool calls, I observe results." The Runtime boundary aligns with that mental model — it does not expose Session bookkeeping, Plexus awareness, Autonomy gating, summarization, calibration internals, tier escalation, or compaction internals to the LLM's reasoning context. Result summarization is interposed by Orchestrator Tool Dispatch on the `invoke_ensemble` return path; Conversation Compaction is invoked by the Runtime at turn boundaries with the messages array as parameter — the Runtime is unaware of compaction internals just as it is unaware of summarizer ensemble selection. The Runtime continues to own Routing Decisions and the Route/Invoke (Dynamic)/Query/Record/Calibrate actions; ownership of Conversation Compaction (the concept) moves to the new module per ADR-012's substantial owned surface.

### Module: Orchestrator Tool Dispatch *(extended in Cycle 4 per ADR-015 + ADR-017)*

**Purpose:** Defines the fixed five-tool surface, dispatches each orchestrator tool call to its downstream service, and runs the structural validation guard on orchestrator responses to detect phantom tool-call confabulation.
**Provenance:** ADR-003; AS-6; domain concepts Orchestrator Tool, Dynamic Invocation; **Cycle 4 additions:** ADR-015 (Tier-Escalation Router interposition on `invoke_ensemble`); ADR-017 (structural validation guard); commit `9f86d0b` typed-error precedent; CAAF arXiv:2604.17025 (class (a) deterministic-override intervention class for confabulation).
**Owns:** Orchestrator Tool (concept); tool-name allowlist enforcement; routing of `invoke_ensemble`, `compose_ensemble`, `list_ensembles`, `query_knowledge`, `record_outcome` to their downstream services; rejection of any other tool name as a tool error; interposition order on `invoke_ensemble` (post-Cycle 4): (1) structural validation guard scans the orchestrator's response text for assertion patterns → `phantom_tool_call` rejection if mismatch; (2) Autonomy Policy gate; (3) Tier-Escalation Router selects Model Profile via Calibration Gate verdict; (4) `EnsembleExecutor.execute` runs the dispatched ensemble; (5) Calibration Gate `check_and_record` (post-result, ADR-007 substrate); (6) Result Summarizer Harness (post-result; intercepts before Runtime sees the dict). **Cycle 4 additions:** Tool-call structural validation guard (concept); Phantom tool-call (concept; the failure mode the guard detects); the operator-extensible default pattern set (assertion patterns: *"the tool returned ..."* / *"I called X and the result was ..."* / *"the result of X is displayed above"* / etc.; future-intent patterns *"I will call X"* are not flagged per the conservative false-positive discipline); the typed `phantom_tool_call` error.
**Depends on:** Ensemble Engine, Composition Validator, Plexus Adapter, Autonomy Policy, Calibration Gate, Result Summarizer Harness, **Tier-Escalation Router** *(new in Cycle 4 per ADR-015)*.
**Depended on by:** Orchestrator Runtime.
**Rationale for separate module:** Keeping dispatch separate from the Runtime's reasoning loop makes the closed-set property of ADR-003 structurally enforceable — a code path that bypasses the dispatch to do something tool-like is mechanically excluded, not merely proscribed. ADR-015's router and ADR-017's structural validation guard inherit this property — they are interposed on the dispatch path, not on the Runtime's reasoning loop.

**Fitness (additions per Cycle 4):**
- **Fitness:** The structural validation guard scans every orchestrator response for the configured assertion patterns; a prose claim of tool-call occurrence with no corresponding structurally-valid tool-call structure in the same response produces `phantom_tool_call` typed error — verified by `test_phantom_tool_call_detected_when_prose_claim_lacks_structure` (integration).
- **Fitness:** Future-intent patterns ("I will call X", "I am going to invoke X") are not flagged — verified by `test_future_intent_patterns_not_flagged` (unit).
- **Fitness:** The pattern set is operator-extensible at deployment configuration — operator-added patterns are scanned alongside default patterns, and operator-added patterns trigger `phantom_tool_call` errors when matched without a corresponding structure — verified by `test_operator_extended_patterns_trigger_validation` (integration).

**Direction-not-constraint note (per ADR-076):** "The pattern-set adequacy as deployment evidence accumulates" is a *direction* the operator-extensibility surface optimizes toward; no automated mechanism verifies pattern-set adequacy. The operator-extension surface IS the operational discovery path — adequacy is what BUILD-time and deployment-time refinement establish.

### Module: Composition Validator

**Purpose:** Validates a proposed ensemble against the existing reference graph using the same routine as load-time validation.
**Provenance:** AS-2, AS-6; ADR-006; Invariant 5 (cross-ensemble acyclicity); Invariant 7 (static references); Invariant 8 (depth limit); Invariant 11 (extras forbidden); scenarios.md refactor 1-3; cycle 1 §FF 21.
**Owns:** Composition (concept); composition-time validation routine shared with `EnsembleLoader`.
**Depends on:** Ensemble Engine (shared validator routine lives in `core/config/ensemble_config.py` as a public function after the refactor).
**Depended on by:** Orchestrator Tool Dispatch.
**Retrofit debt resolved at WP-A:** `_validate_cross_ensemble_cycles` and `_build_reference_graph` were extracted from `EnsembleLoader` private helpers to public `validate_ensemble_reference_graph` in `core/config/ensemble_config.py`. Both load-time and composition-time validation share the single routine (FC-6).

### Module: Ensemble Engine **(existing)**

**Purpose:** Executes ensembles per the existing declarative DAG engine.
**Provenance:** Entire project-level domain model; ADR-001, ADR-002 (Layer 3 unchanged).
**Owns:** Ensemble, Agent, AgentConfig (LLM/Script/Ensemble), Model Profile, Inline Model, Dependency, Phase, Fan-Out, Input Key, Ensemble Reference, Ensemble Reference Graph, Depth, Depth Limit, Artifact, Child Executor, Immutable Infrastructure, Mutable State, Agent Discriminator; actions Load, Validate, Discriminate, Execute, Dispatch, Recurse, Fan Out, Gather, Select, Detect Cycles, Check Depth, Merge Profile.
**Depends on:** (project-level dependencies unchanged)
**Depended on by:** Orchestrator Tool Dispatch (via invoke_ensemble), Composition Validator (shared validator), Result Summarizer Harness (invokes a summarizer ensemble), Bootstrapping Pipeline (reads library), Plexus Adapter (persistence of Routing Decisions derived from executions).

### Module: Result Summarizer Harness

**Purpose:** Positions a summarizer between ensemble completion and the orchestrator's context so unsummarized results never reach reasoning.
**Provenance:** AS-7; ADR-004; domain concepts Result Summarization, Summarize action.
**Owns:** Result Summarization (concept); Summarize (action); raw-output escape-hatch dispatch.
**Depends on:** Ensemble Engine (invokes the summarizer ensemble).
**Depended on by:** Orchestrator Tool Dispatch (interposed on `invoke_ensemble`'s return path).
**Rationale for separate module:** The summarizer itself is an ensemble (a primitive — configured, not coded). What this module owns is the *harness position* — the unskippable interposition between ensemble completion and tool-call result return. The Runtime is not aware of the summarizer; the summarizer is not aware of the Runtime. The harness makes the enforcement of AS-7 structural rather than conventional.

### Module: Autonomy Policy

**Purpose:** Gates orchestrator actions against the Session's Autonomy Level.
**Provenance:** ADR-008; AS-6 (hard limit: no configuration permits primitive authorship); domain concept Autonomy Level.
**Owns:** Autonomy Level (concept); per-action gate resolution; visibility surfacing of composition events when a tightened level requires it.
**Depends on:** Session Registry.
**Depended on by:** Orchestrator Tool Dispatch.
**Note on the pure-tool-user default.** Cycle 1 §FF 25 flags that the default baseline Autonomy Level is calibrated for the operator-as-tool-user persona. Pure tool-user deployments (FF-2) may warrant a tighter default that surfaces composition events. The Autonomy Policy module exposes this as a configuration surface rather than a code change.

### Module: Calibration Gate *(extended in Cycle 4 per ADR-014)*

**Purpose:** Tracks Calibration state, runs quality checks on a composed ensemble's first N invocations (ADR-007 substrate), and produces a dispatch-time calibration verdict (Proceed / Reflect / Abstain trichotomy) for every dispatch decision.
**Provenance:** ADR-007; AS-5; domain concepts Calibration, Quality Signal; **Cycle 4 additions:** ADR-014 (verdict trichotomy + AUQ + HTC + time-decay windowing); AUQ arXiv:2601.15703 (System 1 + System 2 dual-process structure); HTC arXiv:2601.15778 (process-level trajectory features); OI-MAS arXiv:2601.04861 (confidence-gated dispatch pattern that ADR-015's router consumes the verdict from); Chuang et al. arXiv:2502.04428 (uncertainty-quantification method choice dominates threshold choice); essay 005 §"ADR candidate #3" + §"Long-Horizon Reliability Infrastructure".
**Owns:** Calibration (concept); Quality Signal (concept and generation in stateless mode; Plexus Adapter persists when active); per-ensemble calibration counter and transition-to-trusted logic; session-scoped state in stateless mode (ADR-007 clause 4); **Cycle 4 additions:** Calibration verdict (concept; trichotomy *Proceed / Reflect / Abstain*); In-process trajectory-level calibration (concept); Trajectory feature (concept); AUQ verbalized-confidence consumption (System 1 attention-soft + System 2 binary gate at default 0.85 within 0.8–1.0 range; operationally tunable per deployment); HTC trajectory feature extraction (token-level entropy patterns, attention-weight distributions over tool-call sequences, decision-confidence trajectories across reasoning steps); the three Abstain criteria (entropy collapse — token-level entropy in the most recent N tokens drops > 1.5σ below trajectory's running mean; post-hoc result-check hard failure with non-recoverable error class; multiple drift-detection criteria simultaneously exceeding thresholds when ADR-016 mechanism (d) is active and reporting drift); time-decay windowing on trajectory features (60-minute / 100-signal dual-bound, linear decay 1.0 → 0.0 from signal-emission to window-edge; the in-layer instance of ADR-016 mechanism (b)); the typed `calibration_abstain` error; the verdict surface published via `verdict_for(session_id, ensemble_name, dispatch_context)`.
**Depends on:** Ensemble Engine (invokes a check mechanism, which is itself an ensemble); Plexus Adapter (persistence when Plexus is active); **Calibration Signal Channel** *(new conditional dependency in Cycle 4)* — when ADR-016 channel is active, gate reads windowed signals (HTC features extracted once at L0 and propagated upward); when ADR-016 is rejected, gate operates on L1-internal trajectory data only (per ADR-014 §"Feature-extraction location" — the single-extraction-point property is load-bearing for performance and consistency).
**Depended on by:** Orchestrator Tool Dispatch (interposed on `invoke_ensemble` for ensembles in calibration; ADR-007 post-hoc check); **Tier-Escalation Router** *(new in Cycle 4)* — consumes the verdict surface directly with no LLM-mediated translation.

**Composition with ADR-007 (preserved per ADR-014):** the post-hoc layer (ADR-007's first-N output-level mechanism) and the in-process layer (ADR-014's dispatch-time trajectory-level mechanism) compose additively. Post-hoc tracks *whether an ensemble can be trusted*; in-process tracks *whether a specific dispatch should proceed right now*. Both signals govern stabilization (per AS-5, when Plexus is active).

**Fitness:**
- **Fitness:** The verdict trichotomy (Proceed / Reflect / Abstain) is structurally exhaustive: every dispatch produces exactly one verdict; the verdict's value depends only on AUQ confidence, post-hoc result-check signal, and trajectory feature anomaly criteria — verified by `test_verdict_is_exhaustive_and_deterministic_given_inputs` (unit).
- **Fitness:** Time-decay windowing operates only on signals within the dual-bound (60-minute / 100-signal, whichever shorter); signals outside the window contribute weight 0 to the verdict; signals within the window contribute linearly-decaying weight from 1.0 (most recent) to 0.0 (window edge) — verified by `test_time_decay_windowing_dual_bound_linear` (unit).
- **Fitness:** ADR-007's first-N post-hoc result-check continues to fire on every first-N invocation under ADR-014's extension; the existing quality-signal accumulation and trusted-status transition logic continue unchanged — verified by `test_adr_007_post_hoc_calibration_unchanged_under_adr_014` (preservation).
- **Fitness:** When ADR-016 is rejected, ADR-014's in-process layer operates on L1-internal trajectory data only; the verdict trichotomy continues to function with the in-layer feature set — verified by `test_verdict_computation_works_without_signal_channel` (unit).

### Module: Plexus Adapter

**Purpose:** Mediates all Plexus interaction with no-op fallbacks when Plexus is absent.
**Provenance:** ADR-009, ADR-010; AS-4, AS-8; domain concepts Ingestion, Enrichment, Context Injection (data flow), Routing Decision (persistence), Quality Signal (persistence).
**Owns:** Ingestion (push to Plexus); Enrichment (invocation; Plexus performs the actual enrichment); Query (knowledge graph query mechanics); Record (outcome persistence); no-op fallback semantics when Plexus is absent.
**Depends on:** (external — Plexus lib)
**Depended on by:** Orchestrator Tool Dispatch (query_knowledge, record_outcome), Bootstrapping Pipeline (ingestion), Serving Layer's `resolve_session_start_context` (Phase 2 only), Calibration Gate (persistence of Quality Signals).
**Inversion note:** The Operator's mental model is "Plexus is a lib I enable; llm-orc pushes to it." The boundary preserves that — the Adapter is the single place Plexus-aware code lives, so the rest of the system sees a tool interface regardless of Plexus state. This is what makes AS-8 structurally enforceable.

### Module: Bootstrapping Pipeline

**Purpose:** Pushes library source material into Plexus as a deliberate operator operation.
**Provenance:** AS-4; ADR-010; DISCOVER FF #9 and #14; domain concept Bootstrapping.
**Owns:** Bootstrapping (concept and action); operator-triggered batch ingestion flow.
**Depends on:** Plexus Adapter, Ensemble Engine (reads library via existing config manager).
**Depended on by:** (operator — CLI/web trigger)

### Module: Orchestrator Configuration

**Purpose:** Loads and resolves the orchestrator's per-session configuration surface.
**Provenance:** ADR-005 (Budget defaults), ADR-008 (Autonomy defaults), ADR-009 (Plexus enablement), ADR-011 (Orchestrator Model Profile), ADR-012 (Conversation Compaction thresholds), ADR-015 (per-skill tier defaults), ADR-017 (structural validation guard pattern set).
**Owns:** Per-session config resolution; operator-set bounds on per-request overrides; the four Conversation Compaction thresholds (50K-character Layer 0 trigger, 60-minute Layer 2 idle window, 12,288-token Layer 3 cap, 3-failure Layer 4 circuit-breaker); per-skill tier-defaults configuration (8 skills × 2 tiers = 16 Model Profile slots, with sharing permitted); structural validation guard pattern set (default + operator extensions).
**Depends on:** (project config manager — existing)
**Depended on by:** Serving Layer; values flow downward at construction time to L2 modules (Conversation Compaction thresholds; Tier-Escalation Router tier defaults; Tool Dispatch pattern set).

### Module: Conversation Compaction *(new in Cycle 4 per ADR-012)*

**Purpose:** Runs the five-layer cheapest-first compaction pipeline at orchestrator turn boundaries.
**Provenance:** ADR-012; essay 005 §"Long-Horizon Reliability Infrastructure"; Khanal arXiv:2603.29231; Chroma 2025; Liu et al. TACL 2023; Anthropic published Claude Code specification; codebase precedent commit `9f86d0b` for typed-error coupling.
**Owns:** Conversation Compaction (concept; re-allocated from Orchestrator Runtime ownership in v2.0); the five compaction layers — Layer 0 persist-large-tool-results (>50K char to filesystem with 2 KB preview), Layer 1 cache-edit, Layer 2 idle-expiry (60-minute default), Layer 3 free summary via nine-section session-notes template (12,288-token cap; deterministic logic; zero LLM cost), Layer 4 LLM-summary via configured summarizer ensemble; the four operationally-tunable thresholds; circuit-breaker state (per-session; auto-reset at session start); the nine-section template structure; the typed `compaction_layer_4_failure` error.
**Depends on:** Ensemble Engine (Layer 4 invokes summarizer ensemble via existing `EnsembleExecutor.execute`; distinct from Result Summarizer Harness's AS-7 ensemble-output summarization); filesystem (Layer 0 persistence root; operator-configurable). No L3 dependencies — receives messages array as parameter from Runtime.
**Depended on by:** Orchestrator Runtime (invokes compaction at turn boundaries; FC-4 amendment adds `conversation_compaction` to Runtime's allowed import set alongside `budget_controller` and `orchestrator_tool_dispatch`).
**Inversion note:** Operator's mental model — "the orchestrator stays coherent over long sessions because something compacts the conversation." Module name and owned thresholds match; layer ordering is internal mechanics. The five-layer cheapest-first ordering is the load-bearing design property (per ADR-012's published reference; reversal produces the failure mode the pattern was designed to replace).

**Fitness:**
- **Fitness:** Across a multi-turn fixture session that exceeds the configured context threshold by ≥ 200%, the orchestrator's per-turn LLM call receives a context whose token-count stays at or below threshold for ≥ 95% of iterations — verified by `test_compaction_holds_context_below_threshold_across_long_session` (integration).
- **Fitness:** Each compaction invocation applies the five layers in order (0 → 1 → 2 → 3 → 4) and short-circuits on the first layer that brings context below threshold; Layer N+1 fires only if Layers 0..N together cannot — verified by static AST inspection of the compaction main loop plus `test_layer_4_fires_only_after_layers_0_3_attempted` (integration).
- **Fitness:** Layer 4 LLM-summary failure increments a per-session circuit-breaker; the third consecutive failure suspends Layer 4 for the rest of the session and emits a typed `compaction_layer_4_failure` error to operator-readable storage with `recovery_action_required="operator_intervention_required"` — verified by `test_layer_4_circuit_breaker_after_three_consecutive_failures` (unit).
- **Fitness:** Layer 4 circuit-breaker state resets to active at session-start without operator intervention — verified by `test_circuit_breaker_resets_at_session_start` (unit).

### Module: Tier-Escalation Router *(new in Cycle 4 per ADR-015; extended in Cycle 4 architect-gate close per ADR-018)*

**Purpose:** Selects per-dispatch Model Profile (cheap-tier or escalated-tier) for `invoke_ensemble` based on the dispatched ensemble's Topaz skill metadata and the Calibration Gate's verdict. **Per ADR-018:** also fires a periodic out-of-band audit dispatch (analog of ADR-016 mechanism (d)) on the verdict→router edge to detect routing-vs-tier-config drift.
**Provenance:** ADR-015; ADR-018 (amending ADR — (d)-analog audit dispatch responsibility); ADR-014 (verdict producer); ADR-011 (compatibility envelope — orchestrator's own LLM session-boundary scope preserved); ADR-016 mechanism (d) (structural precedent for the audit dispatch via Spike β analytical transfer audit, research log `005h-`); essay 005 §"ADR candidate #4"; OI-MAS arXiv:2601.04861; Topaz paper; SC-MAS, MasRouter literature evidence base.
**Owns:** Tier-escalation router (concept); Per-skill tier default (concept; the operator-configured pair of Model Profiles per Topaz skill); Topaz skill profile (schema owner — the YAML metadata field `topaz_skill: TopazSkill`); the verdict→tier mapping (Proceed → cheap-tier; Reflect → escalated-tier; Abstain → typed `escalation_bypass` error); the typed `missing_skill_metadata` error; the typed `escalation_bypass` error. **Per ADR-018:** the (d)-analog audit dispatch — trigger (every 100 verdict consumptions or 24 wall-clock hours, whichever first; operationally tunable); three drift criteria at quantitative-threshold level — verdict-distribution shift (±15% between consecutive windows), escalation-vs-outcome correlation drift (default: escalation must produce at least +5pp outcome improvement over the audit window to be interpretable as a tier-configuration signal rather than routing-noise — this is the Sub-Q6 evidence surface), bypass-rate trend (default: +25% relative-rate increase per window); typed verdict trichotomy (no drift / advisory / severe drift); operator action surface for advisory drift (diagnostic + parameter-tuning recommendation against the 16-Model-Profile-slot configuration surface); severe-drift fail-safe mode (route-all-to-escalated until operator review); asynchronous-operator-review dynamic.
**Depends on:** Calibration Gate (consumes calibration verdict via `verdict_for(session_id, ensemble_name, dispatch_context)`); Ensemble Engine (reads ensemble YAML metadata via existing config manager; observes dispatched ensemble's outcome for the (d)-analog audit's escalation-vs-outcome correlation criterion). Per-skill tier defaults and (d)-analog audit thresholds flow from Orchestrator Configuration at construction time (no L3 import).
**Depended on by:** Orchestrator Tool Dispatch (interposed on `invoke_ensemble` between dispatch entry and `EnsembleExecutor.execute` — selected tier flows into ensemble execution).
**Inversion note:** Operator's mental model — "I configure per-skill tier defaults at deployment; ensembles declare their primary skill; the system routes by skill+confidence; an audit periodically checks whether routing matches tier-configuration intent." Module name and configuration surface match. Per-ensemble tier alternatives — rejected per ADR-015 (and confirmed by Spike α, 2026-05-11) — would have served *developer convenience* (granularity) over operator's mental model (per-skill defaults).
**Inherited bounding-mechanism properties (per ADR-018, from Spike β analytical transfer audit 2026-05-11):** Three of ADR-016's five bounding mechanisms hold for the verdict→router edge by inheritance from existing infrastructure rather than by new specification: **(a) fresh-context isolation** holds by construction (`select_tier` is a stateless pure function); **(b) time-decay windowing** is operationalized one layer upstream at the Calibration Gate per ADR-014 (the bias-bound for the verdict→router feedback path exists in the producer); **(e) read-only structural validation** is satisfied by the FC-17 typed-error infrastructure (`escalation_bypass`, `missing_skill_metadata`). One mechanism — **(c) categorical anchors** — is structurally inapplicable at this edge (the Router consumes verdicts, not deterministic-tool outputs; (c)'s anchor substrate is at L0). The (d)-analog audit dispatch is the load-bearing addition.
**Falsification trigger (per ADR-018, inherits ADR-016's elaboration-by-evidence discipline):** if WP-G4's BUILD work finds that the (d)-analog audit dispatch cannot be operationalized within the Tier-Escalation Router module's responsibility (e.g., it requires its own top-level module orthogonal to L0–L3, or it requires bidirectional coupling with the Calibration Gate that violates the read-only verdict-consumption contract), the elaboration-by-evidence framing commitment is invalidated for ADR-018; the (d)-analog spec re-opens, and OQ #14 partial closure for the L1→L2 stage reverts to "BUILD evidence will inform" with Sub-Q6 also re-opening.

**Fitness:**
- **Fitness:** The orchestrator's own Model Profile remains constant for the entire Session under any verdict-driven tier escalation; only the dispatched task's tier varies — verified by `test_orchestrator_profile_unchanged_under_tier_escalation` (preservation of FC-13).
- **Fitness:** The orchestrator's tool-call API (`invoke_ensemble`) signature is unchanged under tier escalation; the orchestrator's reasoning surface receives the same shape of tool-call result regardless of which tier was dispatched — verified by `test_invoke_ensemble_api_unchanged_under_tier_escalation` (integration).
- **Fitness:** The verdict-to-action mapping is deterministic: Proceed → cheap-tier dispatch; Reflect → escalated-tier dispatch; Abstain → `escalation_bypass` typed error — verified by `test_verdict_to_tier_mapping_is_deterministic` (unit).
- **Fitness (per ADR-018):** The Router consumes each verdict in a fresh stateless context; no prior verdict state is carried forward through `select_tier` — verified by `test_select_tier_is_stateless_pure_function` (unit). (Inherits ADR-016 mechanism (a) by construction.)
- **Fitness (per ADR-018):** All Router error surfaces (`escalation_bypass`, `missing_skill_metadata`) derive from `LlmOrcStructuralError` with the four common fields per FC-17 — verified by FC-17's existing static class-hierarchy walk. (Inherits ADR-016 mechanism (e) by infrastructure reuse.)
- **Fitness (per ADR-018):** The (d)-analog audit dispatch fires at the configured trigger frequency (default every 100 verdict consumptions or 24 wall-clock hours, whichever first); on severe drift the audit's verdict triggers fail-safe mode (route-all-to-escalated until operator review) plus operator notification — verified by `test_d_analog_audit_dispatch_fires_at_trigger_and_severe_drift_activates_fail_safe` (integration).

**Direction-not-constraint note (per ADR-076, extended for ADR-018):** "Routing-vs-tier-config drift detection" is a *direction* the (d)-analog audit dispatch collectively optimizes toward — the three drift criteria (verdict-distribution shift, escalation-vs-outcome correlation drift, bypass-rate trend) are individually testable at unit/integration level, but whether the aggregate drift-detection property holds under real-deployment dynamics is exactly what first-deployment evidence answers. Spike β's analytical transfer audit established the structural properties; first-deployment evidence on the cycle's North-Star benchmark closes Sub-Q6 in conjunction with closing this audit dispatch's operational-validation gate.

### Module: Calibration Signal Channel *(new in Cycle 4 per ADR-016; CONDITIONAL ACCEPTANCE — first-deployment evidence pending)*

**Purpose:** Carries read-only calibration data upward from L0 (Ensemble Engine outputs) to L1 (Calibration Gate dispatch decisions), enforcing the five bounding mechanisms.
**Provenance:** ADR-016; essay 005 §"ADR candidate #6"; Khanal arXiv:2603.29231 (cycle/scale risk that motivates bounding mechanisms (b) and (d)); CAAF arXiv:2604.17025 (prompt-engineering-artifact finding); Wisdom and Delusion of LLM Ensembles arXiv:2510.21513 (mechanism (c) precedent); RDD methodology susceptibility-snapshot pattern (mechanism (d) structural transfer); architectural-isolation pattern from RDD methodology tooling (mechanism (a) precedent — methodology-level rather than codebase-internal); commit `9f86d0b` typed-error pattern (mechanism (e) precedent).
**Owns:** the read-only signal channel boundary (the only edge in the dependency graph that points L0→L1 — the explicit narrow exception ADR-016 amends ADR-002 to permit); the typed signal-data schema (trajectory features per ADR-014's HTC specification, dispatch outcomes, deterministic-tool-output anchors per mechanism (c)); the five bounding mechanisms — (a) fresh-context isolation in the consumer, (b) time-decay windowing (60-min/100-signal dual-bound, linear decay; smaller-window configurations track better than default per spike (b) finding — operational tuning territory), (c) categorical anchors via deterministic-tool-output (when ensemble has script-model slot; ensemble-composition-conditional; anchor strength scales with output verifiability — binary-verifiable strongest, interpretable-prose weakest), (d) periodic out-of-band audit dispatch (default every 100 verdicts or 24 wall-clock hours; three drift-detection criteria — verdict skew, outcome divergence, signal-to-verdict correlation drift — at quantitative-threshold level), (e) read-only structural validation at the consumer (schema validation; mismatch produces typed `malformed_signal` error and is dropped from verdict computation); the audit verdict trichotomy (no drift / advisory / severe drift) — severe drift triggers fail-safe mode (calibration verdicts default to Reflect-or-Abstain) plus operator notification; the operator action surface for advisory drift (diagnostic + parameter-tuning recommendation; operator approves or overrides at next session boundary); the asynchronous-operator-review dynamic for runtime audit verdicts; the conditional-acceptance status's first-deployment-evidence trigger (per ADR-016 §"Concrete monitoring specification") with sweep responsibility at each cycle that exercises the channel.
**Depends on:** Ensemble Engine (consumes typed signal data from L0 dispatch outputs via observer-pattern callback registration — *this is the upward edge ADR-002 amendment permits, read-only, calibration-data-only, structurally typed at boundary*).
**Depended on by:** Calibration Gate (the L1 consumer reads channel signals — within the consumer's fresh evaluation context per mechanism (a)).
**Inversion note:** The channel is intrinsically an architectural concept (the upward-edge exception ADR-016 amends ADR-002 to permit). The operator-facing surface is audit verdicts and parameter-tuning recommendations — operator interacts with the audit verdict, not the channel internals. Naming the module *Signal Channel* leans architectural; the operator-facing visibility comes through the audit dispatch's diagnostic plus the periodic operator review of advisory drift recommendations.
**Falsification trigger (load-bearing carry-forward):** if BUILD or first-deployment evidence finds that mechanism (b) windowing or mechanism (d) audit dispatch cannot be operationalized within ADR-002's L0-L3 structure (e.g., they require a top-level module orthogonal to the four-layer architecture), the elaboration-by-evidence framing commitment is invalidated, the reorganization branch re-opens, and ADR-016 is re-deliberated with reorganization on the table.

**Fitness:**
- **Fitness:** Every signal traversing the channel is structurally typed at the channel boundary; signals that fail schema validation produce `malformed_signal` typed error and are dropped from the verdict computation — verified by `test_channel_validates_signal_schema_at_boundary` (unit).
- **Fitness:** Each calibration verdict computation runs in a fresh evaluation context — no signal data from prior verdicts is carried forward through the consumer's context; influence on the next verdict is only through the time-decay-windowed feature aggregation — verified by `test_consumer_runs_in_fresh_context_no_carryover` (unit).
- **Fitness:** The channel rejects any upward write attempt at the structural level; no L0 state is mutated through the channel boundary by L1 callers — verified by `test_channel_is_read_only_no_l1_to_l0_writes` (structural).
- **Fitness:** Mechanism (d)'s periodic out-of-band audit dispatch fires at the configured trigger frequency (default every 100 verdicts or 24 wall-clock hours, whichever first); on severe drift the audit's verdict triggers fail-safe mode (calibration verdicts default to Reflect-or-Abstain) — verified by `test_audit_dispatch_fires_at_trigger_and_severe_drift_activates_fail_safe` (integration).

**Direction-not-constraint note (per ADR-076):** "Load-bearing bounding of feedback-bias compounding" is a *direction* the bounding mechanisms collectively optimize toward — bounded under synthetic-data spike (b)/(d), but whether the bound holds under real-deployment dynamics is exactly what first-deployment evidence answers (the conditional-acceptance status's load-bearing question). The five mechanisms are individually testable; the *aggregate* bias-bound property is testable only by first-deployment evidence on the cycle's North-Star benchmark.

### Module: Dispatch Event Substrate *(new in Cycle 6 per ADR-023)*

**Purpose:** Routes typed dispatch events from a unified emission point to two routing destinations per Inversion N+2.
**Provenance:** ADR-023; Cycle 6 DISCOVER snapshot Action 3 (Inversion N+2 framing); MODEL Action C (three architectural alternatives — event-model extension chosen); domain-model §Concepts entries on Dispatch timing, Liveness signal; domain-model §Methodology Vocabulary on Infrastructure-complete / routing-incomplete.
**Owns:** Dispatch timing (concept); Route event (action); the `DispatchTiming` event type (`phase: Literal["start","end"]`, `dispatch_id`, `ensemble_name`, `model_profile`, `timestamp`; `phase="end"` additionally carries `duration_seconds` and `exit_status: Literal["success","error","timeout","aborted"]`); the `dispatch_id` correlation identifier — generation function (`new_dispatch_id(session_id) -> str`), lifecycle (created at Tool Dispatch entry for each `invoke_ensemble`; propagates to all events for that dispatch, to the envelope's `diagnostics.dispatch_id`, and to the artifact path's `<dispatch_id>` segment); the registered-sink dispatcher pattern (`register_sink(sink: EventSink) -> None`; `emit(event)` fans out to registered sinks); the bounded event-model extension policy — additive only; existing event types gain `dispatch_id: str | None` as an optional additive field with `None` allowed during transition.
**Depends on:** Calibration Gate, Tier-Escalation Router, Tier-Router-Audit, Calibration Signal Channel (event producers; emit through the substrate's `emit()` rather than directly to sinks).
**Depended on by:** Orchestrator Tool Dispatch (calls `new_dispatch_id` at entry; emits `DispatchTiming(start)` before tier selection and `DispatchTiming(end)` after harness; queries substrate for event log when constructing the `DispatchEnvelope`'s `diagnostics`); Operator-Terminal Event Sink (registered consumer); Orchestrator-Context Event Sink (registered consumer); Session Artifact Store (consumes `dispatch_id` for path construction).
**Inversion note:** Operator's mental model — "the orchestrator's dispatch decisions are observable through one event surface; what destination the events route to is a configuration question." Module name and the registered-sinks pattern align — operators see one event substrate; tooling teams can extend with additional sinks (TUI dashboard, Plexus ingestion, third-party monitoring) without changing the producer side.
**`dispatch_id` coupling note (per Cycle 6 DECIDE snapshot Finding 2 advisory carry-forward):** the same `dispatch_id` value governs both the observability event correlation (ADR-023) and the artifact filesystem path (ADR-025). A bug in `dispatch_id` generation or lifecycle affects both surfaces simultaneously. The module owns the generation; BUILD scenarios verify cross-surface consistency (envelope's `diagnostics.dispatch_id` == event stream's `dispatch_id` == filesystem path's `<dispatch_id>` for a single dispatch).

**Fitness:**
- **Fitness:** Every event emitted during one `invoke_ensemble` dispatch carries the same `dispatch_id` value; the substrate's `emit()` rejects events whose `dispatch_id` is `None` after the WP-D6 transition closes — verified by `test_dispatch_events_share_dispatch_id_within_one_invoke` (integration).
- **Fitness:** The event-model extension is bounded — one new event type (`DispatchTiming`); one new field on four existing event types (`dispatch_id: str | None`); no other additions in Cycle 6 — verified by static class-hierarchy walk plus `test_event_model_extension_is_bounded` (structural check).
- **Fitness:** The substrate is the **only** path from producers to sinks; producers do not import sinks directly — verified by static AST import inspection (`test_event_producers_only_import_substrate_not_sinks`).
- **Fitness:** A `DispatchTiming(phase="end")` event for a dispatch is preceded by a `DispatchTiming(phase="start")` event with the same `dispatch_id`; orphaned `phase="end"` events without a matching `phase="start"` are not emitted — verified by `test_dispatch_timing_phases_pair_correctly` (unit).

### Module: Operator-Terminal Event Sink *(new in Cycle 6 per ADR-023; extended in Cycle 7 loop-back #3 per ADR-036 — TurnDecision branch + delegation-rate surfacing; extended in loop-back #5 per ADR-037 — finish-policy fields)*

**Purpose:** Routes dispatch events to the operator-terminal destination as line-oriented `key=value` log lines; emits liveness-signal patterns (tool-call-emit logging, inference-wait heartbeats); surfaces validate-once-at-load startup warnings.
**Provenance:** ADR-023 §"Destination 1 — Operator-terminal" + §"Liveness-signal patterns" + §"Noise-floor remediation"; domain-model §Concepts entries on Liveness signal, Bilateral visibility absence; post-hotfix verification 2026-05-14 finding 7.
**Owns:** Liveness signal (concept); Emit (tool call) (action — `emit_tool_call_log(tool_name, dispatch_id) -> None`); Heartbeat (action — `emit_heartbeat(session_id, elapsed_seconds) -> None`); the per-event format strings (one per event type — `DispatchTiming(start) → "INFO: dispatch start: ..."`; `DispatchTiming(end) → "INFO: dispatch end: ..."`; `TierSelection → "INFO: tier selection: ..."`; `CalibrationVerdict → "INFO: calibration verdict: ..."`; `AuditDiagnostic → "INFO: audit diagnostic: ..."` (one line per drift criterion finding); `CalibrationSignal → "DEBUG: calibration signal: ..."`); log-level discrimination (`CalibrationSignal` at DEBUG; all others at INFO); the tool-call-emit logging surface (fires before dispatch when the orchestrator's response stream contains an `invoke_ensemble` tool call); the inference-wait heartbeat surface (fires after `N` seconds of tool inactivity per `heartbeat_interval_seconds` configuration, default 30s; recurs every `N` seconds while wait continues); the validate-once-at-load startup-warning surface (one `WARN` line per invalid YAML at startup or library reload; zero per-enumeration noise thereafter).
**Depends on:** Dispatch Event Substrate (registered consumer); Ensemble Engine library loader (consumes startup validation results to emit `WARN` once); Serving Layer (open-request tracking for heartbeat — heartbeats fire from the open-request side, formatted/emitted by this sink).
**Depended on by:** (external — serve process console / stderr / `logger` infrastructure).
**Inversion note:** Operator's mental model — "the console tells me what dispatch decisions happened and gives me signal during long waits." The line-oriented `key=value` format is grep-friendly; the per-event lines replace the coarse `INFO: tool dispatch: result kind=success` line operators saw across Cycles 4-5 with ensemble identification, duration, verdict, and `dispatch_id` correlation. The format is **one line per event** (line-oriented); JSON-shaped observation is the orchestrator-context destination's responsibility.

**Fitness:**
- **Fitness:** Each typed dispatch event produces exactly one operator-terminal log line at the configured log level; `CalibrationSignal` emits at DEBUG (suppressed by default); all others emit at INFO — verified by per-event-type unit tests plus an integration test exercising a full dispatch and asserting line count + log level (`test_per_event_log_line_emission_levels`).
- **Fitness:** The tool-call-emit log line for an `invoke_ensemble` tool call fires **before** the dispatch — chronologically, `INFO: tool-call emit: tool=invoke_ensemble dispatch_id=<id>` appears in the log stream before the corresponding `INFO: dispatch start: ensemble=<name> ... dispatch_id=<id>` — verified by `test_tool_call_emit_log_precedes_dispatch_start` (integration, timestamp-ordered assertion).
- **Fitness:** When the orchestrator's open request has no tool activity for `heartbeat_interval_seconds` (default 30s), the sink emits `INFO: inference wait: elapsed=<seconds> session_id=<id>`; the heartbeat repeats every `heartbeat_interval_seconds` while inactivity continues — verified by `test_heartbeat_fires_at_interval_during_inactivity` (timing-sensitive integration with mocked clock).
- **Fitness:** Legacy schema-drifted ensemble YAMLs produce exactly one `WARN` line at startup (or library reload) and zero `WARN` lines on subsequent `list_ensembles()` calls within the same serve lifetime; the per-enumeration noise floor measured by Cycle 6 follow-on finding 7 is eliminated — verified by `test_validate_once_at_load_emits_warn_only_at_startup` (integration over multiple `list_ensembles` calls).

**Direction-not-constraint note (per ADR-076):** "Operator-readable" is a *direction* the format optimizes toward — grep / awk / line-oriented tooling work directly on the format; that the format genuinely serves operator debugging during real session work is a UX-shaped property that resists single-test decomposition. The line-oriented `key=value` shape is the testable mechanism; debuggability is the direction.

**Loop-back #3 extension (ADR-036; absorbs the former WP-LB-F sink work):** a `TurnDecision` branch in `consume` — previously the event fell through silently (conformance loop-back #3 F-2) — emitting one INFO line per turn (`INFO: turn decision: turn=<i> action=<action> shape=<turn_shape> delegated=<ensemble|-> carry_held=<bool> replanned=<bool>`); this is simultaneously the axis-2 split-vs-callee diagnostic surface (FC-51, the original WP-LB-F objective) and the delegation-rate numerator surface (FC-59). Delegation-rate surfacing composes the Delegation Rate Meter's `delegation_rate` over consumed events; whether the rate emits per-N-turns, at session close, or on demand is a BUILD open choice — the FC requires computability and an operator-visible reading, not a cadence.

- **Fitness:** A `TurnDecision` event consumed by the sink produces exactly one INFO line carrying turn index, action, turn shape, delegated ensemble, grounded-carry-held, and replanned-after-truncation — verified by `test_turn_decision_log_line` (unit on the branch) + the real-client acceptance run's serve log (the delegation-verified evidence ADR-036's Conditional Acceptance gating condition reads). FC-51 + FC-59.

**Loop-back #5 extension (ADR-037 — finish-policy surfacing):** the `TurnDecision` INFO line gains the two finish-policy fields (`tail=<tail_kind> verdict=<COMPLETE|REMAINING|->`), making termination behavior operator-visible per turn; false-continue and false-stop shares compose over the same consumed stream (FC-67) the way the delegation rate composes (FC-59) — the convergence-verified evidence ADR-037's Conditional Acceptance gating condition (a) reads from this serve log. Surfacing cadence for the shares is the same BUILD open choice as the delegation rate's.

### Module: Orchestrator-Context Event Sink *(new in Cycle 6 per ADR-023)*

**Purpose:** Routes dispatch events into the orchestrator's reasoning context as structured observations between turns; produces end-of-session summaries in `execution.json`.
**Provenance:** ADR-023 §"Destination 2 — Orchestrator-context"; PLAY note 12 load-bearing practitioner question (*"What was the total run-time of the ensemble?"*); domain-model §Concepts entry on Dispatch timing.
**Owns:** Structured-observation construction (the JSON-shaped block prepended to the orchestrator's next ReAct turn: `{dispatched, duration_seconds, model_profile, tier, topaz_skill, calibration_verdict, dispatch_id}`); the in-turn routing pattern (consume events from the just-completed dispatch; format as observation block; prepend to next-turn context at the orchestrator's turn boundary); the end-of-session summary routing pattern (write one entry per `dispatch_id` to `execution.json`'s new `dispatch_log` key on session close); the final-dispatch-before-session-close handling (in-turn routing skipped; end-of-session summary captures the events); the `CalibrationSignal`-exclusion-by-default policy (with operator opt-in flag `agentic_serving.observability.orchestrator_context_routes_calibration_signal: true`).
**Depends on:** Dispatch Event Substrate (registered consumer); Orchestrator Runtime (turn-boundary integration — Runtime calls the sink at each turn boundary to obtain the structured observation, the same arms-length way Runtime invokes Conversation Compaction); Session Registry (session lifecycle hooks for end-of-session summary).
**Depended on by:** Orchestrator Runtime (queries the sink at each turn boundary for the prior dispatch's observation block).
**Inversion note:** Operator's mental model — "the orchestrator can answer dispatch-graph questions from event evidence, not just from reasoning alone." PLAY note 12's load-bearing question (*"how long did the ensemble run?"*) is directly answered by `duration_seconds` in the structured observation. The orchestrator's reasoning surface receives JSON-shaped context (consumable by the model) rather than line-oriented logs (consumable by humans).

**Why this is a separate module rather than folded into Orchestrator Runtime** *(addressing the Appendix A.5 architect-brief examination target; recorded in cycle-6-architect snapshot Advisory 2)*: a Runtime-internal alternative — assembling the structured observation inside the ReAct loop directly — would require Orchestrator Runtime to import the Dispatch Event Substrate. **FC-4 explicitly limits Runtime's allowed imports to Budget Controller, Orchestrator Tool Dispatch, and Conversation Compaction** (per the Cycle 4 amendment); adding a Dispatch Event Substrate import would relax FC-4 and weaken the Runtime's mental-model alignment (Runtime stays unaware of dispatch-event internals the same way it stays unaware of summarization, calibration, and tier-escalation internals). The separate-module shape preserves FC-4; Runtime queries the sink at the turn boundary via the arms-length `observations_for(dispatch_id) -> Observation` contract — analogous to how Runtime invokes Conversation Compaction at turn boundaries without importing compaction internals.

**Fitness:**
- **Fitness:** When the orchestrator's next ReAct turn begins after a dispatch returns control, the turn's context begins with a JSON-shaped observation block carrying `{dispatched, duration_seconds, model_profile, tier, topaz_skill, calibration_verdict, dispatch_id}` for the just-completed dispatch — verified by `test_orchestrator_context_observation_prepended_between_turns` (integration covering a full ReAct-loop iteration).
- **Fitness:** When a dispatch is the session's final operation before close, in-turn routing is skipped (no next turn); the dispatch's events route to the end-of-session summary under `execution.json`'s `dispatch_log` key; the operator-terminal destination still receives all events at emission time — verified by `test_final_dispatch_routes_to_end_of_session_summary` (integration).
- **Fitness:** `CalibrationSignal` events are excluded from orchestrator-context routing by default; the operator opt-in flag `agentic_serving.observability.orchestrator_context_routes_calibration_signal: true` includes them — verified by `test_calibration_signal_excluded_by_default_included_under_opt_in` (unit + integration).
- **Fitness:** The structured observation's field set is exactly the seven canonical fields (`dispatched, duration_seconds, model_profile, tier, topaz_skill, calibration_verdict, dispatch_id`); no additional fields leak through; missing field values fall back to `None` rather than omitting the key — verified by schema-validation test (`test_orchestrator_context_observation_schema_is_canonical`).

### Module: Session Artifact Store *(new in Cycle 6 per ADR-025)*

**Purpose:** Maintains the substrate location for capability-ensemble deliverables — session-dir-scoped, dispatch-keyed, retention-policy-enforced — and enforces the always-scope artifact-as-substrate commitment for capability ensembles.
**Provenance:** ADR-025; practitioner-verbatim 2026-05-14 finding 8 (*"one strategy could be to **always** rely on artifact writing to be the substrate"*); domain-model §Concepts entry on Artifact-as-substrate; AS-7 amendment per ADR-025; OQ #15 resolved-by-codification.
**Owns:** Artifact-as-substrate (concept); the `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>` path layout; `<session_id>` format (`<iso-8601-datetime>-<short-uuid>` — e.g., `2026-05-15T14:32:08Z-a7f3`); the `<deliverable>` default (`<ensemble-name>-output`) when the ensemble does not specify; retention semantics enforcement (`session` artifacts removed on session close; `durable` artifacts persist indefinitely; `ephemeral` artifacts removed at the orchestrator's next turn); the typed `ArtifactReference` shape in `envelope.artifacts[]` (`{path, content_type, size_bytes, summary, retention}`); the `output_substrate` ensemble-YAML field (capability ensembles default `artifact`; system ensembles `inline`; operator opt-out `inline` per capability ensemble); the `output_retention` ensemble-YAML field (default `session`); the `calibration_substrate_access` ensemble-YAML field (default absent → summary-only evaluation; `artifact` → tool-call surface for critic agents to read content); the dial-back falsification criteria (five indicators per ADR-025 §"Dial-back falsification criteria"); the backward-deprecated `.llm-orc/artifacts/agentic-serving/<ensemble>/<timestamp>/` path (deprecated, not actively cleaned up — operators migrate pinned references manually).
**Depends on:** Session Registry (session lifecycle hooks — `on_session_close` triggers cleanup of `retention: session` artifacts); Dispatch Event Substrate (consumes `dispatch_id` for path construction at dispatch entry); Ensemble Engine (reads `output_substrate`, `output_retention`, `calibration_substrate_access` YAML fields via the existing config manager surface); filesystem.
**Depended on by:** Orchestrator Tool Dispatch (calls `write_deliverable(dispatch_id, deliverable_name, content, content_type) -> ArtifactReference` for substrate-routed dispatches; populates `envelope.artifacts[]`); Result Summarizer Harness (queries `output_substrate` for the dispatched ensemble to decide whether to invoke the summarizer); Calibration Gate (under `calibration_substrate_access: artifact`, opens a tool-call surface for critic agents to read the artifact content).
**Inversion note:** Skill Orchestration User's mental model — "the orchestrator dispatches my request to an ensemble that produces a deliverable I consume." Under substrate-routing, the deliverable lives at a known filesystem path the client reads directly; the mental model is structural rather than reconstructable from orchestrator narration (spike γ's fourth-defect surface — orchestrator-narration substitution — dissolves at this boundary). Operator's mental model — "my session's artifacts live under `.llm-orc/agentic-sessions/<session_id>/`; I can navigate by session, not per-ensemble." Session-dir grouping makes session-level review natural; cleanup, archival, and (future) Plexus ingestion operate at session granularity.
**`dispatch_id` coupling note (per Cycle 6 DECIDE snapshot Finding 2 advisory carry-forward):** the artifact path's `<dispatch_id>` segment is the same value carried by ADR-023's dispatch events and ADR-024's envelope `diagnostics.dispatch_id`. The substrate's path-construction code reads `dispatch_id` from the Dispatch Event Substrate (the single source of truth) rather than generating its own.

**Fitness:**
- **Fitness:** A capability ensemble whose YAML declares `output_substrate: artifact` (default for capability ensembles) writes its deliverable to `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>`; the envelope's `primary` field carries a summary line referencing the artifact; the envelope's `artifacts[0]` carries the typed `ArtifactReference` — verified by `test_capability_ensemble_writes_to_session_dir_artifact_path` (integration).
- **Fitness:** A system ensemble (or capability ensemble declaring `output_substrate: inline`) produces an inline envelope — `envelope.artifacts` is empty (`None` or `[]`); `envelope.primary` carries the deliverable content directly — verified by `test_inline_response_ensemble_produces_inline_envelope` (integration).
- **Fitness:** Session-close removes all `retention: session` artifacts (and the per-dispatch directories containing them) under the session's `.llm-orc/agentic-sessions/<session_id>/` directory; `retention: durable` artifacts remain on disk; the session directory itself remains only if at least one durable artifact persists — verified by `test_session_close_cleanup_honors_retention` (integration over a fixture session with mixed retention).
- **Fitness:** When the dispatched ensemble's YAML declares `calibration_substrate_access: artifact`, the Calibration Gate's critic agents receive a tool-call surface to read `artifacts[0].path`; when the field is absent or `summary`, critic agents receive `envelope.primary` + `artifacts[0].summary` only (no file-read tool call) — verified by `test_calibration_substrate_access_opt_in_opens_artifact_read_surface` (integration with mocked critic-agent tool surface).
- **Fitness:** The artifact path's `<dispatch_id>` segment exactly matches the `dispatch_id` carried by the dispatch's event stream and the envelope's `diagnostics.dispatch_id`; cross-surface consistency holds — verified by `test_dispatch_id_consistency_across_events_envelope_artifact_path` (integration; the Finding 2 advisory carry-forward target).
- **Fitness:** Three or more capability ensembles declaring `output_substrate: inline` as opt-outs triggers the dial-back falsification deliberation per ADR-025 Indicator 4; the dial-back deliberation is surfaced as a Cycle 6+ field-note prompt during PLAY — verified at PLAY phase (structural prompt; no automated test fires until BUILD-deployment evidence accumulates).

**Direction-not-constraint note (per ADR-076):** "Cleaner design rule — one scope rule for all capability ensembles" (the always-scope) is a *direction* the architectural commitment optimizes toward — the design rule's simplicity is the testable mechanism (one scope rule, not a per-ensemble decision tree); whether the rule remains *operationally* clean as the library expands is what BUILD's first deployments + the post-BUILD PLAY cycle will surface. The dial-back falsification criteria (five indicators) are the testable mechanism for re-examining the rule on evidence; "cleanliness" itself is the direction.

### Module: Orchestrator Configuration *(extended in Cycle 6 per ADR-022 + ADR-023 + ADR-025)*

*Existing module; v3.0 entry above remains current.* Cycle 6 amendments:

**Provenance additions:** ADR-022 (system-prompt amendment elevating `invoke_ensemble` under capability-matched NL framing); ADR-023 (new config keys for observability — `heartbeat_interval_seconds`, `orchestrator_context_routing`, `orchestrator_context_routes_calibration_signal`); ADR-025 (new config keys for substrate — `agentic_sessions_root` default location, retention defaults per ensemble category).
**Owns (additions):** The amended `DEFAULT_ORCHESTRATOR_SYSTEM_PROMPT` (inserts the ADR-022 paragraph between the existing "Do not pick a client-declared tool for questions about llm-orc's own state" paragraph and the "When you need a client-declared tool, emit it alone in a single assistant turn" paragraph; the new paragraph commits the orchestrator to prefer `invoke_ensemble` over direct completion AND over client-declared tools when a capability ensemble matches the NL request); the operator-overridable surface (`agentic_serving.orchestrator.system_prompt` in `config.yaml`); the `heartbeat_interval_seconds` default (30s); the `orchestrator_context_routing` default (`enabled`); the `orchestrator_context_routes_calibration_signal` default (`false`); the `agentic_sessions_root` default (`.llm-orc/agentic-sessions/`); per-ensemble retention defaults.
**Inversion note (Cycle 6 update):** Operator's mental model — "I configure the orchestrator's prompt and the observability/substrate defaults at deployment time; the orchestrator-LLM honors the prompt's commitments as far as its reasoning shape supports." Per ADR-022's disposition (iii), prompt-amendment effectiveness varies by orchestrator-profile reasoning shape — the configuration surface holds the design-time commitment; first-deployment evidence holds the empirical question.

**Fitness (additions per Cycle 6):**
- **Fitness:** The default `DEFAULT_ORCHESTRATOR_SYSTEM_PROMPT` contains the ADR-022 capability-match clause verbatim (the paragraph beginning *"When a tool-user's natural-language request maps to a capability ensemble in `list_ensembles()`..."*); operator-override via `agentic_serving.orchestrator.system_prompt` replaces the default — verified by `test_default_system_prompt_contains_adr_022_clause` (unit) + `test_operator_override_replaces_default_system_prompt` (integration).
- **Fitness:** Capability-matched NL framing routes to `invoke_ensemble` under at least one orchestrator profile after the prompt amendment ships (acceptance criterion verified at PLAY rather than at architecture-time per ADR-022's disposition-(iii) deferral) — measured by post-BUILD PLAY field-note observation against the spike γ probe under MiniMax M2.5-free. **Direction-not-constraint** per ADR-076 — see below.
- **Fitness:** `heartbeat_interval_seconds` (default 30s), `orchestrator_context_routing` (default `enabled`), and `orchestrator_context_routes_calibration_signal` (default `false`) are config-resolvable via `agentic_serving.observability.*` per ADR-011's session-boundary config discipline — verified by config-loading unit tests.

**Direction-not-constraint note (per ADR-076, new for Cycle 6):** "The orchestrator routes capability-matched NL framing to `invoke_ensemble`" is a *direction* the system-prompt amendment optimizes toward — the prompt's textual commitment is the testable mechanism (the clause is present in the default; operator-override is supported); whether the orchestrator-LLM honors the clause depends on the model's reasoning shape and is empirically per-profile-conditional per ADR-022's disposition (iii). The unit-testable property is **prompt presence**; the integration-testable property is **at least one profile (MiniMax M2.5-free per spike γ) honors the clause on capability-matched NL framing**; cross-profile uniformity is a *direction*, not a testable constraint.

### Module: Orchestrator Tool Dispatch *(extended in Cycle 6 per ADR-023 + ADR-024 + ADR-025)*

*Existing module; v3.0 entry above remains current.* Cycle 6 amendments:

**Provenance additions:** ADR-023 (emits `DispatchTiming` events bracketing each `invoke_ensemble`); ADR-024 (constructs typed `DispatchEnvelope` as the return value of `invoke_ensemble`; the envelope is a shared type owned conceptually here, defined in a shared types module alongside `LlmOrcStructuralError`); ADR-025 (conditionally skips Result Summarizer Harness for substrate-routed dispatches per AS-7 amended; writes deliverables through Session Artifact Store for substrate-routed dispatches).
**Owns (additions):** `DispatchEnvelope` (shared type with frozen dataclass shape per ADR-024); the post-Cycle-6 interposition order on `invoke_ensemble`: (1) structural validation guard *(unchanged)*; (2) `new_dispatch_id` from Dispatch Event Substrate; (3) `DispatchTiming(phase="start")` emit; (4) Autonomy Policy gate *(unchanged)*; (5) Tier-Escalation Router selects Model Profile *(unchanged)*; (6) `EnsembleExecutor.execute` runs the dispatched ensemble *(unchanged)*; (7) Calibration Gate `check_and_record` *(unchanged at evaluation invocation; evaluation surface adapts per ensemble per ADR-025 — handled at the gate)*; (8) **conditional**: if dispatched ensemble's `output_substrate == "artifact"`, write deliverable to Session Artifact Store and construct `envelope.primary` as the summary line, `envelope.artifacts[0]` as the typed `ArtifactReference`; **skip Result Summarizer Harness** per AS-7 amended; (8') **else if `output_substrate == "inline"`**: invoke Result Summarizer Harness per ADR-004 (mandate scope: inline-response path); construct `envelope.primary` as the summarized content; `envelope.artifacts` empty; (9) `DispatchTiming(phase="end")` emit (after harness or after substrate write); (10) construct `envelope.diagnostics` by querying Dispatch Event Substrate for this `dispatch_id`'s events (populates `ensemble`, `dispatch_id`, `duration_seconds`, `model_profile`, `tier`, `topaz_skill`, `calibration_verdict`, `audit_findings`); (11) return `DispatchEnvelope` to the Runtime.
**Depends on (additions):** Dispatch Event Substrate, Session Artifact Store. (Existing dependencies unchanged.)

**Fitness (additions per Cycle 6):**
- **Fitness:** `invoke_ensemble` returns a `DispatchEnvelope` dataclass instance with `status`, `primary`, and `diagnostics` populated for every successful dispatch; optional fields (`structured`, `errors`, `artifacts`) populated per ADR-024 / ADR-025 — verified by `test_invoke_ensemble_returns_typed_envelope` (integration).
- **Fitness:** Every `invoke_ensemble` dispatch emits exactly one `DispatchTiming(phase="start")` event before tier selection and exactly one `DispatchTiming(phase="end")` event after harness/substrate-write; both events share the same `dispatch_id` — verified by `test_invoke_ensemble_emits_paired_dispatch_timing_events` (integration).
- **Fitness:** For substrate-routed dispatches (`output_substrate: artifact`), `invoke_ensemble` writes the deliverable via Session Artifact Store **before** constructing the envelope; the `agentic-result-summarizer` is **not** invoked; the envelope's `primary` carries a summary line referencing `artifacts[0]` — verified by `test_substrate_routed_invoke_ensemble_skips_summarizer_and_writes_artifact` (integration with mock summarizer to assert non-invocation).
- **Fitness:** For inline-response dispatches (`output_substrate: inline`), `invoke_ensemble` invokes `agentic-result-summarizer` per ADR-004; ADR-004's per-invocation `raw_output=True` escape hatch operates unchanged within this scope — verified by `test_inline_response_invoke_ensemble_invokes_summarizer_per_adr_004` (integration).
- **Fitness:** `envelope.diagnostics.dispatch_id` matches the `dispatch_id` on every dispatch event emitted for that dispatch and matches the `<dispatch_id>` segment of `artifacts[0].path` when populated — verified by `test_dispatch_id_consistency_envelope_events_artifact_path` (integration; same test that fitness above on Session Artifact Store references).

### Module: Result Summarizer Harness *(extended in Cycle 6 per ADR-025)*

*Existing module; v3.0 entry above remains current.* Cycle 6 amendments:

**Provenance additions:** ADR-025 §"AS-7 amendment"; domain-model Amendment Log entry #11 (AS-7 amended to default-with-conditional-skip).
**Owns (additions):** The substrate-conditional skip mechanism — queries the dispatched ensemble's `output_substrate` field via Session Artifact Store; when `artifact`, the harness is **not invoked** (the skip happens upstream at Orchestrator Tool Dispatch); when `inline`, the harness runs per ADR-004 mandate. The harness module's own logic does not change; what changes is **whether Tool Dispatch invokes it**. ADR-004's per-invocation `raw_output=True` escape hatch (small classifier outputs) operates unchanged within the inline-response scope; the two skip mechanisms compose without contradiction (substrate-routing is dispatch-shape-level; `raw_output` is per-invocation flag-level).
**Depends on (additions):** (no new direct dependencies — the substrate-conditional skip is implemented at Orchestrator Tool Dispatch's invocation point per the interposition-order update above).

**Fitness (additions per Cycle 6):**
- **Fitness:** When the harness is invoked, its behavior is preserved from v3.0 — summarization fires on full results; raw-output escape hatch fires on flagged ensembles; summarizer-failure returns typed error preserving the artifact on disk — verified by existing v3.0 fitness criteria (preservation; no Cycle 6 changes to harness-internal behavior).
- **Fitness:** A substrate-routed capability ensemble dispatch does not invoke the harness; verification is at Orchestrator Tool Dispatch's interposition-order, not at the harness module — see `test_substrate_routed_invoke_ensemble_skips_summarizer_and_writes_artifact` above.

### Module: Calibration Gate *(extended in Cycle 6 per ADR-024 + ADR-025)*

*Existing module; v3.0 entry above remains current.* Cycle 6 amendments:

**Provenance additions:** ADR-024 (the verdict + audit findings now flow into `envelope.diagnostics` via Dispatch Event Substrate; `CalibrationVerdict` gains `dispatch_id` correlation); ADR-025 §"Calibration-gate evaluation surface under substrate-routing" (evaluation surface adapts per ensemble — three modes: summary-only default, `structured` consumption when `output_schema:` declared, artifact-content tool-call when `calibration_substrate_access: artifact` declared).
**Owns (additions):** The three evaluation surfaces — (a) summary-only (default — critic agents receive `envelope.primary` + `artifacts[0].summary`); (b) `structured`-augmented (when ensemble's YAML declares `output_schema:` — critic agents additionally consume `envelope.structured`); (c) artifact-content (when ensemble's YAML declares `calibration_substrate_access: artifact` — critic agents receive a tool-call surface to read `artifacts[0].path`); the `dispatch_id` field addition on the `CalibrationVerdict` event (additive; `None` allowed during transition).

**Fitness (additions per Cycle 6):**
- **Fitness:** The verdict trichotomy (Proceed / Reflect / Abstain) is structurally preserved under substrate-routing — verified by existing v3.0 fitness criteria on verdict computation (preservation; ADR-014 substrate unchanged at the gate's evaluation logic).
- **Fitness:** Under `calibration_substrate_access` absent or `summary`, critic agents do **not** read artifact content — verified by `test_calibration_gate_default_uses_summary_only_evaluation` (integration with content-read assertion).
- **Fitness:** Under `calibration_substrate_access: artifact`, critic agents receive a tool-call surface to read `artifacts[0].path` and the verdict reflects evaluation of artifact content — verified by `test_calibration_gate_opt_in_reads_artifact_content` (integration with `code-generator` as the lead opt-in case).

### Module: Ensemble Engine *(L0, extended in Cycle 6 per ADR-023 + ADR-024 + ADR-025)*

*Existing module; v3.0 entry above remains current.* Cycle 6 amendments are confined to additive schema extensions and the validate-once-at-load library loader change.

**Provenance additions:** ADR-023 §"Noise-floor remediation" (validate-once-at-load library cache); ADR-024 (optional `output_schema:` YAML field; `structured` typed payload populated by synthesizer or post-dispatch processing when declared); ADR-025 (optional `output_substrate`, `output_retention`, `calibration_substrate_access` YAML fields).
**Owns (additions):** The new optional YAML fields on `EnsembleConfig` — `output_substrate: Literal["artifact","inline"] | None` (default — capability ensembles `artifact`, system ensembles `inline`); `output_schema: dict | None` (JSON Schema / Pydantic-model-derivable; advisory at dispatch time per ADR-024); `output_retention: Literal["session","durable","ephemeral"] | None` (default `session` for `artifact`-substrate ensembles); `calibration_substrate_access: Literal["summary","artifact"] | None` (default `summary`); the validate-once-at-load library loader semantics — `EnsembleLoader.load_from_file` runs at serve startup (or library reload via `SIGHUP` / admin endpoint / restart); `list_ensembles()` returns the cached validated subset; per-enumeration re-validation eliminated.

**Fitness (additions per Cycle 6):**
- **Fitness:** Library validation runs at startup or library reload — exactly once per serve lifetime (or once per reload event); `list_ensembles()` returns the cached validated subset without re-running validation — verified by `test_library_validation_runs_once_at_startup` (integration) + `test_list_ensembles_returns_cached_validated_subset` (unit).
- **Fitness:** Ensemble YAMLs may carry the four new optional fields (`output_substrate`, `output_schema`, `output_retention`, `calibration_substrate_access`) without breaking load; absent fields use the documented defaults — verified by per-field load tests + schema migration test on the existing 8 agentic-serving ensembles (`test_existing_ensembles_load_under_cycle_6_schema_with_defaults`).
- **Fitness:** Project-level Layer 3 (Ensemble Engine) semantics — DAG execution, cross-ensemble references, depth limits, fan-out gather, child executor — are **unchanged** by Cycle 6; the additions are schema-level and loader-level only — verified by the existing Layer 3 regression suite.

**Direction-not-constraint note (per ADR-076, new for Cycle 6 — addressing the Appendix A.5 architect-brief examination target on operator affordance; recorded in cycle-6-architect snapshot Advisory 5)**: "Explicit-reload semantics for ensemble YAML changes" is the *direction* the validate-once-at-load cache invalidation chose. The three reload triggers — `SIGHUP` to the serve process, an admin endpoint, or full restart — are the testable mechanism (FC-27 verifies the validation-event-count property). The rejected alternative is **file-watch auto-reload** (background filesystem watcher; YAML modification triggers re-validation without operator action). The rejection's rationale is deployment-predictability over developer-convenience: file-watch auto-reload introduces background reloads that may silently change runtime behavior in production deployments, and the validate-once-at-load surface is consumed by the Operator-Terminal Event Sink's startup-`WARN` line — silent background reloads would emit `WARN` lines mid-session, breaking the at-startup model. Operators with developer-convenience workflows (live-edit ensembles during library authoring) trigger reload via `SIGHUP` or admin endpoint; auto-reload is not in Cycle 6 scope.

### Shared type: `DispatchEnvelope` *(new in Cycle 6 per ADR-024)*

*Not a module — a shared type with the same cross-cutting status as `LlmOrcStructuralError` (the Cycle 4 shared base class).*

**Definition** (lives alongside `LlmOrcStructuralError` in the shared types module, or as `agentic/dispatch_envelope.py`):

```python
@dataclass(frozen=True)
class DispatchEnvelope:
    status: Literal["success", "error", "timeout", "partial"]
    primary: str
    structured: dict | None = None
    diagnostics: dict = field(default_factory=dict)
    errors: list[dict] | None = None
    artifacts: list[dict] | None = None
```

**Provenance:** ADR-024 (the typed envelope contract); spike α (candidate B selection — additive typed fields path); domain-model §Methodology Vocabulary on Common I/O envelope. **Cross-module producers/consumers:** Orchestrator Tool Dispatch (producer at `invoke_ensemble` return); Orchestrator Runtime (consumer — the envelope flows to the ReAct loop as the tool-call result); Skill orchestration clients via Serving Layer (downstream consumers of the chat-completion response).
**Field-name `diagnostics` (not `metadata`)** aligns with the MODEL-phase vocabulary; the existing `execution.json` artifact retains `metadata` (the rename is at the envelope layer only — Cycle 7+ artifact-shape ADR may align). **`output_schema:` advisory** at dispatch time per spike β's reframing of output-spec drift as `input.data` override; schema validation is **not enforced** at the synthesizer's output.

### Module: Dispatch Pipeline *(RETIRED by ADR-043, loop-back #9, 2026-06-18)* — L2

> **⊘ Retired.** ADR-043 collapsed the two serving surfaces to one; this module (`dispatch_pipeline.py`) and its ensemble-backed planner/synthesizer roles (`ensemble_backed_roles.py`) are deleted. Every request now routes through the loop-driven Client-Tool-Action Terminal. The section below is preserved as historical record of the retired design; other Dispatch-Pipeline references in this companion are likewise superseded by ADR-043.

**Purpose:** Orchestrates the chat-completions plan → dispatch → synthesize flow for every chat-completions request.
**Provenance:** ADR-027 (primary direction); domain-model §Methodology Vocabulary "Framework-driven dispatch pipeline"; AS-9 (structural property satisfied on this surface); AS-10 (request-shape constraint within which the pipeline operates); Spike ε + ε' + μ (synthesis-stage empirical baseline); Spike ζ (planner-stage empirical baseline).
**Owns:** The three-stage orchestration sequence (Plan via Routing Planner; Dispatch via Orchestrator Tool Dispatch when `action: "dispatch"`; Synthesize via Response Synthesizer); the **Plan → InternalToolCall adapter** that translates the routing-planner JSON output `{action, ensemble, input, rationale}` into `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": <plan.ensemble>, "input": <plan.input>})` per ADR-027 §Dispatch stage; the streaming chunk surface — yields the same chunk vocabulary `OrchestratorRuntime` produces (`ContentDelta`, `VisibilityEvent`, `Completion`) so `OpenAiSseFormatter` consumes pipeline output without modification per ARCHITECT Finding 8 disposition; emission of pipeline-stage events (`PlanEmitted`, `DispatchFired`, `SynthesizerCompleted`, `DirectCompletionFallback`) through the Dispatch Event Substrate per ADR-027 §"Relationship to existing infrastructure" + ADR-032 §"Operator-observable degradation signaling".
**Depends on:** Routing Planner (Stage 1); Orchestrator Tool Dispatch (Stage 2 — Plan → InternalToolCall adapter calls `dispatch()`); Response Synthesizer (Stage 3); Capability List Builder (input to Stage 1); Dispatch Event Substrate (pipeline-stage event emission).
**Depended on by:** Serving Layer (replaces the existing `OrchestratorRuntime` call path on `/v1/chat/completions`).
**Inversion Principle check:** The pipeline boundary serves the Population A mental model of "transparent OpenAI-compatible endpoint" (per OQ #18 Q3 — Population A locates routing inside the client and treats the endpoint as transparent single-model proxy). The pipeline is invisible to Population A clients at the protocol layer; OpenAI chat-completions request/response shapes are unchanged.

**Fitness:**
- **Fitness:** Every chat-completions request flows through the Dispatch Pipeline; the chat-completions handler does not construct an `OrchestratorRuntime` instance — verified by `test_chat_completions_uses_dispatch_pipeline_not_runtime` (static: no `OrchestratorRuntime(...)` construction in `v1_chat_completions.py`; integration: any chat-completions request produces dispatch event substrate emissions for `PlanEmitted` followed by either `DispatchFired` or `DirectCompletionFallback`).
- **Fitness:** AS-9 is satisfied universally on the chat-completions surface — every chat-completions request is processed by structurally-bounded LLM roles (Routing Planner for Plan; Response Synthesizer for Synthesize); the orchestrator-LLM is not invoked for routing or post-dispatch synthesis — verified by `test_orchestrator_llm_not_invoked_on_chat_completions_surface` (integration: per-request observability inspection confirms zero LLM calls from `OrchestratorRuntime` or equivalent bundled-reasoning surface; routing decisions originate exclusively from the routing-planner ensemble; response composition originates exclusively from the response-synthesizer ensemble).
- **Fitness:** The Plan → InternalToolCall adapter preserves the existing dispatch contract — `OrchestratorToolDispatch.dispatch()` receives a well-formed `InternalToolCall` and returns a typed `ToolCallResult` (`ToolCallSuccess` with `DispatchEnvelope` or `ToolCallError`) per ADR-024 unchanged — verified by `test_plan_to_internal_tool_call_adapter_preserves_dispatch_contract` (integration: a `{"action": "dispatch", "ensemble": "code-generator", "input": "..."}` plan yields a real dispatch via `OrchestratorToolDispatch.dispatch()` returning a real `DispatchEnvelope`; envelope `primary` + `artifacts[0]` populated as the dispatched ensemble's output specifies).
- **Fitness:** Pipeline chunk vocabulary is identical to the runtime's chunk vocabulary — `OpenAiSseFormatter` consumes pipeline output unchanged — verified by `test_pipeline_chunks_pass_through_openai_sse_formatter_unchanged` (static: pipeline yields only chunk types in the existing union `ContentDelta | VisibilityEvent | ClientToolCall | Completion`; integration: a streaming chat-completions request produces SSE chunks the formatter renders without error).

### Module: Routing Planner *(new in Cycle 7 per ADR-028)* — L2

**Purpose:** Wraps the routing-planner system ensemble; produces a structured dispatch plan from chat-completions request content.
**Provenance:** ADR-028 (specification); ADR-027 (primary direction); domain-model §Concepts "Routing-planner ensemble"; domain-model §Actions "Plan"; AS-9 (structural-bounding satisfied); AS-10 (request-content-alone scope satisfied); Spike ζ (empirical baseline: 100% JSON conformance + 100% schema validity + 90% strict capability-match + 100% defensible-judgment across 20-prompt battery at qwen3:8b; p50=10s latency).
**Owns:** The routing-planner ensemble invocation surface — constructs `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": "agentic-routing-planner", "input": <serialized ORIGINAL REQUEST + CAPABILITY LIST>})` and calls `OrchestratorToolDispatch.dispatch()` (so the planner runs through the existing interposition pipeline — calibration gate, tier-escalation router, autonomy policy — per ADR-028 §"Calibration Gate integration"); JSON output parsing into a typed `DispatchPlan(action: Literal["dispatch","direct"], ensemble: str | None, input: str | None, rationale: str)` dataclass; the bonus-path layering hooks (explicit-naming extractor pre-filter; `tool_choice` interception pre-filter under ADR-030 follow-on; both optional, intercept *before* the planner ensemble runs); the system ensemble YAML at `.llm-orc/ensembles/agentic-serving/agentic-routing-planner.yaml` (promoted from Spike ζ scratch artifact `.llm-orc/ensembles/spike-cycle7-zeta-routing-planner.yaml` with the `input` field added to the output schema per Track A refactor on conformance Finding 4); the 20-prompt regression battery as integration test fixture.
**Depends on:** Orchestrator Tool Dispatch (via the existing `dispatch()` chokepoint — calibration gate, tier-escalation router, autonomy policy interpositions all fire within the planner ensemble's invocation per ADR-028 §"Calibration Gate integration"); Capability List Builder (the CAPABILITY LIST input).
**Depended on by:** Dispatch Pipeline.
**Inversion Principle check:** The boundary serves AS-10's request-shape constraint (operates on chat-completions request body + capability list alone; no out-of-band signals consumed). Population A clients verify the boundary by sending requests with arbitrary custom headers and observing identical routing decisions.

**Fitness:**
- **Fitness:** Routing-planner output JSON conforms to `{action: "dispatch"|"direct", ensemble: string|null, input: string|null, rationale: string}` schema across the 20-prompt regression battery — verified by `test_routing_planner_battery_20_prompts_schema_conformance` (integration: every prompt produces schema-valid JSON parseable into `DispatchPlan`; 100% conformance is the empirical floor from Spike ζ).
- **Fitness:** Routing-planner consumes only chat-completions request content + capability list — no out-of-band signals (HTTP headers, skill-framework manifest, custom metadata) influence routing — verified by `test_routing_planner_ignores_custom_headers` (integration: requests with custom `X-Skill-Framework: rdd` and `X-Capability-Hint: code-generator` headers produce identical routing decisions to baseline requests with no custom headers).
- **Fitness:** Routing-planner runs inside `OrchestratorToolDispatch.dispatch()`'s interposition pipeline — calibration gate emits `CalibrationVerdict` per planner invocation; tier-escalation router emits `TierSelection` per planner invocation — verified by `test_routing_planner_fires_calibration_gate_per_dispatch` (integration: dispatch event substrate emits `CalibrationVerdict` event for every planner invocation; `TierSelection` event for every planner invocation; both events share the planner's `dispatch_id`).

### Module: Response Synthesizer *(new in Cycle 7 per ADR-029)* — L2

**Purpose:** Wraps the response-synthesizer system ensemble; produces the user-facing chat-completion response from structured input.
**Provenance:** ADR-029 (specification); ADR-027 (primary direction); domain-model §Concepts "Response-synthesizer ensemble"; domain-model §Actions "Synthesize (response)"; AS-9; Spike ε + ε' + μ (empirical baseline: 13 tests at qwen3:8b, 0 fabrications across 4 confabulation modes — multi-dispatch fabrication; path hallucination; substrate-path-as-deliverable; coherent factual errors uncalibrated).
**Owns:** The response-synthesizer ensemble invocation surface — constructs `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": "agentic-response-synthesizer", "input": <serialized ORIGINAL REQUEST + PLAN + DISPATCH RESULTS>})` and calls `OrchestratorToolDispatch.dispatch()`; the structured input serialization — full `messages[]` for multi-turn continuity per Spike ε' C1/C2; envelope `primary` + `artifacts[0].summary` extraction from `DispatchEnvelope` per ADR-024 (the synthesizer reads envelope summary fields, NOT substrate file paths, per ADR-029's structural prevention of C4 substrate-path-file-read failure mode); the strict-fidelity rule set's content-layer commitments (Rules 1-6 per ADR-029 §"Strict-fidelity rule set"; including Rule 6 codified from Spike μ.1 per Track A refactor on conformance Finding 6); the streaming output surface (yields `ContentDelta` chunks as the synthesizer LLM emits tokens); the Rule 5 framing content-layer commitment under ADR-029's "load-bearing default with falsification trigger to headers/metadata" disposition; the system ensemble YAML at `.llm-orc/ensembles/agentic-serving/agentic-response-synthesizer.yaml` (promoted from Spike ε scratch artifact with Rule 6 added).
**Depends on:** Orchestrator Tool Dispatch (via `dispatch()` chokepoint — calibration gate fires three new Reflect-trigger criteria per ADR-029 §"Calibration Gate integration": Rule 5 framing absence; Rule 4 rounding drift; Rule 1 fabrication signal).
**Depended on by:** Dispatch Pipeline.
**Inversion Principle check:** The synthesizer's structured input contract (`REQUEST + PLAN + DISPATCH RESULTS`) is non-standard relative to user-shaped capability ensembles (which take prompt-shaped input). The non-standard shape serves the user's mental model — the user never sees the structured input; they see a natural-language response. The non-standard shape is the price of structural bounding per AS-9 and is hidden from the user-facing surface.

**Fitness:**
- **Fitness:** Synthesizer produces `message.content` as a string + `finish_reason: stop`; no tool calls emitted; the response carries content only — verified by `test_synthesizer_emits_content_only_no_tool_calls` (integration: every synthesizer dispatch produces a `ToolCallSuccess` whose envelope's `primary` field is a string; no `ClientToolCall` chunks emitted; `finish_reason` is "stop" not "tool_calls").
- **Fitness:** Synthesizer fabrication-prevention holds — substantive claims trace to DISPATCH RESULTS content; the 13-test + 4-confabulation-mode battery continues to produce 0 fabrications — verified by `test_synthesizer_regression_battery_zero_fabrications` (integration: the spike battery, ported as production regression suite, exercises PLAY-note-22 + Spike δ positive control + simple lookup + 4 direct-completion request shapes + 2 numerical-density shapes + 2 multi-turn continuity shapes + 4 confabulation-mode probes; total n=13; assertion: 0 fabrications across the batch).
- **Fitness:** Rule 5 framing is present on `action: "direct"` responses; absence triggers Calibration Gate Reflect verdict per ADR-029 §"Rule 5 framing requirement scope" — verified by `test_synthesizer_rule_5_framing_present_on_direct_responses` (integration: parses synthesizer output for Rule 5 framing marker; assertion: marker present on every `action: "direct"` response; gate emits `CalibrationVerdict` with Reflect when marker absent).
- **Fitness:** Multi-turn continuity is preserved when prior turns are in input — verified by `test_synthesizer_multi_turn_continuity` (integration: multi-turn `messages[]` with prior assistant + new user turn referencing it produces a correctly-contextual response per Spike ε' C1/C2 — the synthesizer references prior-turn content; the continuity holds without server-side workflow state per ADR-029).

### Module: Capability List Builder *(new in Cycle 7)* — L1

**Purpose:** Produces the framework's capability list — the single source of truth consumed by Routing Planner and the Capability Discovery Endpoint.
**Provenance:** ADR-026 (AS-10 — the request-content-alone constraint requires a capability list source that does not depend on client signals); ADR-028 §Input contract (routing-planner's CAPABILITY LIST input); ADR-032 §"Mechanism: Capability-list discovery" (discovery endpoint consumes the same list).
**Owns:** The filter logic — capability ensembles only: ensembles declaring `output_substrate: artifact` and `topaz_skill` per ADR-019 + ADR-025; system ensembles (`agentic-routing-planner`, `agentic-response-synthesizer`, `agentic-result-summarizer`, `agentic-calibration-checker`) excluded; the capability list shape `[{name, description, topaz_skill}, ...]`; the registry-consumer Protocol (`CapabilityRegistry.list_capability_ensembles()` — abstract interface; concrete implementation reads through the existing loaded-ensemble registry per the validate-once-at-load library cache established Cycle 6 / FC-27).
**Depends on:** Ensemble Engine *(via Protocol — concrete implementation reads from the validate-once-at-load library cache established Cycle 6 / FC-27)*.
**Depended on by:** Routing Planner; Capability Discovery Endpoint.

**Fitness:**
- **Fitness:** Capability list excludes system ensembles — `agentic-routing-planner`, `agentic-response-synthesizer`, `agentic-result-summarizer`, `agentic-calibration-checker` are not in the output — verified by `test_capability_list_excludes_system_ensembles` (unit + integration: list output filtered correctly across the production registry contents).
- **Fitness:** Capability list reflects the live registry — ensembles added/removed via library reload (per FC-27 SIGHUP / admin endpoint / restart) appear/disappear in the list — verified by `test_capability_list_reflects_live_registry_after_reload` (integration: add an ensemble YAML; trigger SIGHUP reload; list output includes the new ensemble).
- **Fitness:** Capability list is the **single source of truth** — both Routing Planner and Capability Discovery Endpoint receive identical capability lists from the same builder call (or from two calls returning identical content under steady registry state) — verified by `test_routing_planner_and_discovery_endpoint_consume_identical_capability_lists` (integration: simultaneous extraction of the planner's input and the discovery endpoint's response confirms identical capability identifiers).

### Module: Response Labeling *(new in Cycle 7 per ADR-032 + ADR-030 bridge)* — L3

**Purpose:** Decorates chat-completion responses with three-layer honest served-by signaling (headers + body metadata; content-layer Rule 5 framing delegated to Response Synthesizer per ADR-029).
**Provenance:** ADR-032 §Sub-promise (1) (configuration honesty delivered by honest response labeling at three layers); ADR-030 §"Bridge advisory specification" (`tool_choice_handling: "deferred"` three-layer signal); OQ #18 Q3 finding (Population A degradation signal is configuration dishonesty — Cline #10551 + OpenCode #20859 cited).
**Owns:** The header decoration logic — emits `X-LLM-Orc-Served-By: ensemble:<name> | direct | direct_fallback | tool_choice:<ensemble>` per response path per ADR-032 §Sub-promise (1); emits `X-LLM-Orc-Tool-Choice-Handling: deferred` on `tool_choice`-bearing requests per ADR-030 §Bridge advisory specification; header names are conventional, not load-bearing (operator-configurable at deployment per ADR-032 §Implementation mechanisms). The body metadata decoration logic — emits `metadata.served_by`, `metadata.tool_choice_handling`, `metadata.population_b_advisory`, `metadata.dispatch_failed` (when relevant) on the chat-completion response shape. The Population B advisory content — universally safe to send per ADR-032 §"Mechanism: Structured advisory for Population B" (Population A clients that don't surface metadata are unaffected); BUILD-phase advisory-content design picks careful wording.
**Depends on:** None at the module-import level (a stateless decorator function consuming the pipeline's `DispatchPlan` + `DispatchEnvelope` to determine the served-by path).
**Depended on by:** Serving Layer (composed onto the chat-completion response body after pipeline completion).
**Inversion Principle check:** Three-layer signaling exists because Population A clients vary in metadata-surfacing behavior (per ADR-032 §"Implementation mechanisms" — Aider surfaces some; OpenCode's per-provider tool framework may or may not; Cursor opaque). The redundancy serves the user's mental model: the configuration-honesty signal reaches the user regardless of which Population A client renders the response.

**Fitness:**
- **Fitness:** Every chat-completions response carries `X-LLM-Orc-Served-By` header + `metadata.served_by` body field with one of four canonical values (`ensemble:<name>`, `direct`, `direct_fallback`, `tool_choice:<ensemble>`) — verified by `test_response_labeling_three_layer_consistency_across_paths` (integration: dispatch path, direct path, dispatch-failure path, tool_choice path each produce expected three-layer signaling per ADR-032 §Sub-promise (1); the four canonical values exhaust the cases).
- **Fitness:** `tool_choice` bridge advisory fires conditionally per ADR-030 §Bridge advisory specification — headers + body metadata on all `tool_choice`-bearing responses; content-layer Rule 5-adjacent acknowledgment on `action: "direct"` responses only — verified by `test_tool_choice_bridge_advisory_conditional_content_layer` (integration: a `tool_choice`-bearing request producing `action: "dispatch"` carries headers + metadata but NO content-layer noise; a `tool_choice`-bearing request producing `action: "direct"` carries all three layers including content-layer acknowledgment).
- **Fitness:** `tool_choice: "auto"` is treated as equivalent to absent `tool_choice` — no bridge advisory fires at any layer — verified by `test_tool_choice_auto_treated_as_absent` (integration: request with `tool_choice: "auto"` produces standard response without `tool_choice_handling` signal).

### Module: Capability Discovery Endpoint *(new in Cycle 7 per ADR-032)* — L3

**Purpose:** OpenAI-protocol-compatible HTTP surface advertising the framework's loaded capability ensembles.
**Provenance:** ADR-032 §"Mechanism: Capability-list discovery" (first-order requirement, not documentation work — per Essay-Outline 006 §C6 E6.3.3); ADR-026 (AS-10 — discovery via the OpenAI surface clients already use).
**Owns:** The implementation of one of the three candidate surfaces per ADR-032 — `/v1/models` extension (existing endpoint adds capability ensemble entries with `type: "ensemble"` marker distinguishing them from model profiles), OR sibling endpoint `/v1/ensembles` (new endpoint dedicated to capability listing), OR response-metadata-only (capability list in chat-completion response metadata under a request flag). **BUILD picks at least one** of the three. The lowest-cost candidate is `/v1/models` extension; ADR-032 names sibling endpoint and metadata-only as alternatives; BUILD-phase design specifies the final shape and may ship more than one.
**Depends on:** Capability List Builder (single source of truth for capability ensembles).
**Depended on by:** External HTTP clients (not other internal modules).

**Fitness:**
- **Fitness:** Capability ensembles surface through the chosen discovery endpoint with `name + description + topaz_skill` metadata; the response reflects the live registry — verified by `test_capability_discovery_endpoint_lists_capability_ensembles_with_metadata` (integration: GET on the chosen endpoint returns the capability list with `topaz_skill` per ADR-015's 8-skill taxonomy; ensembles added/removed via reload appear/disappear).
- **Fitness:** Existing `/v1/models` model-profile listing continues to work — under `/v1/models` extension, model profiles remain in the response alongside ensemble entries; under sibling endpoint, `/v1/models` is unchanged — verified by `test_v1_models_listing_unchanged_or_extended_additively` (integration: GET `/v1/models` returns model-profile entries; if BUILD extended `/v1/models`, capability entries are additive — model-profile clients see no breaking change).

### Module: Orchestrator Runtime *(preserved as architectural option per Cycle 7 ADR-027 disposition (a))*

*Existing module retained per the ARCHITECT-phase disposition for conformance Finding 2 — disposition (a) "preserve as architecture-for-future-surfaces" selected (see ARCHITECT triage record below).*

**Status under Cycle 7 + ARCHITECT disposition (a):** Post-Cycle 7 BUILD, `OrchestratorRuntime` has no production caller. The class definition, the `OrchestratorLLM` Protocol, the `ToolDispatcher` Protocol, the `ContextObservationSink` Protocol, and the ReAct loop implementation are preserved as the implementation surface that any future llm-orc serving surface adopting the ReAct execution model (a Population B direct-ensemble HTTP API; an autonomous-orchestrator REST surface; a CLI extension that grows interactive ReAct capability) reaches for. ADR-001 + ADR-011 codify the ReAct model as architectural option; ADR-022's system-prompt amendment remains in the class's system-prompt construction path as dormant code preserved for re-introduction with any active caller.
**Cost calculus:** "Architectural-option preservation," not "actively-maintained dual surfaces." The class is not deprecated; it carries no scheduled maintenance burden beyond ordinary refactoring discipline as the surrounding codebase evolves. Re-introduction of an active caller — wiring `llm-orc invoke` to use it, or building a new surface — is a feature-cycle event with its own ARCHITECT phase. ADR-027 §Consequences §Negative explicitly accepts this cost.

### Module: Serving Layer *(extended in Cycle 7 per ADR-027 + ADR-030 + ADR-032)*

*Existing module; v3.0 entry above remains current.* Cycle 7 amendments replace the chat-completions handler's `OrchestratorRuntime` call path with Dispatch Pipeline invocation, extend `_ChatCompletionsRequest` to observe `tool_choice`, and compose Response Labeling decoration onto the chat-completion response body.

**Provenance additions:** ADR-027 (chat-completions handler routes via Dispatch Pipeline); ADR-030 §Bridge advisory specification (`tool_choice` observation at request-parsing boundary; the framework processes the request via ADR-027's pipeline normally — does NOT use `tool_choice` for routing in Cycle 7); ADR-032 (Response Labeling composition).
**Owns (additions):** The Dispatch Pipeline invocation surface; the `tool_choice` parameter observation at request-parsing boundary (Pydantic `_ChatCompletionsRequest` extended with the field); the Response Labeling composition onto the chat-completion response body; the conditional Capability Discovery Endpoint extension (if `/v1/models` extension is the BUILD-chosen surface).

**Fitness (additions per Cycle 7):**
- **Fitness:** The chat-completions handler's `OrchestratorRuntime` call path is replaced — `chat_completions()` does not call `_build_runtime()` or construct `OrchestratorRuntime` — verified by `test_chat_completions_handler_no_orchestrator_runtime_construction` (static AST check on `v1_chat_completions.py`: zero `OrchestratorRuntime(...)` constructions; zero `_build_runtime()` calls in the handler post-BUILD).
- **Fitness:** `_ChatCompletionsRequest` accepts `tool_choice` as an optional field — Pydantic no longer silently strips it — verified by `test_chat_completions_request_accepts_tool_choice_field` (unit: model construction with `tool_choice={"name": "code-generator"}` succeeds; integration: handler receives the parameter and propagates it to Response Labeling for bridge advisory emission).
- **Fitness:** Response Labeling decoration is composed onto every chat-completion response — header + body metadata signals are present on all paths (dispatch, direct, dispatch_fallback, tool_choice) — verified by `test_response_labeling_composed_on_all_response_paths` (integration: each path produces a response carrying both header and body metadata signaling per ADR-032).

### Module: Loop Driver *(new in Cycle 7 loop-back per ADR-033; extended in loop-back #2 per ADR-035; extended in loop-back #3 per ADR-036 — user-turn guidance composition + turn-shape stamping; extended in loop-back #5 per ADR-037 — judgment-first trailing composition)* — L2

**Purpose:** Drives a tool-driven client's multi-turn agentic loop — decides each turn's next agentic step and delegates per-turn content generation to a single capability ensemble (the callee).
**Provenance:** ADR-033 (specification — callee resolution of OQ #26); domain-model §Methodology Vocabulary "Layer-A loop-driver" + "Layer-B generation" + "Grounded loop"; AS-9 §Scope boundary (grounded-loop conditional acceptance under single-step enforcement); OQ #27 (two-axis grounded-loop validation); Spike σ (layer-A/B distinction), Spike τ/τ′ (grounding under enforcement), Spike υ (callee-vs-wrapper); ADR-035 (form-directive composition) + Spike χ/χ.2 (directive reliability n=4; deterministic-shaper fragility).
**Owns:** The multi-turn tool-driven control structure (the layer-A role no ADR-027 component held); the per-turn next-action decision (read / edit / write / bash / finish) conditioned on the conversation + the prior turn's observed tool result; the **tool-mapping decision** (deliverable type → client tool: `write` for new files, `edit` after a `read` for in-place changes, `bash` for commands — per ADR-034 §Decision 4 the choice is the Loop Driver's, the marshalling is the Terminal's); the **seat-filler LLM invocation** via a swappable Model Profile (ADR-011 pattern — the driver model fills the client's "model" seat; cheap-tier default, frontier-tier as the named axis-2 fallback); the **seat-filler contract** (BUILD-resolved, Amendment #13): each turn the seat-filler emits either a **client tool call** with literal arguments — passed through to the client **verbatim** (the grounded-carry path; a value observed in a prior tool result reaches the tool-call argument unchanged, FC-45) — or an **`invoke_ensemble`** internal call, the **per-turn callee delegation**: the driver dispatches a *single* capability ensemble via `OrchestratorToolDispatch.dispatch(InternalToolCall(name="invoke_ensemble", arguments={"name": <capability>, "input": ...}))` (not the `plan → dispatch → synthesize` pipeline) and marshals the deliverable into a client tool call; per-turn `TurnDecision` diagnostic event emission through the Dispatch Event Substrate (turn index, action, ensemble delegated, grounded-carry-held, re-plan-after-truncation) for axis-2 split-vs-callee diagnosis; the **wrapper-contingency delegation switch** — the per-turn generation path is single-ensemble (callee) by default and swappable to the full `DispatchPipeline.run()` (wrapper) without changing the control structure (FC-52; the second-order fallback after frontier-tier driver per the fallback ordering below); **form-directive composition (loop-back #2, ADR-035):** when the turn's callee delegation produces a client-tool-bound deliverable, the driver composes a destination-keyed bare-output directive via a named stateless helper (`compose_form_directive(tool)` — `write` → bare file bytes; `bash` → bare command; `edit` → bare replacement content) and injects it into the `invoke_ensemble` dispatch `input`, so the ensemble produces the deliverable already in client-tool form (FC-53/FC-54); the directive is composed per-dispatch by the framework, never baked into ensemble YAML (destination-agnostic preservation, ADR-035 decision 2); one dispatch is scoped to one client-tool deliverable, with multi-file work decomposed across successive write-turns — the granularity invariant's architectural home is the existing structure (Single-Step Enforcer guarantees one tool call per turn structurally; the directive scopes one deliverable per dispatch contractually — FC-55, no new module).
**Loop-back #3 extension (ADR-036 — delegation-decision mechanism):** the driver's seat-filler message composition relocates the delegation guidance from a framework system message to the **user-turn region** — on a first turn, attached to the user task; on trailing turns (tool-result tails), appended as a **standalone trailing user-role message** (the C3 form; C1/C2 are equally-measured alternatives, not violations — the structural property is user-role placement, FC-58). The composed request carries **no framework-authored system message**: the client's system prompt stands alone in the system region (the system slot measurably loses the attention contest — baseline 0/10 vs user-turn 55/55, Spikes ψ/ψ′; delegation is won by composition, not coerced — no model-layer forcing exists on this stack). The composition is internal to the framework ↔ seat-filler hop, never client-visible. The driver additionally stamps each turn's `turn_shape` onto its `TurnDecision` event (generation / carry / boundary-excluded — the delegation-rate denominator, FC-59). **Loop-back #6 amendment (WP-LB-M):** the shape is derived from the turn's *outcome*, not the driving instruction — a write (a delegated `ApplyWork` or a literal `write`/`edit` carry, the C1 inline-write) is `generation`; a read, command, or finish is `carry`; the Meter's instruction-side `classify_turn` owns only the `boundary_excluded` determination (repair-shaped / uncovered-domain, reliable from the instruction). Deriving the shape from the action is what lets the rate instrument multi-file and mixed sessions, not only first turns: a REMAINING delegated-write counts even though its descriptive anchor carries no generation verb, and a mixed-flow read does not count even though the user task framed a write. **Tool-list completeness constraint (ψ.4c):** the driver never dispatches a seat-filler request whose tool list excludes all plausible response tools for the turn shape — qwen3:14b answers a judged-incompatible tool list with an empty response (no tool call, no text); the constraint binds any future detect-and-retry implementation (must not narrow the tool list) and the capability-registration error path (FC-62).
**Loop-back #5 extension (ADR-037 — judgment-first trailing composition):** on every trailing turn (tool-result tail, no new user task) the driver's per-turn decision begins with a **framework-composed termination judgment** (call 1) before any guidance-composed action call: a bare-form request — framework-authored judge system message + one user message carrying the original user task (quoted as data), the digest rendered from the **Session Action Record**, and the **deliverable-accounting standard** (a successful write of a requested file counts as produced; code correctness explicitly out of scope — owned by the capability ensemble, calibration gate, and PLAY); no tools offered; no client system prompt (the judgment call is framework ↔ model, outside the client's attention contest — FC-63). **COMPLETE** → the judgment response, `VERDICT:` line stripped, returns as the text-only `FinishTurn` (the model is the stop mechanism: no tool calls, so the client ends the loop — FC-65). **REMAINING** → one second call composed per ADR-036's trailing form **plus the remaining-work anchor** (loop-back #6, ADR-038): the judge's `VERDICT:`-stripped remaining-work statement + the fixed imperative "Produce that next." are appended to the trailing region (a `remaining_anchor` param on `_seat_filler_messages`, threaded from `decide`'s REMAINING fall-through via `strip_verdict`), routing forward the signal ADR-037 discarded so the seat-filler advances to the next unproduced deliverable rather than re-revising file 1 (the Finding G multi-file-progress fix). Only the stripped statement + imperative carry forward — the judgment question, digest, and `VERDICT:` literal stay discarded (FC-66 **amended by ADR-038**: call 2 = the C3 form + anchor, no longer byte-equal to E4b; the context-bounding property holds). First turns and trailing turns carrying a new user task are untouched (ADR-036's merge branch stands). The judgment-message composition and `VERDICT:` parsing live as **named stateless helpers** (the `compose_form_directive` / guidance-composition pattern — unit-testable in isolation); the COMPLETE/REMAINING branch lives in `decide` (finish-vs-continue *is* the per-turn next-action decision — control flow stays in the control structure). The **judgment seat** is resolved as a Model Profile by Orchestrator Configuration, defaulting to the seat-filler's profile (a split judgment-seat profile is a config choice; re-validation is then per-seat with the instrument matching the seat — FC-68 composing with FC-60). Each trailing-turn decision stamps two new `TurnDecision` fields: `tail_kind` (first_turn / trailing_tool_result / new_user_task) and `judgment_verdict` (COMPLETE / REMAINING / None on non-trailing turns) — distinct from the meter's `turn_shape` field (generation / carry / boundary_excluded, FC-59), which is unchanged; together they make false-continue *and* false-stop shares computable from events alone (FC-67). The ADR-036 "no framework system message" property is **scoped to action-generation calls** by ADR-037's partial update: the judgment call carries a framework system message by design (the client's prompt is absent by construction); call 2 and first-turn composition retain FC-58 unchanged.
**Depends on:** Single-Step Enforcer (truncates the driver's proposed batch to one action before the action fires); Orchestrator Tool Dispatch (per-turn callee generation via the existing `dispatch()` chokepoint — calibration gate + tier-escalation router + autonomy interpositions fire as for any ensemble); Capability List Builder (which capability ensembles exist, for per-turn delegation selection); Budget Controller (per-turn turn/token enforcement across the multi-turn loop — AS-3); Dispatch Event Substrate (`TurnDecision` diagnostic events); Orchestrator Configuration (seat-filler Model Profile resolution per ADR-011); Delegation Rate Meter (per-turn turn-shape classification — `classify_turn`; the meter imports nothing from the Loop Driver, keeping the edge cycle-free); Session Action Record (loop-back #5 — records each emitted client-tool action at decision time, joins the client's per-call result on the next request, and provides the records the judgment composition renders into the digest; the record module imports nothing, keeping the edge cycle-free).
**Depended on by:** Serving Layer (engages the Loop Driver on the tool-driven surface — requests carrying client `tools[]`).
**Inversion Principle check:** The boundary serves the Tool User's mental model of a *parity* session (DISCOVER 2026-05-24): the User drives a tool-rich client against agentic-serving and gets the same kind of agentic session as against a normal single model — their own permission gates, diffs, and tool-result feedback intact, with the work generated by ensembles. The Loop Driver occupying the client's "model" seat is the structural realization of that mental model; the client cannot tell it is talking to a loop-driver-plus-ensembles rather than a single model (the OpenAI chat-completions contract is unchanged).

**Fitness:**
- **Fitness:** Per-turn generation routes to a single capability ensemble invocation, not the `plan → dispatch → synthesize` pipeline, on the tool-driven surface — verified by `test_loop_driver_delegates_to_single_ensemble_not_pipeline` (integration: a turn requiring generated content produces exactly one `invoke_ensemble` dispatch for a capability ensemble; zero routing-planner + zero response-synthesizer invocations on that turn). FC-44.
- **Fitness:** When a turn's action depends on a prior turn's observed tool result, the action uses the observed value, not a placeholder or fabricated value — verified by `test_loop_driver_grounded_carry_on_unbatchable_chain` (integration: the Spike τ probe shape — a value present only in a prior tool result — yields a tool-call argument carrying the observed value; a `${...}`-style unresolved template reference fails the test). FC-45.
- **Fitness:** The driver model is selected via a Model Profile; substituting it (cheap-tier ↔ frontier-tier) requires no change to the Loop Driver control structure, the Single-Step Enforcer, the Terminal, or the delegation path — verified by `test_seat_filler_swap_touches_only_configuration` (diff inspection: a driver-model swap edits only Orchestrator Configuration / profile files). FC-46.
- **Fitness:** A failing long-horizon (axis-2) run is diagnosable as split-incorrect vs. callee-incorrect — per-turn `TurnDecision` events distinguish "driver chose the wrong action" from "generation was wrong" — verified by `test_loop_driver_emits_turn_decision_diagnostics` (integration: each turn emits a `TurnDecision` event carrying action + delegated-ensemble + grounded-carry-held; the event stream reconstructs the trajectory). FC-51.
- **Fitness:** Every callee `invoke_ensemble` dispatch whose deliverable is bound for a client tool carries a destination-keyed form directive in its dispatch input — verified by `test_loop_driver_injects_form_directive` (integration: an apply-work turn's dispatch input contains the `write`-keyed directive; a client-tool-bound dispatch lacking the directive fails) + serve-log inspection on the real-client smoke test. FC-53.
- **Fitness:** The injected directive matches the decided destination tool — verified by `test_form_directive_destination_keying` (unit on the stateless helper: `write` → bare-file-bytes directive; `bash` → bare-command directive; `edit` → bare-replacement directive; a `write`-form directive on a `bash` dispatch fails). FC-54.
- **Fitness:** Every seat-filler dispatch on the tool-driven surface carries the delegation guidance in the user-turn region and no framework-authored system message — verified by `test_seat_filler_guidance_in_user_turn` (unit on message composition: first-turn shape and trailing-tool-result shape both place guidance in a user-role message; a composed request with guidance in a `role: "system"` message fails) + the real-client acceptance run's captured request bytes. FC-58.
- **Fitness:** Every `TurnDecision` event carries a `turn_shape` derived from the turn's outcome (WP-LB-M) — verified by the loop-driver stamping tests (a delegated or literal write emits `turn_shape=generation`, incl. a REMAINING delegated-write whose anchor carries no generation verb; a read turn emits `carry`, incl. a mixed-flow read under a write-framed task; a repair-shaped turn emits `boundary_excluded`) + the events-alone integration test (a trailing multi-file generation turn is counted). FC-59 (numerator+denominator co-location).
- **Fitness:** No seat-filler dispatch carries a tool list excluding all plausible response tools for the turn shape — verified by `test_seat_filler_tool_list_completeness` (unit: the composed tool list always includes the client tools; `invoke_ensemble`-only tool lists are unconstructable through the driver's composition path). FC-62.
- **Fitness:** Every trailing-tail interaction begins with the framework-composed termination judgment; a trailing-tail dispatch that goes straight to guidance-composed action generation fails — verified by `test_trailing_tail_judgment_first` (unit on `decide`: a tool-result-tail context produces the judgment dispatch before any guidance-composed call) + composed-request inspection on the real-client acceptance run. FC-63.
- **Fitness:** A COMPLETE verdict yields a text-only client response with no tool calls and no `VERDICT:` line — verified by `test_complete_verdict_yields_clean_finish` (unit: the stripped judgment text returns as `FinishTurn.content`; a response carrying `VERDICT:` or tool calls fails). FC-65.
- **Fitness:** A REMAINING verdict yields one action call composed per ADR-036's trailing form **plus the remaining-work anchor** (the judge's stripped statement + "Produce that next."), with the rest of the judgment exchange (question, digest, `VERDICT:` literal) excluded from its context — verified by `test_remaining_verdict_call2_form_preserved` + `TestRemainingWorkAnchor` (unit: call 2's trailing region carries the guidance + the judge's actual stripped statement + the imperative; no anchor on first-turn/new-user-task/parse-miss; no question/digest/verdict literal). FC-66 (amended by ADR-038; the byte-equality to E4b is intentionally broken — the anchor is the Finding G fix). ADR-038 introduces the anchor-presence and delegation-preserved-under-anchor FCs.
- **Fitness:** Every trailing-turn `TurnDecision` carries `tail_kind` and `judgment_verdict`; false-continue and false-stop shares are computable from emitted events alone — verified by `test_turn_decision_carries_finish_policy_fields` (integration: a COMPLETE trailing turn emits `tail_kind=trailing_tool_result, judgment_verdict=COMPLETE`; a first turn emits `tail_kind=first_turn, judgment_verdict=None`) + an events-alone share-computation test. FC-67.
- **Fitness:** The tool-driven surface enforces the AS-3 turn cap — a session reaching the cap terminates with a budget stop rather than continuing to dispatch — verified by `test_loop_driver_enforces_turn_cap` (integration: a session driven past the configured cap yields a budget-stop completion, not another seat-filler dispatch; the conformance-scan gap — `get_loop_driver` constructing no `BudgetController` — is the refutation shape). FC-69.

### Module: Single-Step Enforcer *(new in Cycle 7 loop-back per ADR-033)* — L2

**Purpose:** Guarantees single-action-per-turn structurally — truncates any batch the driver proposes to one action so grounding is a framework property, not a model assumption.
**Provenance:** ADR-033 §Decision 3 + §FC (single-step enforcement); domain-model §Concepts "Grounded loop" + AS-9 §Scope boundary; Spike τ (n=4 falsification of the unconstrained driver — it batches and presupposes unobserved state) + Spike τ′ (n=3 grounded pass under enforcement). **D2 resolution (ARCHITECT): batch-truncation** is the selected technique — the only candidate with direct spike evidence (τ′ used it), model-independent (so it does not change when the seat-filler swaps — the precondition for FC-46), and placeable as a stateless framework policy. The two untested candidates (re-planning prompt; one-tool `tool_choice` constraint) remain BUILD-tunable behind this module's boundary; `tool_choice` is the weakest (Spike κ: the framework does not forward `tool_choice`, and MiniMax did not honor it).
**Owns:** The truncation policy — given the driver's proposed action set for a turn, return the first client tool call and discard the rest; the structural guarantee that at most one client tool call is dispatched per assistant turn before its result is observed (re-planning happens on the next client request carrying the tool result). A stateless pure function (no turn-to-turn state), analogous to the Tier-Escalation Router's `select_tier`.
**Depends on:** None at the module-import level (a stateless policy the Loop Driver calls).
**Depended on by:** Loop Driver.
**Inversion Principle check:** The enforcer is invisible to the user and to the driver model — it is a framework guarantee, not a behavior the model is trusted to exhibit. This is the load-bearing distinction the loop-back established (Spike τ vs τ′): grounding is enforced, not assumed. The boundary serves correctness, not a user mental model; it is a structural backstop the Skill Orchestration User can rely on without defending against batch-planning of unobserved state (interaction-specs §Skill Orchestration User loop-back).

**Fitness:**
- **Fitness:** At most one client tool call is dispatched per assistant turn; additional tool calls the driver proposes in the same turn are not dispatched until the first call's result is returned and the driver re-plans — verified by `test_single_step_enforcer_truncates_batch` (integration: a driver response proposing two `write` calls produces exactly one dispatched tool call in the emitted response; the second is dropped, not queued). FC-43.

### Module: Artifact Bridge *(new in Cycle 7 loop-back per ADR-034; the F-ρ.1 marshalling step; extended in loop-back #2 per ADR-035 — form-gate seam)* — L2

**Purpose:** Reads an ensemble's server-side deliverable and marshals its content into the client tool-call content argument with fidelity.
**Provenance:** ADR-034 §Decision 3 + §FC (artifact-bridge fidelity); domain-model §Methodology Vocabulary "Artifact-bridge" (**distinct from the ADR-030 "Bridge mechanism"** per MODEL snapshot Advisory 5); Spike ρ finding F-ρ.1 (production capability ensembles carry `output_substrate: artifact` per ADR-025 — deliverables route server-side by design, so the terminal must bridge artifact-store → tool-call content); ADR-025 (artifact-as-substrate).
**Owns:** The marshalling step — for a substrate-routed deliverable (`output_substrate: artifact`), reads the deliverable via `SessionArtifactStore.read_deliverable(reference)` (the new accessor — advisory #4) and places its content in the tool-call `content` argument; for an inline-response deliverable (`output_substrate: inline`), reads `envelope.primary` directly (the bridge step is a no-op); the artifact-vs-inline branch; the fidelity contract (content equals the stored deliverable, not a summary or paraphrase — AS-7's summarization is upstream and does not apply to the bridge). The bridge is **deterministic framework code, not an LLM generation** — the bytes-to-tool-call step does not reintroduce the orchestrator-LLM confabulation failure mode (PLAY note 22). **Form-gate interface point (loop-back #2, ADR-035 decision 4 + DECIDE snapshot advisory 2):** `marshal()` receives the turn's destination tool from the Terminal (contract extension — the Terminal→Bridge shared types gain `destination_tool`); a named **FormGate** seam evaluates the marshalled content against the destination's form before emission, defaulting to pass-through; an optional conservative normalization (strip a single enclosing code fence) is a BUILD open choice; the **detect-and-refuse escalation** (refuse to emit a clearly non-bare deliverable; degrade to a dispatch-failure completion or one re-dispatch) installs at this seam on PLAY evidence without changing the Terminal→Bridge contract (FC-57). The gate never attempts heuristic multi-fence extraction (Spike χ F-χ.1 — that path is rejected as fragile). Form-gating composes with, and does not weaken, the fidelity contract: the gate may *refuse* or apply the defined single-fence normalization; it never paraphrases or summarizes.
**Depends on:** Session Artifact Store (`read_deliverable()` — the first read-side API on an otherwise write-only store).
**Depended on by:** Client-Tool-Action Terminal.
**Inversion Principle check:** The bridge exists because ADR-025 routes production deliverables server-side by architecture — the disjoint-filesystem problem is *designed in*, not incidental, and does not dissolve under co-location (Spike π Phase A established that a co-located direct write fails parity). The boundary serves the client's execution model: the deliverable returns through a client-executed tool call, so the client observes and owns the write.

**Fitness:**
- **Fitness:** The content marshalled into a tool call equals the ensemble's deliverable as stored in the `SessionArtifactStore` (or `envelope.primary` for inline ensembles), not a paraphrase or summary — verified by `test_artifact_bridge_marshals_deliverable_with_fidelity` (integration: a substrate-routed deliverable round-trips through `read_deliverable()` into a `write` tool call whose `content` byte-matches the stored artifact; an inline deliverable round-trips through `envelope.primary`). Fidelity at scale (large files, encoding edge cases) is BUILD-scope validation — spike evidence is limited to trivially small content (`hello.py` / `calc.py`). FC-49.
- **Fitness:** The bridge's marshal surface receives the destination tool and exposes the named FormGate seam (default pass-through); installing the detect-and-refuse gate touches only the seam — verified by marshal-signature inspection + `test_form_gate_seam_default_passthrough` (unit: the default gate is identity) + diff inspection on gate installation (0 Terminal edits). FC-57.

### Module: Delegation Rate Meter *(new in Cycle 7 loop-back #3 per ADR-036)* — L2

**Purpose:** Owns the delegation-rate measurement — deterministic turn-shape classification (the denominator) and rate computation over `TurnDecision` events (the meter).
**Provenance:** ADR-036 §Decision 3 (delegation-rate instrumentation; refutation threshold); domain-model §Methodology Vocabulary "Generation-shaped turn" + "Delegation rate" (Amendment #17); Spike ψ.4a (the graduated rule — 0/12 clear-case errors; known boundary: repair-shaped tasks + uncovered content domains); Spike ψ′ Arm D (profile-boundedness — the reason the meter is the regression-visibility mechanism, not optional telemetry); conformance-scan loop-back #3 F-3 (graduation from `scratch/spike-psi-delegation-rate/psi4a_prefilter.py`).
**Owns:** Generation-shaped turn (concept — classification); Delegation rate (concept — computation). `classify_turn(message, observed_values, capability_domains) -> TurnShape` — the deterministic rule (generation verb × content object × capability-domain overlap × observed-carry exclusions) over **raw turn inputs**, no LLM call; boundary turns (repair-shaped; uncovered domains) classify as `boundary_excluded` — excluded from the denominator, never guessed, with the boundary-excluded share itself observable (the denominator-degradation signal; re-examined at PLAY and on every capability-ensemble registration, since each registration widens the capability-domain term). `delegation_rate(events) -> RateReading` — numerator = events with `delegated_ensemble` set; denominator = events with `turn_shape=generation`; computed over a rolling window from events alone (FC-59). The provisional production threshold (sustained < 0.9 / 24h rolling; ≥25 generation-shaped-turn qualifying soak window) lives in ADR-036 and is practitioner-revisable; the meter computes, policy interprets.
**Depends on:** None at the module-import level — `classify_turn` is a pure function over raw inputs; `delegation_rate` consumes events through a narrow structural protocol (`turn_shape` + `delegated_ensemble` attributes), importing neither the Loop Driver nor the sinks. This is what keeps the Loop Driver → Meter edge cycle-free.
**Depended on by:** Loop Driver (per-turn `classify_turn` for `TurnDecision.turn_shape` stamping); Operator-Terminal Event Sink (rate computation for operator surfacing).
**Inversion Principle check:** The boundary serves the Operator's mental model directly — "is delegation happening?" is an operator question, and a *meter* is the operator-voice answer (interaction-specs §Watch the delegation-rate meter). The measurement concern is separated from the control structure (Loop Driver) precisely so that the thing watching the bet is not the thing making it.

**Fitness:**
- **Fitness:** The graduated classifier reproduces the spike's labeled-set results — 0 misclassifications on the 12 clear cases — as a package regression test — verified by `test_turn_shape_classifier_labeled_set` (unit: the ψ.4a labeled set ships as fixtures; clear-case misses fail; the 4 ambiguous boundary cases assert `boundary_excluded` or their recorded disposition). FC-59.
- **Fitness:** `delegation_rate` is computable from emitted events alone — verified by `test_delegation_rate_from_events_alone` (integration: a synthetic event stream of `TurnDecision`s yields the expected rate with no access to request content, logs, or replay). FC-59.
- **Fitness:** Classification is deterministic — identical inputs yield identical `TurnShape` across runs and processes — verified by property-style repetition in the labeled-set test (no RNG, no LLM, no clock in the rule). FC-59.

### Module: Client-Tool-Action Terminal *(new in Cycle 7 loop-back per ADR-034)* — L3


**Purpose:** Emits the deliverable as a `tool_calls` response the client executes locally, and participates in the client's multi-turn tool loop.
**Provenance:** ADR-034 §Decision 1 (tool-call terminal) + §Decision 2 (multi-turn loop participation) + §FC (tool-call terminal; no server-side client-filesystem write; loop participation); domain-model §Concepts "Client-tool-action terminal" + "Parity"; Essay-Outline 006 Amendment B §C8 (text-only terminal fails parity — settled rejection); Spike π Phase B + Spike ρ (`tool_calls` round-trip achieves parity); Spike π Phase 0 (OpenCode streaming tool-call delta shape).
**Owns:** The tool-call emission on the tool-driven surface — re-introduces the four pieces commit `0a7a822` removed (the `ClientToolCall` import; the `tool_calls` field on `_NonStreamingResult`; the `isinstance(chunk, ClientToolCall)` branch in `_collect_non_streaming`; the `message["tool_calls"]` + `content: null` shaping in `_build_completion_body`) — on the tool-driven path only; the OpenAI streaming tool-call delta shape (`delta.tool_calls[].function.arguments` fragments + `finish_reason: "tool_calls"`) emitted via the existing `OpenAiSseFormatter` (the formatter's `ClientToolCall` case was retained by `0a7a822` — reused, not rebuilt); **multi-turn loop participation** — consumes the incoming request's trailing `role: "tool"` message (the client's tool result) and routes it to the Loop Driver for the next-turn decision (the current `_extract_request` reads only the last `role: "user"` message and drops tool results — the terminal's message routing must not call it or must extend it).
**Depends on:** Artifact Bridge (resolves the deliverable reference into tool-call content; per ADR-035 the Terminal passes the turn's destination tool so the FormGate seam has form context); Loop Driver (receives the turn's tool-mapping decision — *which* tool — via the Serving Layer; the Terminal owns the *emission mechanism*, the Loop Driver owns the *choice*).
**Depended on by:** Serving Layer (composes the terminal onto the chat-completion response on the tool-driven surface).
**Inversion Principle check:** The terminal emits `tool_calls` rather than writing server-side because the client's *execution model* (not filesystem geography) is load-bearing — the client drives and observes its own filesystem through its own tool calls. Spike π Phase A established that a co-located server-side direct write fails parity even when the bytes land. The boundary serves the Tool User's parity mental model directly.

**Fitness:**
- **Fitness:** A turn whose Loop Driver action is "apply work to the client" produces a response with `finish_reason: "tool_calls"` carrying a client tool call, not a text-only `Completion` — verified by `test_terminal_emits_tool_calls_not_text` (integration: an apply-work turn yields `ClientToolCall` → `finish_reason: "tool_calls"`; a turn returning only `ContentDelta` + `Completion` fails). FC-47.
- **Fitness:** The surface never writes to the client's filesystem directly; deliverables reach the client only via client-executed tool calls — verified by `test_no_server_side_client_filesystem_write` (static + integration: no server-side write to a client workspace path; the Spike π Phase A shape is absent — satisfied by absence today, guarded going forward). FC-48.
- **Fitness:** The surface consumes a `role: "tool"` follow-up and produces a next-turn decision; it does not treat tool-call emission as terminal — verified by `test_terminal_consumes_tool_result_and_continues` (integration: a follow-up request carrying a `role: "tool"` message routes the result to the Loop Driver, which decides the next action or finishes; a surface that drops the tool message fails). FC-50.

### Module: Serving Layer *(extended in Cycle 7 loop-back per ADR-033 + ADR-034)*

*Existing module; Cycle 7 first-pass entry above remains current.* The loop-back adds **surface-mode discrimination** (D1) and tool-driven-surface composition.

**Provenance additions:** ADR-033 §Decision 1 (surface-mode discrimination by client-tools presence); ADR-034 (composes the Client-Tool-Action Terminal on the tool-driven surface).
**Owns (additions):** The **surface-mode discriminator** — a named guard (`len(request.tools) > 0`) that engages the Loop Driver for tool-driven requests and the Dispatch Pipeline (ADR-027 single-turn) for non-tool requests. **D1 resolution (ARCHITECT):** the discriminator lives as a named function in the Serving Layer (which already owns `/v1/chat/completions` request routing), not a separate module — it is one branch tightly coupled to request parsing, and a dedicated module would own a single responsibility (FC-1 smell). **Advisory 2 (validate-not-assumed):** the discriminator *signal validity* — whether `tools[]` presence is the right surface discriminator (a tool-capable client might send `tools[]` for bookkeeping/introspection without expecting an agentic loop) — is a production-traffic confirmation item, not a settled spike result. The safe edge case (a tool-capable client wanting a plain answer on a turn) is handled by the Loop Driver finishing with a text completion, so engaging the driver when tools are present is safe. The tool-driven-surface composition (Loop Driver invocation + Client-Tool-Action Terminal composition).

**Fitness (additions per Cycle 7 loop-back):**
- **Fitness:** A chat-completions request carrying client `tools[]` engages the Loop Driver; a request carrying no client tools engages the Dispatch Pipeline (single-turn) — verified by `test_surface_mode_discrimination_routes_by_client_tools` (integration: a request with `tools[]` produces a `TurnDecision`/loop-driver event; a request without `tools[]` produces `PlanEmitted` pipeline events; the two surfaces are mutually exclusive per request). FC-42.

### Module: Session Artifact Store *(extended in Cycle 7 loop-back per ADR-034)* — L1

*Existing module (Cycle 6 per ADR-025); the loop-back adds the read side.* The store was write-only from the serving surface's perspective (`write_deliverable`, `cleanup_session`, `cleanup_ephemeral`). ADR-034's artifact-bridge requires a read accessor.

**Owns (addition):** `read_deliverable(reference: ArtifactReference) -> str | bytes` — resolves `reference.path` relative to the store root and returns the deliverable content. The first read-side API addition (advisory #4 — the highest-priority BUILD design dependency from the conformance scan: the terminal chain cannot close without it). Scope note: fidelity at scale (large files, binary-ish content, encoding edge cases) is BUILD-scope validation, not an assumed property — spike evidence used trivially small content.

### Module: Dispatch Pipeline *(RETIRED by ADR-043, loop-back #9, 2026-06-18)* — L2

> **⊘ Retired.** ADR-043 collapsed the two serving surfaces to one; this module is deleted (`dispatch_pipeline.py` removed). Section preserved as historical record.

*Existing module (Cycle 7 first-pass per ADR-027); the loop-back scopes its surface.* Per ADR-027's `> Updated by ADR-033` partial-update header, the `plan → dispatch → synthesize` pipeline is now scoped to the **single-turn (non-tool-driven) answer-a-question surface and the layer-B generation role**. Its internals are unchanged; the surface-mode discriminator (Serving Layer) routes only non-tool requests to it. No module-decomposition change; the scoping is recorded so BUILD does not route tool-driven requests through it.

### Module: Orchestrator Tool Dispatch *(extended in Cycle 7 loop-back #2 per ADR-035 — D1 extraction fix)*

*Existing module (Cycle 6 entry above remains current).* Loop-back #2 fixes the substrate-write deliverable extraction (Finding D, D1): `_extract_synthesizer_text` succeeds only for a populated top-level `synthesis` — never set in the dependency-based model (`results_processor.py:21`) — or a single-agent ensemble, so every multi-agent capability ensemble's deliverable falls through to `json.dumps(raw_result)` (Spike φ; the WP-LB-G real-client run is the observed failure).

**Owns (fix):** Terminal-deliverable extraction — store the last *successful* agent's output as the deliverable; fall back across failed terminal nodes (the synthesizer can time out — Spike χ-P1); never store the raw result dict when a successful agent exists (FC-56). *(BUILD resolution 2026-06-03, WP-LB-H — the where-sub-fork resolved **executor-side**, practitioner-framed as "the ensemble abstraction presents a single output": `resolve_deliverable` in `core/execution/results_processor.py` computes the unique terminal node from `depends_on` at `_finalize_execution_results` and populates the `ExecutionResult.deliverable` contract field; the `ExecutionHandler.invoke` projection carries it; this module's substrate write reads the contract — `_resolved_deliverable` — and falls to `json.dumps` only when no agent succeeded. The legacy `synthesis` field and `_extract_synthesizer_text` were excised entirely: the deliverable contract is the system's single output notion, consumed also by the Result Summarizer Harness, Calibration Gate checker extraction, structured-output parse, calibration artifact-content, and the streaming completion event.)*

**Fitness:**
- **Fitness:** A multi-agent capability ensemble's stored deliverable equals the last successful agent's output (code-generator's `synthesizer`; `coder` when the synthesizer fails), never `json.dumps(raw_result)` when at least one agent succeeded — verified by `test_substrate_write_stores_terminal_agent_output` + `test_extraction_falls_back_on_failed_terminal_node` (integration against the real `SessionArtifactStore`). FC-56.

### Module: Session Action Record *(new in Cycle 7 loop-back #5 per ADR-037)* — L1

**Purpose:** Accumulates the framework's per-session action records — the evidence base the termination judgment reads.
**Provenance:** ADR-037 §Decision 2 (the framework-owned digest — the evidence-base guarantee) + §Consequences (digest expressiveness is the mechanism's reliability ceiling — practitioner pre-mortem at the loop-back #5 gate: *"failure looks like not being able to track everything we need to track in the meta-context"*); domain-model §Methodology Vocabulary "Framework-owned digest" (Amendment #18 — the module name derives from that entry's "per-action records"); Spike θ F-θ.1/F-θ.2 (the judgment's accuracy lives in its evidence base and standard — round 1 measured the failure of reconstruction from client-serialized messages alone); conformance-scan loop-back #5 V-03 (the gating-condition pre-condition: no session-scoped action record exists in the codebase).
**Owns:** Framework-owned digest (concept — the records and their schema). The per-session action-record store: `record_action(session_id, action_kind, target_path)` at the driver's decision/emission time; `join_result(session_id, tool_result)` when the client's per-call `role: "tool"` result arrives on the next request; `records(session_id)` — the accumulated (action kind, target file path, result) tuples the judgment composition renders into the digest. Records derive from the framework's own emission records joined with client results — **never reconstructed from client-serialized messages alone** (FC-64; round 1 measured exactly that reconstruction's failure: the client serialization drops what was written). The record schema is the **extensible meta-record seam**: the committed write-log fields (action kind, path, result) are the first increment, not the final form; the false-stop share (FC-67) is the extend-on-evidence enrichment trigger. Lifecycle rides session scope (cleared with the session, the Session Artifact Store retention pattern).
**Depends on:** None at the module-import level — a pure session-scoped store (no LLM, no clock semantics beyond insertion order, no imports of driver or sinks).
**Depended on by:** Loop Driver (records at decision time; joins results on the next request; renders the digest for the judgment call).
**Inversion Principle check:** The boundary serves the practitioner's meta-reasoning mental model directly (the loop-back #5 gate response: being able to reason at the meta-level *by virtue of the framework*). The framework records what happened; the model reasons over the record; neither substitutes for the other (ADR-037's three-layer scope-of-claim, Layer 1/Layer 2 split). Distinct from Session Artifact Store by the Purpose Test: the store persists deliverable *content*; this module records *what was done* — different audiences (judgment evidence vs. deliverable retrieval) and different change rates (the record schema is expected to grow on evidence; the store's layout is stable).

**Fitness:**
- **Fitness:** The digest's action records derive from the framework's own dispatch/emission records joined with client tool results; a digest reconstructed from client-serialized messages alone fails — verified by `test_digest_provenance_from_framework_records` (integration: a session's records carry the target path from the framework's emitted tool call even when the client's serialized messages omit it — the round-1 information-starvation shape is the refutation) + code inspection (no digest composition path reads `context.messages` as its record source). FC-64.
- **Fitness:** Extending the action-record schema with a new field touches only this module (record construction + record provision); the Loop Driver's judgment composition and all other callers are untouched — verified by diff inspection at the first enrichment event (the FC-46 pattern applied to the meta-record seam).

### Module: Orchestrator Configuration *(extended in Cycle 7 loop-back #5 per ADR-037 — judgment-seat profile resolution)*

*Existing module (L3; Cycle 4 + Cycle 6 entries above remain current).* Loop-back #5 adds **judgment-seat Model Profile resolution**: the termination judgment's model seat resolves as a Model Profile (ADR-011 pattern), defaulting to the seat-filler's profile. When the seats share a profile, one recorded re-validation run covers both instruments (ADR-036's ψ′-harness delegation arms AND the θ-harness judgment arms); a split judgment-seat profile is a configuration choice that makes re-validation per-seat with the instrument matching the seat (FC-68 composing with FC-60). The hosted judgment seat (Layer 3 of ADR-037's scope-of-claim — a ~10× latency optimization at measured-indistinguishable accuracy) is an optimization slot with the local path as standing fallback (the practitioner's paid/local scoping principle); adopting it is a profile-resolution config change, not a structural change.

Every concept and action from the agentic-serving domain model and the touched project-level concepts maps to exactly one owning module. Inherited project-level concepts that are not touched by agentic serving live with the existing Ensemble Engine (listed once at the bottom).

| Domain Concept / Action | Owning Module | Provenance |
|------------------------|---------------|------------|
| Orchestrator Agent | Orchestrator Runtime | Domain model; ADR-001 |
| Session | Session Registry | Domain model; ADR-005, ADR-008, ADR-011 |
| Serving Layer | Serving Layer | Domain model; ADR-002 |
| Orchestrator Tool | Orchestrator Tool Dispatch | Domain model; ADR-003 |
| Routing Decision | Orchestrator Runtime (generation); Plexus Adapter (persistence) | Domain model; ADR-009 |
| Dynamic Invocation | Orchestrator Tool Dispatch | AS-1; domain model |
| Composition | Composition Validator | Domain model; AS-2, ADR-006 |
| Primitive | Ensemble Engine (existing — role played by existing concepts) | AS-6; ADR-006 |
| Library | Ensemble Engine (existing — config manager) | Domain model |
| Budget | Budget Controller | AS-3; ADR-005 |
| Result Summarization | Result Summarizer Harness | AS-7; ADR-004 |
| Conversation Compaction | Orchestrator Runtime | Essay 001 §Context Management; AS-7 |
| Context Injection | Serving Layer (function `resolve_session_start_context`) | ADR-009 (structurally reserved via typed function signature) |
| Ingestion | Plexus Adapter | AS-4; ADR-010 |
| Enrichment | Plexus Adapter (invocation); Plexus lib (performance) | AS-4; ADR-010 |
| Quality Signal | Calibration Gate (generation); Plexus Adapter (persistence when active) | AS-5; ADR-007 |
| Stabilization | Plexus Adapter (emergent; surfaced via queries) | AS-5; ADR-007 |
| Bootstrapping | Bootstrapping Pipeline | ADR-010; DISCOVER FF #9, #14 |
| Autonomy Level | Autonomy Policy | ADR-008 |
| Calibration | Calibration Gate | ADR-007 |
| Route (action) | Orchestrator Runtime | Domain model |
| Compose (action) | Orchestrator Tool Dispatch → Composition Validator | ADR-006 |
| Invoke (Dynamic) (action) | Orchestrator Tool Dispatch → Ensemble Engine | AS-1 |
| Summarize (action) | Result Summarizer Harness | ADR-004 |
| Compact (action) | Orchestrator Runtime | Essay 001 §Context Management |
| Inject (action) | Serving Layer (function) | ADR-009 |
| Ingest (action) | Plexus Adapter | ADR-010 |
| Enrich (action) | Plexus Adapter (invocation) | ADR-010 |
| Query (action) | Orchestrator Tool Dispatch → Plexus Adapter | ADR-009 |
| Record (action) | Orchestrator Tool Dispatch → Plexus Adapter | ADR-009 |
| Calibrate (action) | Calibration Gate | ADR-007 |
| Stabilize (action) | (emergent — not owned by a single module) | AS-5 |
| Bootstrap (action) | Bootstrapping Pipeline | ADR-010 |
| Ensemble, Agent, AgentConfig, Model Profile, Inline Model, Dependency, Phase, Fan-Out, Input Key, Ensemble Reference, Ensemble Reference Graph, Depth, Depth Limit, Artifact, Child Executor, Immutable Infrastructure, Mutable State, Agent Discriminator; Load, Validate, Discriminate, Execute, Dispatch, Recurse, Fan Out, Gather, Select, Detect Cycles, Check Depth, Merge Profile | Ensemble Engine (existing) | Project-level domain model |
| **Conversation Compaction** *(re-allocated from Orchestrator Runtime)* | **Conversation Compaction** *(new in Cycle 4)* | ADR-012; essay 005 §"Long-Horizon Reliability" |
| Calibration verdict | Calibration Gate (extended) | ADR-014; domain-model §Concepts |
| Tier-escalation router | Tier-Escalation Router (new) | ADR-015; domain-model §Concepts |
| Per-skill tier default | Tier-Escalation Router (new) | ADR-015; domain-model §Concepts |
| Topaz skill profile | Tier-Escalation Router (schema owner); Ensemble Engine (existing — YAML metadata field parsing via existing config manager) | ADR-015; domain-model §Methodology Vocabulary |
| Trajectory feature | Calibration Gate (extended) | ADR-014; domain-model §Methodology Vocabulary |
| In-process trajectory-level calibration | Calibration Gate (extended) | ADR-014; domain-model §Methodology Vocabulary |
| Structured-handoff artifact | Session Registry (extended) | ADR-013; domain-model §Concepts |
| Write-gate validation | Session Registry (extended) | ADR-013; domain-model §Concepts |
| Cluster determination at session-start | Session Registry (extended) | ADR-013; domain-model §Concepts |
| Tool-call structural validation guard | Orchestrator Tool Dispatch (extended) | ADR-017; domain-model §Concepts |
| Phantom tool-call (failure mode the guard detects) | Orchestrator Tool Dispatch (extended) | ADR-017; domain-model §Concepts |
| Cross-layer calibration channel | Calibration Signal Channel (new; conditional acceptance) | ADR-016; domain-model §Methodology Vocabulary |
| Bounding mechanisms (a)–(e) | Calibration Signal Channel (new; conditional acceptance) — owns mechanism implementations | ADR-016; domain-model §Methodology Vocabulary |
| **Liveness signal** *(new in Cycle 6)* | **Operator-Terminal Event Sink** *(new in Cycle 6)* | ADR-023; domain-model §Concepts |
| **Dispatch timing** *(new in Cycle 6)* | **Dispatch Event Substrate** *(new in Cycle 6)* — owns the `DispatchTiming` event type | ADR-023; domain-model §Concepts |
| **Routing surface behavior** *(new in Cycle 6)* | **Orchestrator Configuration** *(extended)* — owns the system prompt that shapes the behavior; effectiveness empirically per-profile-conditional per ADR-022 disposition (iii) | ADR-022; domain-model §Concepts |
| **Artifact-as-substrate** *(new in Cycle 6)* | **Session Artifact Store** *(new in Cycle 6)* | ADR-025; domain-model §Concepts |
| **Common I/O envelope** *(new in Cycle 6)* | **Orchestrator Tool Dispatch** *(extended)* — owns the `DispatchEnvelope` shared type as producer; defined in shared types module | ADR-024; domain-model §Methodology Vocabulary |
| **Infrastructure-complete / routing-incomplete** *(Cycle 6 — methodology vocabulary; addressed structurally)* | (no single module — addressed by Dispatch Event Substrate + Operator-Terminal Event Sink + Orchestrator-Context Event Sink jointly) | ADR-023; domain-model §Methodology Vocabulary |
| **Bilateral visibility absence** *(Cycle 6 — operator-experience concept)* | (operator-experience term; addressed by the two destination sinks jointly) | ADR-023; domain-model §Concepts |
| **Information-finding overhead** *(Cycle 6 — practitioner-voice metric)* | (operator-experience term; partially addressed by the ADR-022 system-prompt amendment and ADR-025 substrate-routing which together reduce the orchestrator's information-gathering loops) | domain-model §Concepts |
| **Explicit-naming bypass** *(Cycle 6 — failure mode)* | (failure mode; addressed by the ADR-022 system-prompt amendment owned by Orchestrator Configuration) | ADR-022; domain-model §Concepts |
| **Output-spec drift** *(Cycle 6 — failure mode)* | (failure mode; addressed by ADR-024's typed `structured` field + advisory `output_schema:` owned by Ensemble Engine schema extension; spike β reframed root cause as orchestrator's `input.data` override — a separate concern outside Cycle 6 scope) | ADR-024; spike β; domain-model §Concepts |
| **Emit (tool call)** *(action, new in Cycle 6 candidate-to-settled)* | **Operator-Terminal Event Sink** *(new)* — owns `emit_tool_call_log` | ADR-023; domain-model §Actions |
| **Heartbeat** *(action, new in Cycle 6 candidate-to-settled)* | **Operator-Terminal Event Sink** *(new)* — owns `emit_heartbeat`; Serving Layer tracks open-request inactivity duration | ADR-023; domain-model §Actions |
| **Route event** *(action, new in Cycle 6 candidate-to-settled)* | **Dispatch Event Substrate** *(new)* — owns the `emit()` fan-out routing dispatcher | ADR-023; domain-model §Actions |
| **Routing-planner ensemble** *(new in Cycle 7)* | **Routing Planner** *(new in Cycle 7)* — owns the invocation surface, JSON output parsing into `DispatchPlan`, the bonus-path layering hooks, and the production ensemble YAML | ADR-028; domain-model §Concepts |
| **Response-synthesizer ensemble** *(new in Cycle 7)* | **Response Synthesizer** *(new in Cycle 7)* — owns the invocation surface, the structured input serialization, the strict-fidelity rule set's content-layer commitments (Rules 1-6), the streaming output, and the production ensemble YAML | ADR-029; domain-model §Concepts |
| **Plan** *(action, new in Cycle 7)* | **Routing Planner** *(new)* — owns the planner's invocation; the `Plan` action's actor is the routing-planner ensemble per AS-9 §Propagation | ADR-028; domain-model §Actions |
| **Synthesize (response)** *(action, new in Cycle 7)* | **Response Synthesizer** *(new)* — owns the synthesizer's invocation; the action's actor is the response-synthesizer ensemble per AS-9 §Propagation | ADR-029; domain-model §Actions |
| **Strip (`tool_choice` at input)** *(action, current behavior pre-bridge; superseded by bridge mechanism per ADR-030)* | **Serving Layer** *(extended)* — owns the Pydantic `_ChatCompletionsRequest` model; Cycle 7 extends the model to OBSERVE `tool_choice` (not strip) so the bridge mechanism can emit advisory signaling | ADR-030; domain-model §Actions |
| **Transparent OpenAI-compatible endpoint** *(new in Cycle 7)* | **Serving Layer** — IS the endpoint; the OpenAI-protocol contract on `/v1/chat/completions` + `/v1/models` is owned by the Serving Layer | ADR-026; domain-model §Concepts |
| **Bridge mechanism** *(new in Cycle 7; provisional, pending disposition (i) implementation in follow-on cycle per ADR-030)* | **Serving Layer** *(extended; `tool_choice` observation)* + **Response Labeling** *(new; three-layer advisory emission)* — joint ownership: observation at Serving Layer; advisory emission at Response Labeling | ADR-030; domain-model §Concepts |
| **`tool_choice` strip-at-input** *(new in Cycle 7; current framework behavior pre-bridge — superseded by the bridge mechanism)* | (Behavior superseded — see Bridge mechanism row; **Serving Layer** previously silently stripped per Spike κ D0; Cycle 7 BUILD extends `_ChatCompletionsRequest` to observe rather than strip) | ADR-030; Spike κ D0; domain-model §Concepts |
| **Population A** *(new in Cycle 7; stakeholder model — not a code-level concept)* | (Stakeholder model — owned by `product-discovery.md` Stakeholder Map; not allocated to a single module) | ADR-026; domain-model §Concepts |
| **Population B** *(new in Cycle 7; stakeholder model — not a code-level concept)* | (Stakeholder model — owned by `product-discovery.md` Stakeholder Map; Population B accommodation surface is owned by **Response Labeling** via the structured advisory mechanism) | ADR-026 + ADR-032; domain-model §Concepts |
| **Cost-distribution lens** *(new in Cycle 7; project-developer framing — not a code-level concept)* | (Project-developer framing — owned by `product-discovery.md` Cycle 7 update; the cost-distribution-accountability sub-promise the framing motivates is owned by **Dispatch Pipeline** + **Routing Planner** jointly via strict-dispatch-when-capability-matched) | ADR-032; domain-model §Concepts |
| **Framework-driven dispatch pipeline** *(new in Cycle 7; methodology vocabulary — the named architecture)* | **Dispatch Pipeline** *(new)* — IS the framework-driven dispatch pipeline; the methodology-vocabulary entry names the architecture Dispatch Pipeline implements | ADR-027; domain-model §Methodology Vocabulary |
| **Structurally-bounded role** *(new in Cycle 7; methodology vocabulary — architectural property)* | (Architectural property — addressed structurally by **Routing Planner** + **Response Synthesizer** jointly; AS-9's structural-bounding property is satisfied by both modules' role-shape commitments) | AS-9; ADR-028 + ADR-029; domain-model §Methodology Vocabulary |
| **Orchestrator-designs-ensembles** *(new in Cycle 7; methodology vocabulary — north-star context, NOT Cycle 7 scope)* | (North-star context — owned by `product-discovery.md` Cycle 7 update; NOT a Cycle 7 deliverable; future-cycle territory) | domain-model §Methodology Vocabulary |
| **Capability list** *(implicit concept introduced by ADR-028 + ADR-032)* | **Capability List Builder** *(new in Cycle 7)* — owns the canonical capability list shape and production logic | ADR-028 §Input contract + ADR-032 §Capability-list discovery |
| **Layer-A loop-driver** *(new in Cycle 7 loop-back)* | **Loop Driver** *(new)* — owns the multi-turn control structure, the per-turn next-action decision, and the seat-filler LLM invocation | ADR-033; domain-model §Methodology Vocabulary |
| **Layer-B generation (per-turn callee delegation)** *(new in Cycle 7 loop-back)* | **Loop Driver** *(new)* — owns the delegation of per-turn generation to a single capability ensemble (the generation itself runs in the Ensemble Engine via Orchestrator Tool Dispatch) | ADR-033 §Decision 2; domain-model §Methodology Vocabulary |
| **Tool-mapping (deliverable type → client tool)** *(new in Cycle 7 loop-back)* | **Loop Driver** *(new)* — owns the choice of `write` / `edit` / `bash` / `read` per ADR-034 §Decision 4 (the Terminal owns the emission mechanism) | ADR-034 §Decision 4 |
| **Grounded loop / single-action-per-turn enforcement** *(new in Cycle 7 loop-back)* | **Single-Step Enforcer** *(new)* — owns the batch-truncation policy that makes grounding a framework guarantee | ADR-033 §Decision 3; domain-model §Concepts "Grounded loop"; AS-9 §Scope boundary |
| **Artifact-bridge** *(new in Cycle 7 loop-back)* | **Artifact Bridge** *(new)* — owns the artifact-store → tool-call content marshalling (distinct from the ADR-030 "Bridge mechanism") | ADR-034 §Decision 3; ADR-025; domain-model §Methodology Vocabulary |
| **Client-tool-action terminal** *(new in Cycle 7 loop-back)* | **Client-Tool-Action Terminal** *(new)* — owns `tool_calls` emission + the OpenAI streaming delta shape | ADR-034 §Decision 1; domain-model §Concepts |
| **Multi-turn loop participation** *(new in Cycle 7 loop-back)* | **Client-Tool-Action Terminal** *(new)* — owns consuming the `role: "tool"` follow-up and routing it to the Loop Driver | ADR-034 §Decision 2 |
| **Surface-mode discrimination (`tools[]` presence)** *(new in Cycle 7 loop-back)* | **Serving Layer** *(extended)* — owns the named discriminator routing tool-driven vs single-turn requests (D1) | ADR-033 §Decision 1 |
| **`read_deliverable` (artifact read accessor)** *(new in Cycle 7 loop-back)* | **Session Artifact Store** *(extended)* — adds the read side to the formerly write-only store | ADR-034 §Decision 3; conformance-scan advisory #4 |
| **Form directive / FormGate seam** *(new in Cycle 7 loop-back #2)* | **Loop Driver** *(extended)* — composes the destination-keyed directive (`compose_form_directive`); **Artifact Bridge** *(extended)* — owns the FormGate seam | ADR-035; domain-model Amendment Log; FC-53/54/57 |
| **Generation-shaped turn** *(new in Cycle 7 loop-back #3)* | **Delegation Rate Meter** *(new)* — owns the deterministic classification (`classify_turn`) | ADR-036 §Decision 3; domain-model §Methodology Vocabulary (Amendment #17) |
| **Delegation rate** *(new in Cycle 7 loop-back #3)* | **Delegation Rate Meter** *(new)* — owns the rate computation over `TurnDecision` events | ADR-036 §Decision 3; domain-model §Methodology Vocabulary (Amendment #17) |
| **Termination judgment (two-call trailing composition)** *(new in Cycle 7 loop-back #5)* | **Loop Driver** *(extended)* — owns the judgment-first trailing composition, the `VERDICT:` branch enforcement, and the finish-policy event stamping | ADR-037 §Decisions 1/4/6; domain-model §Methodology Vocabulary (Amendment #18); FC-63/65/66/67 |
| **Framework-owned digest** *(new in Cycle 7 loop-back #5)* | **Session Action Record** *(new)* — owns the action records, their schema (the extensible meta-record seam), and digest provision | ADR-037 §Decision 2; domain-model §Methodology Vocabulary (Amendment #18); FC-64 |
| **Parity** *(new in Cycle 7 loop-back; behavioral property — not a code concept)* | (Addressed-by-composition — **Loop Driver** + **Client-Tool-Action Terminal** + **Artifact Bridge** jointly; verified at the live-end-to-end layer of the Cycle 7 loop-back Acceptance Criteria Table) | ADR-034; domain-model §Concepts "Parity" |

**Coverage check (post-Cycle 7 loop-backs #2/#3/#5):** Loop-back #2 adds the form-directive/FormGate vocabulary (Loop Driver + Artifact Bridge extensions); loop-back #3 adds Generation-shaped turn + Delegation rate (Delegation Rate Meter); loop-back #5 adds Termination judgment + Framework-owned digest (Loop Driver extension + Session Action Record). Every loop-back domain-model entry (Amendments #17 + #18) maps to exactly one owning module above. *(The #2/#3 rows were added retroactively at the loop-back #5 amendment — Amendments #14/#16 recorded ownership at module-entry level only; the matrix rows close that drift.)*

**Coverage check (post-Cycle 7 loop-back):** The loop-back adds 8 §Concepts/Methodology-Vocabulary entries with code owners (Layer-A loop-driver, Layer-B generation, Tool-mapping, Grounded loop / single-action enforcement, Artifact-bridge, Client-tool-action terminal, Multi-turn loop participation, Surface-mode discrimination) + 1 extension API (`read_deliverable`) + 1 addressed-by-composition property (Parity) — 10 total new rows. The 4 new modules each own at least one canonical concept and stay within FC-1 (≤ 5 entries): **Loop Driver** owns Layer-A loop-driver + Layer-B generation (callee delegation) + Tool-mapping (3 entries); **Single-Step Enforcer** owns Grounded loop / single-action-per-turn enforcement (1 entry — a single cross-cutting structural guarantee, like the Cycle 6 light modules); **Artifact Bridge** owns Artifact-bridge (1 entry); **Client-Tool-Action Terminal** owns Client-tool-action terminal + Multi-turn loop participation (2 entries). The two extensions (Serving Layer surface-mode discrimination; Session Artifact Store `read_deliverable`) add one responsibility each to existing modules. **Parity** is treated as addressed-by-composition — a behavioral property verified at the live-end-to-end Acceptance Criteria layer (scenarios.md Cycle 7 loop-back Acceptance Criteria Table), consistent with how the Cycle 7 first-pass treated "Structurally-bounded role." The three DECIDE-pending vocabulary terms from MODEL Amendment Log #15 (Layer-A loop-driver, Layer-B generation, Grounded loop) are now allocated to code modules — their operator-voice-vs-research-voice relocation is finalized by this ARCHITECT naming per the Amendment Log #16 note.

**Coverage check (post-Cycle 6):** Every scoped concept (19 v2.0 + 12 Cycle 4 additions + 8 Cycle 6 §Concepts additions + 2 Cycle 6 §Methodology Vocabulary additions = 41 total) is allocated, with some concepts (Bilateral visibility absence, Information-finding overhead, Output-spec drift, Infrastructure-complete / routing-incomplete) treated as *addressed-by-composition* rather than owned by a single module per their cross-cutting character. Every action (13 v3.0 + 3 Cycle 6 additions = 16 total) is allocated to its existing or new owner. Every touched project-level concept stays with the existing Ensemble Engine. *Stabilize* remains marked emergent per v3.0. Cycle 6 concept additions add 11 rows (some addressed-by-composition); Cycle 6 action additions add 3 rows. The four new modules (Dispatch Event Substrate, Operator-Terminal Event Sink, Orchestrator-Context Event Sink, Session Artifact Store) each own at least one canonical concept-or-action; FC-1 (≤ 5 entries per module) holds — Dispatch Event Substrate is at 2 entries (Dispatch timing, Route event); Operator-Terminal Event Sink is at 3 (Liveness signal, Emit tool call, Heartbeat); Orchestrator-Context Event Sink is at 1 (structured-observation construction — implementation surface, not a glossary concept); Session Artifact Store is at 1 (Artifact-as-substrate). The light entries on the destination sinks and Session Artifact Store reflect their narrow ownership — each module owns one cross-cutting concept that is load-bearing for the architecture but does not itself decompose into multiple glossary entries.

**Coverage check (post-Cycle 7):** Cycle 7 adds 7 §Concepts entries (Routing-planner ensemble, Response-synthesizer ensemble, Transparent OpenAI-compatible endpoint, Bridge mechanism, `tool_choice` strip-at-input, Population A, Population B), 1 §Concepts entry treated as project-developer framing not a code concept (Cost-distribution lens), 3 §Methodology Vocabulary entries (Framework-driven dispatch pipeline, Structurally-bounded role, Orchestrator-designs-ensembles), 3 §Actions (Plan, Synthesize (response), Strip (`tool_choice` at input)), and 1 implicit concept (Capability list) — 15 total new rows. The 6 new modules (Dispatch Pipeline, Routing Planner, Response Synthesizer, Capability List Builder, Response Labeling, Capability Discovery Endpoint) each own at least one canonical concept-or-action: Dispatch Pipeline owns "Framework-driven dispatch pipeline" + the Plan→InternalToolCall adapter + pipeline-stage events (3 entries); Routing Planner owns "Routing-planner ensemble" + "Plan" action + bonus-path layering hooks (3 entries); Response Synthesizer owns "Response-synthesizer ensemble" + "Synthesize (response)" action + Rules 1-6 content-layer commitments (3 entries); Capability List Builder owns "Capability list" (1 entry); Response Labeling owns "Bridge mechanism" (joint) + three-layer served-by signaling + Population B advisory (3 entries); Capability Discovery Endpoint owns the discovery surface (1 entry). FC-1 (≤ 5 entries per module) holds across all 6 new modules. Three §Concepts entries (Population A, Population B, Cost-distribution lens) are stakeholder-model / project-developer-framing concepts owned by `product-discovery.md`, not by code modules — consistent with the Cycle 6 pattern of stakeholder concepts living in product discovery; Population B has a code-side affordance via Response Labeling's structured advisory. "Structurally-bounded role" is treated as *addressed-by-composition* by Routing Planner + Response Synthesizer jointly (architectural property satisfied by both). "Orchestrator-designs-ensembles" is north-star context not a Cycle 7 deliverable.

---

## Dependency Graph

**Directed edges (A → B means A imports/calls B):**

```
Serving Layer ──────────────────────────────────▶ Session Registry
Serving Layer ──────────────────────────────────▶ Orchestrator Configuration
Serving Layer ──────────────────────────────────▶ Orchestrator Runtime
Serving Layer ───── (Phase 2 only) ─────────────▶ Plexus Adapter
Session Registry ───────────────────────────────▶ Ensemble Engine (profile lookup)
Orchestrator Runtime ───────────────────────────▶ Budget Controller
Orchestrator Runtime ───────────────────────────▶ Orchestrator Tool Dispatch
Orchestrator Runtime ───────────────────────────▶ Conversation Compaction          (Cycle 4 — new)
Budget Controller ──────────────────────────────▶ Session Registry
Orchestrator Tool Dispatch ─────────────────────▶ Ensemble Engine
Orchestrator Tool Dispatch ─────────────────────▶ Composition Validator
Orchestrator Tool Dispatch ─────────────────────▶ Plexus Adapter
Orchestrator Tool Dispatch ─────────────────────▶ Autonomy Policy
Orchestrator Tool Dispatch ─────────────────────▶ Calibration Gate
Orchestrator Tool Dispatch ─────────────────────▶ Result Summarizer Harness
Orchestrator Tool Dispatch ─────────────────────▶ Tier-Escalation Router            (Cycle 4 — new)
Result Summarizer Harness ──────────────────────▶ Ensemble Engine
Conversation Compaction ────────────────────────▶ Ensemble Engine                   (Cycle 4 — new; Layer 4 summarizer)
Composition Validator ──────────────────────────▶ Ensemble Engine (shared public validator)
Autonomy Policy ────────────────────────────────▶ Session Registry
Calibration Gate ───────────────────────────────▶ Ensemble Engine
Calibration Gate ───────────────────────────────▶ Plexus Adapter
Calibration Gate ───────────────────────────────▶ Calibration Signal Channel        (Cycle 4 — new; conditional on ADR-016)
Tier-Escalation Router ─────────────────────────▶ Calibration Gate                  (Cycle 4 — new; verdict consumer + ADR-018 (d)-analog audit edge: composes stateless consumer (inherited a) + upstream windowing (inherited b via ADR-014) + typed validation (inherited e via FC-17) + periodic audit (novel d-analog))
Tier-Escalation Router ─────────────────────────▶ Ensemble Engine                   (Cycle 4 — new; Topaz skill metadata reader + ADR-018 outcome observer for the (d)-analog escalation-vs-outcome correlation criterion)
Bootstrapping Pipeline ─────────────────────────▶ Plexus Adapter
Bootstrapping Pipeline ─────────────────────────▶ Ensemble Engine
Plexus Adapter ─────────────────────────────────▶ (external — Plexus lib)

╔══════════════════════════════════════════════════════════════════════════════════╗
║  Ensemble Engine ────read-only────▶ Calibration Signal Channel                  ║
║  (Cycle 4 — new; the load-bearing upward exception ADR-016 amends ADR-002 to    ║
║   permit; read-only, calibration-data-only, structurally typed at boundary,     ║
║   gated by 5 bounding mechanisms specified in ADR-016)                          ║
╚══════════════════════════════════════════════════════════════════════════════════╝

Calibration Gate ─────────────────────▶ Dispatch Event Substrate        (Cycle 6 — new; producer registers CalibrationVerdict events)
Tier-Escalation Router ───────────────▶ Dispatch Event Substrate        (Cycle 6 — new; producer registers TierSelection events)
Tier-Router-Audit ────────────────────▶ Dispatch Event Substrate        (Cycle 6 — new; producer registers AuditDiagnostic events)
Calibration Signal Channel ───────────▶ Dispatch Event Substrate        (Cycle 6 — new; producer registers CalibrationSignal events)
Orchestrator Tool Dispatch ───────────▶ Dispatch Event Substrate        (Cycle 6 — new; emits DispatchTiming start/end + reads events for envelope.diagnostics)
Orchestrator Tool Dispatch ───────────▶ Session Artifact Store          (Cycle 6 — new; writes deliverable for substrate-routed dispatches)
Dispatch Event Substrate ─────────────▶ Operator-Terminal Event Sink    (Cycle 6 — new; registered sink — line-oriented format + liveness signals)
Dispatch Event Substrate ─────────────▶ Orchestrator-Context Event Sink (Cycle 6 — new; registered sink — structured observations between turns)
Orchestrator Runtime ─────────────────▶ Orchestrator-Context Event Sink (Cycle 6 — new; Runtime queries sink at turn boundary for prior-dispatch observation)
Session Registry ─────────────────────▶ Session Artifact Store          (Cycle 6 — new; Session Registry triggers retention cleanup at on_session_close — direction L3→L1; Session Artifact Store does NOT depend on Session Registry)
Session Artifact Store ───────────────▶ Dispatch Event Substrate        (Cycle 6 — new; consumes dispatch_id for path construction)
Operator-Terminal Event Sink ─────────▶ Ensemble Engine                 (Cycle 6 — new; consumes validate-once-at-load startup results for the WARN-once surface)
Serving Layer ────────────────────────▶ Operator-Terminal Event Sink    (Cycle 6 — new; tool-call-emit logging at SSE stream tool-call-structure detection + heartbeat firing from open-request tracking)

Serving Layer ────────────────────────▶ Dispatch Pipeline               (Cycle 7 — new; replaces Serving Layer → Orchestrator Runtime on chat-completions surface; Serving Layer → Orchestrator Runtime preserved as dormant edge per ADR-027 disposition (a))
Serving Layer ────────────────────────▶ Response Labeling               (Cycle 7 — new; post-pipeline response decoration)
Serving Layer ────────────────────────▶ Capability Discovery Endpoint   (Cycle 7 — new; intra-L3; sibling endpoint or /v1/models extension)
Capability Discovery Endpoint ────────▶ Capability List Builder         (Cycle 7 — new; single source of truth for capability list)
Dispatch Pipeline ────────────────────▶ Routing Planner                 (Cycle 7 — new; Stage 1 invocation)
Dispatch Pipeline ────────────────────▶ Response Synthesizer            (Cycle 7 — new; Stage 3 invocation)
Dispatch Pipeline ────────────────────▶ Orchestrator Tool Dispatch      (Cycle 7 — new; Stage 2 — Plan → InternalToolCall adapter calls dispatch() directly for the capability ensemble)
Dispatch Pipeline ────────────────────▶ Capability List Builder         (Cycle 7 — new; provides CAPABILITY LIST input for Routing Planner)
Dispatch Pipeline ────────────────────▶ Dispatch Event Substrate        (Cycle 7 — new; producer registers PlanEmitted, DispatchFired, SynthesizerCompleted, DirectCompletionFallback events)
Routing Planner ──────────────────────▶ Orchestrator Tool Dispatch      (Cycle 7 — new; invokes routing-planner ensemble via dispatch() — existing chokepoint preserves calibration gate + tier router + autonomy interpositions per ADR-028)
Response Synthesizer ─────────────────▶ Orchestrator Tool Dispatch      (Cycle 7 — new; invokes synthesizer ensemble via dispatch() — same chokepoint per ADR-029)
Capability List Builder ──────────────▶ Ensemble Engine                 (Cycle 7 — new; via Protocol consumer; reads validate-once-at-load registry cache established Cycle 6 / FC-27)
Orchestrator Tool Dispatch ───────────▶ Dispatch Event Substrate        (Cycle 6 — pre-existing; Cycle 7 emits additional pipeline-stage events through the same substrate when invoked by Dispatch Pipeline)
Operator-Terminal Event Sink ─────────▶ (existing fan-out from substrate; Cycle 7 — extends to consume DirectCompletionFallback event type for direct_completion_rate rolling metric)
Orchestrator-Context Event Sink ──────▶ (existing fan-out from substrate; Cycle 7 — extends to consume DirectCompletionFallback event type for operator-context surfacing)

Serving Layer ────────────────────────▶ Loop Driver                     (Cycle 7 loop-back — new; tool-driven surface, parallel to Serving Layer → Dispatch Pipeline for non-tool requests; surface-mode discriminator selects)
Serving Layer ────────────────────────▶ Client-Tool-Action Terminal     (Cycle 7 loop-back — new; intra-L3; composes the tool-call terminal onto the response)
Loop Driver ──────────────────────────▶ Single-Step Enforcer            (Cycle 7 loop-back — new; intra-L2; driver proposes, enforcer truncates to one action)
Loop Driver ──────────────────────────▶ Orchestrator Tool Dispatch      (Cycle 7 loop-back — new; intra-L2; per-turn callee generation via the existing dispatch() chokepoint — calibration gate + tier router + autonomy fire)
Loop Driver ──────────────────────────▶ Capability List Builder         (Cycle 7 loop-back — new; L2→L1; per-turn delegation selection)
Loop Driver ──────────────────────────▶ Budget Controller               (Cycle 7 loop-back — new; L2→L1; per-turn turn/token enforcement across the multi-turn loop — AS-3)
Loop Driver ──────────────────────────▶ Dispatch Event Substrate        (Cycle 7 loop-back — new; L2→L1; emits per-turn TurnDecision diagnostic events for axis-2 split-vs-callee diagnosis)
Loop Driver ──────────────────────────▶ Delegation Rate Meter           (Cycle 7 loop-back #3 — new; intra-L2; per-turn classify_turn for turn_shape stamping; the meter imports nothing back)
Operator-Terminal Event Sink ─────────▶ Delegation Rate Meter           (Cycle 7 loop-back #3 — new; L3→L2; delegation_rate over consumed TurnDecision events)
Loop Driver ──────────────────────────▶ Session Action Record           (Cycle 7 loop-back #5 — new; L2→L1; records actions at decision time, joins client results, reads records for the judgment digest)
Client-Tool-Action Terminal ──────────▶ Artifact Bridge                 (Cycle 7 loop-back — new; L3→L2; resolves the deliverable reference into tool-call content)
Artifact Bridge ──────────────────────▶ Session Artifact Store          (Cycle 7 loop-back — new; L2→L1; read_deliverable() — the new read-side accessor)
Loop Driver ──────────────────────────▶ (seat-filler LLM — injected by the Serving Layer at construction from the resolved Model Profile, mirroring how the Serving Layer constructs Orchestrator Runtime with its orchestrator-LLM; the Loop Driver does NOT import Orchestrator Configuration — no upward L2→L3 edge; the driver fills the client's "model" seat per ADR-033 §Decision 5; swapping the driver model is a config change resolved at the Serving Layer / Orchestrator Configuration, satisfying FC-46)
```

**Layering (inner → outer; post-Cycle 6):**

| Layer | Modules | Rule |
|-------|---------|------|
| L0 — Core (existing) | Ensemble Engine | May not depend on any agentic-serving module — **with one exception per ADR-016:** the Ensemble Engine emits read-only calibration signals to the Calibration Signal Channel (L1) via observer-pattern callback registration. This is the upward edge ADR-016 amends ADR-002 to permit. |
| L1 — Domain Policy | Composition Validator, Budget Controller, Autonomy Policy, Calibration Gate, Plexus Adapter, Calibration Signal Channel *(Cycle 4; conditional acceptance)*, **Dispatch Event Substrate** *(Cycle 6)*, **Session Artifact Store** *(Cycle 6)*, **Capability List Builder** *(new in Cycle 7)*, **Session Action Record** *(new in Cycle 7 loop-back #5)* | May depend on L0 only — **plus an intra-L1 producer-substrate edge:** the four event-producing L1 modules (Calibration Gate, Tier-Escalation Router (resides at L2 — see note below), Tier-Router-Audit, Calibration Signal Channel) emit events through the Dispatch Event Substrate's `emit()`; Session Artifact Store reads from Dispatch Event Substrate for `dispatch_id` lifecycle. Intra-L1 edges do not form cycles. |
| L2 — Runtime | Result Summarizer Harness, Orchestrator Tool Dispatch, Orchestrator Runtime *(preserved as dormant per Cycle 7 ARCHITECT disposition (a))*, Conversation Compaction *(Cycle 4)*, Tier-Escalation Router *(Cycle 4)*, **Orchestrator-Context Event Sink** *(Cycle 6)*, **Dispatch Pipeline** *(new in Cycle 7)*, **Routing Planner** *(new in Cycle 7)*, **Response Synthesizer** *(new in Cycle 7)*, **Loop Driver** *(new in Cycle 7 loop-back)*, **Single-Step Enforcer** *(new in Cycle 7 loop-back)*, **Artifact Bridge** *(new in Cycle 7 loop-back)*, **Delegation Rate Meter** *(new in Cycle 7 loop-back #3)* | May depend on L0 and L1 |
| L3 — Entry | Serving Layer, Session Registry, Bootstrapping Pipeline, Orchestrator Configuration, **Operator-Terminal Event Sink** *(Cycle 6)*, **Response Labeling** *(new in Cycle 7)*, **Capability Discovery Endpoint** *(new in Cycle 7)*, **Client-Tool-Action Terminal** *(new in Cycle 7 loop-back)* | May depend on L0, L1, and L2 |

**Note on Tier-Escalation Router's layer:** Tier-Escalation Router resides at L2 per v3.0 placement; its event-producer role in Cycle 6 introduces an L2→L1 edge (Tier-Escalation Router → Dispatch Event Substrate). This is consistent with the standard layering rule (L2 may depend on L1). The producer-substrate pattern is uniform across all four producer modules (the L1 producers' edges are intra-L1; the L2 producer's edge is L2→L1).

**No cycles.** Verified by static inspection over all 41 edges (28 v3.0 + 13 Cycle 6 additions): every edge points from a higher layer to a same-or-lower layer, except the single ADR-016 exception (Ensemble Engine → Calibration Signal Channel; read-only) which remains the only L0→L1 upward edge. Cycle 6 intra-L1 edges (Calibration Gate → Dispatch Event Substrate; Tier-Router-Audit → Dispatch Event Substrate; Calibration Signal Channel → Dispatch Event Substrate; Session Artifact Store → Dispatch Event Substrate) and the L2→L1 edge (Tier-Escalation Router → Dispatch Event Substrate) are non-cyclic — the substrate fan-outs to sinks (Operator-Terminal at L3; Orchestrator-Context at L2) and does not loop back to its producers. Session Registry → Session Artifact Store is L3→L1 (standard downward direction; Session Registry triggers retention cleanup at session close — Session Artifact Store does NOT depend on Session Registry). Operator-Terminal Event Sink → Ensemble Engine is L3→L0 (standard direction). Serving Layer → Operator-Terminal Event Sink is intra-L3 (alongside the existing intra-L3 edges Serving Layer → Session Registry, Serving Layer → Orchestrator Configuration).

**Cycle 7 edges (13 new edges across the layers):** Serving Layer → Dispatch Pipeline (L3→L2); Serving Layer → Response Labeling (intra-L3); Serving Layer → Capability Discovery Endpoint (intra-L3); Capability Discovery Endpoint → Capability List Builder (L3→L1); Dispatch Pipeline → Routing Planner (intra-L2); Dispatch Pipeline → Response Synthesizer (intra-L2); Dispatch Pipeline → Orchestrator Tool Dispatch (intra-L2); Dispatch Pipeline → Capability List Builder (L2→L1); Dispatch Pipeline → Dispatch Event Substrate (L2→L1); Routing Planner → Orchestrator Tool Dispatch (intra-L2); Response Synthesizer → Orchestrator Tool Dispatch (intra-L2); Capability List Builder → Ensemble Engine (L1→L0); Operator-Terminal + Orchestrator-Context Event Sinks → (extended to consume new event types — additive to existing substrate fan-out, not a new edge). **All 13 Cycle 7 edges are non-cyclic** — every edge points from a higher layer to a same-or-lower layer; intra-L2 edges (Dispatch Pipeline → Routing Planner, Response Synthesizer, Orchestrator Tool Dispatch; Routing Planner → Orchestrator Tool Dispatch; Response Synthesizer → Orchestrator Tool Dispatch) form a tree rooted at Dispatch Pipeline with no back edges; intra-L3 edges (Serving Layer → Response Labeling, Capability Discovery Endpoint) are alongside existing intra-L3 patterns. **Serving Layer → Orchestrator Runtime edge is preserved as dormant** per ADR-027 disposition (a) — the class definition remains importable; no production call site invokes the path on `/v1/chat/completions` post-BUILD; the edge documents the architectural option per ADR-001 + ADR-011 retention without representing an active runtime dependency.

**Cycle 7 loop-back edges (9 new edges across the layers):** Serving Layer → Loop Driver (L3→L2); Serving Layer → Client-Tool-Action Terminal (intra-L3); Loop Driver → Single-Step Enforcer (intra-L2); Loop Driver → Orchestrator Tool Dispatch (intra-L2); Loop Driver → Capability List Builder (L2→L1); Loop Driver → Budget Controller (L2→L1); Loop Driver → Dispatch Event Substrate (L2→L1); Client-Tool-Action Terminal → Artifact Bridge (L3→L2); Artifact Bridge → Session Artifact Store (L2→L1). **All 9 loop-back edges are non-cyclic** — every edge points from a higher layer to a same-or-lower layer. The intra-L2 edges (Loop Driver → Single-Step Enforcer; Loop Driver → Orchestrator Tool Dispatch) form a tree rooted at Loop Driver with no back edges (Single-Step Enforcer is a leaf stateless policy; Orchestrator Tool Dispatch is the shared dispatch chokepoint, already a fan-in node). The intra-L3 edge (Serving Layer → Client-Tool-Action Terminal) is alongside existing intra-L3 patterns (Serving Layer → Response Labeling, Capability Discovery Endpoint). The Loop Driver's seat-filler LLM is **not** a module edge — it is an injected dependency the Serving Layer constructs from the resolved Model Profile (mirroring Orchestrator Runtime's orchestrator-LLM), so the Loop Driver imports no L3 module and introduces no upward edge. **Two parallel surfaces share the Serving Layer entry:** the surface-mode discriminator routes tool-driven requests (client `tools[]` present) to the Loop Driver and non-tool requests to the Dispatch Pipeline — the two L2 control structures are mutually exclusive per request and do not depend on each other (no Loop Driver ↔ Dispatch Pipeline edge). The wrapper-contingency path (Loop Driver delegating per-turn generation to `DispatchPipeline.run()` instead of a single ensemble) would add a Loop Driver → Dispatch Pipeline (intra-L2) edge **only if the second-order fallback is activated** (see fallback ordering below); it is not an active edge in the callee design.

**Cycle 7 loop-back #3 + #5 edges (3 new edges).** Loop-back #3 (ADR-036): Loop Driver → Delegation Rate Meter (intra-L2; the meter is a pure-function leaf importing neither the driver nor the sinks — cycle-free by construction); Operator-Terminal Event Sink → Delegation Rate Meter (L3→L2, standard downward direction; rate computation over consumed events). Loop-back #5 (ADR-037): Loop Driver → Session Action Record (L2→L1, standard downward direction; the record module imports nothing — a leaf store, cycle-free by construction). All three edges point from a higher layer to a same-or-lower layer or are intra-layer tree edges with leaf targets; no cycles introduced. The judgment seat (ADR-037 call 1) is **not** a module edge — like the seat-filler LLM, it is resolved as a Model Profile via Orchestrator Configuration and injected at construction (no upward L2→L3 edge). *(The loop-back #3 edges were recorded in contracts and the Amendment Log at #16 but omitted from this graph section; drawn retroactively here.)*

**Wrapper-contingency fallback ordering (advisory #1 — F3-1 specification gap closed at ARCHITECT).** If BUILD/PLAY axis-2 validation shows the cheap-tier driver cannot sustain long-horizon coherence, the fallback order is: **(1) frontier-tier driver** — a Model Profile swap (FC-46 swappability), no structural change, the preferred first fallback (it preserves the lean callee shape and only raises per-turn driver cost); **(2) wrapper reversion** — second-order, only if a frontier-tier driver also fails to hold the horizon: the Loop Driver swaps its per-turn generation delegation from single-ensemble to `DispatchPipeline.run()` (the wrapper shape Spike υ measured), accepting the ~3× per-turn latency and synthesizer-redundancy the callee resolution rejected. FC-52 keeps this path architecturally accessible (the delegation target is a swappable strategy behind the Loop Driver's generation boundary); the wrapper reversion is a recorded contingency, not a watched one (the practitioner could name no concrete reopening trigger at the DECIDE gate — F3-1).

**FC-51 gates the fallback branch (loop-back ARCHITECT gate refinement, 2026-06-02).** The axis-2 `TurnDecision` instrumentation's split-incorrect-vs-callee-incorrect distinction is not merely diagnostic — it selects *which* fallback applies. A **callee-incorrect** axis-2 failure (the driver chose the right actions but a single per-turn ensemble produced wrong content for some turn type) is the signal that points toward **wrapper reversion** (the pipeline's multi-capability composition might do better for that turn type). A **split-incorrect** axis-2 failure (the driver chose wrong actions / the trajectory wandered even with correct per-turn content) implicates the **two-layer split itself** — neither callee nor wrapper generation fixes a driver that picks wrong actions, so the response is a **Design Amendment re-opening the architecture**, not wrapper reversion. So the fallback ordering is: frontier-tier driver first (a Model Profile swap, FC-46 — addresses a driver-capability ceiling under either failure mode); then, conditioned on the FC-51 diagnosis, **callee-incorrect → wrapper reversion** (FC-52) / **split-incorrect → Design Amendment**. Without FC-51 the practitioner would not know which branch to take; this is what makes the instrumentation load-bearing rather than ceremony (the ADR-095 Agent Outcome Test on FC-51: the split-vs-callee distinction produces an actionable difference — wrapper-reversion vs. Design-Amendment).

**Fan-out warnings (post-Cycle 4).** Orchestrator Tool Dispatch has seven outbound edges: five correspond to the closed tool set (Ensemble Engine, Composition Validator, Plexus Adapter, Autonomy Policy, Calibration Gate) — intentional per ADR-003; the fan-out *is* the closed tool set. The sixth edge (Result Summarizer Harness) is a cross-cutting interposition on the `invoke_ensemble` return path — orthogonal to the closed set, structurally placed on the Tool Dispatch side rather than the Runtime side so the Runtime stays unaware of summarization (per Amendment #3). The seventh edge (Tier-Escalation Router; new in Cycle 4) is another orthogonal interposition on the `invoke_ensemble` path — selecting Model Profile pre-dispatch based on calibration verdict per ADR-015. Ensemble Engine remains the highest-fan-in module (six agentic-serving modules depend on it post-Cycle 4: the existing five plus the new Tier-Escalation Router and Conversation Compaction reading via callback for signal channel emission). The pattern stays consistent with Layer 3 (Ensemble Engine) being the single shared execution substrate.

**FC-2 / FC-3 amendment for ADR-016 exception.** When FC-2 is implemented as an automated AST-based per-module import layering check (per WP-B4), it must recognize the `Ensemble Engine → Calibration Signal Channel` edge as an annotated allowed exception. The annotation is "read-only, calibration-data-only" and is enforced via the channel's owned schema validation (mechanism (e)) at runtime. FC-3 (cycle detection) computes over the full edge set; the new edge does not introduce a cycle, so FC-3 passes without amendment.

---

## Integration Contracts

### Serving Layer → Session Registry

**Protocol:** Synchronous function call at request entry.
**Shared types:** `SessionIdentity` (derivation-method-agnostic: may be client-supplied `user` field, hash of initial message prefix, or explicit session id header); `SessionState` (current Budget state, Autonomy Level, Calibration state if required).
**Error handling:** A request that fails Session identity resolution is treated as a new Session (cold start). Identity-resolution failures are not client errors.
**Owned by:** Session Registry defines the contract.

### Serving Layer → Orchestrator Runtime

**Protocol:** Asynchronous streaming; the Runtime yields SSE chunks.
**Shared types:** `SessionContext` (messages, tools array, session state); `OrchestratorChunk` (one of: content delta, internal tool call invocation-in-flight, internal tool call result, client tool call in final turn, completion).
**Error handling:** An Orchestrator Runtime exception becomes an SSE `error` chunk. The Runtime guarantees no partial state persists to the Session after a thrown exception unless explicitly committed.
**Owned by:** Orchestrator Runtime defines the contract.

### Orchestrator Runtime → Budget Controller

**Protocol:** Synchronous pre-iteration check.
**Shared types:** `BudgetCheck` (pass, or a typed exhaustion reason: turn or token).
**Error handling:** A failed check raises a typed `BudgetExhausted` event that the Runtime converts into a graceful session termination with explicit exhaustion signaling (ADR-005).
**Owned by:** Budget Controller defines the contract.

### Orchestrator Runtime → Orchestrator Tool Dispatch

**Protocol:** Synchronous tool invocation; returns the summarized/gated result.
**Shared types:** `InternalToolCall` (tool name from the fixed five, arguments); `ToolCallResult` (summarized result or typed error).
**Error handling:** A tool name outside the fixed five returns a `ToolCallResult` error (not an exception). The Runtime passes the error back to the orchestrator LLM as an observation (scenarios §Invocation outside the tool set).
**Owned by:** Orchestrator Tool Dispatch defines the contract.

### Orchestrator Tool Dispatch → Autonomy Policy

**Protocol:** Synchronous gate check before every tool dispatch.
**Shared types:** `AutonomyGateInput` (tool name, tool arguments, current Session Autonomy Level, tool-user persona flag); `AutonomyGateOutput` (allow, require_approval, or deny).
**Error handling:** `require_approval` surfaces an event to the operator via the visibility surface; `deny` is returned as a tool error to the orchestrator.
**Owned by:** Autonomy Policy defines the contract.

### Orchestrator Tool Dispatch → Ensemble Engine

**Protocol:** Existing `EnsembleExecutor.execute` (Layer 3 API, unchanged). Wrapped by Result Summarizer Harness on the return path.
**Shared types:** (existing Layer 3 types)
**Error handling:** Invariant 14 applies — runtime errors recorded; structural errors raised at load time.
**Owned by:** Ensemble Engine (existing).

### Orchestrator Tool Dispatch → Composition Validator

**Protocol:** Synchronous validation at composition time.
**Shared types:** `CompositionRequest` (proposed ensemble config, library search_dirs); `CompositionResult` (accepted and stored to local tier, or typed validation error naming the specific invariant violated).
**Error handling:** Validation failures are returned to the orchestrator as tool errors. No partial or pending ensemble state persists (ADR-006; scenarios §Composition that would introduce a reference-graph cycle).
**Owned by:** Composition Validator defines the contract.

### Composition Validator ↔ Ensemble Engine (shared validator routine)

**Protocol:** Shared public function call (`validate_ensemble_reference_graph` — public API in `core/config/ensemble_config.py`).
**Shared types:** (existing `EnsembleConfig`, `AgentConfig` union, search_dirs list).
**Error handling:** Raises `ValueError` with cycle description on failure; returns `None` on success.
**Owned by:** Ensemble Engine owns the shared routine after extraction from private helpers (WP-A). Both load-time (`EnsembleLoader.load_from_file`, `list_ensembles`) and composition-time callers use the same function (scenarios refactor 1-3; regression scenario "shared single routine").

### Orchestrator Tool Dispatch → Plexus Adapter

**Protocol:** Synchronous for `query_knowledge`; asynchronous-with-immediate-ack for `record_outcome`.
**Shared types:** `QueryRequest`, `QueryResult` (possibly empty — AS-8); `OutcomeRecord`, `RecordAck`.
**Error handling:** When Plexus is absent, both tools return well-formed empty/ack responses — no exception surfaces (scenarios §query_knowledge returns empty gracefully, §record_outcome writes asynchronously).
**Owned by:** Plexus Adapter defines the contract.

### Orchestrator Tool Dispatch → Calibration Gate

**Protocol:** Synchronous pre-invoke check and post-invoke Quality Signal attachment; interposed transparently on `invoke_ensemble` for ensembles in calibration.
**Shared types:** `CalibrationState` (in_calibration, trusted); `QualitySignal` (positive, negative, absent).
**Error handling:** A failing calibration check does not prevent invocation (ADR-007 clause 2) — it attaches the signal and returns the result normally.
**Owned by:** Calibration Gate defines the contract.

### Result Summarizer Harness → Ensemble Engine

**Protocol:** The Harness invokes a summarizer ensemble (configured primitive) via `EnsembleExecutor.execute`.
**Shared types:** (existing Layer 3 types)
**Error handling:** A summarizer failure is a tool failure — the original ensemble result is still persisted to its artifact (Invariant 9), but the orchestrator receives a typed summarization error as a tool result. Raw-output escape hatch (ADR-004) bypasses the Harness entirely for flagged ensembles.
**Owned by:** Result Summarizer Harness defines the contract.

### Serving Layer → `resolve_session_start_context` (internal function)

**Protocol:** Synchronous session-start hook; returns optional system-prompt augmentation. The hook is a typed function, not a module; call site and signature are the structural reservation.
**Shared types:** `SessionContext` (Session identity and state at start); `list[PromptFragment]` (empty in Phase 1; populated from Plexus Adapter in Phase 2).
**Error handling:** Injection failure in Phase 2 falls through to no injection rather than failing the session start.
**Owned by:** Serving Layer owns the function and its contract. **Phase 1 status:** returns `[]` unconditionally.

### Bootstrapping Pipeline → Plexus Adapter, Ensemble Engine

**Protocol:** Batch operation: reads from the library via the config manager, pushes file content to Plexus via the Adapter's ingestion path.
**Shared types:** `LibraryArtifactStream`, `IngestionAck` (AS-4: source material only — never LLM summaries).
**Error handling:** Ingestion is non-blocking; per-artifact failures are logged and the batch continues.
**Owned by:** Bootstrapping Pipeline defines the contract.

### Autonomy Policy → Session Registry; Calibration Gate → Session Registry; Budget Controller → Session Registry

**Protocol:** Read-only synchronous queries for Session state.
**Shared types:** `SessionState` (subsets scoped to consumer).
**Error handling:** Missing Session (identity unresolved) is treated as cold-session defaults for every consumer.
**Owned by:** Session Registry defines the contract.

### Orchestrator Runtime → Conversation Compaction *(new in Cycle 4 per ADR-012)*

**Protocol:** Synchronous turn-boundary call. Runtime invokes `compact(messages, session_state) -> CompactedContext` at each iteration boundary before passing messages to the LLM.
**Shared types:** `ConversationMessages` (the orchestrator's accumulated messages); `SessionState` (subset: turn count, token spend; needed for token-budget targeting); `CompactedContext` (the message array possibly with persisted-tool-result references substituted for raw payloads, possibly with cache-edits applied, possibly with a session-notes block prepended).
**Error handling:** Layer 4 LLM-summary failure does not raise to the Runtime — the circuit-breaker tracks failures and produces a typed `compaction_layer_4_failure` error in operator-readable diagnostics (logged); compaction returns the best-available reduction from Layers 0–3. After three consecutive Layer 4 failures, Layer 4 is suspended for the rest of the session; circuit-breaker auto-resets at session start.
**Owned by:** Conversation Compaction defines the contract.

### Conversation Compaction → Ensemble Engine *(new in Cycle 4 per ADR-012)*

**Protocol:** Layer 4 invokes a configured summarizer ensemble via `EnsembleExecutor.execute` with a context-summarization prompt. Distinct from Result Summarizer Harness's invocation (which targets ensemble outputs, AS-7) — this targets the conversation history.
**Shared types:** existing Layer 3 types (`EnsembleConfig`, ensemble result dict).
**Error handling:** ensemble-execution exception → Layer 4 failure (counted toward circuit-breaker). The conversation context is returned at the best Layer 0–3 reduction.
**Owned by:** Conversation Compaction defines the contract; Ensemble Engine API is unchanged.

### Orchestrator Tool Dispatch → Tier-Escalation Router *(new in Cycle 4 per ADR-015)*

**Protocol:** Synchronous interposition on `invoke_ensemble`. Tool Dispatch passes the dispatched ensemble's name and the calibration verdict (already retrieved from Calibration Gate) to `select_tier(ensemble_name, verdict) -> TierSelection`.
**Shared types:** `TierSelection` (one of: `CheapTier(model_profile)`, `EscalatedTier(model_profile)`, `Bypass(reason)`); `CalibrationVerdict` (`Proceed | Reflect | Abstain` per ADR-014).
**Error handling:** missing Topaz skill metadata produces `LlmOrcStructuralError(error_kind="missing_skill_metadata", recovery_action_required="reformulate")`. Abstain verdict produces `LlmOrcStructuralError(error_kind="escalation_bypass", recovery_action_required="reformulate")`.
**Owned by:** Tier-Escalation Router defines the contract.

### Tier-Escalation Router → Calibration Gate *(new in Cycle 4 per ADR-014 + ADR-015)*

**Protocol:** Synchronous read of calibration verdict. Router calls `verdict_for(session_id, ensemble_name, dispatch_context) -> CalibrationVerdict`.
**Shared types:** `CalibrationVerdict`; `DispatchContext` (the dispatch-time inputs needed for verdict computation: orchestrator's recent trajectory features when ADR-016 is active, or empty when the channel is not active).
**Error handling:** verdict computation does not raise; if ADR-016 is rejected (no cross-layer signals available), the verdict computation operates on L1-internal trajectory data only (per ADR-014 §"Feature-extraction location").
**Owned by:** Calibration Gate defines the contract; the verdict surface is the public extension to ADR-007's `QualitySignal` API.

### Tier-Escalation Router → Ensemble Engine *(new in Cycle 4 per ADR-015)*

**Protocol:** Synchronous read of ensemble metadata. Router reads the dispatched ensemble's Topaz skill via the existing config manager surface.
**Shared types:** existing `EnsembleConfig` extended with optional `topaz_skill: TopazSkill` field; `TopazSkill = Literal["code_generation", "tool_use", "mathematical_reasoning", "logical_reasoning", "factual_knowledge", "writing_quality", "instruction_following", "summarization"]`.
**Error handling:** missing or invalid `topaz_skill` field → `missing_skill_metadata` typed error (raised before dispatch — the orchestrator gets the error as a tool-call observation; the ReAct loop continues).
**Owned by:** Ensemble Engine owns the YAML schema extension; Tier-Escalation Router owns the metadata-read contract.

### Calibration Gate → Calibration Signal Channel *(new in Cycle 4 per ADR-016; conditional)*

**Protocol:** Synchronous read of windowed signal data. Gate calls `recent_signals(window) -> SignalWindow`.
**Shared types:** `SignalWindow` (the time-decay-windowed signals — only the most recent window; per mechanism (b) 60-min/100-signal dual-bound, linear decay); `TrajectorySignal` (typed shape per ADR-016 §"The signal channel": trajectory features per ADR-014's HTC specification, dispatch outcomes, deterministic-tool-output anchors when applicable).
**Error handling:** when ADR-016 is rejected, Calibration Signal Channel returns an empty window; the Gate's verdict computation operates on L1-internal trajectory data only (per ADR-014 §"Feature-extraction location").
**Owned by:** Calibration Signal Channel defines the contract.

### Ensemble Engine → Calibration Signal Channel *(new in Cycle 4 per ADR-016 — THE LOAD-BEARING UPWARD EXCEPTION)*

**Protocol:** Observer-pattern callback. Ensemble Engine invokes `emit_signal(signal: TrajectorySignal)` after each dispatch completes. Read-only — the channel does not mutate ensemble state through this edge.
**Shared types:** `TrajectorySignal` (typed schema validated at the channel boundary by mechanism (e)); `SignalEmitter` (Protocol; the registration interface the Calibration Signal Channel exposes for L0 to call into).
**Error handling:** mechanism (e) — read-only structural validation at the consumer — rejects malformed signals with `LlmOrcStructuralError(error_kind="malformed_signal", recovery_action_required=internal)`. Malformed signals are logged but do not influence verdicts; the verdict computation skips them as if outside the time window. This typed error is internal — not raised to the orchestrator's reasoning surface.
**Constraints:**
1. **Read-only** — no upward writes; ADR-002's write-path layering rule is preserved.
2. **Calibration-only** — the channel rejects non-calibration data via mechanism (e)'s structural validation guard.
3. **Bounded by mechanisms (a)–(e)** — the L1 consumer reads in fresh evaluation context (mechanism (a)), with time-decay-windowed feature aggregation (mechanism (b)), with deterministic-tool-output anchors when the dispatched ensemble has script-model slots (mechanism (c)), with periodic out-of-band audit dispatch detecting drift (mechanism (d)), with read-only structural validation rejecting malformed signals (mechanism (e)).
**Owned by:** Calibration Signal Channel defines the upward-data-flow contract; the L0-side hook registration is implemented by Ensemble Engine but the schema and discipline live with the channel.

### Orchestrator Tool Dispatch → Dispatch Event Substrate *(new in Cycle 6 per ADR-023)*

**Protocol:** Synchronous calls for `dispatch_id` generation and event emission. Tool Dispatch calls `new_dispatch_id(session_id) -> str` at `invoke_ensemble` entry; calls `emit(DispatchTiming(phase="start", ...))` before tier selection; calls `emit(DispatchTiming(phase="end", ...))` after harness/substrate-write; calls `events_for(dispatch_id) -> list[Event]` when constructing the envelope's `diagnostics`.
**Shared types:** `DispatchTiming`, `dispatch_id: str`, the substrate's `emit()` and `events_for()` signatures.
**Error handling:** event emission is best-effort — if a sink raises during emit, the substrate logs the failure and continues fanning out to other sinks; the producer's logic is not affected. Substrate-level failures do not propagate to `invoke_ensemble`'s return path (the dispatch completes; observability is degraded for that dispatch only).
**Owned by:** Dispatch Event Substrate defines the contract.

### Producer → Dispatch Event Substrate *(four edges, new in Cycle 6 per ADR-023)*

**Edges:** Calibration Gate (emits `CalibrationVerdict` with `dispatch_id`); Tier-Escalation Router (emits `TierSelection` with `dispatch_id`); Tier-Router-Audit (emits `AuditDiagnostic` with `dispatch_id`); Calibration Signal Channel (emits `CalibrationSignal` with `dispatch_id`).
**Protocol:** Synchronous `emit(event)` calls at the producer's existing emission points. Producers gain a Dispatch Event Substrate dependency; the substrate fans out to registered sinks. **Backward-compatible transition:** the four existing event types each gain an optional `dispatch_id: str | None` field; events emitted from migration-incomplete call sites carry `None` and are logged with a "call-site-orphaned" marker at consumers.
**Shared types:** `CalibrationVerdict`, `TierSelection`, `AuditDiagnostic`, `CalibrationSignal` — each extended with `dispatch_id: str | None`.
**Error handling:** same as Tool Dispatch edge — emission is best-effort.
**Owned by:** Dispatch Event Substrate defines the substrate's `emit()` contract; the producer modules retain ownership of their event types' semantic content.

### Dispatch Event Substrate → Operator-Terminal Event Sink *(new in Cycle 6 per ADR-023)*

**Protocol:** Observer-pattern callback. The sink registers via `register_sink(sink_instance)` at serve startup; the substrate invokes `sink_instance.consume(event)` for each emitted event.
**Shared types:** the union of `DispatchTiming`, `TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`.
**Error handling:** sink-side exceptions are caught by the substrate and logged at WARN level; the substrate continues to fan out to other registered sinks.
**Owned by:** Operator-Terminal Event Sink defines the `consume()` contract for its registered surface.

### Dispatch Event Substrate → Orchestrator-Context Event Sink *(new in Cycle 6 per ADR-023)*

**Protocol:** Observer-pattern callback (same shape as the operator-terminal edge); plus a query surface `observations_for(dispatch_id) -> Observation` the Orchestrator Runtime calls at turn boundaries.
**Shared types:** the same event-type union as the operator-terminal edge; additionally the `Observation` (the JSON-shaped structured-observation block).
**Error handling:** `observations_for` returns an empty observation block when called with an unknown or already-consumed `dispatch_id`; the orchestrator's next turn proceeds without the observation.
**Owned by:** Orchestrator-Context Event Sink defines the `consume()` and `observations_for()` contracts.

### Orchestrator Runtime → Orchestrator-Context Event Sink *(new in Cycle 6 per ADR-023)*

**Protocol:** Synchronous turn-boundary query. Runtime calls `observations_for(last_dispatch_id) -> Observation` after a dispatch returns control and before assembling the next turn's context.
**Shared types:** `Observation` — `{dispatched: str, duration_seconds: float | None, model_profile: str | None, tier: str | None, topaz_skill: str | None, calibration_verdict: Literal["Proceed","Reflect","Abstain"] | None, dispatch_id: str}`. Missing field values fall back to `None`; the schema is canonical.
**Error handling:** if `observations_for` raises, Runtime proceeds with no observation block (degraded mode); FC-22 below verifies the degradation path.
**Owned by:** Orchestrator-Context Event Sink defines the contract; the Runtime is the consumer.

### Orchestrator Tool Dispatch → Session Artifact Store *(new in Cycle 6 per ADR-025)*

**Protocol:** Synchronous call for substrate-routed dispatches only. Tool Dispatch calls `write_deliverable(dispatch_id, deliverable_name, content, content_type) -> ArtifactReference` after `EnsembleExecutor.execute` returns, when the dispatched ensemble's `output_substrate == "artifact"`.
**Shared types:** `ArtifactReference` — `{path: str, content_type: str, size_bytes: int, summary: str, retention: Literal["session","durable","ephemeral"]}`.
**Error handling:** filesystem failures (permission denied; disk full) raise typed errors; Tool Dispatch converts them to `DispatchEnvelope(status="error", errors=[{stage: "substrate_write", error_type: "ArtifactWriteFailed", ...}])`. Substrate-write failures are observable via the envelope's `errors[]`; the dispatch event stream still records `DispatchTiming(phase="end", exit_status="error")`.
**Owned by:** Session Artifact Store defines the contract.

### Session Registry → Session Artifact Store *(new in Cycle 6 per ADR-025)*

**Protocol:** Lifecycle callback. Session Registry calls `on_session_close(session_id) -> None` when a session terminates; Session Artifact Store removes all `retention: session` artifacts (and their per-dispatch directories) under `.llm-orc/agentic-sessions/<session_id>/`.
**Shared types:** `session_id: str`.
**Error handling:** filesystem cleanup failures are logged at WARN level; orphaned artifacts under closed-session directories accumulate and are surfaced for operator review at next startup (a `WARN: orphaned session artifacts: <session_id>` line); the session-close flow does not block on cleanup completion.
**Owned by:** Session Artifact Store defines the contract; Session Registry is the trigger source.

### Session Artifact Store → Dispatch Event Substrate *(new in Cycle 6 per ADR-025)*

**Protocol:** Synchronous read. Session Artifact Store calls `dispatch_id_for_current_dispatch() -> str` to read the active dispatch's `dispatch_id` when constructing the artifact path. The substrate is the single source of truth for `dispatch_id`.
**Shared types:** `dispatch_id: str`.
**Error handling:** if no active dispatch context exists (substrate-write called outside dispatch lifecycle), raises `IllegalStateError`; this is a programming error, not a runtime condition.
**Owned by:** Dispatch Event Substrate defines the contract.

### Serving Layer → Operator-Terminal Event Sink *(new in Cycle 6 per ADR-023)*

**Protocol:** Synchronous fire-and-forget calls. Serving Layer calls `emit_tool_call_log(tool_name, dispatch_id)` when the SSE stream contains an `invoke_ensemble` tool-call structure; calls `emit_heartbeat(session_id, elapsed_seconds)` from a timer thread when an open request reaches `heartbeat_interval_seconds` of tool-inactivity.
**Shared types:** `tool_name: str`, `dispatch_id: str`, `session_id: str`, `elapsed_seconds: int`.
**Error handling:** emit calls are best-effort; sink-side failures do not interrupt SSE streaming.
**Owned by:** Operator-Terminal Event Sink defines the contract.

### Operator-Terminal Event Sink → Ensemble Engine *(new in Cycle 6 per ADR-023)*

**Protocol:** Synchronous read at serve startup or library reload. Sink calls `validation_results() -> list[ValidationResult]` to retrieve the validated-once subset's startup errors; emits one `WARN` per invalid YAML at startup.
**Shared types:** `ValidationResult` — `{yaml_path: str, error: str}`.
**Error handling:** when validation_results returns an empty list (no startup errors), no WARN lines emit.
**Owned by:** Ensemble Engine defines the contract; the validation cache is library-loader-internal state.

### Calibration Gate → Session Artifact Store *(new in Cycle 6 per ADR-025; conditional on `calibration_substrate_access: artifact`)*

**Protocol:** Synchronous query at calibration-evaluation entry. Gate calls `artifact_read_surface_for(dispatch_id) -> ArtifactReadTool | None`; when the ensemble's YAML declares `calibration_substrate_access: artifact`, the surface is a callable the critic agent can invoke as a tool; when absent or `summary`, the surface is `None`.
**Shared types:** `ArtifactReadTool` — a thin callable wrapping filesystem read at `artifacts[0].path`.
**Error handling:** filesystem read failures during critic-agent evaluation surface as content-evaluation failures within the gate's existing error handling (the verdict computation continues with whatever content was readable).
**Owned by:** Session Artifact Store defines the contract.

### Cross-cutting integration: shared `LlmOrcStructuralError` typed-error infrastructure *(new in Cycle 4 per ADR-017 §"Shared typed-error base class")*

**Not a single edge — a cross-module integration contract.**

**Base class** (lives in `models/structural_errors.py` or extends `models/base.py` to preserve the codebase precedent at commit `9f86d0b`):

```python
class LlmOrcStructuralError(Exception):
    error_kind: str
    dispatch_context: dict
    recovery_action_required: Literal[
        "reformulate", "escalate", "abstain", "operator_intervention_required"
    ]
    operator_diagnostic: str
```

**Eight initial `error_kind` subclasses (one per Cycle 4 typed-error surface):**

| `error_kind` | Producer | `recovery_action_required` | ADR |
|--------------|----------|---------------------------|-----|
| `tool_call_rejected_per_model` | Provider adapter (existing — migrated as first concrete subclass) | `reformulate` | commit `9f86d0b` |
| `phantom_tool_call` | Tool Dispatch (structural validation guard) | `reformulate` | ADR-017 |
| `compaction_layer_4_failure` | Conversation Compaction (Layer 4 circuit-breaker) | `operator_intervention_required` (after 3 consecutive failures) | ADR-012 |
| `write_gate_rejection` | Session Registry (write-gate validation; three classes) | `reformulate` | ADR-013 |
| `calibration_abstain` | Calibration Gate (Abstain verdict) | `reformulate` | ADR-014 |
| `escalation_bypass` | Tier-Escalation Router (Abstain verdict produces typed bypass) | `reformulate` | ADR-015 |
| `missing_skill_metadata` | Tier-Escalation Router (ensemble lacks `topaz_skill` field) | `reformulate` | ADR-015 |
| `malformed_signal` | Calibration Signal Channel (mechanism (e) — schema validation; internal — not raised to orchestrator) | (internal — verdict computation skips signal) | ADR-016 |

**Provenance:** ADR-017 §"Shared typed-error base class" + commit `9f86d0b` typed-error precedent. Naming and field finalization is BUILD-time work (WP-A4); ARCHITECT commits to the structural commitment.

### Serving Layer → Dispatch Pipeline *(new in Cycle 7 per ADR-027)*

**Protocol:** Async function call on the request entry. Serving Layer's `chat_completions()` handler invokes `DispatchPipeline.run(context)` which returns an `AsyncIterator` of chunks (same vocabulary the runtime produces: `ContentDelta | VisibilityEvent | ClientToolCall | Completion`).
**Shared types:** `SessionContext` (existing — input); chunk union from `agentic/orchestrator_chunk.py` (existing — output stream).
**Error handling:** Pipeline-stage failures (planner failure, dispatch failure, synthesizer failure) are caught inside the pipeline and surfaced as either `VisibilityEvent` chunks (recoverable) or `ToolCallError`-equivalent chunks (irrecoverable, falling through to direct-completion path per ADR-032). The Serving Layer sees chunks; HTTP-level errors are reserved for upstream Pydantic validation or session-resolution failures.
**Owned by:** Dispatch Pipeline (it owns the pipeline's chunk surface; the Serving Layer is the chunk consumer).

### Dispatch Pipeline → Routing Planner *(new in Cycle 7 per ADR-027 + ADR-028)*

**Protocol:** Async function call. Pipeline calls `RoutingPlanner.plan(context, capability_list)` which returns a typed `DispatchPlan(action: Literal["dispatch","direct"], ensemble: str | None, input: str | None, rationale: str)` dataclass.
**Shared types:** `SessionContext`, `CapabilityList` (list of `CapabilityEntry(name, description, topaz_skill)`), `DispatchPlan` (new in Cycle 7; ADR-028 §Output contract).
**Error handling:** Planner schema-non-conformance after Calibration Gate retries → `ToolCallError` surfaced; the pipeline falls through to `DispatchPlan(action="direct", ...)` with a synthetic `rationale` documenting the planner failure (per ADR-032 §"Fallback response shape (when `action: 'direct'`)"); operator-observable degradation event fires.
**Owned by:** Routing Planner.

### Dispatch Pipeline → Response Synthesizer *(new in Cycle 7 per ADR-027 + ADR-029)*

**Protocol:** Async generator. Pipeline calls `ResponseSynthesizer.synthesize(context, plan, dispatch_results)` which yields chunks (`ContentDelta` for streamed tokens; terminal `Completion(finish_reason="stop")`). Non-streaming consumption collects chunks into the response body.
**Shared types:** `SessionContext`, `DispatchPlan`, `DispatchResults` (typed wrapper around `DispatchEnvelope | None`; the new Cycle 7 envelope-around-the-envelope when stage 2 ran successfully; `None` when `action: "direct"`).
**Error handling:** Synthesizer dispatch failure → `ToolCallError` surfaced; pipeline yields a terminal `Completion(finish_reason="error")` with a fallback content body explaining the failure. Calibration Gate Reflect verdicts (per ADR-029 §"Calibration Gate integration") trigger tier-escalation retries within the synthesizer ensemble — transparent to the pipeline.
**Owned by:** Response Synthesizer.

### Dispatch Pipeline → Orchestrator Tool Dispatch *(new in Cycle 7 per ADR-027 §Dispatch stage)*

**Protocol:** Async function call via the Plan → InternalToolCall adapter. Pipeline constructs `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": plan.ensemble, "input": plan.input})` and calls `OrchestratorToolDispatch.dispatch(call, session_id=session_id)` which returns `ToolCallResult` (`ToolCallSuccess` with `DispatchEnvelope` or `ToolCallError`).
**Shared types:** `InternalToolCall` (existing — Cycle 6); `ToolCallResult` (existing — Cycle 6); `DispatchEnvelope` (existing — Cycle 6 per ADR-024).
**Error handling:** Dispatch failure (Autonomy Policy denial, Calibration Gate Abstain, tier-router `escalation_bypass`, ensemble invocation failure) returns `ToolCallError`; the pipeline routes to `DispatchPlan`-shaped fallback behavior — the synthesizer is invoked with `dispatch_results` set to the error state per ADR-029 §Input contract DISPATCH RESULTS "when action=dispatch and dispatch failed" case.
**Owned by:** Orchestrator Tool Dispatch (the contract is pre-existing; the Plan → InternalToolCall adapter conforms to it without modification).

### Routing Planner → Orchestrator Tool Dispatch *(new in Cycle 7 per ADR-028 §Calibration Gate integration)*

**Protocol:** Async function call. Routing Planner constructs `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": "agentic-routing-planner", "input": <serialized REQUEST + CAPABILITY LIST>})` and calls `OrchestratorToolDispatch.dispatch()`; receives `ToolCallSuccess` with `DispatchEnvelope`; parses `envelope.primary` (or `envelope.structured` if produced) as JSON conforming to the dispatch-plan schema.
**Shared types:** `InternalToolCall`, `ToolCallResult`, `DispatchEnvelope` (all existing).
**Error handling:** Calibration Gate Reflect → tier-escalation retry handled within the planner ensemble (transparent); persistent Reflect → planner returns `ToolCallError` after retries exhaust → Routing Planner surfaces `ToolCallError` to Dispatch Pipeline. JSON-non-conformance after the calibration gate's structural-validation Reflect retries → same path.
**Owned by:** Orchestrator Tool Dispatch (existing contract). Routing Planner is one of many callers of the same chokepoint.

### Response Synthesizer → Orchestrator Tool Dispatch *(new in Cycle 7 per ADR-029 §Calibration Gate integration)*

**Protocol:** Async function call mirroring Routing Planner's contract. Response Synthesizer constructs `InternalToolCall(id=new_dispatch_id(), name="invoke_ensemble", arguments={"ensemble_name": "agentic-response-synthesizer", "input": <serialized REQUEST + PLAN + DISPATCH RESULTS>})` and calls `OrchestratorToolDispatch.dispatch()`. For streaming requests, the synthesizer ensemble dispatches in streaming mode (existing EnsembleExecutor streaming surface) and the synthesizer module yields `ContentDelta` chunks as tokens arrive.
**Shared types:** Same as Routing Planner contract.
**Error handling:** Three new ADR-029 Calibration Gate Reflect-trigger criteria (Rule 5 framing absence; Rule 4 rounding drift; Rule 1 fabrication signal) trigger tier-escalation retries within the synthesizer ensemble (transparent to the synthesizer module); persistent Reflect → `ToolCallError` after retries exhaust → Response Synthesizer surfaces the error to Dispatch Pipeline.
**Owned by:** Orchestrator Tool Dispatch (existing contract).

### Capability Discovery Endpoint → Capability List Builder *(new in Cycle 7 per ADR-032 + ADR-026)*

**Protocol:** Synchronous function call. Endpoint invokes `CapabilityListBuilder.list_capabilities()` which returns `list[CapabilityEntry(name, description, topaz_skill)]`.
**Shared types:** `CapabilityEntry` (new dataclass in Cycle 7; lives in `agentic/capability_list.py` or equivalent module).
**Error handling:** Registry-empty edge case → returns empty list; the endpoint surfaces an empty `data: []` (or sibling-endpoint equivalent) per the OpenAI protocol. Schema-drifted YAMLs that fail validate-once-at-load are excluded at the registry layer per FC-27.
**Owned by:** Capability List Builder.

### Dispatch Pipeline → Capability List Builder *(new in Cycle 7 per ADR-028 §Input contract)*

**Protocol:** Same as Capability Discovery Endpoint's contract — single shared canonical capability list source. Dispatch Pipeline calls `CapabilityListBuilder.list_capabilities()` per request (or per session, with operator-tuneable cache TTL per ADR-031 §"Cached planner decisions") to feed the Routing Planner's CAPABILITY LIST input.
**Shared types:** Same as Capability Discovery Endpoint contract.
**Error handling:** Same — empty registry produces empty list; the routing-planner sees an empty CAPABILITY LIST and emits `action: "direct"` for every request (degenerate but predictable).
**Owned by:** Capability List Builder.

### Dispatch Pipeline → Dispatch Event Substrate *(new in Cycle 7 per ADR-027 §"Relationship to existing infrastructure" + ADR-032 §"Operator-observable degradation signaling")*

**Protocol:** Synchronous `substrate.emit(event)` calls at pipeline-stage transitions. Pipeline emits `PlanEmitted(dispatch_id, plan)` after Stage 1; `DispatchFired(dispatch_id, ensemble_name)` at Stage 2 entry (when `action: "dispatch"`); `SynthesizerCompleted(dispatch_id, finish_reason)` at Stage 3 exit; `DirectCompletionFallback(dispatch_id, request_shape_category, planner_rationale)` when `action: "direct"` per ADR-032.
**Shared types:** Four new event dataclasses (Cycle 7). All carry `dispatch_id` (allocated by Dispatch Pipeline at request entry per ADR-023 §"`dispatch_id` correlation identifier"; consistent with FC-22 cross-surface coupling — pipeline-stage events share `dispatch_id` with the dispatched capability ensemble's dispatch events when stage 2 fires).
**Error handling:** Substrate emission is fire-and-forget; emission errors are caught by the substrate's Protocol-based sink fan-out (each sink errors independently per existing pattern).
**Owned by:** Dispatch Event Substrate (existing contract; the four new event types are additive — duck-typed Protocol per FC-24 confirmed structurally extensible).

### Capability List Builder → Ensemble Engine *(new in Cycle 7)*

**Protocol:** Synchronous function call via Protocol consumer. `CapabilityListBuilder` consumes a `CapabilityRegistry` Protocol (interface defined in Capability List Builder's module) which is satisfied by the validate-once-at-load library cache established Cycle 6 / FC-27. Concrete implementation reads through existing infrastructure (`EnsembleLoader` + cached `EnsembleConfig` collection).
**Shared types:** `EnsembleConfig` (existing — read-only consumption of `name`, `description`, `topaz_skill`, `output_substrate` fields for filter logic).
**Error handling:** Validate-once-at-load already excludes schema-drifted YAMLs; the builder's filter logic excludes system ensembles (no `topaz_skill` + system-ensemble naming convention `agentic-*` per ADR-019 + ADR-025).
**Owned by:** Capability List Builder (defines the Protocol); Ensemble Engine + `EnsembleLoader` (existing concrete implementation).

### Serving Layer → Response Labeling *(new in Cycle 7 per ADR-032 + ADR-030)*

**Protocol:** Synchronous function call. Serving Layer invokes `ResponseLabeling.decorate(response_body, response_headers, plan, envelope, tool_choice_present)` returning the decorated `(response_body, response_headers)` tuple. The decorator is stateless.
**Shared types:** Plain `dict[str, Any]` for body/headers; `DispatchPlan`, `DispatchEnvelope | None`, `bool` for `tool_choice_present`.
**Error handling:** Decoration is best-effort; if Response Labeling raises, the Serving Layer logs at WARN and returns the undecorated response (no honest-labeling signal but no client-facing error). This is graceful degradation — the configuration-honesty signal is best-effort, not blocking.
**Owned by:** Response Labeling.

### Operator-Terminal Event Sink — `DirectCompletionFallback` consumption *(extension in Cycle 7 per ADR-032)*

**Protocol:** Existing `EventSink.consume(event)` per ADR-023 (Protocol-based sink fan-out). Cycle 7 adds an `isinstance` arm for `DirectCompletionFallback` events that emits a line-oriented `key=value` log entry per ADR-023's operator-terminal format. The sink also maintains a rolling-window aggregator producing the `direct_completion_rate` metric per ADR-032 §"Operator-observable degradation signaling".
**Shared types:** `DirectCompletionFallback` event dataclass (new in Cycle 7).
**Error handling:** Sink-side aggregation errors are bounded — a malformed event silently drops (per existing ADR-023 sink pattern) and emits a WARN line.
**Owned by:** Operator-Terminal Event Sink (existing module; additive isinstance arm + aggregator).

### Orchestrator-Context Event Sink — `DirectCompletionFallback` consumption *(extension in Cycle 7 per ADR-032)*

**Protocol:** Existing `EventSink.consume(event)` per ADR-023. Cycle 7 adds an `isinstance` arm for `DirectCompletionFallback` events that appends a structured-observation block to the orchestrator-context surface per the existing pattern (per ADR-023 §Destination 2). Whether the orchestrator-context surface is meaningful under ADR-027 (where the orchestrator-LLM is removed from the chat-completions surface) is a *new question* the BUILD phase addresses: if the orchestrator-context destination's consumer is the dormant `OrchestratorRuntime`, its activity on chat-completions is structurally dormant; the sink's consumption stays defined (Cycle 6 contract) but its surface is post-Cycle 7 BUILD reserved for future surfaces adopting `OrchestratorRuntime`.
**Shared types:** Same as Operator-Terminal extension.
**Error handling:** Same — sink-side error bounded; WARN-line on malformed events.
**Owned by:** Orchestrator-Context Event Sink (existing module; additive isinstance arm).

### Serving Layer → Loop Driver *(new in Cycle 7 loop-back per ADR-033)*

**Protocol:** Async function call on the tool-driven surface. After the surface-mode discriminator detects client `tools[]`, `chat_completions()` invokes the Loop Driver for the turn, passing the conversation (including any trailing `role: "tool"` result) + the client's declared tool surface + the seat-filler LLM (constructed by the Serving Layer from the resolved Model Profile). The Loop Driver returns a turn outcome — either a tool-call decision (`{client_tool, deliverable_reference}`) or a finish (text completion).
**Shared types:** `SessionContext` (existing — input, extended to surface trailing `role: "tool"` messages, since `_extract_request`'s last-user-message extraction drops them); a typed `TurnOutcome` (the per-turn decision — client tool + deliverable reference, or finish) the Terminal consumes.
**Error handling:** Driver-model failure or budget exhaustion produces a typed terminal outcome (finish with an error completion); the loop does not hang. Single-step enforcement is applied inside the Loop Driver's outcome construction (the Single-Step Enforcer truncates before the outcome is returned), so the Serving Layer never sees a multi-action outcome.
**Owned by:** Loop Driver (it owns the `TurnOutcome` shape; the Serving Layer is the consumer + the seat-filler-LLM injector).

### Loop Driver → Single-Step Enforcer *(new in Cycle 7 loop-back per ADR-033)*

**Protocol:** Synchronous pure-function call. The Loop Driver passes the driver model's proposed action set (zero or more client tool calls) to the enforcer, which returns at most one client tool call (the first) and discards the rest. Stateless — no turn-to-turn state.
**Shared types:** The driver's proposed action list (the parsed tool-call set from the seat-filler response) in; a single client tool call (or "finish") out.
**Error handling:** An empty proposal returns "finish" (the driver proposed no action — treat as task-complete-this-turn). A malformed proposal is rejected to "finish" with a diagnostic event rather than dispatching an unparseable action.
**Owned by:** Single-Step Enforcer (it owns the truncation policy contract).

### Loop Driver → Orchestrator Tool Dispatch *(new in Cycle 7 loop-back per ADR-033 §Decision 2)*

**Protocol:** Async function call through the existing `dispatch()` chokepoint. For a turn requiring generated content, the Loop Driver constructs `InternalToolCall(name="invoke_ensemble", arguments={"ensemble_name": <selected capability>, "input": <serialized step task>})` and calls `OrchestratorToolDispatch.dispatch()` for a **single** capability ensemble — calibration gate, tier-escalation router, and autonomy interpositions fire as for any ensemble. Returns a typed `ToolCallResult` (`ToolCallSuccess` with `DispatchEnvelope` per ADR-024).
**Shared types:** `InternalToolCall` (existing — input); `ToolCallResult` / `DispatchEnvelope` (existing — output, carrying `artifacts[0]` for substrate-routed deliverables or `primary` for inline).
**Error handling:** A `ToolCallError` from dispatch is surfaced to the Loop Driver's next-turn decision (the driver may retry, choose a different capability, or finish with an error); it does not crash the loop.
**Owned by:** Orchestrator Tool Dispatch (the existing chokepoint contract; ADR-024 envelope). Identical to the contract Routing Planner and Response Synthesizer use — the loop-back reuses the chokepoint rather than adding a new dispatch path.

### Client-Tool-Action Terminal → Artifact Bridge *(new in Cycle 7 loop-back per ADR-034 §Decision 3)*

**Protocol:** Synchronous function call. The Terminal passes the turn's deliverable reference (`DispatchEnvelope.artifacts[0]` for substrate-routed, or `envelope.primary` for inline) to the Artifact Bridge, which returns the deliverable content for the tool-call `content` argument.
**Shared types:** `ArtifactReference` (existing, ADR-024/ADR-025) or `envelope.primary` (existing) in; deliverable content (`str | bytes`) out.
**Error handling:** A missing or unreadable artifact produces a typed bridge error the Terminal surfaces as a dispatch-failure completion (it does not emit a `write` with empty or fabricated content — the fidelity FC forbids a paraphrase substitute).
**Owned by:** Artifact Bridge (it owns the marshalling contract + the artifact-vs-inline branch).

### Artifact Bridge → Session Artifact Store *(new in Cycle 7 loop-back per ADR-034 §Decision 3; advisory #4)*

**Protocol:** Synchronous function call to the new `read_deliverable(reference: ArtifactReference) -> str | bytes` accessor. The store resolves `reference.path` relative to its root and returns content. This is the first read-side API on the formerly write-only store (`write_deliverable` / `cleanup_*` only).
**Shared types:** `ArtifactReference` (existing) in; deliverable content (`str | bytes`) out.
**Error handling:** A path that does not resolve (artifact cleaned up, never written, or a malformed reference) raises a typed `artifact_not_found` error the bridge propagates. Retention semantics are unchanged — the bridge reads within the artifact's retention window (the deliverable is read during the same session that produced it, before `cleanup_session`).
**Owned by:** Session Artifact Store (it owns the store layout + the new read accessor; the bridge is the consumer). **Scope (advisory #4):** fidelity at scale (large files, encoding edge cases) is BUILD-scope validation, not an assumed property — spike evidence used trivially small content.

### Serving Layer → Client-Tool-Action Terminal *(new in Cycle 7 loop-back per ADR-034 §Decision 1 + 2)*

**Protocol:** Synchronous composition on the tool-driven response path. The Serving Layer hands the Loop Driver's `TurnOutcome` to the Terminal, which emits the wire response: for a tool-call outcome, a streamed `ClientToolCall` chunk (`finish_reason: "tool_calls"`, OpenAI streaming delta shape via the existing `OpenAiSseFormatter`); for a finish outcome, a text `Completion`. The Terminal also owns consuming the incoming request's trailing `role: "tool"` message and surfacing it to the Loop Driver via the `SessionContext` extension (loop participation).
**Shared types:** `TurnOutcome` (from Loop Driver) in; `ClientToolCall` / `Completion` chunks (existing union, `OpenAiSseFormatter`-consumed) out; the marshalled tool-call content from the Artifact Bridge.
**Error handling:** A bridge error or an empty deliverable produces a dispatch-failure text completion, not a malformed tool call. The Terminal never writes to the client filesystem (FC-48) — it only emits tool calls the client executes.
**Owned by:** Client-Tool-Action Terminal (it owns the emission mechanism + loop-participation message routing; the Loop Driver owns the tool *choice*, the Terminal owns the tool *emission*).

### Loop Driver → Delegation Rate Meter *(new in Cycle 7 loop-back #3 per ADR-036 §Decision 3)*

**Protocol:** Synchronous pure-function call per turn: `classify_turn(message, observed_values, capability_domains) -> TurnShape`. The driver stamps the result onto the turn's `TurnDecision` event (`turn_shape` field).
**Shared types:** Raw turn inputs (`str` message, observed-value list, capability-domain patterns) in; `TurnShape` enum (generation / carry / boundary_excluded) out. The meter imports nothing from the Loop Driver — the edge is one-directional and cycle-free by construction.
**Error handling:** Classification cannot fail operationally (a pure rule over strings); an unclassifiable shape IS the `boundary_excluded` result, not an error. A missing classification on an emitted `TurnDecision` violates FC-59.
**Owned by:** Delegation Rate Meter (it owns the rule and the `TurnShape` vocabulary).

### Operator-Terminal Event Sink → Delegation Rate Meter *(new in Cycle 7 loop-back #3 per ADR-036 §Decision 3)*

**Protocol:** Synchronous call to `delegation_rate(events) -> RateReading` over the sink's consumed `TurnDecision` events (rolling window).
**Shared types:** An iterable of objects satisfying the meter's narrow structural protocol (`turn_shape`, `delegated_ensemble` attributes) in; `RateReading` (rate, numerator, denominator, boundary-excluded count, window) out. The boundary-excluded count is part of the reading — the denominator-degradation signal is observable, not internal.
**Error handling:** An empty window yields a typed no-reading result (not a 0.0 rate — an absent denominator is not a zero rate).
**Owned by:** Delegation Rate Meter (it owns the rate semantics; the sink owns surfacing cadence — a BUILD open choice).

### Loop Driver → Session Action Record *(new in Cycle 7 loop-back #5 per ADR-037 §Decision 2)*

**Protocol:** Synchronous calls — `record_action(session_id, action_kind, target_path)` at the driver's tool-call decision/emission; `join_result(session_id, tool_result)` when the next request's `role: "tool"` message arrives; `records(session_id)` read at judgment composition.
**Shared types:** Action records as (action kind, target file path, result) tuples — the committed first-increment schema of the extensible meta-record seam; the digest *text* is composed by the Loop Driver's judgment helper from the records, so schema extensions land in this module and render generically downstream.
**Error handling:** A missing or empty record set for a trailing turn does NOT silently degrade to client-message reconstruction (FC-64 — round 1 measured that failure); the judgment call is composed with an explicitly empty digest, which the deliverable-accounting standard treats as no-deliverables-produced (an honest evidence state, not a fabricated one).
**Owned by:** Session Action Record (it owns the record schema and join semantics; the driver owns digest rendering and the judgment question text).

---

## Fitness Criteria

| # | Criterion | Measure | Threshold | Derived From |
|---|-----------|---------|-----------|-------------|
| FC-1 | No module owns more than 5 scoped glossary entries as primary owner | Count rows per module in the Responsibility Matrix | ≤ 5 | God-class prevention (Essay §Guardrails; ARCHITECT principle) |
| FC-2 | Dependency edges point from higher layer to same-or-lower layer only | Static inspection of module imports against L0-L3 assignment | 0 violations | Layering rule (Dependency Graph) |
| FC-3 | No cycles in the dependency graph | Static cycle detection over the dependency edge list | 0 cycles | ARCHITECT principle |
| FC-4 | Orchestrator Runtime imports only Budget Controller and Orchestrator Tool Dispatch — no Result Summarizer Harness, no Plexus, no config, no Autonomy, no Calibration. Result Summarizer Harness is interposed on the `invoke_ensemble` return path by Orchestrator Tool Dispatch (Amendment #3). | Static import check | Exact match | Orchestrator LLM's mental model alignment; structural ADR-003 enforcement |
| FC-5 | Orchestrator Tool Dispatch has exactly five public entry points — one per committed tool | Static count of public dispatch methods | = 5 | ADR-003 (closed tool set) |
| FC-6 | Composition Validator and Ensemble Engine's load path call the same public validator function | Static check: single definition, two call sites | 1 definition, 2+ call sites | Scenarios refactor 1-3; ADR-006 negative consequence |
| FC-7 | Every Plexus-facing code path has a no-op fallback exercised by a stateless-mode test | Per-edge coverage of the stateless branch | 100% | AS-8 |
| FC-8 | `unsummarized-result` cannot reach the Orchestrator Runtime's context | Static check: Runtime imports `ToolCallResult`; no path from `EnsembleExecutor` to Runtime bypasses the Harness | 0 bypass paths | AS-7; ADR-004 |
| FC-9 | Session-start flow calls `resolve_session_start_context` exactly once; the function has a typed signature returning `list[PromptFragment]` | Static inspection | Exactly 1 call; signature present | ADR-009 (structural reservation via typed function) |
| FC-10 | Budget check executes before every ReAct iteration begins | Integration test covering the iteration-boundary contract | 100% of iterations | AS-3; ADR-005 |
| FC-11 | Autonomy Policy check executes before every Orchestrator Tool Dispatch | Integration test | 100% of dispatches | ADR-008 |
| FC-12 | Composed ensembles enter Calibration Gate transparently on `invoke_ensemble` during calibration | Integration test | 100% of in-calibration invocations | ADR-007 |
| FC-13 | Changing the orchestrator Model Profile requires touching only Orchestrator Configuration and Session start-logic — not Runtime, not Tool Dispatch | Diff inspection on a profile-swap change | No edits to Runtime or Tool Dispatch | ADR-011 |
| FC-14 | Conversation Compaction's five-layer pipeline applies layers in cheapest-first order; Layer 4 LLM-summary fires only after Layers 0–3 have been attempted | Static inspection of `compact()` implementation; integration test exercising each layer's trigger | Layer ordering preserved in code + test demonstrates Layer 4 only fires after Layers 0–3 reduction attempted | ADR-012 §Decision (load-bearing design property); essay 005 §"Long-Horizon Reliability" |
| FC-15 | Every `invoke_ensemble` dispatch passes through the Tier-Escalation Router; the Tool Dispatch cannot reach `EnsembleExecutor.execute` for an `invoke_ensemble` call without passing through tier selection | Static AST dominance check on `orchestrator_tool_dispatch.py::invoke_ensemble`; pattern follows FC-8 | 0 bypass paths | ADR-015 (router operates inside Tool Dispatch's `invoke_ensemble` interposition) |
| FC-16 | Calibration Signal Channel boundary is read-only at the L1 consumer; no upward writes from L1 to L0 through the channel | Static check: channel module exposes only read-side methods to L1 callers; no write hooks register at L0; mechanism (e) rejects malformed signals | 0 write hooks; all upward write attempts produce `malformed_signal` rejection at boundary | ADR-016 §"The signal channel" (read-only constraint) |
| FC-17 | All eight Cycle 4 typed errors derive from `LlmOrcStructuralError` base class with the four common fields (`error_kind`, `dispatch_context`, `recovery_action_required`, `operator_diagnostic`) | Static class-hierarchy walk + AST inspection of error construction sites | 100% of Cycle 4 typed errors are `LlmOrcStructuralError` subclasses with all four fields populated | ADR-017 §"Shared typed-error base class" |
| FC-18 | Every ensemble in the library declares `topaz_skill` metadata; dispatch of an ensemble lacking the field produces `missing_skill_metadata` typed error before tier selection runs | Static check across `.llm-orc/ensembles/*.yaml` + integration test on metadata-missing case | All library ensembles have `topaz_skill`; `missing_skill_metadata` raised pre-dispatch when absent | ADR-015 (Topaz skill metadata required) |
| FC-19 | Tier-Escalation Router's `select_tier` is a stateless pure function — each verdict consumption runs in fresh context with no prior verdict state carried forward through the call | Static AST inspection of `tier_router.select_tier` (no instance state mutated; no module-level state read) + unit test exercising stateless property under sequential dispatches | 0 statefulness violations; sequential `select_tier` calls produce identical output for identical inputs regardless of call history | ADR-018 (inherits ADR-016 mechanism (a) by construction) |
| FC-20 | Tier-Escalation Router's (d)-analog audit dispatch fires at the configured trigger frequency (default every 100 verdict consumptions or 24 wall-clock hours, whichever first); severe-drift verdict activates route-all-to-escalated fail-safe mode plus operator notification | Integration test exercising the trigger boundary + a separate test exercising severe-drift fail-safe mode activation; pattern follows FC-14 (cheapest-first-order audit) | Audit fires within ±10% of configured trigger frequency; severe-drift triggers fail-safe within one dispatch cycle | ADR-018 §"Trigger" + §"Severe-drift response" |
| FC-21 | All dispatch events (`DispatchTiming`, `TierSelection`, `CalibrationVerdict`, `AuditDiagnostic`, `CalibrationSignal`) emitted during one `invoke_ensemble` dispatch share the same `dispatch_id` value; the substrate's `emit()` rejects events with mismatched `dispatch_id` (the in-progress dispatch's `dispatch_id` is the only valid value) | Integration test exercising a full dispatch and asserting `dispatch_id` equality across all emitted events | 100% of intra-dispatch events share the same `dispatch_id` | ADR-023 §"`dispatch_id` correlation identifier" |
| FC-22 | `dispatch_id` consistency holds across three surfaces — the event stream's `dispatch_id`, the envelope's `diagnostics.dispatch_id`, and the artifact path's `<dispatch_id>` segment — for every substrate-routed dispatch | Integration test asserting cross-surface equality for at least one substrate-routed dispatch end-to-end | 100% three-way `dispatch_id` consistency | ADR-023 + ADR-024 + ADR-025; Cycle 6 DECIDE snapshot Finding 2 (`dispatch_id` coupling advisory carry-forward) |
| FC-23 | Every `invoke_ensemble` dispatch passes through Dispatch Event Substrate emission (`DispatchTiming(start)` before tier selection; `DispatchTiming(end)` after harness/substrate-write); a dispatch path that skips substrate emission is structurally impossible | Static AST dominance check on `orchestrator_tool_dispatch.py::invoke_ensemble`; pattern follows FC-8 (Runtime cannot see unsummarized result) and FC-15 (every dispatch through tier router) | 0 bypass paths | ADR-023 §"Event-emission substrate" |
| FC-24 | The Operator-Terminal Event Sink and Orchestrator-Context Event Sink consume from the **same** Dispatch Event Substrate; no parallel-emission path exists for the same data; the unified-substrate Inversion N+2 commitment is structurally enforced | Static check: both sinks import only Dispatch Event Substrate (not other producers directly); the substrate's `register_sink()` is the only registration path | 0 parallel-emission paths | ADR-023 §"Inversion N+2"; Cycle 6 DISCOVER snapshot Action 3 |
| FC-25 | Every capability ensemble in the agentic-serving library (currently 6: `code-generator`, `prose-improver`, `argument-mapper`, `claim-extractor`, `web-searcher`, `text-summarizer`) writes its deliverable via Session Artifact Store under `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/`; system ensembles (`agentic-calibration-checker`, `agentic-result-summarizer`) produce inline envelopes; opt-outs via `output_substrate: inline` are observable in YAML | Static YAML scan over `.llm-orc/ensembles/agentic-serving/`; integration test exercising one capability dispatch end-to-end + one system-ensemble dispatch | 100% capability ensembles substrate-route (or carry explicit `output_substrate: inline` opt-out); 100% system ensembles inline-respond | ADR-025 §"Scope: always, for capability ensembles" |
| FC-26 | The Result Summarizer Harness is **not invoked** for substrate-routed dispatches (`output_substrate: artifact`); is invoked per ADR-004 mandate for inline-response dispatches (`output_substrate: inline`); ADR-004's per-invocation `raw_output=True` escape hatch composes with substrate-routing without contradiction | Integration test in mixed-mode session (one substrate-routed + one inline + one `raw_output=True` inline); assertion on harness invocation per dispatch | AS-7 amended scope holds; substrate-routed = 0 harness invocations; inline = 1 harness invocation per dispatch (or 0 with `raw_output=True` escape hatch fired) | ADR-025 §"AS-7 amendment"; domain-model Amendment Log entry #11 |
| FC-27 | Ensemble library validation runs exactly once at serve startup (or on library reload via `SIGHUP` / admin endpoint / restart); per-`list_ensembles()` re-validation is eliminated; legacy schema-drifted YAMLs produce one `WARN` line at startup and zero per-enumeration warnings | Integration test exercising serve startup + multiple `list_ensembles()` calls within one serve lifetime; pattern follows FC-2 (static import check) at the validation-event-count level | 1 validation pass per serve-startup-or-reload event; 0 per-enumeration re-validations | ADR-023 §"Noise-floor remediation"; post-hotfix verification 2026-05-14 finding 7 |
| FC-28 | Every chat-completions request flows through the Dispatch Pipeline; the chat-completions handler does not construct an `OrchestratorRuntime` instance | Static AST check on `web/api/v1_chat_completions.py` (no `OrchestratorRuntime(...)` construction; no `_build_runtime()` call inside `chat_completions()`) + integration test asserting any chat-completions request emits `PlanEmitted` + either `DispatchFired` or `DirectCompletionFallback` on the substrate | 0 OrchestratorRuntime constructions in chat-completions handler; 100% of requests emit pipeline-stage events | ADR-027 §Decision; AS-9 §Propagation |
| FC-29 | AS-9 satisfied universally on chat-completions surface — no orchestrator-LLM invocation for routing or post-dispatch synthesis on this surface; both surfaces are exercised exclusively by structurally-bounded ensembles (Routing Planner; Response Synthesizer) | Integration test inspecting per-request LLM-call observability: zero `OrchestratorRuntime` LLM calls; routing decisions trace to `agentic-routing-planner` ensemble dispatch events; response composition traces to `agentic-response-synthesizer` ensemble dispatch events | 100% AS-9 satisfaction across the chat-completions test suite | AS-9; ADR-027 §"Relationship to AS-9 and AS-10" |
| FC-30 | AS-10 satisfied — Routing Planner consumes only chat-completions request body + capability list; no out-of-band signals (custom HTTP headers, skill-framework manifest fields, custom metadata) influence routing | Integration test: requests with arbitrary custom headers (e.g., `X-Skill-Framework: rdd`, `X-Capability-Hint: code-generator`) produce identical routing decisions to baseline requests with no custom headers | 100% identical decisions across custom-header / no-custom-header pairs | AS-10; ADR-026 §Decision; ADR-028 §Input contract |
| FC-31 | Routing Planner output JSON conforms to the dispatch-plan schema `{action: "dispatch"|"direct", ensemble: string|null, input: string|null, rationale: string}` across the 20-prompt regression battery; conformance is 100% (the empirical floor from Spike ζ) | Integration test running the 20-prompt battery against the production routing-planner ensemble + JSON-schema validation on every output | 20/20 schema-valid outputs | ADR-028 §Output contract; Spike ζ empirical baseline |
| FC-32 | Response Synthesizer fabrication-prevention regression battery — 13-test + 4-confabulation-mode batch produces 0 fabrications at the deployed cheap-tier model (qwen3:8b empirical baseline; operator-substituted models bear their own characterization burden per ADR-029 §Negative) | Integration test running the spike battery (Spike ε + Spike ε' + Spike μ tests, ported as production regression suite) against the production synthesizer ensemble | 0 fabrications across the 13-test batch | ADR-029 §Strict-fidelity rule set + §"Empirical basis"; Spike ε / ε' / μ batteries |
| FC-33 | Response Synthesizer Rule 5 framing is present on `action: "direct"` responses; absence triggers Calibration Gate Reflect verdict per ADR-029 §Calibration Gate integration | Integration test parsing synthesizer output for the Rule 5 framing marker on direct-completion responses; assertion: marker present; assertion: gate emits Reflect when an experimentally-marker-stripped response is fed back | 100% Rule 5 framing presence on `action: "direct"`; Reflect verdict 100% on marker-absent fixture | ADR-029 §"Rule 5 framing requirement scope"; OQ #23 |
| FC-34 | Plan → InternalToolCall adapter preserves the existing `OrchestratorToolDispatch.dispatch()` contract — adapter produces a well-formed `InternalToolCall` accepted by `dispatch()` returning a typed `ToolCallResult` (`ToolCallSuccess` with `DispatchEnvelope` per ADR-024) | Integration test: a `{"action": "dispatch", "ensemble": "code-generator", "input": "..."}` plan yields a real dispatch via `OrchestratorToolDispatch.dispatch()` returning a real `DispatchEnvelope`; envelope `primary` + `artifacts[0]` populated as the dispatched ensemble specifies | 100% successful dispatch round-trip on the test fixture | ADR-027 §Dispatch stage; ADR-021 §Per-capability dispatch contract; ADR-024 |
| FC-35 | Pipeline chunk vocabulary is identical to runtime chunk vocabulary — `OpenAiSseFormatter` consumes pipeline output unchanged across streaming + non-streaming paths | Static check: Dispatch Pipeline yields only chunk types in the existing union `ContentDelta | VisibilityEvent | ClientToolCall | Completion`; integration test: streaming chat-completions request produces SSE chunks the formatter renders without error; non-streaming request produces the same response shape (subject to synthesizer-vs-orchestrator-LLM content difference) | 0 new chunk types introduced; 100% formatter pass-through on both paths | ADR-027 §Decision (chunk vocabulary preservation); ARCHITECT Finding 8 disposition |
| FC-36 | Capability List Builder excludes system ensembles — `agentic-routing-planner`, `agentic-response-synthesizer`, `agentic-result-summarizer`, `agentic-calibration-checker` are not in the capability list output | Unit + integration: static YAML inspection + runtime list inspection across the production registry contents | 0 system ensembles in capability list output | ADR-028 §Input contract; ADR-019 + ADR-025 |
| FC-37 | Capability List Builder is the single source of truth — Routing Planner's CAPABILITY LIST input and Capability Discovery Endpoint's response contain identical capability identifiers under steady registry state | Integration test: simultaneous extraction of planner input + discovery endpoint response confirms identical capability identifiers | 100% identity across the two consumers | ADR-026 + ADR-028 + ADR-032 (single source of truth principle) |
| FC-38 | Honest Response Labeling fires three-layer signaling consistently — `X-LLM-Orc-Served-By` header + `metadata.served_by` body field on every chat-completions response with one of four canonical values (`ensemble:<name>`, `direct`, `direct_fallback`, `tool_choice:<ensemble>`); content-layer Rule 5 framing on `action: "direct"` only per ADR-029 + ADR-032 | Integration test exercising all four canonical paths; assertion on header + body metadata presence + content-layer presence conditional on path | 100% header + metadata presence; 100% content-layer presence on direct path; 0% content-layer noise on dispatch path | ADR-032 §Sub-promise (1); ADR-029 Rule 5 |
| FC-39 | `tool_choice` bridge advisory fires conditionally per ADR-030 §Bridge advisory specification — headers + body metadata on all `tool_choice`-bearing responses; content-layer Rule 5-adjacent acknowledgment on `action: "direct"` only; `tool_choice: "auto"` treated as absent (no advisory) | Integration test: `tool_choice={"name":"X"}` + `action: "dispatch"` produces headers + metadata only; `tool_choice={"name":"X"}` + `action: "direct"` produces all three layers; `tool_choice: "auto"` produces no advisory | 100% conditional adherence across the matrix | ADR-030 §Bridge advisory specification |
| FC-40 | Streaming response delivers synthesizer tokens via existing `OpenAiSseFormatter` — first-token latency drops to synthesizer-first-token time for streaming-default clients per ADR-031 §"Streaming as a load-bearing surface" | Integration test on streaming-mode chat-completions request: measure time-to-first-byte after handler entry; assertion: TTFB ≤ planner-latency + dispatch-latency + 5 s (synthesizer-first-token allowance at qwen3:8b per Spike ε' baseline) | TTFB within budget on the steady-state empirical baseline | ADR-031 §"Streaming as a load-bearing surface"; Spike ε / ε' / ζ baseline timings |
| FC-41 | `direct_completion_rate` rolling metric is computable from emitted `DirectCompletionFallback` events — operator-terminal sink's aggregator produces the rate over a configurable sliding window (default 24-hour rolling per ADR-032 §"Refutation threshold") | Integration test emitting a fixture sequence of dispatch / direct events over a synthetic time axis; assertion: aggregator produces correct rate; assertion: refutation threshold (sustained `direct_completion_rate` > ~15pp above operator-estimated baseline) fires the configured operator-notification surface | Aggregator correctness on the fixture; threshold-fire correctness | ADR-032 §"Operator-observable degradation signaling" + §"Refutation threshold for the cost-distribution accountability sub-promise" |
| FC-42 | Surface-mode discrimination — a chat-completions request carrying client `tools[]` engages the Loop Driver; a request carrying no client tools engages the Dispatch Pipeline (single-turn); the two surfaces are mutually exclusive per request | Integration test: a `tools[]`-bearing request emits a `TurnDecision` (loop-driver) event; a no-tools request emits `PlanEmitted` (pipeline) events; no request emits both | 100% correct routing across the tool / no-tool pair; 0 requests on both surfaces | ADR-033 §Decision 1; conformance-scan #1 + D1 |
| FC-43 | Single-action-per-turn enforcement — at most one client tool call is dispatched per assistant turn; additional tool calls the driver proposes in the same turn are not dispatched until the first call's result is observed (re-planning on the next client request) | Integration test: a driver response proposing two `write` calls produces exactly one dispatched tool call in the emitted response; the second is dropped, not queued | exactly 1 client tool call per assistant turn; batch truncated | ADR-033 §Decision 3 / FC (single-step enforcement); Spike τ′; D2 (batch-truncation) |
| FC-44 | Callee generation — per-turn content generation on the tool-driven surface routes to a single capability ensemble invocation, not through the `plan → dispatch → synthesize` pipeline | Integration test: a turn requiring generated content produces exactly one `invoke_ensemble` dispatch for a capability ensemble; zero routing-planner + zero response-synthesizer invocations on that turn | 1 capability dispatch; 0 planner + 0 synthesizer per generation turn | ADR-033 §Decision 2 / FC (callee generation); Spike υ |
| FC-45 | Grounded carry — when a turn's action depends on a prior turn's observed tool result, the action uses the observed value, not a placeholder or fabricated value | Integration test on the Spike τ probe shape (a value present only in a prior tool result): the dependent action's tool-call argument carries the observed value; a `${...}`-style unresolved template reference (the τ failure signature) fails the test | 0 unresolved-template / fabricated arguments on the un-batchable chain | ADR-033 §Decision 3 / FC (grounded carry); Spike τ vs τ′ |
| FC-46 | Seat-filler swappability — the driver model is selected via a Model Profile; substituting it (cheap-tier ↔ frontier-tier) requires no change to the Loop Driver control structure, the Single-Step Enforcer, the Client-Tool-Action Terminal, or the ensemble-delegation path | Diff inspection on a driver-model swap: edits touch only Orchestrator Configuration / profile files; no edits to `loop_driver` / `single_step_enforcer` / `client_tool_action_terminal` / the delegation path | 0 edits to the four control-structure surfaces on a driver swap | ADR-033 §Decision 6(a) / FC (seat-filler swappability); ADR-011 |
| FC-47 | Tool-call terminal — a turn whose Loop Driver action is "apply work to the client" produces a response with `finish_reason: "tool_calls"` carrying a client tool call, not a text-only `Completion` | Integration test: an apply-work turn yields `ClientToolCall` → `finish_reason: "tool_calls"`; a turn returning only `ContentDelta` + `Completion` fails the assertion | 100% tool-call-terminal on apply-work turns | ADR-034 §Decision 1 / FC (tool-call terminal); Spike π Phase B |
| FC-48 | No server-side client-filesystem write — the surface never writes to the client's filesystem directly; deliverables reach the client only via client-executed tool calls | Static + integration: no server-side write to a client workspace path (the Spike π Phase A rejected shape); satisfied by absence today, guarded going forward | 0 server-side client-filesystem writes | ADR-034 §Decision 1 / FC (no server-side write); Spike π Phase A |
| FC-49 | Artifact-bridge fidelity — the content marshalled into a tool call equals the ensemble's deliverable as stored in the `SessionArtifactStore` (or `envelope.primary` for inline ensembles), not a paraphrase or summary | Integration test: a substrate-routed deliverable round-trips through `read_deliverable()` into a `write` tool call whose `content` byte-matches the stored artifact; an inline deliverable round-trips through `envelope.primary`. Fidelity at scale (large files, encoding edge cases) is BUILD-scope validation (spike content was trivially small) | byte-identity on the fixture; large-deliverable case added in BUILD | ADR-034 §Decision 3 / FC (artifact-bridge fidelity); Spike ρ F-ρ.1 |
| FC-50 | Loop participation — the surface consumes a `role: "tool"` follow-up and produces a next-turn decision; it does not treat tool-call emission as terminal, and does not drop the tool result | Integration test: a follow-up request carrying a `role: "tool"` message routes the result to the Loop Driver, which decides the next action or finishes; a surface that ignores the follow-up (or whose request extraction drops tool messages — the current `_extract_request`) fails | tool result reaches the Loop Driver on 100% of follow-ups | ADR-034 §Decision 2 / FC (loop participation); conformance-scan #6 |
| FC-51 | Axis-2 diagnostic instrumentation — a failing long-horizon run is diagnosable as split-incorrect vs. callee-incorrect: each turn emits a `TurnDecision` event carrying action + delegated-ensemble + grounded-carry-held + re-plan-after-truncation, so the trajectory reconstructs from the event stream | Integration test: a multi-turn run emits one `TurnDecision` per turn; the event stream reconstructs the action sequence and distinguishes a wrong-action turn (driver/split) from a wrong-content turn (callee) | 1 `TurnDecision` per turn; trajectory reconstructable | ARCHITECT advisory #5 (axis-2 split-vs-callee diagnosability); OQ #27 axis 2 |
| FC-52 | Wrapper-contingency accessibility — the per-turn generation delegation target is a swappable strategy (single-ensemble callee default; full `DispatchPipeline.run()` wrapper reversion) behind the Loop Driver's generation boundary; switching it requires no change to the Loop Driver control structure or the Terminal | Diff inspection on a callee→wrapper switch: edits touch only the Loop Driver's delegation-target selection; no edits to the control structure, Single-Step Enforcer, or Terminal | 0 control-structure edits on a callee→wrapper switch | ARCHITECT advisory #1 (F3-1 wrapper-contingency); ADR-033 §Rejected (wrapper residual) |
| FC-53 | Form-directive presence — every callee `invoke_ensemble` dispatch whose deliverable is bound for a client tool carries a destination-keyed bare-output directive in its dispatch input | Dispatch-input assertion in Loop Driver integration tests; serve-log inspection on the $0 real-OpenCode smoke test | 100% of client-tool-bound dispatches carry the directive | ADR-035 §Decision 1; Spike χ F-χ.2 (directive reliability) |
| FC-54 | Destination-keying — the injected directive matches the decided client tool (`write` → bare file bytes; `bash` → bare command; `edit` → bare replacement content) | Unit tests on the stateless `compose_form_directive` helper | 0 mismatched directives | ADR-035 §Decision 1 |
| FC-55 | Deliverable granularity — one dispatch produces one client-tool deliverable; multi-file work decomposes across successive write-turns | Turn level: FC-43 single-action enforcement (structural); dispatch level: deliverable inspection in integration tests (no multi-file `filename\ncontent` encoding) | 0 multi-file-encoded deliverables reaching the Terminal | ADR-035 §Decision 3; Spike χ-P6 (the granularity-violation shape); ADR-033 F3-1 (across-turn composition) |
| FC-56 | D1 extraction — the stored substrate deliverable is the last successful agent's output; the raw-dict fallback never fires when at least one agent succeeded | Integration tests against the real `SessionArtifactStore` (multi-agent + failed-terminal-node cases) | 0 raw-envelope deliverables for ensembles with ≥1 successful agent | ADR-035 §Decision 5; Spike φ F-φ.1 + Spike χ F-χ.3 |
| FC-57 | Form-gate seam — `ArtifactBridge.marshal` receives the destination tool; the named FormGate seam defaults to pass-through; the detect-and-refuse gate installs at the seam without Terminal→Bridge contract change | Marshal-signature inspection + FormGate default-pass-through unit test; diff inspection on gate installation | 0 Terminal edits on gate installation | ADR-035 §Decision 4; DECIDE loop-back #2 snapshot advisory 2 |
| FC-58 | Directive-in-user-turn presence — every seat-filler dispatch on the tool-driven surface carries the delegation guidance in the user-turn region, never as a framework system message (first turn: attached to the user task; trailing tool-result tail: standalone trailing user-role message — C3 preferred; C1/C2 equally measured, not violations) | Message-composition unit tests (both turn shapes) + captured request bytes on the real-client acceptance run | Guidance in a `role:"system"` message, or absent, fails | ADR-036 §Decision 1 + FC; Spikes ψ/ψ′ (baseline 0/10 vs user-turn 55/55) |
| FC-59 | Delegation-rate measurability — the rate on generation-shaped turns is computable from emitted events alone: `TurnDecision.turn_shape` (denominator; stamped per turn from the turn's **outcome** — a write is `generation`, a read/command/finish is `carry`, with the Meter's instruction-side `classify_turn` owning the `boundary_excluded` determination; WP-LB-M) × `TurnDecision.delegated_ensemble` (numerator); boundary turns excluded, exclusion share observable | Labeled-set regression test (0/12 clear-case errors) + events-alone rate computation test (incl. a trailing multi-file generation turn) + TurnDecision sink-branch test | A deployment from which the rate cannot be computed without log archaeology or replay fails | ADR-036 §Decision 3 + FC; Spike ψ.4a; conformance F-2/F-3; WP-LB-M (outcome-derived denominator) |
| FC-60 | Profile-swap re-validation — a seat-filler Model Profile change is accompanied by a recorded delegation-rate re-validation (pre-swap replay run or post-swap soak window ≥25 generation-shaped turns) | Process FC: inspection at swap time — the swap commit/config change references the recorded run | A profile change with no recorded re-validation fails | ADR-036 §Decision 4 + FC; Spike ψ′ Arm D (lever non-transfer: qwen3.5:9b 1/5, mistral-nemo:12b 2/5); composes with FC-46 (swap stays config-only) |
| FC-61 | Carry-side preservation — guidance composition does not alter grounded-carry behavior: carry-shaped turns select client tools (never `invoke_ensemble`); literal payloads reach client-tool arguments verbatim | Carry-side scenario tests (read/command/literal-write) with verbatim-payload assertion (the tool-name dimension alone cannot see paraphrase violations — methods-review P1-A) | A carry-shaped turn that delegates, or a paraphrased literal payload, fails | ADR-036 §FC; ψ′ Arm B (0/15 false delegations; verbatim 5/5); FC-45 compatibility |
| FC-62 | Tool-list completeness — no seat-filler dispatch carries a tool list excluding all plausible response tools for the turn shape (the ψ.4c empty-response break: qwen3:14b emits neither a tool call nor text under a judged-incompatible tool list) | Composition-path unit test (invoke_ensemble-only lists unconstructable via the driver) + constraint check on any future detect-and-retry implementation | A composed seat-filler request offering only `invoke_ensemble` fails | ADR-036 §Consequences (ψ.4c tool-list design constraint); gate-applied framing item P2-F1 |
| FC-63 | Judgment-first trailing composition — every trailing-tail interaction begins with the framework-composed termination judgment (bare form: framework judge system message; quoted task + digest + accounting standard; no tools; no client prompt) | Composed-request inspection (unit on `decide` + real-client acceptance-run capture) | A trailing-tail dispatch that goes straight to guidance-composed action generation fails | ADR-037 §Decision 1 + FC; Spike θ round 2 (59/60) |
| FC-64 | Digest provenance — the digest's action records derive from the framework's own dispatch/emission records joined with client tool results, never reconstructed from client-serialized messages alone | Code inspection (no digest path reads `context.messages` as record source) + provenance integration test | A digest carrying only what the client serialization preserves fails (the round-1 information-starvation shape) | ADR-037 §Decision 2 + FC; Spike θ F-θ.1/F-θ.2; conformance V-03 |
| FC-65 | Finish protocol cleanliness — a COMPLETE verdict yields a text-only client response (no tool calls) with no `VERDICT:` line; the model remains the stop mechanism | Emitted-response inspection (unit + acceptance-run serve log) | A finish turn carrying tool calls or the `VERDICT:` line fails | ADR-037 §Decision 4 + FC; θ.3 finish-text quality |
| FC-66 *(amended by ADR-038)* | Call-2 form preservation — a REMAINING verdict yields one action call composed per ADR-036's trailing form **plus the remaining-work anchor** (the judge's stripped statement + "Produce that next."); the question, digest, and `VERDICT:` literal stay excluded | Composed-request inspection (unit: call 2's trailing region = the C3 guidance + the judge's actual stripped statement + the imperative; no question/digest/verdict literal) | A call 2 carrying the judgment question/digest/verdict literal, or one with no anchor on a REMAINING tail, fails | ADR-037 §Decision 4 + FC, amended by ADR-038 (call 2 = C3 form + anchor; byte-equality to E4b intentionally broken — the Finding G fix) |
| FC (ADR-038) anchor presence | A REMAINING trailing turn's call 2 carries the judge's remaining-work statement + the framework imperative in its trailing region | Composed-request inspection (`TestRemainingWorkAnchor`) | A REMAINING call 2 whose trailing region is the bare guidance with no anchor fails | ADR-038 (Finding G); Spike ρ (advance 0/10 unanchored → 19/20 anchored) |
| FC (ADR-038) delegation preserved under anchor | The anchored call 2 still delegates generation; no inline-write (Finding B) and no premature-finish regression beyond the ρ-measured ≤1/10 | Real-client run + composition inspection | An inline `write` of generated content, or a rise in no-tool-call turns, on an anchored REMAINING call 2 fails | ADR-038; Spike ρ (delegation 9–10/10, no-tool-call ≤1/10) |
| FC-67 | Termination observability — finish-vs-continue behavior on trailing turns is computable from emitted events alone: `TurnDecision.tail_kind` + `TurnDecision.judgment_verdict`; false-continue AND false-stop shares meterable (the false-stop share is the digest-enrichment extend-on-evidence trigger) | Events-alone share-computation test + field-presence integration test | A deployment where false-continue/false-stop frequency cannot be computed without log archaeology fails | ADR-037 §Decision 6 + FC; conformance V-06; settles the WP-LB-J event shape |
| FC-68 | Judgment-seat re-validation — a judgment-seat profile change is accompanied by a recorded judgment-rate re-validation (θ-harness judgment arms at registered thresholds); shared profile = one recorded run covers both instruments (ψ′ delegation arms + θ judgment arms); split seats = per-seat with the matching instrument | Process FC: inspection at swap time — the swap commit/config change references the recorded run | A profile change with no recorded re-validation covering the affected seat's instrument fails | ADR-037 §Decision 7 + FC; composes with FC-60; hosted annotation 20/20 (favorable prior, not a measurement) |
| FC-69 | Loop-driver AS-3 backstop — the tool-driven surface enforces the turn cap: a session reaching the configured cap terminates with a budget stop | Integration test driving a session past the cap; wiring inspection (`get_loop_driver` constructs the BudgetController) | A session that continues dispatching past the cap fails; the conformance-scan state (no `BudgetController` on the loop-driver path) is the refutation shape | AS-3; ADR-037 §Layer 1 (the cap as absolute ceiling); conformance loop-back #5 AS-3 incidental |

**ADR-076 qualitative-claim decomposition complete for Cycle 7 loop-back.** Every loop-back qualitative claim is decomposed into a refutable Fitness property recorded inline within each module's entry (above) or surfaced as a system-level fitness criterion FC-42 through FC-52 (this table). The qualitative claims and their decompositions: "grounded loop" / "single-action-per-turn" (ADR-033) → FC-43 (enforcement) + FC-45 (grounded carry); "callee generation" → FC-44; "seat-filler swappability" → FC-46; "parity" (behavioral, ADR-034) → addressed-by-composition, verified at the live-end-to-end layer of the scenarios.md Cycle 7 loop-back Acceptance Criteria Table (FC-47 + FC-50 + FC-44 + FC-49 compose toward it, but parity itself is only observable when a real client executes the synthesized tool calls — no static FC asserts parity directly, consistent with the acceptance table's "Layer-match: no" entries); "tool-call terminal" → FC-47; "no server-side write" → FC-48; "artifact-bridge fidelity" → FC-49; "loop participation" → FC-50; "surface-mode discrimination" → FC-42. **Direction-not-constraint note for the loop-back (per ADR-076)**: the single-step *enforcement technique* is selected (batch-truncation, D2) and fixed as FC-43's mechanism-agnostic behavioral constraint — FC-43 constrains *the observable single-action behavior*, not the technique, so the two untested candidate techniques (re-planning prompt; one-tool `tool_choice` constraint) remain BUILD-tunable behind the Single-Step Enforcer boundary without changing the FC. **Honest-residual-uncertainty labels for the loop-back (per ADR-076)**: (1) FC-49 artifact-bridge fidelity carries a fidelity-at-scale residual — spike evidence is trivially small content; the large-deliverable case is BUILD-added. (2) **Axis-2 long-horizon coherence is structurally non-decomposable into a static FC** — no synthetic test reaches it (scenarios.md Cycle 7 loop-back Acceptance Criteria Table, axis-2 row: "no — no synthetic test reaches axis 2"); FC-51 makes the *failure diagnosable* but does not assert the property holds. Axis 2 is the recorded load-bearing risk (ADR-033 §Decision ¶5), validated in PLAY/first-deployment under ADR-097 Conditional Acceptance — surfaced here as a direction the architecture is validated toward, not a constraint a BUILD test verifies. FC-1 (≤ 5 entries per module) holds across all 4 new modules (Loop Driver at 3; the other three at 1-2).

**ADR-035 (loop-back #2) qualitative-claim decompositions:** "bare client-tool content" → FC-53 (directive presence) + FC-54 (destination keying) + the scenarios.md Finding-D feature block's bare-content integration scenario (the refutation: fences / prose / "Example Usage" in a `write` body fails); "destination-agnostic" (ensembles) → the destination-agnostic preservation scenario — no ensemble YAML field (`system_prompt`/`default_task`/`output_schema`/baked-in tool) couples to file-production, verified by config inspection; "conservative" (normalization) → defined as single-enclosing-fence-strip only, with multi-fence heuristic extraction excluded by FC-57's seam scope; "one dispatch → one deliverable" → FC-55 (structural at the turn level via FC-43; contractual at the dispatch level via the directive). **Honest-residual-uncertainty (ADR-035, same pattern as axis-2 above):** *sustained trajectory-scale form compliance is non-decomposable into a static FC* — FC-53/54 verify directive *presence and keying*, not sustained model compliance across a long horizon; that is the ADR-097 Conditional Acceptance PLAY target, with the detect-and-refuse gate (FC-57 seam) as the named first escalation. FC-1 holds post-ADR-035 (Loop Driver at 4 with form-directive composition; Artifact Bridge at 2 with the FormGate seam; Orchestrator Tool Dispatch unchanged in count — the D1 fix corrects an existing extraction responsibility rather than adding one).

**ADR-036 (loop-back #3) qualitative-claim decompositions:** "delegation is won, not coerced" → FC-58 (the composition that wins) + FC-59 (the meter that detects losing) — with the explicit scope parenthetical in the decision statement (the win is a (composition × qwen3:14b × OpenCode 1.15.5) property; per the loop-back #3 snapshot Advisory 1, downstream phases read it as a stack-scoped bet with instrumentation as the load-bearing safety net, not a structural guarantee); "deterministic" (classifier) → the labeled-set regression + no-RNG/no-LLM/no-clock property in the meter's fitness lines; "computable from events alone" → FC-59's events-alone test; "client-invisible" (composition) → the scenarios.md client-invisibility scenario (no client-visible surface carries guidance text). **Honest-residual-uncertainty (ADR-097 pattern, same as axis-2 and ADR-035 trajectory-form):** *the delegation win at unmeasured depths and on the post-read generation turn in repair-shaped flows is not decomposable into a static FC* — FC-58 verifies composition presence, FC-59 makes the rate observable; the residuals (depth >3; fix-after-read; client-version drift) are watched by the real-client acceptance run (gating condition) and the production meter (trailing confirmation), per ADR-036 §Empirical grounding. The portability failure boundary is uncharacterized by design scope (two Arm D negatives, no failure model) — FC-60 is a go/no-go gate, not a diagnostic; seat-filler transferability is a practitioner-named **candidate** future cycle (modal, not committed).

**ADR-037 (loop-back #5) qualitative-claim decompositions:** "sessions converge" → FC-63 (judgment-first composition) + FC-65 (protocol-clean finish) + FC-67 (the shares that detect non-convergence), with the end-to-end convergence claim itself verified at the real-client layer (ADR-037's Conditional Acceptance gating condition (a) — the composed ~0.9 estimate is from independently-measured n=10 arms, not an end-to-end measurement; the acceptance run and the false-continue share are the live verification); "the framework guarantees the judgment's evidence base" → FC-64 (digest provenance); "extensible meta-record seam" → the Session Action Record's diff-inspection fitness (schema extension touches only the record module) + FC-67's false-stop share as the named extend-on-evidence trigger — the seam is decomposed as *extension locality* (testable now) plus *enrichment trigger* (observable in production), not as a claim that the first-increment schema suffices for real RDD-session complexity (the practitioner pre-mortem; honest-residual below); "judgment within a structurally-bounded role" → the AS-9 §Termination-judgment instance annotation (a new instance evidenced by Spike θ, not by AS-9's recorded basis) + FC-68 (per-profile rate re-validation). **Honest-residual-uncertainty (ADR-037, the ADR-097 pattern):** (1) *digest expressiveness for real RDD-session complexity is non-decomposable into a static FC* — multi-part asks, mid-session intent refinement, and non-write-shaped deliverables are outside the measured scope; the committed write-log digest degrades exactly the way round 1 measured if the record cannot represent completeness; watched by FC-67's false-stop share (the enrichment trigger) with the practitioner pre-mortem recorded as the lead signal. (2) *Non-write-shaped deliverable boundary* — deliverable accounting may not bound the judgment there; recorded, not guessed across (the ADR-036 denominator-boundary pattern). (3) *Judgment-seat portability* — one hosted pair (20/20) is supporting context, not a transferability measurement; FC-68 is a go/no-go gate like FC-60. FC-1 holds post-ADR-037 (Loop Driver at 5 with the judgment composition — at the threshold, but the entries all compose into the per-turn decision and share the same change rate, the recorded Calibration-Gate precedent; Session Action Record at 1).

**ADR-076 qualitative-claim decomposition complete for Cycle 7.** Every Cycle 7 qualitative claim in module responsibilities is decomposed into refutable Fitness properties recorded inline within each module's entry (above) or surfaced as system-level fitness criteria FC-28 through FC-41 (this table). The qualitative claims and their decompositions: "structurally-bounded LLM roles" (AS-9) → FC-29 (universal satisfaction) + per-module fitness on Routing Planner + Response Synthesizer (role-shape commitments); "capability matching from request content alone" (AS-10) → FC-30 (no out-of-band signals); "honest response labeling" → FC-38 (three-layer signaling); "configuration honesty sub-promise" → FC-38 (delivery mechanism); "cost-distribution accountability sub-promise" → FC-41 (operator-observable signaling + refutation threshold); "framework-driven dispatch pipeline" → FC-28 (pipeline-flow universal) + FC-35 (chunk vocabulary preservation); "transparent OpenAI-compatible endpoint" → FC-30 + FC-39 (no opt-in signals; OpenAI-protocol contract preserved); "deferred bridge mechanism" → FC-39 (conditional advisory). **Direction-not-constraint note for Cycle 7 (per ADR-076)**: "Latency tuning playbook" is the *direction* operators apply, not a single testable constraint — the four tuning mechanisms (classifier pre-filter, cached planner decisions, smaller faster planner model, streaming) are operator-tuneable per ADR-031 §"Tuning playbook (operator-facing)"; FC-40 fixes streaming as load-bearing default but the other three are direction-not-constraint, applied per deployment-shape evidence. The cost-distribution-accountability sub-promise carries an *honest-residual-uncertainty* label per ADR-032 §Sub-promise (2) — Population A voice is silent on the sub-promise; the sub-promise is project-developer-lens grounded; FC-41 measures the operationalization (the refutation threshold) without claiming production validation of the sub-promise's user-side framing.

**ADR-016-style bounding mechanisms disposition for Cycle 6 integration contracts** *(addressing the Appendix A.5 architect-brief examination target; closes the Cycle 4 OQ #14 carry-forward explicitly per cycle-6-architect snapshot Advisory 4)*: The Cycle 6 cross-module integration contracts (Dispatch Event Substrate → sinks; Session Artifact Store → filesystem; Orchestrator-Context Event Sink → Orchestrator Runtime; the `dispatch_id` coupling across three surfaces) were examined for ADR-016-style bounding-mechanism applicability and found **structurally inapplicable**. The mechanisms (a) fresh-context isolation, (b) time-decay windowing, (c) categorical anchors, (d) periodic out-of-band audit dispatch, and (e) read-only structural validation were designed for cross-layer feedback paths where signal data accumulates across windows of dispatches (ADR-016's L0→L1 calibration channel; ADR-018's verdict→router edge). The new Cycle 6 contracts have different data shapes: synchronous calls; typed-error boundaries; no long-horizon feedback accumulation; point-in-time `dispatch_id` correlation rather than windowed feature aggregation. Where mechanism-analog properties exist they hold by construction: the substrate's single-source-of-truth pattern for `dispatch_id` is mechanism (a)-analog (each `dispatch_id` value is generated once and not re-used); typed-error infrastructure (FC-22) is mechanism (e)-analog for cross-surface consistency. No periodic-audit-analog is needed because the contracts have no accumulating state to audit. **This closes Cycle 4 OQ #14's "L1→L2 verdict→router stage was the test case; will other cross-layer stages need analog grounding?" question for the Cycle 6 stage set: the answer is no, by data-shape inapplicability.**

All criteria are automatable via a combination of static import analysis, test coverage, and dependency-graph reconstruction. **Three structural prerequisites must land before any ADR-by-ADR BUILD work begins** (per the Cycle 4 conformance scan): (1) FC-2 and FC-3 as automated tests (currently specified but not implemented as test artifacts; pre-existing gap); (2) the shared `LlmOrcStructuralError` base class (FC-17 prerequisite — without it, the eight typed errors would diverge as separate classes and re-merging post-hoc is BUILD-burden); (3) ensemble-YAML migration to add `topaz_skill` metadata field (FC-18 prerequisite). FC-1 (≤ 5 entries per module) holds with Cycle 4 additions — Calibration Gate is at the threshold (5 entries) but the entries all compose into the verdict and share the same change-rate; splitting would not be load-bearing.

---

## Test Architecture

### Boundary Integration Tests

Every dependency edge must have at least one integration test that exercises real data flow with real types on both sides. No mocking at the boundary under test.

| Edge | Integration Test | Verifies |
|------|-----------------|----------|
| Serving Layer → Session Registry | `test_serving_resolves_session_identity` | HTTP request with/without session continuity correlates to correct SessionState |
| Serving Layer → Orchestrator Runtime | `test_serving_streams_runtime_output` | SSE chunks flow end-to-end; client-tool round trip resumes same Session |
| Orchestrator Runtime → Budget Controller | `test_runtime_honors_budget_at_iteration_boundary` | Turn-limit and token-limit exhaustion both terminate at an iteration boundary |
| Orchestrator Runtime → Orchestrator Tool Dispatch | `test_runtime_dispatches_internal_tools_only` | Any tool name outside the five returns a tool error observation, not an exception |
| Orchestrator Tool Dispatch → Result Summarizer Harness | `test_runtime_never_sees_unsummarized_result` | `invoke_ensemble` returning a large dict reaches the Runtime as a summary via the Harness; escape-hatch flag bypasses. Interposition lives on Tool Dispatch per Amendment #3 — the Runtime is unaware of the Harness |
| Orchestrator Tool Dispatch → Ensemble Engine | `test_invoke_ensemble_executes_real_ensemble` | End-to-end ensemble execution with real `EnsembleExecutor` |
| Orchestrator Tool Dispatch → Composition Validator | `test_compose_ensemble_rejects_cycle` | Cyclic reference graph rejected at composition time |
| Orchestrator Tool Dispatch → Plexus Adapter | `test_query_knowledge_and_record_outcome_round_trip` | Real Plexus-active path; also run in stateless mode to verify no-op returns |
| Orchestrator Tool Dispatch → Autonomy Policy | `test_autonomy_gate_fires_before_every_dispatch` | Every tool path passes through the gate; baseline level allows invoke, composes, denies promotion |
| Orchestrator Tool Dispatch → Calibration Gate | `test_calibration_interposes_on_in_calibration_ensembles` | First N invocations run the check; (N+1)th does not |
| Composition Validator ↔ Ensemble Engine (shared) | `test_shared_validator_same_result_both_paths` | `validate_ensemble_reference_graph` returns identical outcome when called from load path and composition path on the same input (scenarios regression) |
| Result Summarizer Harness → Ensemble Engine | `test_summarizer_failure_preserves_artifact` | Summarizer exception: original artifact persists on disk, Runtime receives typed summarization error |
| Serving Layer's `resolve_session_start_context` | `test_session_start_context_is_empty_in_phase_1` | Function called once per session start, returns `[]`, never touches Plexus Adapter in Phase 1 |
| Bootstrapping Pipeline → Plexus Adapter | `test_bootstrap_pushes_source_material_not_summaries` | Ingested content is file content; no LLM-generated summaries in the push stream (AS-4) |
| Bootstrapping Pipeline → Ensemble Engine | `test_bootstrap_reads_library_via_config_manager` | Pipeline uses the existing config-manager path; respects tiered storage |
| Autonomy Policy → Session Registry | `test_autonomy_reads_session_state` | Autonomy Level resolved from per-session config, not global |
| Budget Controller → Session Registry | `test_budget_derives_cumulative_spend_from_session` | Token and turn spend sums are session-scoped, not request-scoped |
| Calibration Gate → Plexus Adapter | `test_calibration_persists_across_sessions_when_plexus_active` | Session 2 sees cleared calibration from Session 1 when Plexus is active; not when absent |
| Orchestrator Runtime → Conversation Compaction *(Cycle 4)* | `test_runtime_invokes_compaction_at_turn_boundary` | Compaction runs at every turn boundary; the resulting `CompactedContext` is what flows into the next LLM call |
| Conversation Compaction → Ensemble Engine *(Cycle 4)* | `test_compaction_layer_4_invokes_summarizer_ensemble` | When Layers 0–3 cannot reduce context below threshold, Layer 4 dispatches a summarizer ensemble via real `EnsembleExecutor`; circuit-breaker counts failures |
| Conversation Compaction → filesystem *(Cycle 4)* | `test_compaction_layer_0_persists_large_tool_results` | Tool results above 50K chars persist to disk; orchestrator context receives 2 KB preview + path; path queryable later |
| Orchestrator Tool Dispatch → Tier-Escalation Router *(Cycle 4)* | `test_invoke_ensemble_routes_through_tier_router` | Every `invoke_ensemble` dispatch passes through tier selection; verdict input flows from Calibration Gate; selected tier flows into `EnsembleExecutor` |
| Tier-Escalation Router → Calibration Gate *(Cycle 4)* | `test_router_consumes_calibration_verdict` | Router reads the verdict directly (no LLM-mediated translation); the verdict's three values map deterministically to router actions (cheap-tier / escalated-tier / `escalation_bypass` typed error) |
| Tier-Escalation Router → Ensemble Engine *(Cycle 4)* | `test_router_reads_topaz_skill_from_ensemble_yaml` | Router resolves the dispatched ensemble's `topaz_skill` field via existing config manager; ensembles without the field produce `missing_skill_metadata` typed error |
| Calibration Gate → Calibration Signal Channel *(Cycle 4; conditional)* | `test_gate_reads_windowed_signals_in_fresh_context` | When ADR-016 active, gate reads windowed signal data within a fresh evaluation context (mechanism (a)); when ADR-016 rejected, gate operates on L1-internal data only |
| Ensemble Engine → Calibration Signal Channel *(Cycle 4 — read-only upward exception)* | `test_ensemble_emits_typed_signals_to_channel_read_only` | L0 emits typed signals through the registered hook; channel validates schema (mechanism (e)) and rejects malformed signals with `malformed_signal` error; channel is read-only — no upward write paths from L1 to L0 |
| Orchestrator Tool Dispatch → Dispatch Event Substrate *(Cycle 6)* | `test_invoke_ensemble_emits_paired_dispatch_timing_events` | Every `invoke_ensemble` emits one `DispatchTiming(start)` before tier selection and one `DispatchTiming(end)` after harness/substrate-write; both share `dispatch_id`; the envelope's `diagnostics` is populated by querying `events_for(dispatch_id)` |
| Calibration Gate → Dispatch Event Substrate *(Cycle 6)* | `test_calibration_gate_emits_verdict_event_with_dispatch_id` | Verdict computation emits `CalibrationVerdict(dispatch_id, verdict)` through the substrate; the verdict is consumable from `events_for(dispatch_id)` |
| Tier-Escalation Router → Dispatch Event Substrate *(Cycle 6)* | `test_tier_router_emits_selection_event_with_dispatch_id` | Tier selection emits `TierSelection(dispatch_id, model_profile, tier, topaz_skill)` through the substrate |
| Tier-Router-Audit → Dispatch Event Substrate *(Cycle 6)* | `test_router_audit_emits_audit_diagnostic_with_dispatch_id` | Audit verdicts emit `AuditDiagnostic(dispatch_id, criterion, finding, threshold)` through the substrate; one event per drift-criterion finding |
| Calibration Signal Channel → Dispatch Event Substrate *(Cycle 6)* | `test_signal_channel_emits_calibration_signal_with_dispatch_id` | Signals emit `CalibrationSignal(dispatch_id, signal_type, value)` through the substrate; exclusion-from-orchestrator-context default is honored via sink-side filtering, not producer-side suppression |
| Dispatch Event Substrate → Operator-Terminal Event Sink *(Cycle 6)* | `test_substrate_fans_out_events_to_operator_terminal_sink` | Each emitted event reaches the operator-terminal sink as exactly one log line at the per-event log level; lines are line-oriented `key=value` |
| Dispatch Event Substrate → Orchestrator-Context Event Sink *(Cycle 6)* | `test_substrate_fans_out_events_to_orchestrator_context_sink` | Each emitted event reaches the orchestrator-context sink; the sink composes events for one `dispatch_id` into the structured-observation block consumed at next turn boundary |
| Orchestrator Runtime → Orchestrator-Context Event Sink *(Cycle 6)* | `test_orchestrator_context_observation_prepended_between_turns` | Runtime queries `observations_for(last_dispatch_id)` at each turn boundary; the returned observation block is prepended to the next turn's messages; final dispatch (no next turn) routes to end-of-session summary instead |
| Orchestrator Tool Dispatch → Session Artifact Store *(Cycle 6)* | `test_capability_ensemble_writes_to_session_dir_artifact_path` | Tool Dispatch calls `write_deliverable` for substrate-routed dispatches; the deliverable lands at `.llm-orc/agentic-sessions/<session_id>/<dispatch_id>/<deliverable>.<ext>`; the envelope's `artifacts[0]` carries the typed reference |
| Session Registry → Session Artifact Store *(Cycle 6)* | `test_session_close_triggers_retention_cleanup` | Session Registry invokes `on_session_close(session_id)` at session termination; Session Artifact Store removes `retention: session` artifacts; `retention: durable` artifacts remain on disk |
| Session Artifact Store → Dispatch Event Substrate *(Cycle 6)* | `test_session_artifact_store_reads_dispatch_id_from_substrate` | Substrate is the single source of truth for `dispatch_id`; Session Artifact Store does NOT generate its own — verified by static check on path-construction logic + integration test asserting cross-surface `dispatch_id` consistency (FC-22) |
| Serving Layer → Operator-Terminal Event Sink *(Cycle 6)* | `test_tool_call_emit_log_precedes_dispatch_start` + `test_heartbeat_fires_at_interval_during_inactivity` | Serving Layer fires tool-call-emit before SSE-stream tool-call dispatch; heartbeat fires from open-request inactivity timer at `heartbeat_interval_seconds` cadence |
| Operator-Terminal Event Sink → Ensemble Engine *(Cycle 6)* | `test_validate_once_at_load_emits_warn_only_at_startup` | Library validation runs once at startup; the sink consumes `validation_results()` and emits one `WARN` per invalid YAML; subsequent `list_ensembles()` calls produce zero validation re-emission |
| Calibration Gate → Session Artifact Store *(Cycle 6; conditional)* | `test_calibration_substrate_access_opt_in_opens_artifact_read_surface` | Under `calibration_substrate_access: artifact`, the gate's critic agents receive an `ArtifactReadTool`; under absent / `summary`, the surface is `None` and critic agents evaluate from envelope summary only |
| Serving Layer → Dispatch Pipeline *(Cycle 7)* | `test_serving_layer_invokes_dispatch_pipeline_not_runtime` | Chat-completions handler invokes `DispatchPipeline.run()`; no `OrchestratorRuntime(...)` constructed; pipeline yields chunk stream consumed by handler unchanged |
| Dispatch Pipeline → Routing Planner *(Cycle 7)* | `test_dispatch_pipeline_invokes_routing_planner_per_request` | Pipeline calls `RoutingPlanner.plan()` exactly once per chat-completions request; returns typed `DispatchPlan`; planner output substrates `PlanEmitted` substrate event |
| Dispatch Pipeline → Response Synthesizer *(Cycle 7)* | `test_dispatch_pipeline_invokes_synthesizer_per_request` | Pipeline calls `ResponseSynthesizer.synthesize()` exactly once per request; chunk stream flows back to pipeline; pipeline yields chunks to handler |
| Dispatch Pipeline → Orchestrator Tool Dispatch *(Cycle 7)* | `test_plan_to_internal_tool_call_adapter_dispatches_capability_ensemble` | When `action: "dispatch"`, pipeline constructs `InternalToolCall(name="invoke_ensemble", arguments={"ensemble_name": plan.ensemble, "input": plan.input})` and calls `OrchestratorToolDispatch.dispatch()`; returns real `DispatchEnvelope` per ADR-024 |
| Routing Planner → Orchestrator Tool Dispatch *(Cycle 7)* | `test_routing_planner_dispatches_via_tool_dispatch_chokepoint` | Routing Planner invokes the planner ensemble via `dispatch()` (same chokepoint as capability ensembles); calibration gate + tier router + autonomy interpositions fire as for any other ensemble |
| Response Synthesizer → Orchestrator Tool Dispatch *(Cycle 7)* | `test_response_synthesizer_dispatches_via_tool_dispatch_chokepoint` | Response Synthesizer invokes the synthesizer ensemble via `dispatch()`; calibration gate fires three new Reflect-trigger criteria (Rule 5 framing absence, Rule 4 rounding drift, Rule 1 fabrication) per ADR-029 |
| Capability Discovery Endpoint → Capability List Builder *(Cycle 7)* | `test_capability_discovery_endpoint_returns_canonical_capability_list` | GET on the chosen discovery surface (`/v1/models` extension or sibling endpoint) returns `name + description + topaz_skill` for each capability ensemble; system ensembles excluded |
| Dispatch Pipeline → Capability List Builder *(Cycle 7)* | `test_dispatch_pipeline_feeds_planner_with_capability_list` | Pipeline calls `CapabilityListBuilder.list_capabilities()` and includes the result in the planner's CAPABILITY LIST input |
| Dispatch Pipeline → Dispatch Event Substrate *(Cycle 7)* | `test_dispatch_pipeline_emits_pipeline_stage_events` | Pipeline emits `PlanEmitted` + (`DispatchFired` or `DirectCompletionFallback`) + `SynthesizerCompleted` per request; all events share `dispatch_id`; substrate's existing fan-out to both sinks fires |
| Capability List Builder → Ensemble Engine *(Cycle 7)* | `test_capability_list_reflects_validated_registry` | Capability List Builder reads through the validate-once-at-load registry cache (FC-27); list reflects the cached validated subset; reload via SIGHUP propagates correctly |
| Serving Layer → Response Labeling *(Cycle 7)* | `test_serving_layer_composes_response_labeling_on_all_paths` | Each response path (dispatch, direct, dispatch_fallback, tool_choice) carries `X-LLM-Orc-Served-By` header + `metadata.served_by` body field; content-layer Rule 5 framing on direct only |
| Operator-Terminal Event Sink — `DirectCompletionFallback` consumption *(Cycle 7 extension)* | `test_operator_terminal_sink_aggregates_direct_completion_rate` | Sink consumes `DirectCompletionFallback` events; aggregator produces `direct_completion_rate` over configurable window; refutation-threshold fires operator-notification when sustained delta exceeds threshold |
| Orchestrator-Context Event Sink — `DirectCompletionFallback` consumption *(Cycle 7 extension)* | `test_orchestrator_context_sink_consumes_direct_completion_fallback` | Sink's isinstance arm consumes the event; surface stays defined per Cycle 6 contract; under ADR-027 the orchestrator-context destination is structurally dormant on chat-completions (the consumer-Runtime is preserved but inactive) — test confirms the sink consumes without error rather than asserting consumer-side behavior |
| Serving Layer → Loop Driver *(Cycle 7 loop-back)* | `test_serving_layer_engages_loop_driver_on_tool_driven_request` | A `tools[]`-bearing request engages the Loop Driver (not the Dispatch Pipeline); the Loop Driver receives the conversation including trailing `role: "tool"` results + the client's declared tool surface + the injected seat-filler LLM; returns a typed `TurnOutcome` |
| Loop Driver → Single-Step Enforcer *(Cycle 7 loop-back)* | `test_loop_driver_truncates_batch_via_enforcer` | A driver proposal of two client tool calls passes through the enforcer and yields exactly one dispatched action; the enforcer is stateless (sequential turns produce identical truncation for identical proposals) |
| Loop Driver → Orchestrator Tool Dispatch *(Cycle 7 loop-back)* | `test_loop_driver_callee_dispatch_single_ensemble` | A generation turn constructs `InternalToolCall(name="invoke_ensemble", ...)` for one capability ensemble and calls `dispatch()`; calibration gate + tier router + autonomy interpositions fire; returns a real `DispatchEnvelope`; zero planner + zero synthesizer invocations |
| Client-Tool-Action Terminal → Artifact Bridge *(Cycle 7 loop-back)* | `test_terminal_marshals_deliverable_via_bridge` | The Terminal passes the turn's `DispatchEnvelope.artifacts[0]` (substrate) or `envelope.primary` (inline) to the Artifact Bridge and receives content byte-identical to the stored deliverable; the bridge handles both branches |
| Artifact Bridge → Session Artifact Store *(Cycle 7 loop-back)* | `test_artifact_bridge_reads_deliverable_from_store` | `read_deliverable(reference)` resolves `reference.path` and returns content; an unresolvable reference raises `artifact_not_found`; the read occurs within the artifact's retention window (before `cleanup_session`) |
| Serving Layer → Client-Tool-Action Terminal *(Cycle 7 loop-back)* | `test_serving_layer_composes_tool_call_terminal` | A tool-call `TurnOutcome` produces a streamed `ClientToolCall` (`finish_reason: "tool_calls"`) the existing `OpenAiSseFormatter` renders unchanged; a finish `TurnOutcome` produces a text `Completion`; an incoming `role: "tool"` follow-up routes to the Loop Driver (loop participation) |
| Loop Driver → Delegation Rate Meter *(Cycle 7 loop-back #3; outcome-derived per WP-LB-M loop-back #6)* | loop-driver stamping tests + `test_delegation_rate_counts_trailing_multi_file_generation` | Each turn derives `turn_shape` from its outcome (write → `generation`, read/command/finish → `carry`) with `classify_turn` owning `boundary_excluded`, and stamps it onto the emitted `TurnDecision`; a delegated or literal write yields `generation` (incl. a trailing REMAINING write whose anchor carries no generation verb), a read `carry`, a repair `boundary_excluded` — real classifier, no stub |
| Operator-Terminal Event Sink → Delegation Rate Meter *(Cycle 7 loop-back #3)* | `test_delegation_rate_from_consumed_events` | The sink's consumed `TurnDecision` stream feeds `delegation_rate()` and yields the expected `RateReading` (rate + numerator + denominator + boundary-excluded count) from events alone; an empty window yields the typed no-reading result, not 0.0 |
| Loop Driver → Session Action Record *(Cycle 7 loop-back #5)* | `test_judgment_digest_from_real_action_records` | A real record-join-render round-trip: the driver records an emitted `write` action, joins the client's `role: "tool"` result on the follow-up request, and the judgment call's composed digest carries the framework-recorded path and result — real store, no fixture digest (the production-join shape the Conditional Acceptance gating condition requires) |

### Invariant Enforcement Tests

| Invariant | Enforcement Module | Test |
|-----------|--------------------|------|
| AS-1 (dynamic invocations outside reference graph) | Orchestrator Tool Dispatch | `test_invoke_ensemble_does_not_register_reference` — an invoke_ensemble call leaves the static reference graph unchanged |
| AS-2 (composed ensembles validated before loading) | Composition Validator | `test_compose_ensemble_validates_before_write` — no file written until validation passes |
| AS-3 (Budget is control-plane) | Budget Controller | `test_orchestrator_llm_cannot_observe_budget_state_in_context` |
| AS-4 (ingestion boundary is source material) | Plexus Adapter + Bootstrapping Pipeline | `test_ingestion_rejects_llm_summary_marker` (source-material assertion by type) |
| AS-5 (quality signals govern stabilization, not frequency) | Calibration Gate | `test_frequency_without_quality_does_not_trust` |
| AS-6 (compose from existing primitives only) | Composition Validator + Orchestrator Tool Dispatch | `test_compose_ensemble_rejects_new_script_or_profile` |
| AS-7 (amended Cycle 6 — Result summarization is the default; substrate-routed ensembles skip content summarization) | Result Summarizer Harness + Orchestrator Tool Dispatch + Session Artifact Store | `test_runtime_never_sees_unsummarized_result` (preservation — inline-response path, also FC-8); `test_substrate_routed_invoke_ensemble_skips_summarizer_and_writes_artifact` (Cycle 6 amendment — substrate-routed path); FC-26 verifies the amended scope holds in mixed-mode sessions |
| AS-8 (Plexus is optional) | Plexus Adapter + Serving Layer session-start function | `test_all_operations_work_with_plexus_absent` — full coverage of every Plexus-facing edge, including the session-start function in Phase 2 |
| Invariant 5 (cross-ensemble acyclicity) | Composition Validator + Ensemble Engine | `test_compose_ensemble_rejects_cycle` (also FC-6 regression) |
| Invariant 7 (static ensemble references) | Orchestrator Tool Dispatch | `test_invoke_ensemble_uses_existing_name_no_template_expression` |
| Invariant 9 (child artifacts nested) | Ensemble Engine (existing) + Result Summarizer Harness | `test_summarizer_failure_preserves_full_artifact` |
| Invariant 13 (execution resilient) | Ensemble Engine (existing) | (existing test suite) |
| AS-7 generalization to conversation context (per ADR-012) | Conversation Compaction | `test_orchestrator_context_stays_below_threshold_across_long_session` — multi-turn fixture exceeding threshold; compaction maintains coherence |
| AS-3 (control-plane discipline preserved) under tier escalation | Budget Controller, Tier-Escalation Router | `test_tier_escalation_preserves_budget_enforcement_at_iteration_boundary` — escalated dispatches do not bypass turn/token limits |
| AS-1 (dynamic invocations outside reference graph) under tier escalation | Orchestrator Tool Dispatch + Tier-Escalation Router | `test_tier_routed_invocation_does_not_register_reference` — same as existing AS-1 test plus tier-routed dispatches |
| ADR-016 read-only constraint on signal channel | Calibration Signal Channel | `test_signal_channel_rejects_upward_write_attempts` — any write toward L0 through the channel boundary is structurally rejected |
| ADR-016 calibration-only constraint on signal channel | Calibration Signal Channel (mechanism (e)) | `test_signal_channel_rejects_non_calibration_data` — non-calibration data (arbitrary upward import) is rejected via structural validation |
| ADR-002 layering rule preserved for write paths and non-calibration upward signaling | (cross-cutting) | `test_fc2_no_upward_imports_except_calibration_channel` — FC-2 prerequisite from WP-B4 |
| ADR-023 unified-substrate constraint (one event source, no parallel-emission infrastructure for the same data) | Dispatch Event Substrate | `test_substrate_is_only_path_from_producers_to_sinks` — static AST inspection confirms sinks do not import producers directly; FC-24 |
| ADR-025 always-scope constraint for capability ensembles (every capability ensemble in the library substrate-routes unless explicitly opted out via `output_substrate: inline`) | Session Artifact Store | `test_capability_ensembles_default_to_substrate_routing` — static YAML scan over the agentic-serving library + integration end-to-end; FC-25 |
| AS-7 amended scope (default-with-conditional-skip) | Result Summarizer Harness + Orchestrator Tool Dispatch | `test_substrate_routed_dispatch_skips_summarizer_inline_response_invokes_it` — mixed-mode session; FC-26 |
| AS-9 (structurally-bounded LLM roles produce reliable output on single-decision-shaped tasks) | Routing Planner + Response Synthesizer + Dispatch Pipeline | `test_chat_completions_surface_has_no_bundled_reasoning_invocation` — per-request observability inspection: zero `OrchestratorRuntime` LLM calls; routing decisions trace exclusively to `agentic-routing-planner` ensemble dispatch events; response composition traces exclusively to `agentic-response-synthesizer` ensemble dispatch events; FC-29 |
| AS-10 (capability matching from request content alone — chat-completions surface) | Routing Planner + Serving Layer + Capability List Builder | `test_routing_decisions_invariant_under_custom_headers` — requests with arbitrary custom HTTP headers (`X-Skill-Framework: rdd`, `X-Capability-Hint: code-generator`) produce identical routing decisions to baseline requests with no custom headers; FC-30 |
| ADR-027 chat-completions pipeline-flow constraint | Dispatch Pipeline + Serving Layer | `test_chat_completions_handler_has_zero_orchestrator_runtime_construction` — static AST check on `v1_chat_completions.py`: zero `OrchestratorRuntime(...)` construction in `chat_completions()`; FC-28 |
| ADR-029 strict-fidelity rule set | Response Synthesizer | `test_synthesizer_regression_battery_zero_fabrications` — the 13-test + 4-confabulation-mode spike battery ported as production regression suite produces 0 fabrications at the deployed cheap-tier model; FC-32 |
| ADR-030 bridge mechanism advisory shape | Serving Layer + Response Labeling | `test_tool_choice_bridge_advisory_conditional_content_layer` — tool_choice-bearing requests carry headers + body metadata on all paths; content-layer Rule 5-adjacent acknowledgment fires on `action: "direct"` only; tool_choice="auto" produces no advisory; FC-39 |
| ADR-032 configuration-honesty signaling | Response Labeling | `test_honest_response_labeling_three_layer_consistency` — every chat-completions response carries `X-LLM-Orc-Served-By` header + `metadata.served_by` body field with one of four canonical values; content-layer Rule 5 framing on `action: "direct"` only; FC-38 |
| ADR-032 cost-distribution-accountability operationalization | Routing Planner + Operator-Terminal Event Sink | `test_direct_completion_rate_metric_computable_with_refutation_threshold` — fixture sequence of dispatch + direct events produces correct `direct_completion_rate` over window; refutation-threshold fires operator-notification when sustained delta exceeds threshold; FC-41 |
| AS-9 §Scope boundary (grounded loop conditionally accepted under single-step enforcement; loop-back) | Single-Step Enforcer + Loop Driver | `test_grounded_loop_holds_under_single_step_enforcement` — on the Spike τ un-batchable probe shape, single-action-per-turn enforcement yields a tool-call argument carrying the prior turn's *observed* value (the τ′ grounded pass); without enforcement the unconstrained driver's batch presupposes unobserved state (the τ failure) — the test asserts the enforced path carries observed values; FC-43 + FC-45. Axis 2 (long-horizon drift) is NOT enforced by a static test — it is the recorded BUILD/PLAY validation target (ADR-033 §Decision ¶5; ADR-097 Conditional Acceptance) |
| AS-10 (capability matching from request content alone) preserved on the tool-driven surface | Loop Driver + Serving Layer | `test_tool_driven_routing_invariant_under_custom_headers` — the loop-back surface routes per-turn capability delegation on request/conversation content alone; the client's declared `tools[]` is the surface-mode discriminator (FC-42), not a routing-input signal; Spike ρ reaffirmed AS-10 holds on the tool-driven surface (the planner routed indifferent to declared tools) |
| ADR-033 single-action-per-turn enforcement | Single-Step Enforcer | `test_single_step_enforcer_truncates_batch` — a driver proposal of two client tool calls produces exactly one dispatched action; FC-43 |
| ADR-033 callee generation (pipeline is not the per-turn primitive) | Loop Driver | `test_loop_driver_callee_not_pipeline` — a generation turn produces one capability `invoke_ensemble` dispatch; zero routing-planner + zero response-synthesizer invocations; FC-44 |
| ADR-034 tool-call terminal + no-server-side-write | Client-Tool-Action Terminal | `test_apply_work_turn_emits_tool_calls_no_server_write` — an apply-work turn yields `finish_reason: "tool_calls"`; no server-side write to a client workspace path (Spike π Phase A shape absent); FC-47 + FC-48 |
| ADR-034 artifact-bridge fidelity | Artifact Bridge | `test_artifact_bridge_marshals_with_fidelity` — marshalled tool-call content byte-matches the stored deliverable (or `envelope.primary` inline), not a summary; FC-49. Fidelity at scale is BUILD-added |
| ADR-034 loop participation | Client-Tool-Action Terminal | `test_surface_consumes_tool_result_and_continues` — a `role: "tool"` follow-up routes to the Loop Driver for the next-turn decision; a surface dropping the tool message (the current `_extract_request` last-user-message behavior) fails; FC-50 |

### Test Layers

- **Unit.** Verify logic within a single module. Mocks are acceptable for neighbor modules. Every module has unit tests for its core state transitions (e.g., Budget Controller's exhaustion check; Calibration Gate's N-invocation transition; Composition Validator's cycle detection on synthetic graphs).
- **Integration.** Verify real data flow across module boundaries. No mocks at the boundary under test. All 18 edges in the Boundary Integration Tests table are integration tests.
- **Acceptance.** Verify scenarios end-to-end using real wiring. The scenarios in `scenarios.md` map to acceptance tests. The scenario "Tool user completes a task against the stateless orchestrator" is the happy-path acceptance test; "Session terminates gracefully on turn limit exhaustion" and "Composition with ensemble-to-ensemble reference passes validation" are the representative stress paths.

---

## Appendix A: Per-Phase Susceptibility Snapshot Briefs

This appendix holds the canonical brief content for each phase skill's `Phase Boundary: Susceptibility Snapshot Dispatch` subsection when operating within this scoped cycle. The briefs are phase-specific per ADR-056's non-formulaic requirement and reference concrete prior-cycle findings where applicable. Briefs may need updating as future cycles accumulate evidence; the `/rdd-conform` dispatch prompt format audit verifies structural compliance with the canonical skeleton but does not audit brief content substance.

**Structural wrapping (applied identically to every phase skill that runs in this cycle):**

```
## Phase Boundary: Susceptibility Snapshot Dispatch

Before completing this phase, dispatch the susceptibility-snapshot-evaluator subagent with the following brief:

<phase-specific brief content from Appendix A>

Output path: <canonical path from Appendix A>
```

The appendix records the **post-v0.8.5 canonical paths** at `housekeeping/audits/...` (this scoped corpus uses ADR-070 housekeeping placement; if `/rdd-conform migrate-to-rdd` is run later, paths swap to `.rdd/audits/...`).

### A.1 Research phase (`skills/research/SKILL.md`)

> This is the research → discover boundary for the agentic-serving cycle. Cycle 1's research phase produced two essays: 001 (architecture, pre-build) and 002 (capability floor and observability, backward loop from PLAY). Essay 002 surfaced a confabulation gap in tool-calling benchmarks and a hybrid-deployment latency-closing finding. Cycle 2's research enters the multi-turn + live-composition territory the prior cycle named but did not exercise; the structural machinery (`compose_ensemble`, Composition Validator, Calibration Gate, retry pattern) is in place but unentered. The primary risk at this boundary is framing adoption at essay crystallization moments — particularly any drift from "what does multi-turn composition look like in practice?" toward "the architecture handles it" without the empirical entry. Evaluate whether the essay that is about to enter downstream phases has been shaped by Cycle 1's structural commitments without those commitments being independently re-tested for the Cycle 2 question. Apply constraint-removal per ADR-082 at the research-entry moment: was *"what would we build if compose_ensemble were not available?"* posed against the prior cycle's machinery, and did the answer move the framing or merely confirm it?
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-research.md

### A.2 Discover phase (`skills/discover/SKILL.md`)

> This is the discover → model boundary for the agentic-serving cycle. Cycle 1's product-discovery established stakeholder maps (Tool User, Operator, Pure Tool User, Orchestrator LLM), seven value tensions, and six assumption inversions. Cycle 1's PLAY findings (FF #128–#135) routed bilateral observability gap, capability-floor experience, and experience-conditional visibility back to discover as candidate updates. Discover for Cycle 2 will run in update mode against the existing artifact; the canonical failure mode at this boundary is research-phase framings propagating into product vocabulary without attribution, and value tensions surfacing as spectra but collapsing into binary framings without alternatives examined. Evaluate the attached AID signals for two specific patterns: (a) Cycle 2's research-essay framings inherited into product-facing language without being tested against user voice, and (b) llm-conductor-pattern framings (composition + multi-turn) that may have been adopted from the user's explicit framing without belief-mapping against alternatives like Cycle 1's "stateless orchestrator suffices" position.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-discover.md

### A.3 Model phase (`skills/model/SKILL.md`)

> This is the model → decide boundary for the agentic-serving cycle. Cycle 1's domain-model layered AS-1 through AS-8 onto the project's Invariants 1-14, with AS-8 (Plexus is optional) as the load-bearing architectural constraint. Cycle 2 may need to amend invariants if multi-turn + composition surfaces tensions with AS-1 (dynamic invocations outside the reference graph) or AS-6 (compose from existing primitives only — the user has flagged eventual orchestrator authorship of scripts and model profiles as desirable). Invariant amendments are the highest-stakes commitment type in the methodology. The canonical failure mode is preference-accelerated commitment: user-stated preference precedes implications analysis; alternatives not engaged at comparable depth. Evaluate the attached AID signals for warrant-elicitation failures and preference-accelerated commitments. For any invariant amendment or consequential concept addition, check whether belief-mapping was performed before adoption — not after. The test is whether the user could name what they would need to believe for a different commitment to be right.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-model.md

### A.4 Decide phase (`skills/decide/SKILL.md`)

> This is the decide → architect boundary for the agentic-serving cycle. Cycle 1 produced 11 ADRs (adr-001..011); Cycle 2 may extend or amend several — particularly ADR-006 (composition palette), ADR-007 (calibration gate), ADR-008 (autonomy levels), and ADR-011 (orchestrator as Model Profile) — depending on what multi-turn + live composition surfaces. The primary risk is that ADR framings originate from agent synthesis during drafting rather than from architectural drivers (research findings, domain model concepts, prior ADRs). Cycle 1's WP-F mini-cycle produced a strong example of provenance discipline (the retry pattern was committed as conditional on capability evidence, with explicit reopen conditions). Evaluate the attached AID signals for rebuttal-elicitation failures on rejected alternatives, and for cross-ADR compositions where one ADR's framing was adopted by another within the same cycle without being independently tested. Check whether each ADR's core framing traces to its driver chain or to drafting-time composition.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-decide.md

### A.5 Architect phase (`skills/architect/SKILL.md`)

> This is the architect → build boundary for the agentic-serving cycle. Cycle 1's architecture composed twelve modules across four layers plus the typed `resolve_session_start_context` function reservation; Cycle 4 ARCHITECT integrates six new ADRs (012-017) by adding three new modules (Conversation Compaction L2; Tier-Escalation Router L2; Calibration Signal Channel L1 — conditional acceptance), extending four existing modules (Session Registry, Orchestrator Runtime, Orchestrator Tool Dispatch, Calibration Gate), and amending ADR-002's layering rule for the load-bearing read-only L0→L1 calibration signal channel exception. **Cycle 6 ARCHITECT** integrates four new ADRs (022 / 023 / 024 / 025) by adding four new modules — *Dispatch Event Substrate* (L1), *Operator-Terminal Event Sink* (L3), *Orchestrator-Context Event Sink* (L2), *Session Artifact Store* (L1) — and extending five existing modules (Orchestrator Configuration, Orchestrator Tool Dispatch, Result Summarizer Harness, Calibration Gate, Ensemble Engine L0). One invariant (AS-7) is amended to default-with-conditional-skip; the amendment triggers backward propagation per Step 3.7 (sweep complete at DECIDE close per Amendment Log entry #11).
>
> The specific risk at this boundary is that the architectural integration encodes unexamined assumptions inherited from DECIDE-phase ADRs — particularly the **Inversion N+2 framing inherited from ADR-023** (one substrate, two destinations) which the system design accepts as the organizing principle for the new modules, and the **always-scope inherited from ADR-025** (every capability ensemble substrate-routes) which the system design encodes as Session Artifact Store's primary commitment. The Inversion Principle check at architecture is the designed counterweight but can be performed pro forma.
>
> **Specific framings inherited from DECIDE that ARCHITECT should examine:**
>
> 1. **Inversion N+2 (unified emission substrate; two routing destinations).** The system design adopted this as the organizing principle for the Cycle 6 module set — was the inheritance examined or automatic? An alternative (parallel-emission infrastructure: separate event sources for operator-terminal and orchestrator-context destinations) was rejected at DECIDE per ADR-023's "Rejected alternative (ii) — sidecar log"; the system design's substrate-and-sinks pattern operationalizes the rejection structurally. Evaluate whether the rejection holds under the system design's actual module shapes — particularly whether the substrate's `dispatch_id` lifecycle ownership (in Dispatch Event Substrate L1) creates new coupling with Tool Dispatch and Session Artifact Store that the DECIDE-phase substrate framing did not anticipate.
>
> 2. **Always-scope for ADR-025 substrate-routing (every capability ensemble substrate-routes; system ensembles inline-respond).** The system design encodes this as Session Artifact Store's primary commitment. The DECIDE-phase rationale was practitioner-verbatim ("always for now; dial back later if cumbersome"). Per the Cycle 6 DECIDE snapshot Finding 1 advisory carry-forward, the dial-back falsification criteria's Indicator 1 (latency overhead for deliverables under 1 KB) and Indicator 4 (three or more `output_substrate: inline` opt-outs) are testable at BUILD's first deployments — `web-searcher` (small-deliverable, already-handle-shaped) is the clearest candidate for early friction. Evaluate whether the architecture's encoding of always-scope creates a structural-vs-empirical gap (the rule is structural; the cost is empirical).
>
> 3. **`dispatch_id` coupling across three surfaces (events, envelope diagnostics, artifact filesystem path).** Per Cycle 6 DECIDE snapshot Finding 2 advisory carry-forward — same `dispatch_id` value governs ADR-023's event correlation, ADR-024's envelope `diagnostics.dispatch_id`, and ADR-025's artifact filesystem path's `<dispatch_id>` segment. A bug in `dispatch_id` generation or lifecycle affects all three surfaces simultaneously. The system design positions `dispatch_id` ownership at Dispatch Event Substrate (L1) as the single source of truth — Session Artifact Store reads from the substrate rather than generating its own (FC-22 verifies cross-surface consistency). Evaluate whether this ownership allocation is the right boundary, or whether the coupling warrants a different module shape.
>
> 4. **Orchestrator-Context Event Sink at L2 (alongside Orchestrator Runtime).** The system design places this module at L2 because it operates between turns of the ReAct loop and produces structured observations the Runtime consumes. An alternative placement (folding the structured-observation construction into the Runtime itself, the way v3.0 Runtime owns Routing Decision generation) was not deliberately examined at DECIDE. Evaluate whether the module separation is load-bearing (substrate consumption is cross-cutting; not all event-consumption needs to live in Runtime) or whether the separation introduces unnecessary boundary cost.
>
> 5. **Calibration Gate's three evaluation surfaces (summary-only / `structured`-augmented / artifact-content) under substrate-routing.** ADR-025 §"Calibration-gate evaluation surface under substrate-routing" enumerated three surfaces; the system design encodes them as a per-ensemble configuration (`calibration_substrate_access` field). Evaluate whether the three-surface enumeration is exhaustive — are there evaluation modes the architectural integration overlooks (e.g., the gate's audit-dispatch under ADR-018 operating on substrate-routed deliverables)?
>
> 6. **Validate-once-at-load library cache (ADR-023 §"Noise-floor remediation").** The change to `EnsembleLoader.list_ensembles` runs validation once at startup; subsequent calls return the cached subset. The system design encodes this in the Ensemble Engine (L0) extension. Evaluate whether the cache invalidation contract (operators must trigger `SIGHUP` / admin endpoint / restart for YAML changes to take effect) is the right operator-affordance shape — or whether watch-file-and-reload-on-change is a hidden expectation that surfaces during BUILD.
>
> **Carry-forward asymmetric-grounding concern (Cycle 4 OQ #14 territory).** The Cycle 6 modules introduce new cross-module integration contracts; whether they need ADR-016-style bounding mechanisms (a)-(e) is open. Cycle 6 BUILD should explicitly disposition this: do the new contracts need fresh-context isolation, time-decay windowing, deterministic anchors, periodic out-of-band audit, structural validation? Or does their data shape (synchronous calls; no long-running feedback loops; typed-error-bounded failure modes) make the bounding mechanisms structurally inapplicable?
>
> **Inherited from prior Architect brief content (preserved):** Evaluate the AID signals for (a) solution-space narrowing on the four new module boundaries — were Dispatch Event Substrate's separation from the four producers, Operator-Terminal Event Sink's separation from Serving Layer, Orchestrator-Context Event Sink's separation from Orchestrator Runtime, and Session Artifact Store's separation from Session Registry / Ensemble Engine examined as alternatives or accepted from ADR framings? (b) framing adoption from DECIDE-phase ADRs — see the six specific framings above. (c) whether the proposed module boundaries would survive a product-facing reading — do they track the operator's mental model (compose, calibrate, observe, route, store deliverables) or only developer convenience? (d) whether the cross-module-stage grounding-mechanism question shows up explicitly or remains implicit.
>
> Output path: housekeeping/audits/susceptibility-snapshot-cycle-6-architect.md

> **Cycle 7 loop-back addendum (architect → build boundary; BUILD → RESEARCH → DISCOVER → MODEL → DECIDE → ARCHITECT re-entry).** This loop-back ARCHITECT decomposes ADR-033 (layer-A loop-driver, callee resolution of OQ #26) + ADR-034 (client-tool-action terminal + artifact-bridge + multi-turn loop participation) into four new L2/L3 modules (Loop Driver, Single-Step Enforcer, Artifact Bridge, Client-Tool-Action Terminal) plus two extensions (Serving Layer surface-mode discrimination; Session Artifact Store `read_deliverable`). The DECIDE→ARCHITECT snapshot (`housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-decide.md`) returned **No Grounding Reframe** with six advisories, all addressed in this design. The specific risks at this boundary:
> 1. **Callee resolution inherited from ADR-033 (n=1 wrapper evidence — Spike υ).** The callee-vs-wrapper resolution rests on a single wrapper-shaped probe; the snapshot named this the most legitimate residual risk and warned "ARCHITECT should not treat the callee resolution as stronger than the ADR's own hedging warrants." Evaluate whether the module decomposition over-commits to callee (does it foreclose the wrapper reversion, or keep it architecturally accessible per FC-52?).
> 2. **Layer split rests on small-n (Spike σ, n=1) and the callee resolution within it (Spike υ, n=1) — both tested simultaneously at axis-2 (snapshot Advisory 5).** Evaluate whether the design makes a failing axis-2 result diagnosable as split-incorrect vs. callee-incorrect (the FC-51 `TurnDecision` instrumentation is the designed answer — is it sufficient?).
> 3. **Surface-mode discriminator is drafting-time synthesis, not spike-validated (Advisory 2).** Evaluate whether the discriminator is treated as validate-not-assumed (it is flagged in the Serving Layer extension as a production-traffic confirmation item) rather than a settled signal.
> 4. **Single-step enforcement technique selection (Advisory 3).** Evaluate whether the technique was selected at ARCHITECT (batch-truncation, D2) rather than deferred to BUILD, and whether the selection's module-boundary implication (a framework-layer Single-Step Enforcer, not a driver-prompt or `tool_choice` coupling) is honored.
> 5. **Wrapper-contingency specification (Advisory 1 — F3-1).** Evaluate whether the fallback ordering (frontier-tier driver first via FC-46 swappability; wrapper reversion second-order) is made explicit, since the practitioner could name no concrete reopening trigger at the DECIDE gate.
> 6. **Inversion Principle on the new boundaries.** The Loop Driver occupying the client's "model" seat and the Client-Tool-Action Terminal emitting `tool_calls` both encode the Tool User's *parity* mental model (DISCOVER 2026-05-24). Evaluate whether the boundaries track that user mental model or only developer convenience — particularly whether the Single-Step Enforcer / Loop Driver split serves correctness (the framework-grounding guarantee) or is developer-convenient modularization. Apply the product-facing reading: would a Population A operator recognize "the endpoint drives the loop and delegates generation to ensembles" as the system they expect?
>
> Output path: housekeeping/audits/susceptibility-snapshot-cycle-7-loopback-architect.md

### A.6 Build phase (`skills/build/SKILL.md`)

> This is the build phase boundary for the agentic-serving cycle (typically build → play or build → next-WP). Cycle 1's build (WP-A through WP-I plus four post-PLAY production changes) was strongly empirically grounded: each WP closed with a green test suite, fitness-criteria static checks, and stewardship-clean reports. Cycle 2's build extends this surface to multi-turn + live composition. Build is the most empirically grounded phase per the sycophancy gradient — test execution grounds commitments in observable outcomes. The residual risks are in the spaces tests do not reach: stewardship-checkpoint commitments that adopt rejected-alternative framings without surfacing them, debug hypotheses that absorb a "this must be because X" framing without belief-mapping X, and mode-shift transitions where the user's mental model may drift without being tested. Evaluate the attached AID signals with build's empirical grounding as the baseline — concerning signals here are patterns the tests did not catch.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-build.md

### A.7 Play phase (`skills/play/SKILL.md`)

> This is the play → synthesize boundary (or play → next-cycle if synthesize is deferred) for the agentic-serving cycle. Cycle 1's PLAY produced field-notes that surfaced the bilateral observability gap, capability-floor experience verdict, and the deferred Note 6 / Note 7 reframings. Cycle 2's play will enter the live-composition territory: stakeholder inhabitation against an orchestrator actually composing and using ensembles in real time. The specific risk at this boundary is that field notes flatten from observation into advocacy — discoveries that "confirm the design" crowd out discoveries that challenge it. Two signal patterns matter most: (a) selection bias in the six-category classification, and (b) gamemaster/player role blur under task load (especially likely under multi-turn fatigue). Evaluate the attached AID signals for framing adoption in field-note language (user voice vs. methodology voice) and for declining alternative engagement on discoveries that would challenge the design. The field notes entering synthesize should read as observation, not endorsement. Cycle 1's Grounding Reframe items 6 and 7 (deferral named, practitioner prior restored) are the reference for honest reframing under play.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-play.md

### A.8 Synthesize phase (`skills/synthesize/SKILL.md`)

> This is the synthesize phase boundary for the agentic-serving cycle (typically terminal; or synthesize → research re-entry if structural experiments surface new questions). Cycle 1 did not run synthesis — the cycle declared complete without it. Cycle 2 may run synthesis if multi-turn + composition produces a publishable finding (the conductor pattern's empirical character would be a candidate). The canonical failure mode at synthesis is the "softer than sycophancy but real" framing-adoption pattern where narrative framings composed at synthesis moments shape what the cycle will be remembered as. The four-dimension framing navigation provides structural scaffolding, but emerging framings can still be shaped by synthesis-moment adoption. Evaluate the attached AID signals with particular emphasis on framing adoption and declining alternative engagement during the framing conversation. The outline about to become the writer's essay should carry framings the writer owns, not framings they inherited from agent composition or from the artifact trail without examining the alternatives.
>
> Output path: housekeeping/audits/susceptibility-snapshot-{cycle-id}-synthesize.md

### Maintenance note for future cycles

Each brief references concrete prior-cycle findings where applicable (Cycle 1's two essays, eight-phase progression, eleven WPs, four production changes from the backward research loop). These references are anchored in time. As Cycle 2 and subsequent cycles accumulate evidence, briefs may need updating. Brief refresh is a deliberate cycle operation, not automated.
