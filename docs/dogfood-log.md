# Dogfood log

Standing practice (practitioner, 2026-08-12): discrete checks from the lead
session's own work get routed through the serve via `opencode run` before
being done by hand. Small at first, expanding toward parity-with-the-lead
over time. Every attempt is a data point on the meta-task realism axis;
honest refusals are data, not failures of the practice. One entry per
attempt: the ask, the route observed on the wire, the outcome, and a
graded note.

| # | date | ask | route observed | outcome | note |
|---|---|---|---|---|---|
| 1 | 2026-08-12 | "explain the subagent adapter" (llm-orc repo root) | glob→read chain fired; multi-component filename resolved to `benchmarks/agentic_serving/subagent_adapter.py` | HONEST REFUSAL: "Refused: could not read ...: file exceeds the 24 KB read cap" | Routing correct on a file merged the same day. The bound is the 24KB read cap — real repo files routinely exceed it, so the meta-task rung needs chunked reads or a raised cap before repo-scale explains land. No speculation observed. |
| 2 | 2026-08-12 | "run the tests" (pristine ladder-fixture copy; the Arm-1 session's seeded-red precondition check) | run rung fired: delegated `pytest -q` client-side | HONEST RED VERDICT: "1 failed, 1 passed" with the failing test named; matches client ground truth exactly | Protocol slip disclosed: the same check was first done by hand this session before routing through the serve; the serve's verdict agreed with the by-hand result. The run rung handles a battery precondition check end-to-end. Scheduling note: dogfood inference is kept OUT of paid-battery windows so local load cannot inflate a paid arm's wall-clock column. |
| 3 | 2026-08-13 | "explain the subagent adapter" (llm-orc repo root — entry 1's exact ask, re-run as #145's live exit gate) | glob→read chain fired; multi-component stem resolved; FULL 25.8KB read of `benchmarks/agentic_serving/subagent_adapter.py` under the raised cap | GROUNDED EXPLAIN: answer describes the file's actual content (JSONL transcript parsing, turn splitting, the arm-agnostic IR, drop-don't-raise malformed-line handling) — no speculation observed | Entry 1's bound CONVERTED: the 24KB refusal became a grounded answer on merged #145 (96KB cap, token-denominated read budget, runtime truncation backstop). classify.py-sized files (>35K projected tokens) still refuse over-budget honestly — the documented bound; chunked reads are the recorded path. |
| 4 | 2026-08-13 | "how does classify decide routing?" (#144 live gate 1, flag-on branch serve) | glob→union: dot-dir invisible to client glob; serve-native self candidate resolved; native read ran the shared budget | HONEST REFUSAL naming `.llm-orc/scripts/agentic_serving/classify.py` with the single-whale wording — no speculation, no client read of the dot-path | The literal self-referential gate converts from conceptual speculation to a true refusal; the "answers grounded" exit stays blocked by the whale (#106/#151/chunked reads), not by discovery. Record: `docs/plans/2026-08-13-144-live-gate/`. |
| 5 | 2026-08-13 | "how does resolve pick the seat?" (#144 live gate 2) | glob→union: 0 workspace candidates (subset rule rejected htmlcov noise) → sole self candidate → native read (3,190 tokens, admits) → explainer | GROUNDED EXPLAIN: names resolve.py's real classify/decide merge, `_decider_target` extraction, recall/recap fallback, shape-catalog mapping | First self-referential grounded answer through the serve; the dot-dir capability the client cannot provide, delivered serve-native. Misfire on attempt 1 (PATH-less serve → resolve crash → junk write cascade) kept as `discarded-*`; cascade filed as its own issue. |
| 6 | 2026-08-13 | "how does the serve detect runtime truncation?" (#121 live gate, run 3) | glob→grep (def-anchored)→menu→pick→read `turn_trace.py`→grounded | GROUNDED EXPLAIN citing real constants (`_DEEP_TRUNCATION_RATIO = 0.35`); the first content-search-grounded answer through the serve | Runs 1-2 of the same question picked caller-defined identifiers and refused honestly at the client's 50KB read cap — the pick gravitates to the 80KB caller, which defines something for every serve word; #153's offset reads are the recovery. Record: `docs/plans/2026-08-13-121-live-gate/`. |
| 7 | 2026-08-13 | "where is the recall ledger built?" (#121 live gate, run 2 — pre-fix) | grep→pick→read of the 80KB caller; OpenCode CAPPED the read at 50KB with a complete-looking result | AST confirmation REFUSED (wrong reason pre-fix) — the guard caught a silently half-shown file | DISCOVERY: the client's 50KB read cap sits inside the serve's 96KB window; whole-file-or-refuse was silently broken for 50-96KB files. Filed #153; fixed on the branch (deterministic trailer detection → honest `(truncated)` refusal). |
