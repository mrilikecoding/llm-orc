# Temperature Conversion Library

## Installation
Install the library using:
```
pip install .
```

## Usage
Use the `convert` function in the CLI:
```
python cli.py --value <value> --from <from_unit> --to <to_unit>
```

## Tests
Run the tests with:
```
pytest
```

The tests cover various conversions, including edge cases and negative values.