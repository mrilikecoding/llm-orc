# Spike ψ.0 TEMPORARY capture variant of agentic-orchestrator-offline-tools.
# Identical except base_url routes through the hop-2 capture proxy
# (127.0.0.1:11435 -> 11434) so the seat-filler request bytes are dumped
# verbatim. Delete after Spike ψ capture; never the active profile outside
# the capture session. See scratch/spike-psi-delegation-rate/.
name: agentic-orchestrator-offline-tools-psi-capture
model: qwen3:14b
provider: openai-compatible/ollama
base_url: http://127.0.0.1:11435/v1
cost_per_token: 0.0
timeout_seconds: 300
