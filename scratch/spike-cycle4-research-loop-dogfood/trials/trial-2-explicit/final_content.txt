I'll start by listing the available ensembles, then invoke the specified one.

The ensemble `spike-cycle4-research-loop` is confirmed available (3-agent research loop for cheap-orchestrator testing, local Ollama). Now invoking it:
Here is the research-log entry integrating the ensemble's actual returned result verbatim:

---

## Research Log Entry

**Ensemble:** `spike-cycle4-research-loop`  
**Timestamp:** *(implicit — logged at time of receipt)*  
**Orchestrator tier:** cheap-orchestrator (free tier, local Ollama)

---

### Question
What conditions make pre-specifiable routing (class-c decomposition) reliable at the cheap-orchestrator tier vs. when does it degrade?

---

### Findings
*(verbatim from ensemble return)*

> The findings indicate that high network density, low traffic, and stable topology enhance pre-specifiable routing reliability, while congestion, increased traffic, or high node count degrade it. Theoretical analysis balances cost and efficiency, highlighting the interplay between routing algorithms and network structure. The execution is successful.

**Key reliability enhancers (verbatim):**
- High network density
- Low traffic
- Stable topology

**Key degradation drivers (verbatim):**
- Congestion
- Increased traffic
- High node count

---

### Tensions
1. **Cost vs. fidelity tradeoff at cheap-orchestrator tier** — the ensemble itself notes that theoretical analysis "balances cost and efficiency"; pre-specifiable (class-c) routing is cheap to execute but its reliability envelope is narrow. The findings suggest that once traffic or topology complexity crosses a threshold, the cheap tier's static routing logic becomes a liability rather than a feature.

2. **Density × scalability tension** — high network density aids reliability, but the same structural property (more edges, tighter coupling) amplifies the blast radius of congestion and node-count growth. What stabilizes pre-specifiable routing at small scale actively destabilizes it at larger scale.

3. **Execution-success ≠ routing-correctness** — the ensemble reported "The execution is successful," but this refers only to the three-agent loop completing locally on Ollama, not to the quality or correctness of the routing decisions themselves. A successful execution of a degraded routing policy is still a degraded routing outcome.

---

*End of research-log entry.*