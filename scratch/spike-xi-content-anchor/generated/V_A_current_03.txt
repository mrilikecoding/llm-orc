```python
import text_tools

def test_clean_text_removes_extra_whitespace():
    assert text_tools.clean_text("  Hello   ") == "Hello"
    assert text_tools.clean_text("This   is a test.") == "This is a test."

def test_split_text_splits_on_punctuation():
    assert text_tools.split_text("Hello. How are you?") == ["Hello", " How are you?"]
    assert text_tools.split_text("Split! This is a test.") == ["Split", " This is a test."]

def test_count_words_counts_correctly():
    assert text_tools.count_words("Hello world") == 2
    assert text
```