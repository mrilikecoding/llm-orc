Here's a focused implementation of `scheduler.py` that directly accesses the specified configuration keys from `settings.json`:

```python
import json

# Load the configuration file
with open('settings.json') as config_file:
    config = json.load(config_file)

# Directly access the required settings using exact keys
retry_backoff_ms = config['rbo_ms']
max_queue_depth = config['qdepth_max']
affinity_salt = config['aff_salt']

# Example usage (you can expand this with scheduling logic)
print(f"Retry backoff: {retry_backoff_ms} ms")
print(f"Max queue depth: {max_queue_depth}")
print(f"Affinity salt: {affinity_salt}")
```

This implementation:
- Loads `settings.json` once
- Directly accesses the three required keys via `config['rbo_ms']`, `config['qdepth_max']`, and `config['aff_salt']`
- Avoids any generic key iteration or invented configuration keys
- Provides a simple output to demonstrate usage of the retrieved values