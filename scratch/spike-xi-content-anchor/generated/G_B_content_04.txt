Here's a focused implementation of `scheduler.py` that directly reads the three specified configuration keys from `settings.json`:

```python
import json

# Load configuration directly from settings.json
with open('settings.json') as f:
    config = json.load(f)

# Access each required setting by its exact config key
retry_backoff_ms = config['rbo_ms']
max_queue_depth = config['qdepth_max']
shard_affinity_salt = config['aff_salt']
```

This implementation:
- Loads the JSON file once
- Uses direct subscript access for the three required keys
- Stores each value in a variable with descriptive names
- Avoids any key iteration or generic configuration loading logic

The code assumes `settings.json` is in the same directory as `scheduler.py`.