```python
import json

# Load settings from the JSON file
with open('settings.json') as f:
    config = json.load(f)

# Directly access the three required configuration keys
retry_backoff_ms = config['rbo_ms']
max_queue_depth = config['qdepth_max']
affinity_salt = config['aff_salt']
```

This code reads the `settings.json` file and directly accesses the three specified configuration keys using subscript notation, as requested. The values are stored in variables for use in the scheduler logic.