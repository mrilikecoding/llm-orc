```python
import json

with open('settings.json') as f:
    config = json.load(f)

rbo_ms = config['rbo_ms']
qdepth_max = config['qdepth_max']
aff_salt = config['aff_salt']
```

This implementation directly accesses the three specified configuration keys using subscript notation as requested. It reads the `settings.json` file, extracts the required values, and stores them in variables for use in the scheduler logic.